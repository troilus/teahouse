# 茶话间（Teahouse）需求文档

> [简体中文](requirements.md) · [English](en/requirements.md)

| | |
|---|---|
| 状态 | v0.60.0 诊断与反馈已完成（#315）；屏幕协助目标平台真机验收待完成；内网通兼容仍按 #199 暂缓 |
| 日期 | 2026-09-17 |
| 关系 | 本文是**功能需求的唯一事实来源**；技术选型与平台约束见根目录 README（Electron 22 焊死、平台矩阵、开发红线），本文不重复只引用 |

## 1. 背景与目标

茶话间是纯内网、无服务器、基于 IP 的局域网 IM + 文件传输工具。设计原则：

1. **零服务器、零配置**：装上即用，自动发现，所有节点对等
2. **纯内网**：数据不出局域网，无外网依赖、无遥测
3. **办公可用**：消息可靠（不静默丢失）、文件传输跑满内网带宽
4. **老设备友好**：Win7 / Debian 10 / macOS 26 起（见 README）

## 2. 目标场景与用户

- **主场景：中大型办公内网**——几十到几百人，多 VLAN/网段，跨网段互通是刚需，存在部门结构。
- 次场景（自然兼容，不专门优化）：小团队单网段、个人多设备互传。
- **规模设计目标：单网络 ≤ 1000 在线节点**（决议 #15，由 500 上调），性能与协议设计按此预算。
- 用户画像：普通办公人员（非技术背景，要求开箱即用）；IT 管理员（负责分发安装、网段配置）。
- 环境事实：既有 Win7 终端以 64 位虚拟机为主，同时需要覆盖新增的 **32 位 Windows 终端**（决议 #213；VM 弱显卡的渲染对策见 tech-design.md §9）。

## 3. 非目标（Non-Goals）

- 不做互联网/跨公网通信，不做云同步、云存储
- 不做账号体系与中心服务器（包括"轻服务器"）
- 不做移动端（仅三平台桌面）
- 不做富文本消息（加粗/字号等），不做视频通话；语音对讲、远程键鼠控制继续列为远期评估（决议 #7）。**只读远程桌面查看已实现**（决议 #310、§6.11），共享文件柜已由决议 #271 转正（§6.10）
- 不做已读回执，消息状态只到"已送达"（决议 #1）
- 不做传输加密：信任内网边界，沿用 ipmsg / iptux 的明文做法（决议 #5）
- 茶话间主协议保持自有 UTF-8 JSON 报文；内网通 / 飞鸽兼容只通过独立兼容模式实现，不改变主协议身份、发现、消息和文件通道（决议 #5/#194/#195）

## 4. 术语

| 术语 | 含义 |
|---|---|
| 节点（peer） | 一个运行中的茶话间客户端实例 |
| 节点 ID | 首次启动生成的本机唯一标识，身份不随 IP/昵称变化 |
| 会话 | 与某个节点或讨论组的聊天上下文 |
| 讨论组 | 固定成员列表的多人会话（P2P 语义，消息向成员逐个直发） |
| 分组 | 通讯录的"公司 > 部门 > 团队"三级维度，由用户在设置中自报 |
| 补发 | 对方离线时消息存于发送端本地队列，探测到上线后自动重发 |

## 5. 功能总览与优先级

P0 = 没有就不可用；P1 = 正式版应有；P2 = 增强。版本列为当前规划，随开发节奏调整。

| 模块 | 功能 | 优先级 | 版本 |
|---|---|---|---|
| 发现 | 同网段自动发现、心跳、在线列表 | P0 | v0.1 |
| 发现 | 手动添加 IP / 导入节点列表 | P0 | v0.1 |
| 发现 | 跨网段：网段定向探测 + 节点间列表交换 | P0 | v0.3 |
| 发现 | 跨网段：扫描范围低频自动分享与受控后台扫描 | P0 | v0.17 |
| 发现 | 主界面一键刷新全部已保存扫描网段 | P0 | v0.18 |
| 发现 | 三级分组树（公司>部门>团队）、列表搜索 | P1 | v0.3 |
| 发现 | 历史联系人持久保留、资料自动刷新 | P0 | v0.1 |
| 发现 | 打开会话探活（在线二次校验） | P0 | v0.1 |
| 发现 | 联系人备注名 | P1 | v0.3 |
| 消息 | 单聊文本（送达状态、失败重试） | P0 | v0.1 |
| 消息 | 私聊窗口震动（限流防骚扰） | P1 | v0.16 |
| 消息 | PK 分歧解决（猜拳 / 骰子） | P1 | v0.22 |
| 消息 | 离线暂存补发 | P0 | v0.1 |
| 消息 | 图片消息（粘贴/拖拽） | P0 | v0.2 |
| 消息 | 表格粘贴图片消息（图片/文字切换） | P1 | v0.31 |
| 消息 | 表情 emoji / 自定义表情包收藏 | P0 / P1 | v0.2 / v0.3 |
| 消息 | 讨论组（群聊） | P0 | v0.3 |
| 消息 | 多选收件人群发 | P1 | v0.3 |
| 消息 | 历史记录（SQLite） | P0 | v0.2 |
| 消息 | 全局搜索（联系人/聊天记录/文件）、链接识别 | P1 | v0.2+ |
| 消息 | 聊天记录导出（HTML/TXT/备份包）与导入 | P1 | v0.4+ |
| 消息 | 撤回 | P1 | v0.4+ |
| 消息 | 转发 / 群内 @ | P1 | v0.4 / v0.3 |
| 截图 | 内置框选截图 + 热键 | P0 | v0.3 |
| 文件 | 文件/多文件传输（进度、取消、队列） | P0 | v0.2 |
| 文件 | 文件夹传输 | P0 | v0.2 |
| 文件 | 接收策略（确认/私聊直接发送）、重名处理 | P0 | v0.2 / v0.28 |
| 文件 | 断点续传 | P1 | v0.4+ |
| 文件 | 传输记录 | P1 | v0.2+ |
| 文件 | 共享文件柜（自设共享目录 + 权限，浏览/下载/上传） | P1 | v0.47 |
| 文件 | 文件柜一等入口（导航栏第三项 + 同事柜子浏览器 + 我的柜子管理） | P1 | v0.50 |
| 系统 | 托盘常驻、新消息提醒、桌面通知 | P0 | v0.1 |
| 系统 | 开机自启（默认开，可关） | P0 | v0.1 |
| 系统 | 首次启动向导 | P0 | v0.1 |
| 系统 | 全局快捷键（截图、唤起主窗） | P1 | v0.3 |
| 系统 | 局域网 P2P 自更新（同平台同架构、发现更高版本即提示、确认后同步安装） | P1 | v0.27 |
| 设置 | 用户信息（昵称/公司/部门/团队）、端口、接收目录、通知开关 | P0 | v0.1+ |
| 设置 | 头像（昵称色块 + 内置头像集 + 自定义图片） | P1 | v0.2 / v0.42 |
| 设置 | 深色主题 | P1 | v0.4+ |
| 设置 | 简体中文 / English 即时切换；新安装跟随系统，已有用户保留中文 | P1 | v0.57.0 |
| 文档 | 简体中文主事实源 + 英文当前规范、双向入口与同步校验 | 已交付 | v0.51.1 |
| 协助 | 一对一、逐次同意、自动/3/5/10 帧远程桌面查看 | v0.58.0（#310） | 已实现，目标机权限与性能待验收 |
| 兼容 | 内网通兼容模式（设计见 nwt-compat-design；**#199 暂缓，不排期**） | 待办 | 未定 |
| 扩展 | 本地开放接口（AI 读取/接管聊天，默认关闭） | P2 | 远期 |

## 6. 功能需求明细

### 6.1 发现与在线状态

- **F-DISC-1 同网段自动发现（P0）**：启动时 UDP 广播上线，在线节点应答（带随机延迟，避免 1000 节点同时应答造成风暴）；收到上线广播即更新列表。下线发广播，异常退出靠心跳超时判离线。
- **F-DISC-2 跨网段发现（P0，主场景刚需）**，三个手段按序落地：
  1. 手动添加对端 IP / 导入 IP 列表（v0.1，保底手段）；
  2. 指定网段定向单播探测（如 `10.1.0.0/24`，可配多段，限速扫描）；
  3. 节点间在线列表交换（gossip 散播：A 知道的节点告诉 B，跨网段只要有一个"桥"节点即可全网打通——内网通同思路）。
- **F-DISC-2a 扫描范围自动分享（P0，决议 #114）**：用户手动新增的扫描 CIDR 会作为“网段记录”在已在线节点间低频同步；收端只自动加入本机网段记录并标注来源，不立刻全段扫描。自动接收的网段进入后台节流扫描：首次随机延迟 30–90 分钟，同一网段最短 12 小时自动扫一次；在线节点超过 50 时按稳定 hash 抽样，仅约 10% 客户端参与该网段后台扫描，其余节点仍靠既有 `peers` gossip 学到在线节点。用户手动点击“扫描/再次扫描”不受后台节流影响。用户移除某个同步来的网段后，本机记入忽略表，远端再次分享时不自动加回；自己重新手动添加会清除忽略。
- **F-DISC-2b 主界面全局网段刷新（P0，决议 #115 / #197）**：主窗口左侧导航栏在设置按钮上方提供“刷新全局用户”按钮。**每次点击先弹居中二次确认**（决议 #197，用户拍板：非破坏性但会向局域网发大量探测，防误触），确认后才按本机已保存的所有合法扫描网段汇总、去重主机地址，并以手动扫描同等限速逐个单播探测；扫描期间在左侧栏显示环形进度，进度含义为“已发出探测地址数 / 总地址数”。该动作由用户显式触发，不受自动后台扫描的 30–90 分钟延迟、12 小时去重或大网抽样约束；运行中按钮禁用、不并发启动第二轮。设置页「网段表格 · 刷新该网段」为单段即时探测，**不**套本确认（范围小、已在设置上下文）。
- **F-DISC-3 节点身份（P0）**：首次启动生成节点 ID 并持久化；昵称、IP、主机名都可变，**会话与历史跟着节点 ID 走**。同一机器多实例不在 v1 范围。**同一人多台设备 = 多个独立身份**（设备即身份，无服务器下的天然语义，不做账号合并——明示防误解，决议 #22）。
- **F-DISC-4 通讯录（P0）**：显示昵称、IP、平台图标、在线状态；**三级折叠树：公司 ▸ 部门 ▸ 团队 ▸ 成员**（决议 #10，每级显示在线/总数，字段空缺逐级跳过，全空归"未分组"）；组内在线优先、离线灰显；即时搜索过滤（1000 人规模下搜索是必需品）。布局明细见 [ui-design.md](ui-design.md) §4。
- **F-DISC-5 分组/部门（P1）**：无服务器、无组织架构中心——**用户在"我的资料"设置页自填"公司""部门""团队"三个字段**，随在线协议广播，各端据此聚合成树（决议 #8/#10）。IT 预置配置文件（安装目录放 JSON 锁定字段）降为 P2 可选增强。
- **F-DISC-6 头像（P1，决议 #45/#47/#243/#245/#249）**：设置提供“动物表情 / 昵称首字 / 自定义头像”三种模式；动物表情可组合 20 个 Twemoji 本地 SVG 与 10 种背景色，昵称首字可单独选择同一套背景色，自定义头像从本地静态图片上传。用户选择 JPG/PNG/WebP/BMP 后，通过全局圆形裁剪弹窗拖动和缩放图片（最大缩放随源图分辨率动态放宽，可裁到 192px 输出分辨率对应的最小区域，决议 #249），确认后生成受限 WebP 并立即应用；取消不修改资料。协议继续携带数字头像作为旧端回退，另以 SHA-256 哈希声明自定义头像，新端在纯内网中按需获取和缓存图片。加载失败、来源离线或旧端场景继续显示数字头像；首次启动向导维持现状。
- **F-DISC-7 联系人持久保留与资料刷新（P0）**：出现过的节点持久保留在通讯录（离线头像与姓名灰显，行尾使用灰色状态点；在线使用绿色状态点；姓名旁不再重复显示“离线”，决议 #11/#17/#252，重启不丢）；资料在对方上线、主动变更、心跳资料版本号失配三种时机自动刷新，防"机器没换、用的人换了"（协议机制见 protocol.md §6.2）。不提供手动删除；"清理 90 天未上线"入设置-高级（P2）。
- **F-DISC-8 在线状态二次校验（P0，决议 #16）**：心跳有 90s 窗口期，"显示在线"可能实际已掉线——**打开会话时**向对方单播探活（复用 entry/alive，约 2s 超时即转离线显示）；**发送消息时**以 ACK 为准，未确认不标"已送达"，重试失败自动转离线补发。双保险杜绝"看着在线、消息黑洞"。
- **F-DISC-9 联系人备注名（P1，v0.3，决议 #22/#37/#40）**：联系人资料页与私聊头部资料弹窗均可设本地备注，备注覆盖昵称显示（重名刚需），列表/会话/搜索全部按备注命中；通讯录单击联系人时在右侧展示完整资料页，双击联系人时直接进入该联系人单聊；备注仅存本机、不广播。

### 6.2 消息

- **F-MSG-1 单聊文本（P0，决议 #46）**：UTF-8 + emoji；短消息优先走 UDP+ACK，UDP 重试耗尽后自动用现有 TCP 控制帧兜底一次；超长消息直接走 TCP（阈值由协议定）；每条消息有全局唯一 ID（去重、补发、撤回的基础）。发送状态三态：发送中 / 已送达 / 失败（可点重发）；不做已读回执（决议 #1）。
- **F-MSG-2 离线暂存补发（P0）**：目标离线时消息入发送端本地持久化队列，UI 标"等待对方上线"；探测到目标上线后按原顺序自动补发；收端按消息 ID 去重。队列默认保留 7 天、单节点上限 200 条，超限提示（决议 #6）。
- **F-MSG-3 图片消息（P0）**：剪贴板粘贴（Ctrl+V 发截图）、拖图、选文件三种入口；UI 内显示缩略图、点击看原图；传输复用文件通道，对端自动接收入图片缓存（不走文件确认流程，有大小上限，超限转普通文件发送）。**表格粘贴增强（P1，v0.31，决议 #190；v0.46 按决议 #270 改为不自动发送）**：当用户从 Excel / WPS / LibreOffice / 网页表格复制表格并在输入框粘贴时，粘贴**只把原文插入输入框、绝不自动发送**，输入区上方出现轻提示条「检测到表格 · 发送为图片 · 忽略」；直接回车即发纯文字，点「发送为图片」才走表格图片。聊天气泡默认展示表格图片；新版本双方若支持 `tbl1` 能力，则同一条图片消息附带原始制表符文本，并在气泡上沿显示小型分段滑块「图片 / 文字」。切到「文字」后显示只读原始 TSV 文本，方便选择复制回表格软件；文字元数据上限为 4096B UTF-8，超出时只截断文字视图并显示轻提示，图片视图仍完整。切换只影响本机显示，不产生新消息、不通知对方。旧版本或不支持 `tbl1` 的对端仍只收到普通图片。表格判定必须"整段确实就是一张表"才成立（决议 #270），随手输入的制表符、Tab 缩进的代码与日志一律按普通文本粘贴。
- **F-MSG-3a 聊天图片前后切换（P1，v0.55，决议 #303）**：点击单聊或群聊图片后，在独立图片查看窗口的画布两侧显示“上一张 / 下一张”箭头，按当前会话消息顺序浏览完整本地图片历史（含截图和表格图片）；首末张对应箭头置灰，不循环。表情和普通文件不参与；撤回、尚未下载完成或本地失效的图片跳过，群发同一图片消息只出现一次。历史搜索打开的图片使用同一会话范围。切换保留窗口位置与尺寸，新图居中适应窗口并重置旋转、平移和 OCR 文字选区；原有方向键平移与其他看图操作保持。查询期间禁用切换以避免连点串图，失败提供可见提示。
- **F-MSG-3b 图片内选字与低配 OCR（P1，v0.56，决议 #304，替代 #105 的独立文本框）**：所有平台首次识别由用户点击“识别文字”触发；打开图片、连续切图均不自动启动 OCR。识别后在原图文字位置直接拖选部分文字，支持跨行选区、Ctrl/Cmd+C、选区复制按钮和复制全部；不再弹独立文字结果框。已有缓存直接恢复文字层，不重复推理。文字区域拖动用于选字，图片空白区域仍可平移；缩放和旋转时文字层跟随原图，切图清理旧选区。沿用本地模型、2200px 输入长边和960px检测基线，采用单个本地 Worker、单线程推理且同时最多识别一张；切图或关闭终止未完成任务。闲置与缓存命中不推理。文字层以每行一个 DOM 节点和浏览器原生选区实现，不新增逐字命中循环、轮询或外网请求；整行坐标按检测框及字体度量拟合，特殊字体与倾斜文字的字符位置可能存在偏差。识别期间可能短时占用一个 CPU 核心，不承诺跨设备固定耗时或CPU百分比。
- **F-MSG-4 讨论组/群聊（P0）**：任意节点可建组（组 ID + 名称 + 成员列表）；文本消息向成员逐个直发，离线成员走补发；成员可增删，组元数据随消息携带版本号同步。**一致性按"尽力而为"设计**（P2P 无仲裁，接受短暂的成员视图不一致），上限 **200 人/组**（决议 #198，原 50）。支持**群内 @ 成员**（P1，被 @ 者通知加强）；成员可自行退组（决议 #22）。群角色分为群主、管理员、普通成员（决议 #241）：群主可任免管理员、改群名、移出管理员或普通成员；管理员可改群名、移出普通成员；任意在群成员均可直接邀请联系人入群，无需管理权限或受邀确认。群成员面板的添加入口使用搜索多选弹窗（决议 #242）：隐藏已有成员，支持按备注、昵称、公司、部门、团队、IP、主机名过滤并一次批量邀请，选择人数受 200 人群上限约束。群主退出时按成员顺序优先转让给首位管理员，没有管理员则转让给首位剩余成员。建群仍可选管理密码，群主和显式管理员免密码；知道密码的普通成员可改群名、移出普通成员，不能任免管理员或移出群主/管理员。群主、管理员和持有正确管理密码的普通成员可设置或恢复默认群头像（决议 #243），图片裁剪、存储和按需传播复用用户头像管线；群头像显示于会话列表、群聊头部和群成员面板。群成员变化、角色变化、改名、头像变化与群主转让均写入幂等系统提示。群聊图片/文件首版复用单聊文件通道，发送端按在线群成员逐个发 `file-ctl offer`，离线成员不入队，收端按 offer 中的 `groupId/groupRev` 入群会话（决议 #32）；群聊图片仅当单图 ≤10MB 时按图片消息发送，超过 10MB 自动退化为普通文件卡片，由接收者手动接收后才开始 TCP 拉取（决议 #33）。**群简介与群公告（P1，v0.54）**：群主、管理员和持有正确管理密码的普通成员可在群成员面板设置群简介（≤ 200 字符）与群公告（≤ 1024 字符）；允许提交空串清空已有内容，空内容不展示；设置后随 `group.info` 全网广播，所有成员在群成员面板同步查看；旧端报文缺少两字段时保留本地已知值，首次接收时默认为空；迁移备份完整保留两字段；修改写入幂等系统提示（如"你修改了群简介"）。
- **F-MSG-5 历史与搜索（P0）**：本地 SQLite 存储，按会话无限滚动。切换不同会话时，消息区必须按会话恢复离开时的滚动位置；若该会话此前停在底部，则回来时仍贴到底部以包含新消息。首次打开一个会话、从通知/托盘/震动等后台入口直达会话、或主窗后台恢复后重新点开会话时，默认定位到最新消息（决议 #111）。**全局搜索（P1，决议 #12）**：分类返回联系人（全量含离线）/讨论组/聊天记录/文件名，命中聊天记录可跳转会话并定位高亮（参考微信，见 ui-design.md §6）。**会话内历史搜索（P1，决议 #34/#35/#36）**：群聊与私聊输入区工具条最右侧提供历史搜索入口，点击弹出设置页尺度的当前会话搜索面板，默认显示最近记录，内置图片、文件、连续日期范围筛选；图片结果直接展示缩略图，结果点击后跳转并高亮对应消息。**导出（P1）**：按会话 + 时间范围，阅读格式 HTML/TXT + **迁移备份包**（消息库+媒体，供导入用）；**导入（P1，决议 #19）**：仅认备份包，导入时旧机器的"我"自动映射为本机身份，与现有记录按消息 ID 去重合并——**换电脑不丢数据**；清空记录带双重确认。
- **F-MSG-6 撤回（P1，v0.4+；媒体撤回 v0.30.0）**：自己的文本 / 群文本 / PK / 图片 / 文件消息 **5 分钟内**可撤回（决议 #63/#139/#188，原 2 分钟）。文本、群文本、PK 维持既有语义：收发两端隐藏原消息并插入系统提示行，撤回 PK 只影响被撤回的那一条 PK 消息，不级联撤回之后别人另发的 PK。图片撤回要求新版本 `file-ctl offer` 携带跨端一致 `msgId`；发送方 5 分钟内可撤回自己发出的图片，撤回后收发两端隐藏图片气泡并禁止继续通过该消息打开图片。文件撤回同样依赖跨端一致 `msgId`，但**已接收完成的文件不支持撤回**：仅在对端仍未完成接收（等待接收、已接受但传输中、直接发送传输中、失败/取消但未完成落盘）时允许撤回；撤回后停止传输并清理未完成 `.part`。群聊文件只有所有在线收件人的对应 transfer 都未完成时才可撤回；任一成员已接收完成则整条群文件消息不再提供撤回。对端离线或暂时不可达时，撤回指令随补发机制送达；迟到的文件撤回不得影响已经完成保存的文件。表情包撤回不纳入本轮入口，后续另议。通用右键菜单中的撤回项与其他操作等高并保持单行：左侧固定显示“撤回”，右侧用小号等宽数字显示 `mm:ss`；不可用原因同样置于右侧，最后 10 秒才增强倒计时危险色（决议 #251）。
- **F-MSG-7 表情（P0/P1，决议 #18/#45）**：emoji 面板为 P0（v0.2），但面板与消息正文显示不得依赖系统彩色 emoji 字体；Win7 / UOS 等缺字平台使用项目内本地 SVG 兼容渲染，协议与剪贴板仍保留原始 UTF-8 emoji 字符。**自定义表情包收藏（P1，v0.3）**：聊天中的图片右键"添加到表情"，或从表情面板多选本地图片导入；保存时**自动压缩**（静图最长边 512px 转 WebP，GIF 动图 ≤2MB 原样保留）。表情面板内可删除/排序；发送表情包为轻量图片消息（协议 `kind:"sticker"`），支持单聊和群聊，对端免确认接收，气泡内固定小尺寸展示；群聊与图片一致，仅投递给当时在线的其他成员。
- **F-MSG-8 转发与引用（决议 #22/#230/#288）**：消息**转发**到其他会话（文本/图片/文件，P1，v0.4）；转发目标弹窗必须挂在 renderer 全局顶层，遮罩完整覆盖主窗口并始终高于消息列表、聊天头部、输入区与局部浮层，支持遮罩 / 取消 / `Esc` 关闭；**引用回复**（P2，v0.53，决议 #288）：群聊输入框点击某条消息气泡时可将其设为引用目标，发送时仅携带源消息 ID（`replyTo`）；接收侧在同一群会话内按 ID 查询源消息，生成展示用 senderName（备注优先、其次昵称）与首行文本摘要写入 `ReplyMeta`，渲染层据此在气泡上方显示引用缩略信息栏，点击可跳转定位到源消息所在会话；目标消息不在本地群会话内时接收正常，跳转提示"原消息不可用"由渲染层处理；线上只传源消息 ID，不传 senderName/text，防止伪造身份。
- **F-MSG-9 私聊窗口震动（P1，决议 #109/#110/#112）**：仅单聊可用，作为可靠即时提醒动作。震动走 `msg(kind:"nudge")` 投递，有 ACK 才算发送成功；对端离线或无响应时直接失败，不进入离线补发队列，避免上线后补发旧震动造成骚扰。发送成功后，本端插入“你发送了一次窗口震动”本地系统提示行；收端收到且未被限流时，唤起/震动主窗口、自动切到发起人的单聊，并插入“对方发来一次窗口震动”本地系统提示行；若该单聊已设免打扰，收端仍可保留系统提示行，但不得唤起、置前或震动窗口。系统提示行与撤回提示同样居中展示，不参与全文搜索。发送端与接收端均按“同一对端 60 秒最多 2 次、任意两次至少间隔 15 秒”限流；被限流的震动静默丢弃或提示稍后再试，不影响普通消息收发。
- **F-MSG-10 PK 分歧解决（P1，v0.22，决议 #139）**：聊天输入区在表情按钮后、窗口震动前新增小图标按钮 `PK`，图标语义为斜体 `PK` 字标。点击后弹出小浮层，两个并排玩法按钮，图标在上、文字在下，文字为「猜拳」和「骰子」。用户点玩法后才由主进程在发送瞬间随机生成结果并发出结构化 `msg(kind:"pk")`，发送前不展示也不允许用户预选结果；渲染层只负责把已生成结果播放成约 **1.5 秒**的“像随机 GIF 一样”的本地动画。单聊与群聊均可用，但 PK 定义为在线小娱乐：单聊仅对方在线时可发；群聊仅至少一位其他成员在线时可发，并且只投递给当时在线的成员，离线成员不入队、不补发。若发送途中对端掉线，消息标失败，可点重试，但重试沿用同一条消息和同一个已生成结果，不重新随机。每次参与都发送一条新的独立 PK 消息，不修改原消息、不做回合聚合、不输出谁赢谁输，让用户自己看结果。收到他人的 PK 消息时，在气泡旁显示小按钮：猜拳为「我也来」，骰子为「掷一下」；自己的 PK 消息旁不显示参与按钮。允许反复参与，不做每人一次限制或额外防刷屏。骰子结果为 1–6 点，UI 使用真实点数组合的自绘骰面；猜拳结果为 `rock/paper/scissors`，UI 文案显示「出了石头 / 剪刀 / 布」，图形使用本地 Twemoji 原色手势资源（缺资源时补本地 SVG 与署名，禁止 CDN / 远程图片）。新发 / 新收 PK 播放一次动画，历史加载、翻页和重开会话直接显示最终结果。PK 参与普通未读与系统通知，但通知、会话列表预览和历史搜索结果只显示「PK · 猜拳 / PK · 骰子」或「[PK] 猜拳 / [PK] 骰子」，不提前暴露结果；导出 HTML/TXT 可展示完整结果。PK 可撤回，但不可复制、不可转发。旧版本客户端不做 fallback 文本，混用时按未知消息能力处理，要求双方升级后才能看到 PK。该能力是内网轻娱乐，不提供防作弊的承诺揭示、签名或加密公平性证明。
- 链接自动识别可点击（P1）；"正在输入"状态（P2）。

### 6.3 内置截图

- **F-CAP-1 框选截图（P0，决议 #208/#221）**：全局热键（默认 `Ctrl/Cmd+Alt+A`，可改）→ 屏幕蒙层框选 → 进剪贴板并可一键发送到当前会话。完成框选后，标注工具条必须基于真实尺寸与可用视口定位：优先放在选区下方，空间不足时翻到上方，横向及极小视口统一夹紧；视口小于工具条时至少保证可操作起点留在屏内。截图窗口沿用主窗口导航安全基线，所有 `will-navigate` 均拦截。Windows 使用 `display.workArea` 作为框选窗口与截图可选范围，按桌面源真实像素裁掉任务栏区域，避免 Win7 将普通置顶窗约束到工作区后出现“截图内任务栏被压缩、真实任务栏再次顶在下方”的双层任务栏与画面变形。截图覆盖层只绘制一份桌面位图，框选高亮通过四块纯色遮罩露出原图；拖动更新按动画帧合并，位图解码并完成首帧布局后才显示截图窗口，降低 Win7 软渲染负担与黑屏 / 闪屏。
- **F-CAP-2 简单标注（P1，决议 #265/#266/#267）**：矩形、箭头、文字、马赛克。文字工具点击选区后必须在截图层内原地显示单行输入框并立即聚焦，Enter 提交、Esc 取消本次文字；中文输入法处于 composition 时不得把候选确认 Enter 当成提交。发送 / 复制截图前提交当前非空文字，文字预览与最终 PNG 使用相同左上基线。截图链路不得依赖 Electron 不支持的 `window.prompt()`。标注工具条的功能入口统一使用本地线性 SVG 图标，不显示重复的按钮文字；八个按钮必须分别提供中文自定义悬停提示与 `aria-label`，工具按钮同步暴露 `aria-pressed` 状态，工具组与发送 / 复制 / 取消操作组以分隔线区分。提示在鼠标悬停或键盘聚焦时出现，工具条靠近视口顶部时自动显示在按钮下方。
- 平台注意：macOS 需引导"屏幕录制"权限；Windows 注意多屏与高 DPI；Linux/X11 直接抓屏，Wayland 启用 PipeWire portal 后先实际尝试系统选屏，不按会话类型提前拒绝。屏幕源不可用、返回空图或抓屏异常时必须恢复主窗口并显示应用内提示；原本从隐藏状态用全局快捷键触发时优先使用系统通知，通知不可用则显示主窗口。所有失败提示都给出“系统截图后在聊天框 Ctrl+V”的退路。截图前隐藏主窗口时须等待隐藏事件和合成器退场，并在抓屏前复核窗口已不可见。

### 6.4 文件传输

- **F-FILE-1 文件/多文件（P0，决议 #208）**：拖拽/选择/粘贴入口；显示进度、速率、剩余时间；可取消；发送端数据流并发上限默认 3，超额授权拉取进入 FIFO 队列；TCP 服务同时限制连接总数 256，握手阶段 15 秒无有效帧即断开，活跃连接 60 秒无读写即断开，防止慢连接长期占用文件描述符与读流。
- **F-FILE-2 文件夹（P0）**：递归整夹发送，保留目录结构。
- **F-FILE-3 接收策略（P0，决议 #174/#179）**：普通文件默认仍需接收方点击「接收」后开始传输；接收方点击「接收」或私聊「直接发送」自动接收时，若未选择「另存为」，统一保存到 `文件保存位置/联系人名称/`。联系人目录名以本地备注优先、其次对方昵称生成，并做文件名清洗；重名仍自动加后缀，不覆盖。**私聊**新增发送方在已发送文件卡片上触发的「直接发送」能力：发送者先普通发送文件/文件夹，文件卡片出现后，若对方在线且双方版本支持，可点击「直接发送」请求接收方免确认拉取。接收方设置提供总开关「允许同事直接发送文件」，默认开启，关闭后直接发送退化为普通文件卡片。**群聊文件不允许直接发送**，只能走现有文件卡片手动接收；群聊手动接收同样按发送人显示名进入联系人子目录；离线文件仍不入队。
- **F-FILE-4 断点续传（P1，v0.4+）**：分块 + 校验，中断后从断点继续。
- **F-FILE-5 对方离线时发文件**：v1 行为 = 提示对方不在线、不入队（文件在磁盘上随时会变，离线暂存风险高）；群聊文件/图片同样只投递给当前在线成员，离线成员不会进入补发队列；"上线后提醒我重发"作为 P2（决议 #4/#32）。
- **F-FILE-6 传输记录（P1）**：成功/失败/取消的历史，可打开所在目录。
- **F-FILE-7 文件领取期限（P0，决议 #263）**：私聊与群聊的普通文件统一从发送时刻起提供 **24 小时**领取窗口；接收方只可在期限内首次接收、继续或重新下载。截止前已经进入传输中的当前尝试允许完成；该尝试在截止后失败、取消或重启中断时不再恢复。到期的未完成 transfer 进入 `expired` 终态，发送方文件卡显示「发送已到期」，接收方显示「文件已过期」，接收、另存为、直接发送、继续与重新下载入口全部关闭；群发卡片保留已接收人数，并在剩余成员领取窗口结束后显示「发送已到期」。发送端持久化统一截止时间与单份源文件清单，应用重启后仅在期限内恢复供流授权，避免长期开机无限保留授权或重启提前失效。`purpose:"image"|"sticker"|"update"` 的自动媒体/更新通道不套用该领取期限。旧发送端未携带期限时，新接收端按收到完整 offer 起最多 24 小时执行本地期限；旧接收端逾期再请求时由新发送端拒绝供流。

- **与共享文件柜的分工（决议 #271）**：本节全部是"发起方推给对方"的**推模式**——发送人挑好文件、对方在聊天流里接收。反过来"我先把目录摆好、同事自己来取 / 来放"的**拉模式**由 §6.10 共享文件柜承担。两者互不替代、互不影响：文件柜的传输不进聊天流、不占用文件消息的 24 小时领取期限，文件消息也不因为对方开了文件柜而改变行为。

### 6.5 广播与公告

- 中大型内网场景常见诉求（内网通的核心卖点之一），但"人人可全员广播"必被滥用，无服务器下又没有权限中心。**v1 仅做"多选收件人群发"（P1，v0.3，发出后各自成单聊上下文）**（决议 #62：v0.5.26 起取消该入口，改用讨论组替代）；正式的"公告"（置顶、强提醒、全员/分组范围、发布权限）列 P2，等权限/预置配置机制想清楚再做（决议 #3）。

### 6.6 系统集成

- **F-SYS-1 托盘 / 菜单栏（P0，决议 #42）**：常驻托盘；关窗=最小化到托盘。未读总数变化时跨平台刷新系统提示：macOS 菜单栏图标旁显示未读数字并同步 Dock 角标；Windows 使用任务栏 overlay 数字并闪烁托盘图标；Linux 尝试系统角标（Unity 等支持时生效），并以托盘图标闪烁兜底。所有平台 tooltip 都显示未读总数，未读清零后数字与闪烁全部清除。
- **F-SYS-2 桌面通知（P0）**：横幅显示发信人与摘要；点击直达会话；全局免打扰与按会话免打扰（P1）。按会话免打扰不弹系统通知、不触发震动前台动作，会话列表仍累计未读但未读数字显示为灰色；检测到投屏/演示场景自动勿扰（P2，决议 #22）。
- **F-SYS-3 开机自启（P0）**：默认开启（办公场景装完即长驻），安装器与设置中均可关。
- **F-SYS-4 快捷键（P1，决议 #293）**：全局截图、显示/隐藏主窗快捷键可自定义、可禁用（防冲突）。主窗口另支持无修饰键的 `Esc` 隐藏；输入法组词、长按重复、已消费事件不触发隐藏，弹窗与菜单优先处理。隐藏独立于关闭按钮的退出偏好；托盘不可用时最小化，保留恢复入口。设置窗、截图窗和图片窗继续使用各自键盘行为。
- **F-SYS-5 局域网 P2P 自更新（P1，v0.27，决议 #166/#181/#208）**：纯内网、零服务器、无中心的点对点自动更新。节点互相已知彼此平台与版本，发现**同平台、版本更高、且声明可作更新源**的在线节点时，提示"内网有新版 vX.Y.Z（来自 某同事）"；用户确认后从该节点拉取同平台同架构安装包（复用文件传输通道，沿用 SHA-256 完整性校验），安装并重启。**谁先升级谁自然成源**——下载过的节点保留包、接力扩散，无"管理员"角色、无人工放包（升级动作本身使其成源）。本期形态：**Windows 安装版（nsis）**安装时自留一份安装器到数据目录、更新时静默装（per-user 免管理员授权）；**Linux deb** 运行态用 `dpkg-deb` 把自身重打包成 deb、更新时经系统授权（pkexec）安装。触发为**发现即提示、用户确认才更新**（不静默）；安全以用户确认 + 一次性请求授权 + 来源节点匹配 + 平台/架构/精确包名匹配 + **单包最大 512 MiB** + SHA-256 + 包内版本核对 + 内网信任边界（决议 #5）为准。授权仅绑定最近一次用户确认的源节点与目标版本，120 秒过期，成功接收一次后消费；未授权、来源错误、包名错误或超限的 `purpose:"update"` offer 必须拒绝。**纯内网零外网，不违反红线 #5**（红线禁的是外网更新检查 / CDN）。mac（dmg/zip）因形态识别与签名 / Gatekeeper 成本暂缓、列后续。分三步落地见 tech-design §12。
  - 决议 #213 补充：Windows 更新包按 `x64` / `ia32` 精确区分，32 位客户端只接受 `win-ia32-setup.exe`。
- **F-SYS-6 首次启动向导（P0，v0.1，决议 #22）**：首次打开必经：昵称（必填，默认取系统用户名）→ 公司/部门/团队（选填）→ 文件保存目录确认 → 进主界面。避免全网一片"未命名"。
- **F-SYS-7 设置窗口层级（P0，决议 #222）**：Windows / Linux 打开独立设置窗口时，主窗口立即覆盖一层静态半透明暗色遮罩并停止接收交互，设置窗作为主窗的模态子窗保持在前；关闭设置窗后主窗立即恢复。设置窗四周必须有与内容背景可区分的实色内描边，并显式请求系统窗口阴影。macOS 继续使用原生窗口阴影与既有父子窗口行为，不增加主窗遮罩。遮罩不使用 `backdrop-filter`、透明 BrowserWindow 或持续动画，确保 Win7 / UOS 软渲染稳定。

### 6.7 设置

设置页按常见桌面软件的信息架构组织为 8 组（决议 #14/#31）：账号资料、通用外观、消息通知、聊天与文件、网络连接、快捷键、安全与高级、关于。**明细以 [ui-design.md](ui-design.md) §8 为准**。所有设置本地持久化；提供"重置为默认"。

### 6.8 本地开放接口（远期预研，决议 #21）

为未来 AI 集成预留：本机的外部程序可经接口**读取**聊天（会话/消息/联系人查询、新消息订阅）与**接管**聊天（以本人身份发消息、自动回复）。**不进当前任何版本计划**，但约束现在立好：

- **默认完全关闭**，需用户在设置中显式开启；
- 仅监听 `127.0.0.1`（数据不出本机，与"纯内网"原则一致）；
- token 鉴权；**读与写（接管）分级授权**；
- 接口操作写审计日志；AI 代答的消息建议带可见标识（细则届时定）；
- 接口形态（本地 HTTP/WebSocket 或 MCP 服务器）按届时 AI 生态选型；
- 架构按"可挂接"预留，见 tech-design.md §3。

### 6.9 内网通兼容模式（**待办 / 暂缓**，决议 #194/#195/#196/#199）

> **决议 #199**：调研与 VM 复测周期长，**暂不实现、不排近期**；设计文档保留，代码零落地。恢复开发前须用户再拍板。

为已部署内网通的办公内网提供迁移期互通能力（目标能力，非当前交付）。该能力默认关闭，开启后通过独立 UDP 兼容适配器绑定 IPMSG / 飞鸽常用端口 `2425/UDP`，扫描用户单独配置的兼容 IP / CIDR，发现开启飞鸽 / 飞秋互通的内网通节点，并支持普通文本收发与 `SENDCHECK` 送达确认。兼容联系人在通讯录中独立分组展示，使用独立会话 ID，不写入茶话间 `peers` 身份表，不参与茶话间 gossip、`scan-ranges`、群聊、自更新或离线补发。兼容会话必须显示“内网通”标志，并按兼容能力矩阵隐藏 PK、窗口震动、群聊、截图、图片、文件、直接发送、媒体撤回等主协议能力。文件与图片按 IPMSG `FILEATTACHOPT` / `CLIPBOARDOPT` 协议先做 parser、fixture 和实验开关；已实测内网通会 ACK 并在文件传输页展示标准 offer，但 TCP 拉取尚未闭环，不能进入默认可用范围。首版范围包含发现、在线状态、普通文本收发、退出通知、端口占用提示、兼容 UI 降级和 VM 手测；内网通私有 `901x` 协议、文件夹、公告、远程协助、共享文件柜等能力留后续评估。详细设计见 [nwt-compat-design.md](nwt-compat-design.md)。

### 6.10 共享文件柜（P1，v0.47，决议 #271–#277；独立入口 v0.50，决议 #283/#284）

面向"资料我先摆好，同事自己来拿；要给我的东西自己放进来"的日常场景，替代内网里常见的 Windows 共享盘 / FTP。与 §6.4 文件消息的分工见该节末条。**全程复用既有 UDP 17878 与 TCP 17879，不新增任何端口、不引入服务器，双方必须都在线才能访问**（决议 #275）。

- **F-SHARE-1 我的文件柜（P1，决议 #271；入口改在文件柜窗口，决议 #283）**：每个节点最多设置**一个**共享根目录（本机任选的普通目录，默认未设置 = 功能关闭）。可修改路径、可一键打开该目录。共享根只在本机配置，不随资料广播，别人看不到它在磁盘上的真实位置——协议里出现的永远只有相对路径。换目录立即生效，正在进行的传输不受影响。**共享目录 / 默认权限 / 按人例外这套设置自 v0.50 起不在设置窗，改在主界面「文件柜」页签的「我的文件柜」里**（决议 #283/#284，理由见 F-SHARE-9）。
- **F-SHARE-2 权限模型（P1，决议 #271）**：采用「**一个默认档 + 按人例外**」。默认档三选一：**不共享**（默认值）/ **只读**（可浏览、可下载）/ **可读可传**（浏览、下载，外加上传）。在此之上可对个别联系人单独指定权限，既能调高（默认只读、给张三开可读可传），也能调低（默认只读、对李四关闭）。**有效权限 = 命中例外则用例外，否则用默认档**。例外按 nodeId 记录，跟着身份走，对方改昵称、换 IP 都不失效。权限判定完全在共享方本机完成，浏览方无从绕过。
- **F-SHARE-3 浏览（P1，决议 #273；入口扩为两个，决议 #283）**：两条路都能进——① 导航栏的「文件柜」按钮切到文件柜页签，列表栏选同事、内容区浏览（F-SHARE-9）；② 与某位同事的私聊里点头部的「文件柜」按钮，聊天区右侧盖出 320px 面板，边聊边看。两者显示对方共享根下的文件与子目录（名称、大小、修改时间），可逐级点进子目录、按面包屑返回上级、手动刷新。对方未设共享目录、把我关成"不共享"、不在线、或版本过旧不支持该功能时，按钮灰显并说明原因（口径与决议 #17 的离线灰显一致）。列表按「目录在前、再按名称」排序，单页最多 200 条、超出滚动加载；单个目录超过 5000 条只展示前 5000 条并提示"目录内容过多，仅显示前 5000 项"。**以 `.` 开头的隐藏文件与目录一律不列出**，避免误共享 `.ssh`、`.git` 等内容。
- **F-SHARE-4 下载（P1，决议 #275）**：面板内可勾选一个或多个文件 / 子目录下载，目录按整夹递归取回并保留结构。传输走既有文件传输通道，进度、速率、取消、断点续传、失败重试与普通文件一致。**默认落到 `文件保存位置/文件柜-对方名称/`**（名称取本地备注优先、其次对方昵称，并做文件名清洗），也可「另存为」自选目录；重名自动加后缀，不覆盖本机已有文件。文件柜下载**不进聊天流**、不生成文件消息、不套用 §6.4 的 24 小时领取期限——它是"当场去取"，取消了随时可以再来。
- **F-SHARE-5 上传（P1，决议 #272）**：仅当我对该联系人的有效权限是「可读可传」时，面板底部的「上传到 TA 的文件柜」可用，支持选择文件、多文件或整个文件夹（也支持拖到面板里）。**"传"的范围被刻意限死为"只能放新东西"**：上传内容统一落到对方**共享根下以我的名字命名的子目录**（`共享根/我的显示名/`），重名自动加后缀；**不允许删除、改名、覆盖对方任何已有文件，也不允许指定落点**。落点由接收方本机计算、不进协议，因此上传方无法把文件写到共享根以外的任何位置。权限不足、对方离线或对方中途关闭共享时，上传被拒并给出明确原因。
- **F-SHARE-6 提示与记录（P1，决议 #274；新增汇总列表，决议 #283）**：**只有"有人往我的文件柜里传了东西"会提示**——上传完成后在与 TA 的私聊里插一条系统提示「张三 上传了 3 个文件到你的文件柜」，点击直接打开本机对应目录。该提示让会话在列表里冒泡并计入未读，但**不弹桌面通知**（避免与文件消息的通知互相刷屏）；单次上传只生成一条汇总提示。**单纯浏览、下载我的文件柜不产生任何提示**，也不写聊天记录。文件柜的上传下载与普通文件一样出现在「传输记录」里，可区分来源。此外文件柜窗口的「我的文件柜」页有一块**「最近有人放进来」**汇总列表（决议 #283）：按人按次列最近 10 条（谁、几个文件、多大、什么时候），点任意一条直接打开本机对应目录——原来这件事只在私聊飘一条系统提示，翻聊天记录才找得回。数据取自既有传输记录，不新增存储。
- **F-SHARE-7 安全边界（P0 级约束，决议 #276）**：① 所有来自网络的路径一律按相对路径校验——拒绝绝对路径、`..`、盘符与保留字符，解析后还要复核真实路径仍落在共享根内，**符号链接指向共享根之外的条目直接跳过**，杜绝越权读取；② 目录层级超过 16 层不再展开；③ 同一联系人的列目录请求限流（10 秒 5 次），超限直接拒绝，防止有人拿它反复扫盘；④ 共享根**禁止选择**用户主目录根、系统盘根与本应用数据目录，设置时即拦截并说明原因；⑤ 日志沿用决议 #6 的脱敏口径，只记录节点 ID、条目数量、字节数，**不记录文件名与目录内容**；⑥ 传输仍不加密，沿用内网信任边界（决议 #5）。
- **F-SHARE-8 本期不做**：不做跨节点的搜索共享内容、不做订阅 / 变更推送（内容变化靠手动刷新）、不做在线预览与在线编辑、不做群聊共享目录（仅私聊一对一）、不做匿名或免发现访问、不做删除与改名等远程写操作。这些留待用起来之后再评估。
- **F-SHARE-9 一等入口与文件柜页（P1，v0.50，决议 #283；形态由独立窗口改为主窗页签，决议 #284）**：文件共享是每天都用的功能，却藏在设置窗第三组、浏览又只能从私聊进——本条把它提成一等入口。① **导航栏入口**：主窗导航栏底部工具组最上一格新增「文件柜」按钮（更新提示之下、刷新之上），点击**在主界面内切到文件柜页签**（不弹新窗口，决议 #284：主窗本来就是「导航 + 列表栏 + 内容区」，文件柜正好占后两格，多开一个窗口只是把同一件事拆到两处）；**不加未读角标**（有人上传已在私聊冒泡计未读，同一件事不提醒两次），未设共享目录时按钮照常可点、进去是引导设置的空态。② **文件柜页**：**列表栏**（272px）上半是「我的文件柜」摘要卡（共享目录、默认权限、几人被单独设过），下半是**所有支持文件柜的联系人**（在线在上、离线灰显在下，带搜索框）；**内容区**是完整的文件浏览器或「我的文件柜」管理页。③ **浏览手感对齐系统文件管理器**：单击选中、**双击才进目录**、`Ctrl`/`⌘` 加选、`Shift` 选范围、`Ctrl+A` 全选已加载、空格勾选、`Backspace` 上级、`F5` 刷新、右键给下载 / 另存为 / 复制文件名；**没有删除与重命名**（协议层就不提供改动别人文件的能力，决议 #272）。④ **两种视图**：默认详情列表（名称、大小、修改时间三列，等宽数字），可切大图标网格；视图偏好记在本机。**排序固定"目录在前、再按名称"、不提供排序切换**（列表是分页取回的，本地重排会让翻页顺序错乱），**不做缩略图预览**（要给协议加取图报文，还会把"翻个目录"变成成片图片传输）。⑤ **私聊内 320px 面板保留**并按同一套样式重画，头部新增「在文件柜里打开」——带着当前浏览位置切到文件柜页签。⑥ 权限徽标出现在内容区头部而非列表栏：有效权限由对方当场判定、只有列过一次目录才知道，为列表批量探测会撞对方的列目录限流。本条**不动协议、不动库表、不动权限与安全边界**，纯前台入口与界面重组。

<a id="remote-view"></a>

### 6.11 远程桌面查看（决议 #308–#310，v0.58.0 已实现）

来源：[Issue #13](https://github.com/skyjt/teahouse/issues/13)。用户确认先以查看屏幕辅助办公排障：协助方看清操作、报错和表格，通过现有聊天指导对方。本阶段采用 **JPEG 画面 + 现有 TCP 端口直连、明文传输**；无新增监听端口、服务器、WebRTC 传输或第三方原生模块。v0.58.0 已接入私聊入口、独立协助窗口、帧流与按本机前置条件声明的角色能力。

用户在多档方案后确认开发（#310）：采用独立窗口，默认自动，起始及最高目标 **10 帧/秒**，可按整轮耗时降到 5/3 帧；查看方可手动选节省资源/标准/流畅。各档共用画质，实际帧率受处理负载影响，不累计历史帧。画质参数与目标机验证见 tech-design §3.1。

| 编号 | 要求 |
|---|---|
| F-VIEW-1 请求 | 仅茶话间在线联系人私聊可发起「查看屏幕」。查看方先阅读说明并二次确认发送，共享方每次明确同意并选择一块屏幕；收到请求本身不得开始采集。请求可取消、拒绝或超时，不进入离线补发队列。 |
| F-VIEW-2 单次授权 | 同意仅对本次会话、当前查看者和选中的屏幕有效。双方显示对方身份；共享方持续看到「某某正在查看你的屏幕」及停止入口。结束后清除授权，再次查看必须重新请求；不设永久信任或自动接受。 |
| F-VIEW-3 查看 | 一对一、单向、单屏，仅传桌面画面。默认自动、起始目标 10 帧/秒（#310），优先文字可读性与适度压缩；负载过高时降帧、不堆积。查看方可适应窗口、按收到图像的 100% 尺寸查看并平移，鼠标键盘只操作本地界面。切换共享屏幕需结束后重新选择。 |
| F-VIEW-4 生命周期 | 任意一方结束、关闭协助窗口、锁屏、休眠、退出、采集失效或连接中断均结束本次会话并释放采集。断网按有限超时结束，恢复网络/解锁不自动续传。结束后查看端清空画面，不保留冻结桌面冒充实时画面。 |
| F-VIEW-5 负载 | 首版按每节点最多一个待处理或进行中的协助会话设计；忙时明确反馈。只保留当前帧与至多一个最新待发结果，不累计历史帧。共享保持独立于当前聊天选择，聊天和文件传输继续可用。 |
| F-VIEW-6 数据边界 | 画面和会话凭据仅在内存中，不写消息、传输记录、SQLite、缩略图缓存、导出或备份。#312 仅将请求、状态、开始/结束时间和实际协助时长作为本地系统卡片持久保存，随既有聊天记录导出/备份，不含画面、选屏信息或凭据。日志仅含会话 ID、节点 ID、尺寸、字节数、耗时和枚举原因；不记画面、屏幕标题、凭据。明文与原内网信任边界一致，不承诺抵御内网监听或身份伪造。 |
| F-VIEW-7 能力与平台 | 「查看他人屏幕」与「共享本机屏幕」分开声明能力。旧端隐藏入口；新版能力不足或离线时显示具体原因。Win7、UOS/X11、macOS 都须验证持续采集与实际性能；ARM64 Wayland 保留 #289 原生采集保护，暂不开放发屏，收屏另行验证。 |
| F-VIEW-8 本期边界 | 首版不含键鼠控制、音频/摄像头、录屏/保存画面、剪贴板同步、文件拖入远端、多方观看、无人值守、自动重连、公网穿透及与其他远控软件互通。以上能力不预留空实现。 |
| F-VIEW-9 聊天记录（#312） | 每次有效请求在双方私聊各保留一张卡片，原位更新等待、拒绝、取消、超时、进行中及结束状态；记录请求时间，连接成功后记录开始/结束与持续时间。未连接不计协助时长；异常退出的未完成记录显示中断、结束时间/时长未知，不推算离线时段。不增加普通消息未读，不允许转发/撤回卡片；重复控制包不重复插入。 |

锁屏自动结束属于验收要求。Electron 22 的原生锁屏事件不覆盖 Linux；Linux 须读到 DDE/UKUI 的明确锁定状态并建立监视，检测失败的环境不声明能力，详见 tech-design §3.1。低画质不能消除 #289 的采集崩溃。

协议字段、状态与硬上限只在 [protocol §8.3](protocol.md#remote-view) 定义；本地采集、IPC、平台关卡与测试在 tech-design §3.1；用户流程在 ui-design §7.9。当前默认长边 1600、JPEG 0.60；真实办公文字可读性及各目标平台支持结论仍须实机验证。

#### 6.11.1 流畅度档位（决议 #310）

用户确认按多档方案开发：**默认自动**，10 帧作为起始目标和最高档；会话内切换即时生效，每次新会话重置为自动，不增加持久配置。

| 模式 | 目标帧率 | 适用情形 |
|---|---|---|
| 自动（默认） | 起始 10，在 10 / 5 / 3 帧/秒间调整 | 根据实际采集/编码/网络/解码整轮耗时降档，持续稳定后再升档 |
| 节省资源 | 3 帧/秒 | 老电脑、虚拟机或正在运行较重的办公任务 |
| 标准 | 5 帧/秒 | 主要查看报错、设置和表格，兼顾操作连贯性与资源占用 |
| 流畅 | 10 帧/秒 | 希望更容易跟上鼠标移动、菜单展开和滚动 |

各档共用长边 1600、JPEG 0.60 的默认画质及相同的超大帧降质策略，**档位只调整帧率**。手动档固定目标，实际吞吐仍受负载约束；自动档依据采集、编码、网络、解码与 IPC 的整轮耗时调节，不按 CPU 型号或内网环境猜测。工具栏显示目标帧率；最小化查看窗时暂降至 3 帧目标，恢复后回到所选模式。

## 7. 非功能需求

| 维度 | 指标 |
|---|---|
| 规模 | 1000 在线节点：列表流畅滚动/搜索，发现协议无广播风暴 |
| 发现时延 | 同网段上线后 ≤ 3s 互相可见；跨网段（gossip）≤ 30s |
| 消息可靠性 | 不静默丢消息：ACK + 重试 + 持久化补发队列；送达状态真实可信 |
| 传输性能 | 千兆内网单流 ≥ 80 MB/s（受写盘限制时以磁盘为准） |
| 资源占用 | 常驻内存目标 ≤ 300 MB，空闲 CPU ≈ 0%，安装包 ≤ 100 MB |
| 数据安全 | 历史库与队列崩溃不损坏（SQLite WAL）；卸载可选择保留/清除数据；本地为明文存储——不做库加密（与决议 #5 同理），依赖系统账户隔离；诊断日志只记元数据、永不记消息正文（决议 #22） |
| 兼容性 | 严格遵守 README「开发红线」（Chrome 108 / Node 16.17 基线、三平台冒烟） |
| 隐私 | 零遥测、零外网请求；不做传输加密，内网明文（决议 #5） |

## 8. 参考软件功能对照

> 凭公开资料与使用印象整理，协议细节以 `references/` 下源码为准。✅ 有 ／ ➖ 部分/变通 ／ ❌ 无。

| 功能 | ipmsg | 飞秋 | 内网通 | iptux | 茶话间（取舍） |
|---|---|---|---|---|---|
| 自动发现 | ✅ | ✅ | ✅ | ✅ | ✅ P0 |
| 跨网段 | ➖ 手动 | ➖ | ✅ 散播 | ➖ | ✅ P0（手动+扫描+gossip） |
| 部门/分组 | ➖ 自报组 | ✅ | ✅ 组织架构 | ✅ | ✅ P1（自报+预置配置） |
| 离线补发 | ❌ | ❌ | ✅ | ❌ | ✅ P0（发送端暂存） |
| 图片消息 | ➖ | ✅ | ✅ | ✅ | ✅ P0 |
| 内置截图 | ❌ | ✅ | ✅ | ❌ | ✅ P0 |
| 群聊/讨论组 | ➖ 多选群发 | ✅ | ✅ | ➖ | ✅ P0（≤50人） |
| 文件/文件夹 | ✅ | ✅ | ✅ | ✅ | ✅ P0 |
| 断点续传 | ❌ | ❌ | ✅ | ❌ | P1 |
| 消息加密 | ✅ RSA+AES | ➖ | ➖ | ❌ | ❌ 不做，内网明文 |
| 公告/全员广播 | ❌ | ❌ | ✅ | ❌ | ➖ v1 多选群发；公告 P2 |
| 远程协助 | ❌ | ❌ | ✅ | ❌ | 已实现只读桌面查看（#310）；键鼠控制继续远期评估 |
| 共享文件柜 | ❌ | ➖ | ✅ | ✅ 共享管理 | 远期评估（决议 #7） |
| 历史记录 | ➖ log | ✅ | ✅ | ✅ | ✅ P0 |

## 9. 决议记录

### 第一轮（2026-06-10，需求轮）

| # | 问题 | 决议 |
|---|---|---|
| 1 | 已读回执 | **不做**，消息状态只到"已送达" |
| 2 | 头像 | v1 = 昵称色块 + 内置头像集；自定义图片头像 P2 |
| 3 | 公告/全员广播 | v1 只做"多选收件人群发"；公告 P2，待权限机制想清楚 |
| 4 | 对方离线时发文件 | 提示不在线、不入队；"上线提醒重发"P2 |
| 5 | 传输加密与协议取舍 | **不做加密**，内网明文；协议**借鉴 ipmsg/iptux 的机制**（广播发现、UDP+ACK 消息、TCP 文件，实现可对照 `references/` 源码），**报文格式自有**（JSON），不与飞秋/ipmsg 互通，兼容模式留远期 |
| 6 | 保留策略 | 历史永久、可手动清理；补发队列 7 天 / 200 条 |
| 7 | 内网通特色功能 | 远程协助、共享文件柜、语音对讲：**后续版本评估**，不进 v1 规划 |
| 8 | 分组来源 | 用户在"用户信息设置"页自填分组字段，联系人列表树形显示；IT 预置 JSON 降为 P2（层级后经第三轮 #10 升级为三级） |

### 第二轮（2026-06-10，协议轮）

协议层 6 项决议见 [protocol.md](protocol.md) 第 11 节：心跳 30s/90s、图片自动接收 20MB（用户指定）、仅 IPv4、多网卡默认全接口、v1 不做签名、端口 17878/17879 拍板。

### 第三轮（2026-06-10，UI 轮）

| # | 问题 | 决议 |
|---|---|---|
| 9 | 主窗布局 | **三栏**：导航栏 + 列表栏（会话 / 通讯录两个菜单分离）+ 聊天内容区；整体参考微信设计语言，简约克制不老气 |
| 10 | 分组层级 | 升级为**公司 ▸ 部门 ▸ 团队**三级折叠树（修订第一轮 #8 的两级） |
| 11 | 联系人保留与刷新 | 历史在线过的节点**持久保留**（离线灰显）；资料随上线/变更/心跳版本号**自动刷新**，防"机器换人" |
| 12 | 搜索 | 两个列表页均可搜**全量联系人**；会话页搜索还覆盖**聊天记录与文件关键词**，分类结果可跳转定位（微信式） |
| 13 | 交互参照 | 收文件卡片、群聊发起（从单聊加人/多选联系人）、托盘点击直达对应会话，均参考微信 |
| 14 | 设置页范围 | 用户点名：IP 范围、通知弹窗、用户信息、自启、截图键、高级（网卡/端口）、聊天记录存储位置、文件保存位置、导出；Claude 补充：关闭行为、字体缩放、提示音、发送键、缓存清理、手动加节点、诊断日志等；明细见 [ui-design.md](ui-design.md) §8 |

### 第四轮（2026-06-10，补充轮）

| # | 问题 | 决议 |
|---|---|---|
| 15 | 规模预算 | 在线节点设计目标 **500 → 1000**，性能与防风暴按 1000 重新核算 |
| 16 | 在线二次校验 | 打开会话时探活刷新状态；发送以 ACK 为准，未确认不算送达（F-DISC-8 新增） |
| 17 | 离线展示 | 灰显之外**加"离线"副文字**，不只靠颜色区分 |
| 18 | 表情 | emoji 面板 P0；**自定义表情包收藏** P1：聊天图片右键添加、保存时自动压缩、面板内管理（F-MSG-7 新增） |
| 19 | 记录导入 | 导出新增迁移备份包格式；**支持导入**——旧机器的"我"自动映射为本机身份、按消息 ID 去重合并，换电脑不丢数据 |

### 第五轮（2026-06-10）

| # | 问题 | 决议 |
|---|---|---|
| 20 | 32 位支持 | **历史决议，已由 #213 覆盖**：当时 Windows 仅出 x64 包，Linux 维持 x64/arm64，macOS 本无 32 位 |

### 第六轮（2026-06-10）

| # | 问题 | 决议 |
|---|---|---|
| 21 | AI 开放接口 | **远期版本**支持本地外部接口：AI 可读取聊天数据、可接管本地聊天（发消息/自动回复）；**当前版本不做**，仅立安全边界并在架构上预留挂接点（见 6.8 节） |

### 第七轮（2026-06-10，查漏轮）

| # | 问题 | 决议 |
|---|---|---|
| 22 | 设计终检查漏 | Claude 终检补齐：① 首次启动向导（P0，防全网"未命名"）② 联系人备注名（P1，修复 README 早有承诺的不一致 + 重名刚需）③ 消息转发（P1）/引用回复（P2）/群内 @（P1）④ 批量开机广播风暴对策（协议 §6.1，统一 VM 集中开机场景）⑤ 日志脱敏（永不记消息正文）⑥ "同一人多设备=多身份"语义明示 ⑦ twemoji 署名合规 ⑧ 投屏自动勿扰（P2）⑨ 截图可选隐藏主窗 ⑩ 本地明文存储声明 |

### 第八轮（2026-06-11，v0.4 撤回轮）

| # | 问题 | 决议 |
|---|---|---|
| 23 | 消息撤回语义 | **2 分钟内可撤回自己的文本 / 群文本消息**（#63 起放宽为 5 分钟）；撤回后本端与对端均隐藏原消息，只保留系统提示行；撤回指令复用可靠消息通道，离线对端随补发机制送达；图片、文件、表情撤回留后续扩展 |

### 第九轮（2026-06-11，P1 设置默认值轮）

| # | 问题 | 决议 |
|---|---|---|
| 24 | P1 系统设置默认值 | 开机自启默认开、可关闭；关主窗默认最小化到托盘、可改为退出；截图快捷键默认 `Ctrl/Cmd+Alt+A`，显示/隐藏主窗默认 `Ctrl/Cmd+Alt+P`，快捷键留空即禁用；提示音默认关；通知内容预览默认开、可隐私遮罩；深色主题先做手动切换；字体缩放限定 100% / 110% / 125% |

### 第十轮（2026-06-11，UI 图标与群管理轮）

| # | 问题 | 决议 |
|---|---|---|
| 25 | 系统图标 | 系统按钮、工具栏、状态与文件类图标全部改为项目内自绘线性 SVG；不再用 emoji / 彩色系统字形充当 UI 图标。emoji 仅作为用户可发送的消息内容保留。 |
| 26 | 主窗口标题 | 首次启动向导完成或资料保存后，主窗口与页面标题显示为 `<用户名>-🍵Teahouse`；未完成向导前仍显示“茶话间”。 |
| 27 | 讨论组管理权限 | 建群记录创建 IP；创建时可选管理密码。填密码则成员输入密码后可管理群；不填密码则仅创建 IP 可管理。群管理指改名、添加成员、移出他人；成员自行退组不需要管理权限。 |

### 第十一轮（2026-06-12，头像模板与设置图标修正）

| # | 问题 | 决议 |
|---|---|---|
| 28 | 头像模板与设置入口识别 | 头像模板调整为 20 个亲和动物 emoji 图标 + 背景色选择，并保留昵称首字模式；设置入口图标重画为明确齿轮，避免误读成太阳。 |

### 第十二轮（2026-06-12，左侧导航图标修正）

| # | 问题 | 决议 |
|---|---|---|
| 29 | 左侧导航图标 | 仅重画主窗口左侧导航里的聊天/通讯录图标，参考用户给定样式调整图标本体；导航栏仍保持既有 56px 茶青侧栏、按钮尺寸与交互，不改整条容器。 |

### 第十三轮（2026-06-12，讨论组创建搜索与密码提示）

| # | 问题 | 决议 |
|---|---|---|
| 30 | 讨论组发起流程 | 建群弹窗改为两步：第一步用联系人快速搜索替代组名输入，支持几百人规模下按昵称、备注、组织、IP 即时过滤；第二步再设置组名、管理密码。管理密码必须输入两次一致才可创建，并可填写密码提示；提示随群元数据同步、备份和展示，密码明文仍不入库不入协议。 |

### 第十四轮（2026-06-12，设置页信息架构重排）

| # | 问题 | 决议 |
|---|---|---|
| 31 | 设置页 UI 结构 | 设置页按常见 IM / 桌面软件规范重排为：账号资料、通用外观、消息通知、聊天与文件、网络连接、快捷键、安全与高级、关于。右侧内容统一为“页头说明 + 分组面板 + 设置行”，表单项提供说明文本、即时保存反馈、空态与禁用态；不再按实现先后把控件平铺。 |

### 第十五轮（2026-06-12，群聊媒体轮）

| # | 问题 | 决议 |
|---|---|---|
| 32 | 群聊图片/文件传输语义 | 群聊图片/文件不新增服务器或群组数据面，复用现有点对点 `file-ctl` + TCP 拉取式文件通道。发送端为每个在线群成员创建独立 transfer，并在 offer 中携带 `groupId/groupRev`；收端据此把本地消息写入群会话。离线群成员遵守决议 #4，不做文件离线队列；发送端 UI 以一条群消息汇总多条点对点传输状态。 |
| 33 | 群聊图片大小与手动接收 | 群聊图片内联上限收紧为 **10MB**。单图 ≤10MB 仍按图片消息投递；超过 10MB 自动按普通文件发送，接收端显示文件卡片，必须由成员手动点击接收后才开始传输，用成员分散接收来错开大群流量高峰。单聊图片仍沿用决议 #2 的 20MB 上限。 |

### 第十六轮（2026-06-12，会话内历史搜索轮）

| # | 问题 | 决议 |
|---|---|---|
| 34 | 群聊/私聊内搜索历史 | 在聊天输入区工具条最右侧新增“历史搜索”图标按钮，群聊与私聊一致。点击后弹出小浮层，搜索范围固定为当前会话；支持关键词搜索，并可筛选图片、文件、日期范围。结果点击后复用现有消息上下文加载与高亮定位，不改变线上协议、不新增外网或服务端能力。 |

### 第十七轮（2026-06-12，会话内历史搜索 UI 精修轮）

| # | 问题 | 决议 |
|---|---|---|
| 35 | 搜索面板尺度与图片结果展示 | 会话内历史搜索不再使用输入区上方的小贴边浮层，改为设置页尺度的居中弹窗，左侧放关键词、类型、日期筛选，右侧展示结果列表。图片命中必须直接展示缩略图，缩略图不可用时才回退到图片占位；不新增协议、不暴露本地文件路径。 |

### 第十八轮（2026-06-12，会话内历史搜索默认展示轮）

| # | 问题 | 决议 |
|---|---|---|
| 36 | 空关键词结果与日期范围选择 | 打开会话内历史搜索时，即使没有输入关键词也显示当前会话最近记录；类型、日期筛选可在空关键词下直接生效。日期筛选改为单月日历范围选择，第一次点击起始日，第二次点击结束日并高亮连续区间，清空筛选同时清除日期范围。 |

### 第十九轮（2026-06-12，私聊头部资料弹窗轮）

| # | 问题 | 决议 |
|---|---|---|
| 37 | 私聊查看对方资料与备注 | 私聊头部的用户名字区域可点击打开对方资料弹窗，展示头像、备注/昵称、组织、IP、主机、平台、在线状态，并允许直接修改本地备注。保存备注复用 `peers:set-remark`，仅本机生效，不产生线上协议报文。 |

### 第二十轮（2026-06-12，输入框 hint 颜色修正轮）

| # | 问题 | 决议 |
|---|---|---|
| 38 | 输入框占位提示视觉层级 | 聊天输入框、搜索框、备注框和设置输入项的 placeholder/hint 统一使用独立浅色 token `--text-placeholder`，颜色低于 `--text-3`，避免占位提示看起来像已输入正文。该改动只影响渲染层样式，不改变消息、搜索或备注保存逻辑。 |

### 第二十一轮（2026-06-12，品牌 logo 轮）

| # | 问题 | 决议 |
|---|---|---|
| 39 | 产品 logo 系统 | logo 使用统一“茶杯气泡”标识：杯身兼具对话气泡轮廓，右侧杯把，上方两笔蒸汽。菜单栏使用单色模板图；界面小 logo 使用茶青 `#3D8B6B` + 暖茶色；空状态 / 大图标使用同一轮廓置于浅底图标面内。logo 只使用本地自绘 SVG / 生成 PNG，不引入远程图片、字体或图标依赖。 |

### 第二十二轮（2026-06-12，联系人资料页交互轮）

| # | 问题 | 决议 |
|---|---|---|
| 40 | 联系人列表点击与资料页形态 | 通讯录中单击联系人仍在右侧展示联系人资料，但资料卡改为更贴合内容区的完整资料页，使用头像区、备注分组、详细信息分组和底部动作区。双击联系人直接进入该联系人的单聊界面，复用现有打开会话与探活流程，不新增协议或存储字段。 |

### 第二十三轮（2026-06-12，左侧主导航图标尺寸轮）

| # | 问题 | 决议 |
|---|---|---|
| 41 | 左侧聊天/通讯录图标视觉权重 | 左侧茶青导航栏保持 56px 宽、按钮热区保持 40px，不改信息架构；仅将聊天与通讯录两个主入口图标从 21px 放大到 25px，设置入口仍保持 21px，避免底部工具入口抢主导航层级。 |

### 第二十四轮（2026-06-12，托盘未读提示轮）

| # | 问题 | 决议 |
|---|---|---|
| 42 | 菜单栏 / 托盘未读数字与跨平台兜底 | 新消息未读总数必须同步到系统托盘提示。macOS 使用 `Tray.setTitle` 显示菜单栏数字，并同步 Dock badge；Windows 使用任务栏 overlay 图标显示未读数字，同时托盘图标闪烁；Linux 因桌面环境差异大，仅尝试 `app.setBadgeCount`，主要以托盘图标闪烁作为可靠兜底。跨平台 tooltip 均显示未读总数，未读清零时清除数字、overlay 与闪烁。 |

### 第二十五轮（2026-06-12，菜单栏 logo 尺寸轮）

| # | 问题 | 决议 |
|---|---|---|
| 43 | 菜单栏单色 logo 视觉尺寸 | 菜单栏 / 托盘单色 logo 继续使用“茶杯气泡”标识，但在 32px 画布内缩至约 82% 内容区，保留透明边距，避免在 macOS 菜单栏和 Windows/Linux 托盘里显得过大。界面彩色小 logo 与大图标不随本轮缩放。 |

### 第二十六轮（2026-06-12，GitHub Actions 发布轮）

| # | 问题 | 决议 |
|---|---|---|
| 44 | GitHub Actions 发布矩阵 | 新仓库启用 `.github/workflows/release.yml`：push 到 `main` 或手动触发时构建可下载 artifact；推送 `v*` tag 时自动创建/更新 GitHub Release。Linux 产物只发 x64，由 `node:18-buster` / Debian 10 容器编译 better-sqlite3 并输出 deb + AppImage，作为 Debian 10 / UOS 20 x64 发布包；Windows 产物由 Windows Server 2022 runner 构建 Electron 22 x64 的 NSIS 安装包与 portable exe，Win7 SP1 x64 真实启动冒烟仍在目标 VM 执行。 |

### 第二十七轮（2026-06-12，Win7 / UOS 真实平台兼容轮）

| # | 问题 | 决议 |
|---|---|---|
| 45 | Win7 / UOS 缺彩色 emoji 字体 | 头像模板和 emoji 面板 / 消息正文显示都不能再依赖系统 emoji 字形。头像从“动物 emoji 字符”改为“同编号本地 SVG 动物图标”，协议字段仍是 `avatar:number`；emoji 面板和消息气泡对内置 emoji 子集使用本地 SVG 兼容渲染，点击、发送、复制仍保留原始 UTF-8 emoji 字符，不引入远程图片、CDN 或外网字体。 |
| 46 | UOS UDP 可发现但消息收发不稳 | 保持现有短文本 UDP+ACK 首选路径，但三次 UDP 退避仍无 ACK 时，发送端自动复用已有 TCP 控制帧再尝试一次；TCP 仍失败才入离线补发队列并标离线。该兜底不新增端口、不改报文字段，专门覆盖 UOS / 部分 Linux 桌面或内网策略下 UDP 可发现但可靠消息 ACK 不稳定的环境。 |

### 第二十八轮（2026-06-12，头像美术资源修正轮）

| # | 问题 | 决议 |
|---|---|---|
| 47 | 本地自绘头像辨识度不足 | 头像和内置 emoji 子集改用 Twemoji SVG 原图子集，作为本地静态资源打包进应用；运行时不访问外网、不加载 CDN。Twemoji 图形为 CC-BY 4.0，源码 README、第三方声明文件和设置 About 页均保留署名。 |

### 第二十九轮（2026-06-12，输入框 emoji 兼容补齐轮）

| # | 问题 | 决议 |
|---|---|---|
| 48 | 输入框编辑态仍依赖系统 emoji 字体 | 聊天输入框在草稿包含内置 emoji 子集时，使用同一套 Twemoji 本地 SVG 做镜像渲染层；底层仍保留原生 textarea 负责键盘、选区、粘贴与发送，协议、复制、存储继续是原始 UTF-8 emoji 字符。该实现不引入远程资源、新依赖或 contenteditable 风险。 |

### 第三十轮（2026-06-12，沉浸式窗口与头像编辑器轮）

| # | 问题 | 决议 |
|---|---|---|
| 49 | 系统标题栏与界面割裂、观感差 | 主窗口与设置窗口改为沉浸式无标题栏（用户拍板）：macOS 用 `titleBarStyle: 'hiddenInset'` 保留原生红绿灯；Windows / Linux 用 `frame: false` 并在右上角自绘最小化 / 最大化 / 关闭按钮（设置窗仅关闭）。窗口顶部 32px 为隐形拖拽带，各栏内容相应下移避让；页面内弹层 z 高于拖拽带不受影响。不使用透明窗口与系统特效，Win7（已禁硬件加速）与 UOS 行为不变；截图窗本就 frameless，不动。 |
| 50 | 设置页头像编辑区版式杂乱 | 头像编辑器重排（用户拍板）：左侧 64px 大预览 + 当前样式文字，右侧「图标头像 / 昵称首字」分段切换；下方"图标"网格（10×2，悬停放大、选中描边）与"背景色"色板（24px 圆点、选中带白点）分节展示。数据编码不变（仍是图标下标 × 背景色下标组合编号，决议 #45/#47）。 |

### 第三十一轮（2026-06-12，沉浸式跨平台修正轮）

| # | 问题 | 决议 |
|---|---|---|
| 51 | macOS 红绿灯横跨 56px 导航栏分界线，视觉割裂 | 主窗红绿灯三钮总宽约 52px，56px 茶青导航栏内放不下；`trafficLightPosition` 移至列表栏顶部留白区（x=68, y=10），与列表搜索框左对齐、背景单一不跨色块。设置窗侧栏 174px 足宽，红绿灯保持 (12, 10) 不动。 |
| 52 | UOS（Linux/DDE）下双击联系人、发起讨论组等点击无反应，Win7 / macOS 正常 | 判定为 Electron 在 Linux 上 CSS 拖拽区（`-webkit-app-region: drag`）命中区域计算不可靠的已知问题（受桌面环境与缩放影响，可吞掉客户区鼠标事件）。对策：Linux 上完全弃用 CSS 拖拽区——拖拽带改为 JS 实现（Pointer Capture + 主进程 `win:begin-drag`/`win:end-drag` 光标跟随移窗，双击经既有 `win:toggle-maximize` 最大化/还原）；macOS / Windows 维持系统原生 / CSS 拖拽区不变。若 UOS 真机复测仍异常，预案为 Linux 回退系统标题栏（frame: true）。 |
| 53 | UOS 安装新包提示"已安装同样版本"被拒装 | **每轮迭代必须递增版本号**（用户拍板）：每个增量 commit 同步把 `package.json` version 的 patch 位 +1（本轮起补齐为 0.5.18），deb/NSIS 升级判断与产物文件名都以它为准；版本号纳入 AGENTS.md 提交约定与验证清单，任何代理交付前自查。 |
| 54 | UOS20 启动时报 `GLIBC_2.29`，存储退化为发现-only | UOS20 的 glibc 基线为 2.28，Linux 发布包内 `better_sqlite3.node` 不允许依赖更高 GLIBC。CI Linux job 继续在 Debian 10 / buster 容器内源码编译 better-sqlite3，但 electron-builder 必须关闭二次 `npmRebuild`，避免打包阶段重新安装 glibc 2.29 的预编译 native 包；新增 `scripts/check-native-glibc.cjs`，在源码重建后和最终 `app.asar.unpacked` 内各检查一次，最高 GLIBC 符号超过 `GLIBC_2.28` 即失败。 |
| 55 | UOS20 终端启动持续报 GPU `ContextResult::kTransientFailure` | Linux 桌面（UOS/Debian 目标机多为国产 GPU 或旧驱动）与 Win7 同样列为软渲染优先平台：**Linux 默认禁用硬件加速**（`app.disableHardwareAcceleration()`），消除 GPU 进程命令缓冲失败噪音；2D 聊天界面软渲染流畅度无感知差异。Win7 原策略不变，macOS 保持硬件加速。 |

### 第三十二轮（2026-06-12，Win7/UOS 桌面体验修复轮）

| # | 问题 | 决议 |
|---|---|---|
| 56 | 输入框 emoji 镜像与光标的对齐 / 大小矛盾（Win7/UOS 缺字平台字符槽过窄：图标按槽缩则"特别小"，固定大则错位重叠） | **等宽空白字形字体为终局方案**：构建期生成 `PantryEmojiBlank` 字体（脚本 `scripts/gen-emoji-blank-font.mjs`，开发依赖 opentype.js，产物 ttf 提交仓库），cmap 覆盖内置 emoji 子集全部码点——基础码点 advance 固定 1.3em、`FE0F` 变体选择符零宽；该字体仅置于聊天输入框 textarea 与其镜像层 font-family 首位（其余 UI 不受影响）。自此 textarea 内 emoji 字符在三平台都精确占 1.3em，镜像 Twemoji 图标（`min(1.3em, 100%)`）恰好满槽：与光标逐字符对齐、连续输入不重叠、与消息正文同尺寸。新增测试校验字体 cmap 覆盖 `COMPAT_EMOJIS` 全部码点，防"加表情忘更新字体"。字体缺失时优雅降级回按真实字符测宽（对齐优先、图标随槽缩小）。 |
| 57 | UOS 默认快捷键被系统占用（深度截图 Ctrl+Alt+A），且设置页只能手填 accelerator 字符串、无注册结果反馈 | 快捷键设置完善（用户拍板）：① 设置页改**录制式输入**——聚焦后直接按组合键自动填入（要求至少一个修饰键 + 主键），Esc / 退格清空表示禁用；② 主进程把每个全局快捷键的**注册结果**随 `SettingsView` 回传（`shortcutStatus`），注册失败（系统占用）时设置行下方红字提示更换组合；③ 提供**恢复默认**按钮一键回到出厂组合；默认组合常量收敛到 `shared/ipc.ts` 单一来源。 |
| 58 | UOS 开始菜单 / 任务栏显示系统默认图标；Win7/UOS 托盘单色线条图标辨识度差（"太丑"） | Linux 桌面图标三板斧：① deb 改装**多尺寸 hicolor 图标**（`build/icons/linux/` 16–512px，由品牌 png 生成）；② desktop 文件补 `StartupWMClass` 关联运行窗口；③ Linux 上 `BrowserWindow` 显式设置窗口 icon（extraResources 随包分发 256px png），任务栏图标不再依赖桌面环境关联。托盘图标 Win/Linux 改**彩色填充版**（茶青圆角块 + 白色茶杯线条，未读闪烁帧同步彩色化），macOS 保持单色 template 适配菜单栏；overlay 数字角标不变。 |
| 59 | Win7/UOS 点窗口关闭按钮直接退出，未进托盘（macOS 正常） | 根因：自绘关闭按钮调用 DOM `window.close()`，Electron 对渲染层发起的关闭走 `CloseImmediately` 路径——**绕过主进程 `close` 事件**，"关闭进托盘"拦截逻辑无机会执行，窗口销毁后应用随 window-all-closed 默认行为退出。修复：新增 `win:close` IPC，自绘关闭按钮统一改走主进程 `BrowserWindow.close()`（标准流程触发 close 事件 → 拦截 → 隐藏进托盘）。**渲染层永远不要调用 DOM `window.close()` 关闭 Electron 窗口**（已写入 tech-design 防回归）。 |
| 60 | UOS 安装报"新建硬链接 /opt/茶话间/…/pantry.png 出错：粘贴子进程被信号（断开的管道）终止"；用户要求安装路径不得出现中文 | 两个根因合修（用户拍板）：① **安装路径全 ASCII**——`productName` 改为 `Teahouse`（Linux 装 `/opt/Teahouse`、Windows 默认装 `Teahouse`），**显示名保持「茶话间」**：Linux desktop `Name=茶话间`、NSIS `shortcutName=茶话间`、mac `CFBundleDisplayName/CFBundleName=茶话间`；主进程启动最早处 `app.setName('茶话间')` 固定 userData 目录与通知应用名，**已有用户数据零迁移**；deb 包名仍为 `pantry`，dpkg 升级自动清理旧 `/opt/茶话间`。② **deb 内禁止硬链接条目**——窗口图标 extraResources 改用独立物理文件 `build/icons/window-icon.png`（不与 hicolor 图标同 inode），`dist:linux` 与 CI 均设 `USE_HARD_LINKS=false` 关闭 electron-builder 复制硬链接优化；CI 新增校验步骤：解开 deb 的 data.tar，发现硬链接条目或中文路径即失败。 |

### 第三十三轮（2026-06-13，Win7/UOS 体验修复与功能调整轮）

| # | 问题 | 决议 |
|---|---|---|
| 61 | Win7 下输入框首行文字 / placeholder 顶部被裁几像素；聊天滚动条闪烁抽动（内容不抖） | 两个 Win7 软渲染细节合修：① **首行遮挡**——`line-height: 1.5` 的顶部 half-leading 在微软雅黑（ascent 偏高）下不足，首行字形与 hint 顶被裁；`.input` 与 `.input-mirror` 同步加 `padding: 5px 0 0`（镜像 padding 必须与 textarea 完全一致，否则 emoji 与光标错位）。② **滚动条抖动**——Win7 软渲染下系统 overlay 滚动条淡入淡出重绘抽搐；全局改用 Chromium 自绘经典滚动条（`tokens.css` 的 `::-webkit-scrollbar` 自定义：10px 槽 + 透明 border 内缩细 thumb，深浅主题分色），三端观感统一且稳定。 |
| 62 | 多选群发功能无实际沉淀价值、易滥用 | 取消消息群发（用户拍板）：建讨论组即可满足"给多人发同样内容"的诉求，群发只是拆成多个单聊逐发、不留沉淀。停用方式：注释 `App.vue` 群发入口（import / ref / 按钮 / 挂载）并备注，`MassSender.vue` 保留全文留痕（已无引用）；底层本就复用单聊 `sendText`、无群发协议或 IPC，无需后端清理。原决议 #3 的"多选群发"与功能矩阵相应标注为已取消。 |
| 63 | 撤回窗口 2 分钟偏短；右键"撤回"无剩余时间提示 | 撤回窗口 2→5 分钟（`recallWindow` 常量改 `5 * 60_000`；主进程不另设时间校验，沿用前端 UI 限制）。右键菜单"撤回"项实时显示剩余时间「撤回（mm:ss）」、每 0.5s 递减；超时后该项变灰禁用、文案「撤回（超时）」。仅"我的未撤回文本消息"显示该项（超时也显示便于知晓），他人消息不显示。 |
| 64 | 整套 logo / 图标偏单薄、"有点丑" | 重设计为「饱满扁平茶杯气泡」（用户拍板方向）：茶青实心圆角杯身兼作对话气泡（左下伸出气泡尾）+ 暖茶汤面 + 杯把 + 双蒸汽。三件套 SVG（icon 含浅茶青圆角方块底 / small 彩色小标 / mono 单色线条）与 `PantryBrandLogo.vue` 同造型。新增 `scripts/gen-app-icons.mjs`：rsvg-convert 渲染高清主位图 → png2icons 出 `.ico`（BMP，兼容 Win7）/ `.icns`，链式重跑 gen-linux-icons 出 hicolor 多尺寸 + 窗口图标。托盘沿用决议 #58 彩色填充版（32px 极小、同色系，不重构 SDF 闪烁帧）。png2icons 为纯 JS 开发依赖。 |
| 65 | 内网各机系统时间常年不准，消息显示时间跟着发送方的钟走、会乱跳 | 接收侧时钟偏移矫正（用户选定方向）：复用每条报文已带的发送方时间戳（`Envelope.ts`，**零协议改动**），新增 `src/main/net/peer-clock.ts`（PeerClock，零 Electron）——收到「实时」报文（发现层 entry/alive/profile/presence + 非补发聊天消息）时以「本机收到时刻 − 报文时间」EMA 估出各节点时钟偏移；chat/groups 入库对方消息时把显示时间换算到本机钟（`correct`），上界钳到本机当前杜绝"来自未来"。效果：全部消息按本机一把尺子、时间一致且近似真实，离线补发的旧消息也按本机钟还原对方发出时刻。**消息排序不受影响**（仍由本地 seq 兜底，决议 #23）。本轮覆盖单聊文本 / 群文本 / 会话列表时间；图片·文件卡片时间暂沿用原值（同一入库点后续可扩）。 |

### 第三十四轮（2026-06-13，Win11/mac 桌面细节轮）

| # | 问题 | 决议 |
|---|---|---|
| 66 | Win10/11 通知顶部显示默认应用名 "electron.app.Teahouse"；群消息通知标题是发送人而非群名 | ① **Windows AUMID**：启动设 `app.setAppUserModelId('com.pantry.app')`（= appId，与 NSIS 安装快捷方式一致），toast 顶部应用名显示「茶话间」。② **群消息通知**改为标题=群名（@到本机时「群名（有人@你）」）、正文「发送人：内容」（微信式）；单聊仍标题=发送人昵称、正文=内容。隐私预览关闭时正文为「收到一条新消息」。 |
| 67 | 群信息面板只占消息右侧、把消息往左挤；成员无头像 | 群信息面板改为**绝对定位覆盖右侧一整列**（相对 `.chat`，top/bottom 0、宽 248px、左边框 + 阴影、z 高于消息），打开时盖在消息上方而非挤压布局；顶部留 32px 让出沉浸式拖拽带。成员列表（含"添加成员"候选）每项显示**紧凑圆形头像**（26px，离线灰显），替代原在线小圆点。 |
| 68 | mac 红绿灯移到列表栏顶部（决议 #51 的 x=68）后看着奇怪、与三栏 UI 不协调 | 修正 #51：导航栏由 56→68px 容纳标准红绿灯，`trafficLightPosition` 改回左上角 `{x:9,y:7}`（贴左上、贴合窗口圆角视觉中心、落在导航栏顶部留白拖拽带内、不再横跨列表栏），贴合微信三栏式观感；导航栏内容 padding-top 38 让出红绿灯。设置窗红绿灯 (12,10) 不变。Windows/Linux 自绘按钮不受影响。 |
| 69 | macOS 未打包（CI 只有 Win/Linux），用户要 M 系列包 + 以后三平台默认都打 | CI 新增 `macos-arm64` job（`macos-14` runner、原生 arm64）：npm ci → 五连 → `dist:mac` → dmg/zip；package.json mac target 显式 `arch:['arm64']`。GitHub Release（tag 触发）改为 Win7 / Debian·UOS / **macOS arm64** 三平台齐发。mac 包未签名/未公证（内网自用，首次右键「打开」绕 Gatekeeper）。Intel x64 / universal 列后续（M 系列为当前主力）。 |
| 70 | mac 红绿灯压在茶青深色侧栏上显丑 | 导航栏底色由茶青深色改**中性浅灰**（用户选定·微信式）：新增 token `--rail-bg`（light `#edeef0` / dark `#1a1d1b`，比列表栏略深 + 右细线分隔），系统红绿灯落在浅色上自然；图标默认深灰（`--text-2`）、选中/hover 茶青高亮（`--primary-weak` 底 + `--primary` 字）。茶青主色保留在选中态、气泡、按钮，品牌不丢。 |
| 71 | 会话列表选中态太淡（茶青 12% 透明、与 hover 难分），列表不精致 | 对齐微信：选中当前会话整条**实心茶青底 + 白字**（名称白、时间/预览/旗标半透白、未读角标白底茶青字），一眼看清当前会话；hover 用淡灰（`--line`）区分。茶青主色在此强化品牌选中态。 |
| 72 | mac `npm run dev` 时 Dock 显示 Electron 默认图标 | 属正常现象（dev 跑未打包 Electron，Dock 用其默认图标；打包版靠 `.app` 内嵌 icns 显示茶杯）。顺手优化：仅 dev 模式（`!app.isPackaged`）下 mac `app.dock.setIcon` 补设茶杯图标，开发时也显示品牌图标；打包版不覆盖（icns 多尺寸更精细）。Windows/Linux 窗口图标 dev 下本就正确（决议 #58）。 |
| 73 | 版本号递增规则细化（用户拍板） | 在决议 #53「每轮必改版本号」基础上细化幅度：**功能更新 = minor +1 且 patch 归 0**（如 0.6.x→0.7.0）；**bug 修复 / 微调 = patch +1**（如 0.6.2→0.6.3）；遵循语义化版本「高位进位、低位归零」。仍每轮必改版本号（UOS dpkg 同版本拒装，决议 #53 不变）。 |
| 86 | 仓库公开后仍有旧内部仓库名与发布说明残留 | 项目公开仓库统一为 `skyjt/pantry`，README / Release / 本地交接与打包测试文档均以公开仓库为准；旧 `pantry_codex` 仅作为历史工作目录名出现，不再作为 GitHub 远端或用户指引。v0.11.4 作为公开发布整理版本。 |
| 87 | 群聊改名后消息流没有明确上下文，成员只看到头部群名变化 | 群名变化必须在群会话内生成一条幂等系统提示：改名人本机显示「你把群名『旧名』改成了『新名』」，其他成员收到 `group.info` 且本地已有旧名时显示「显示名把群名『旧名』改成了『新名』」。显示名优先本地备注，其次对方昵称，仍不新增协议字段、不做存储迁移，系统消息不进 FTS。 |
| 88 | 私聊顶部副标题的 IP 在部分窗口宽度下仍可能被省略，内网排障时看不全 | 单聊头部副标题里的 IP 地址必须完整展示：副标题不再用 ellipsis 截断，保持单行完整文本；当空间不足时优先让标题区域按内容宽度展示，而不是隐藏 IP。该项只影响本机 UI，不改变协议、发现、通讯录数据。 |
| 89 | 文件消息卡片状态文字单独占一行、信息零散 | 文件卡片改「左信息·右状态」：完成/取消/拒收/失败等状态移到卡片右侧、垂直居中、带状态色徽标，不再单独占行；传输中进度并入大小行，群发速率补回大小行（ui-design §5 同步）。 |
| 90 | 关于页把运行时内核、节点 ID 等开发者信息平铺，普通用户看着杂乱 | 关于页信息分层：默认只露版本、许可（MIT）、源码 GitHub 链接、「纯内网·无服务器·无遥测」安心说明；开发者向运行时信息（Electron/Chromium/Node/节点 ID/Twemoji 署名）收进可折叠「更多」就地展开（Chrome 108 无原生 popover）。新增 info/external 自绘图标（ui-design §8 同步）。 |
| 91 | 关于页虽已分层，但源码链接与「更多」按钮漏写样式（裸链接 + 浏览器默认灰按钮）、品牌区与信息区是两个割裂的盒子，用户反馈「太丑」 | 关于页整合为单卡片视觉重设计（taste-skill 适配本项目原生 CSS / 茶青 / 明暗双主题）：顶部居中品牌标识（圆标 + 茶话间·Pantry + 一句话定位 + shield 信任徽条「无服务器·无遥测·数据不出局域网」）；中部版本/许可/源码键值行（版本号等宽字体右对齐、源码为带 hover 的茶青真链接）；底部「更多信息」全宽折叠按钮 + 灰底开发者信息托盘（淡入、尊重 prefers-reduced-motion）。统一 8px 圆角与茶青单主色，shield 图标并入 PantryIcon。 |
| 92 | ① 关于页品牌标识仍是圆圈里一个「茶」字占位，不是真 app 图标；② 设置里点「保存」只有顶部一闪而过的小字提示，反馈不够明显（用户反馈） | ① 关于页改用真实 app 图标——复用 `PantryBrandLogo` 的 icon 变体（茶杯气泡 logo，与 Dock/任务栏、`build/icons/*` 同造型），`filter: drop-shadow` 跟随圆角方块真实轮廓；② 保存类按钮（资料 / 端口 / 快捷键 / 文件位置）点击保存成功后就地变「✓ 保存成功！」+ 整体确认弹动 + 对勾弹入，3 秒后自动恢复原文案；次要弱底保存按钮成功时提升为实心茶青白字以保证对勾对比，动效尊重 `prefers-reduced-motion`。保存校验失败（如端口非法）不触发成功态，仍走顶部提示。 |
| 93 | 内置大图查看器只有遮罩原图、另存为和关闭，缺少常见看图器的缩放、旋转与平移能力 | 大图查看器升级为桌面图片查看器：全屏暗色遮罩 + 顶部浮动工具条，提供缩小 / 放大 / 适应窗口 / 原始大小 / 左旋 / 右旋 / 另存为 / 关闭；支持鼠标滚轮缩放、拖拽平移、双击在适应窗口与原始大小间切换，键盘 `Esc` 关闭、`+/-` 缩放、`0` 原始大小、`F` 适应窗口、`R` 旋转。所有状态仅在渲染层完成，不新增 IPC、协议或存储；图片源继续走 `pantry-img://transferId`，另存为继续复用 `saveImageAs`。UI 使用茶青单主色、8px 圆角、明暗主题变量与 `prefers-reduced-motion`。 |
| 94 | 全屏覆盖层会挡住聊天和搜索面板，查看图片时影响继续使用主窗口（用户反馈） | 图片查看器改为独立普通窗口：主聊天和历史搜索只通过 `img:open-viewer` 打开/复用图片窗口，不再在主 Vue 树挂全屏遮罩。图片窗口用 `#/image-viewer` 独立入口，保留 #93 的缩放、旋转、平移、快捷键和另存为；窗口可移动/缩放，有系统标题栏，不设 modal/always-on-top，用户可继续操作主聊天。`saveImageAs` 的保存对话框挂在当前图片窗口上；图片源仍走 `pantry-img://transferId`，不暴露本地路径。 |
| 95 | 图片窗口已有系统标题栏关闭按钮，内部再放顶部标题/关闭与菜单显得重复；打开尺寸也应按图片大小而不是固定大窗 | 图片窗口细化为常见看图窗口：系统标题栏标题只显示图片文件名；内部操作菜单改为底部半透明浮层覆盖在图片下方，不再显示内部关闭按钮；加载图片后按当前屏幕工作区 70% 计算初始显示比例——图片宽高都小于 70% 阈值时按 100% 原始尺寸显示，否则按先达到 70% 阈值的宽或高等比缩放，并在底部菜单显示缩放比例。主进程根据图片原始宽高与当前屏幕工作区调整图片窗口内容尺寸，渲染层只负责显示与交互。 |
| 96 | #95 初版在大图/极端比例图片上仍可能因原图布局盒子与 `transform: scale()` 后的视觉尺寸不一致、以及图片窗口最小尺寸过大，出现窗口内多余空白 | 图片查看器缩放改为显式计算 `<img>` 的显示宽高（`naturalSize * zoom`），`transform` 只负责平移和旋转，使布局盒子与视觉图片尺寸一致；图片窗口最小内容尺寸降到系统允许的最低，避免超宽/超高大图按 70% 阈值缩放后被最小窗口尺寸硬撑出空白。用小图、常规大图、超宽图、超高图、超大方图进行截图与 DOM 尺寸验证后交付。 |
| 97 | 用户希望图片查看器先支持类似微信看图复制文字的拖选识别，但暂不做“大爆炸”分词/爆炸选择界面 | 图片查看窗口内建本地 OCR：小图片在加载后自动识别，大图片由底部菜单按钮手动触发；识别完成后在图片上叠加可拖选的文字命中框，用户直接框选并复制选中文字，`Cmd/Ctrl+C` 复制当前选择，底部菜单提供“复制全部”。本轮不做“大爆炸”拆词面板、不做跨图片持久索引、不写入数据库、不发送任何外网请求。OCR 资源必须随应用本地打包（worker/core/lang），默认中英简体，主进程只按已登记 transferId 读取本地图片字节并限制大小，日志不得记录识别文本或图片内容。 |
| 98 | OCR 拖选初版更像框选文字块，鼠标也不是系统文本选择光标，用户希望能像真实文本一样逐字选 | OCR 叠加层交互改为文字选择语义：鼠标移到可选 OCR 区域显示 `text` 光标；Tesseract 有 symbol bbox 时按字符粒度生成 token（中英文都可逐字选），复制时按行和词边界恢复文本，英文词内字符不插空格、词与词之间保留空格，中文字符连续拼接。该改动只影响渲染层 OCR 选择体验，不改变 OCR IPC、协议、存储或语言包。 |
| 99 | 图片双击放大仍按窗口中心放大；OCR 已识别后再次点击或缩放不应重新取图源/跑识别 | 双击从适应窗口切到原始大小时，以鼠标所在图片坐标为锚点反推缩放后的偏移，保证同一图片位置停在鼠标下；快捷键/按钮的原始大小仍保持居中语义。OCR 结果按 `transferId + naturalSize` 缓存在渲染层 OCR 工具内存中，组件启动识别前先查缓存，命中则直接恢复选择层，不再读取图片字节或重新调用 Tesseract；缩放、平移、旋转不得触发 OCR。该改动不新增协议、存储、IPC 或外网请求。 |
| 100 | #99 后用户实测双击仍像从左上角放大；OCR 识别后整张 OCR 透明层吃掉图片拖拽 | 图片查看器改为显式以窗口中心定位图片平面：`left/top = 50% + offset`，`transform` 只做自身居中与旋转，避免 grid 在大图溢出时把元素对齐到左上。双击锚点计算抽成可测几何函数，单测验证放大前后鼠标下的图片坐标不漂移。OCR 层默认不接管整张图片的 pointer 事件，只允许文字 token 和复制按钮接收事件；在无文字区域拖拽仍由图片查看器平移处理。该改动只影响图片查看器渲染层。 |
| 101 | OCR 文字拖动选择时仍出现一个矩形框，用户希望只有类似文字光标拖选的效果 | OCR 选择交互取消可见矩形框选层：按按下字符 token 与当前拖到的字符 token 形成连续 token 范围，只高亮被选中的字符背景，不再渲染 `ocr-selection` 矩形。未选中文字不显示淡框/描边，选中态去掉边框阴影，视觉更接近真实文本选择。空白区域仍穿透给图片拖拽。 |
| 102 | 每次重新打开图片都需要再次 OCR，用户要求同一图片识别过一次后以后打开都能直接选择文字 | OCR 结果缓存从图片窗口 renderer 内存上移到主进程会话级内存缓存：识别完成后按 `transferId + naturalSize` 保存归一化 OCR 结果；图片窗口加载后先向主进程查询缓存，命中即直接恢复 token 选择层，不读取图片字节、不启动 Tesseract。缓存不写数据库、不做全文索引、不跨应用重启持久化，仍只在本机运行态保存识别文本，符合“不落库、不做大爆炸”的边界。 |
| 103 | OCR 选择位置不准，用户从想要的位置开始选时会把前面不想要的文字也选进来 | OCR 拖选从“包含起止 token”改为“插入光标边界”模型：根据鼠标在字符 bbox 中心线左/右判断光标落在字符前还是字符后，选择两个光标边界之间的 token（半开区间），不再天然包含起点所在字符。拖到字间空隙时按最近行的字符中心线求边界，避免把前一个字符误选进去；补单测覆盖“从 C 前开始只选 C，不选 B”的场景。 |
| 104 | 用户给定新的 Teahouse 品牌参考图，要求替换原有 logo 等素材并优化整体品牌效果 | 品牌标识改为参考图方向的“线性茶杯气泡 + 右杯把 + 左下对话尾 + 三点消息 + 双叶”：应用图标使用白色圆角背板、柔和阴影和茶青/浅叶绿渐变；彩色小标、空状态、关于页与打包图标复用同一轮廓；macOS 菜单栏继续使用约 82% 内容区的单色模板；Windows/Linux 托盘继续使用茶青圆角块 + 白色线性标识，并让未读闪烁帧保持同一缩放，避免图标跳动。全部素材仍为本地 SVG / 生成 PNG，不引入外网图片、字体或新依赖。 |
| 105 | 图片上 OCR 逐字拖选连续多轮仍难以选准，用户要求取消图片覆盖拖选，改为点击识别后弹出文本窗 | 图片 OCR 交互从“图片上覆盖 token 选择层”改为“独立文本结果窗”：小图仍可自动 OCR，但不自动弹窗；大图或用户主动点击底部 OCR 按钮后识别，识别完成或缓存命中时打开无全屏遮罩的文本结果窗。文本窗内使用原生 textarea 展示全部 OCR 文本，用户用系统文本选择 / 复制；底部菜单保留“复制全部文字”快捷按钮。图片上不再渲染 OCR token、hover 命中框、拖选高亮、局部复制按钮，也不再由 OCR 层接管 pointer 事件，图片拖拽 / 缩放 / 旋转始终按看图器本体处理。OCR 缓存、只读取图源 IPC、本地 Tesseract 资源与“不落库、不联网、不做大爆炸”边界保持不变。 |
| 106 | 用户反馈 #104 初稿仍然偏方，不像参考图里的茶杯；参考图杯身下方是半圆感 | 订正品牌标识轮廓：杯身不得再用 rounded-rect 方杯表达，改为上沿较平、下缘连续半圆下坠的线性茶杯气泡；左下对话尾与杯身外轮廓自然相接，右杯把保持外挑弧线，三点与双叶沿用 #104。该修正同步三件套 SVG、`PantryBrandLogo`、托盘/未读图标生成逻辑和全部生成位图，目标是在 Dock/任务栏、菜单栏、空状态、关于页都读作“茶杯 + 聊天”。仍不引入外网素材、字体或新依赖。 |
| 107 | 用户提供完整 SVG 压缩包，要求按该套 SVG 接入，而不是继续手动画 | 品牌源改以用户提供的 `pantry-svg-icons.zip` 为准：`build/icons/pantry-logo-icon.svg` 使用 taskbar/dock 版本并生成 `.png/.ico/.icns` 与 Linux hicolor；`pantry-logo-standard.svg`、`pantry-logo-small.svg`、`pantry-logo-menu.svg`、`pantry-logo-mono.svg`、`pantry-mark.svg`、`pantry-horizontal-logo.svg` 保留为可审阅 SVG 源；渲染层 `PantryBrandLogo` 直接加载 renderer assets 里的 SVG，不再手写 SVG path；`scripts/gen-tray-icon.mjs` 从 `pantry-logo-menu.svg` / `pantry-logo-mono.svg` 光栅化生成 Data URL，并导出彩色 RGBA 底图给未读闪烁帧叠角标，保证托盘与 SVG 套件一致。仍不引入外网素材、字体、远程请求或新 npm 依赖。 |
| 108 | UOS20 系统通知显示 emoji 方框，且通知图标显示为系统默认图标 | 桌面通知正文不再直接依赖系统 emoji 字体：内置 emoji / 常见 emoji 在通知摘要中统一降级为 `[表情]`，表情包消息显示 `[表情]`、图片显示 `[图片]`、文件继续显示 `[文件] 文件名`。Linux / Windows 创建 `Notification` 时显式传入本地应用图标（开发态 `build/icons/window-icon.png`，打包态 `resources/icons/pantry.png`），UOS 通知中心不再依赖桌面环境从 `.desktop` 反查图标；macOS 仍交给系统 app 图标。该修复不改变协议、存储或聊天气泡内 Twemoji 渲染。 |
| 109 | 用户要求新增私聊震动，并防止连续震动骚扰 | 私聊新增“窗口震动”即时提醒：仅单聊显示入口，协议使用 `msg(kind:"nudge")`，发送成功依赖 ACK；收端只触发本地窗口短抖和轻量提示，不写 `messages`、不产生未读、不进入搜索/导出。震动不做离线补发，发送失败或对方离线即提示未送达。限流按同一对端双向执行：60 秒滑动窗口最多 2 次，且两次至少间隔 15 秒；超过限制时发送端提示剩余等待时间，接收端超限则 ACK 后丢弃动作，避免重传放大骚扰。 |
| 110 | 用户反馈震动收端应精准定位到发起人，且震动消息应像撤回一样有提示 | 修正私聊震动体验：收端收到未限流震动后必须唤起主窗口并自动打开 `single:<发送者 nodeId>` 会话，而不是只停留在原页面；收发两端都写入本地 `messages.kind='system'` 提示行，发送端为“你发送了一次窗口震动”，接收端为“对方发来一次窗口震动”。系统提示行沿用撤回/群变更的居中灰字样式，不写 FTS，不改变可靠 ACK、不离线补发和 15s/60s 限流规则；收端超限仍 ACK 后丢弃本地动作与提示，防止骚扰。 |
| 111 | 切换聊天时消息区滚动位置上下跳动，新开/后台直达又应默认看最新 | 会话滚动位置改为“会话内记忆 + 明确入口贴底”：用户在会话列表间切换时按 convId 记住离开时的 scrollTop，返回后恢复同一位置；若离开时已在底部，则返回后仍滚到最新。首次打开会话、通知/托盘/震动直达、主窗隐藏后恢复并重新点开会话、以及“回到最新”按钮，都强制定位到最新消息。当前会话新增消息时，只有用户本来贴近底部或消息是自己刚发送才自动贴底，避免阅读历史时被新消息拉走。该修复只影响渲染层状态，不新增协议、IPC、存储或外网能力。 |
| 112 | 免打扰会话仍用红色未读角标，且对方震动会把窗口拉出来 | 按会话免打扰的未读数字改为灰色，保留数量但视觉上与普通红色提醒区分；免打扰会话收到 `nudge` 时不得唤起、置前或震动主窗口，只保留本地系统提示行供用户之后查看。Windows 上非免打扰震动若主窗只是被其他窗口遮挡而非最小化，也必须短暂置前到最上层再执行窗口震动，避免只在最小化/隐藏时才弹出。 |
| 113 | 宿主机多网卡/虚拟机网络下，宿主机改群名后虚拟机不更新群名也没有群名变更提示 | 无密码群原来只按创建端 IPv4 校验 `group.info`，但宿主机多网卡时建群保存的 `creatorIp` 可能不是虚拟机实际看到的源 IP，合法改名会被拒绝。群元数据新增 `creatorId`（创建者 nodeId），无密码群远端管理变更接受条件改为“源 IP 匹配创建 IP，或信封发送者等于创建者 nodeId 且 `updatedBy` 也等于该创建者”。SQLite 追加 v9 迁移，旧无密码群用现有 `updated_by` 回填 `creator_id`；有管理密码群仍按密码摘要校验。 |
| 114 | A/B 用户新增不同扫描网段后，希望同一局域网内可自动互相获得扫描范围，但不能诱发全网同时扫描 | 新增 `scan-ranges` 网段记录同步：同步“配置候选”与真正“执行扫描”解耦。节点低频分享本机已保存的合法 CIDR（最大 /22），收端自动加入记录并标注来源；收到后不立即扫描，而是进入随机延迟、12 小时去重、在线规模抽样的后台扫描策略。远端不能删除或覆盖本机配置；用户删除后写入忽略表，避免同步记录反复回来。 |
| 115 | 用户希望在主界面直接刷新所有已配置网段用户，并看到扫描进度 | 左侧导航栏设置按钮上方新增全局刷新按钮；点击后扫描 `config.scanRanges` 中全部合法 CIDR 的去重主机地址，进度条按已投递探测地址推进。该功能是显式手动扫描，不走自动同步扫描的延迟和抽样限制；若上一轮仍在运行，按钮禁用，避免并发扫描造成局域网压力。 |
| 116 | 左侧导航图标只有图形，用户希望鼠标悬停后出现中文说明；悬停自己头像时希望看到自己的信息框 | 左侧导航栏按钮统一改为自绘延迟 tooltip：悬停约半秒后淡入中文说明，移出即淡出，避免系统 `title` 抢先弹出。自己头像不显示普通 tooltip，而是在头像右侧浮出个人信息卡，展示头像、昵称、组织路径、主机名和本机节点 ID 短码；信息卡只读、轻量、随鼠标移出收起。该交互纯渲染层实现，不新增 IPC、协议或存储。 |
| 117 | 用户反馈软件刚打开时未悬停头像也会弹出个人信息卡，且信息卡内容过密、可读性差 | 自己头像信息卡只允许鼠标悬停触发，不再响应焦点态，避免启动后焦点落在头像导致误弹。信息卡改为更松的名片结构：顶部头像 + 昵称 + 本机资料标签，下面按组织、主机、端口、节点分组展示，减少小字堆叠，保留单行截断和克制阴影。该修正仍为纯渲染层，不新增 IPC、协议、存储或外网能力。 |
| 118 | 用户反馈左侧导航中文说明浮层显示/消失时能明显看到菱形箭头和圆角矩形分离，视觉突兀 | 左侧导航按钮 tooltip 取消独立菱形箭头，只保留一个单体圆角标签；去掉位移动画，改为约 90ms 的轻量透明度显隐，并保留约半秒悬停延迟。目标是让说明框像桌面软件的低干扰提示，不再出现“菱形 + 矩形”拆件感。该修正只影响 `App.vue` 样式，不新增逻辑、IPC、协议或存储。 |
| 119 | 用户反馈软件一打开就弹出“聊天”中文说明，未移动鼠标也触发 hover 提示 | 左侧导航按钮 tooltip 不再由 CSS `:hover` / `:focus-visible` 直接控制，改为渲染层显式 pointer 状态：只有鼠标在按钮上真实移动后，才启动约半秒计时并显示说明；pointer 离开立即清理计时和显示态。这样窗口刚打开、默认焦点落在聊天按钮、或窗口出现在静止鼠标下方，都不会弹出说明。该修正只影响主界面左侧导航 tooltip 触发逻辑，不新增 IPC、协议或存储。 |
| 120 | Windows Release workflow 中 `npm test` 失败仍继续后续步骤并发布，且通知图标路径测试在 Windows runner 上暴露出 Linux 路径被宿主平台分隔符污染 | Release workflow 的 Windows 五连验证改为 PowerShell 逐条检查外部命令退出码，macOS 五连验证也显式 `set -euo pipefail`，任何本地测试/类型/构建/冒烟失败都必须阻断平台构建与 Release 发布。通知图标路径按目标平台选择 `posix` / `win32` 路径实现，Linux 产物始终使用 `/`，Windows 产物始终使用 `\`，不再受运行测试的宿主系统影响。该修正不改变协议、存储、IPC 或用户可见功能。 |
| 121 | 用户反馈全新启动后左侧仍出现黄色焦点框，根因是系统焦点环，和 tooltip 文案无关 | 主界面启动完成后若焦点落在左侧导航按钮，主动释放该焦点；点击左侧导航、全局刷新、设置按钮后也立即 blur，避免鼠标操作留下系统黄色焦点框。左侧导航按钮显式 `type="button"`，并取消原生 appearance/focus outline；tooltip 仍只由 `pointermove` 延迟触发。该修正只影响 `App.vue` 交互与样式，不新增 IPC、协议、SQLite 或配置迁移。 |
| 122 | 用户希望用 taste 设计原则把全局细节 UI、尤其文字密集处再精修一轮 | 在不改设计语言（仅用现有 token 与茶青主色、Chrome 108 安全、不引组件库）前提下做一轮细节精修：① 首次启动向导首屏改用真实品牌标识 `PantryBrandLogo`（icon 变体）+ 中英文字标，与关于页 / 空状态品牌呈现一致；修正第三步"文件传输功能即将到来"的过期文案（文件传输已是核心能力），改为目录用途说明。② 私聊头部资料浮层在线状态由内嵌「● 在线」文本字形改为与联系人资料页一致的状态点 + 文案，在线/离线对称（补齐决议 #81 在该浮层遗漏的同款修正）。③ 设置页会话标签与传输记录文案分隔符由 ` - ` 统一为全局一致的 ` · ` 中点。④ 全局搜索结果分类标题去掉贴标签的下划线，改用组间分隔线 + 间距节奏，文字密集结果更易扫读。⑤ 表情包空状态文案改为与会话空态一致的两行结构。全部为纯渲染层文案与样式精修，不新增 IPC、协议、SQLite、配置迁移或外网能力。 |
| 123 | 设置窗弹出时居中在屏幕中心，用户希望居中在主窗（聊天软件）的几何中心 | 设置窗创建时按主窗 `getBounds()` 计算居中坐标（主窗几何中心对齐设置窗中心），主窗被拖到副屏或角落时设置窗跟随主窗弹出，而非固定屏幕中心。结果坐标夹取到主窗所在显示器 `workArea`，避免主窗贴边或小屏时设置窗越界；主窗不可用时回退 Electron 默认屏幕居中。该修正只影响 `settings-window.ts` 创建定位，不改协议、存储、IPC 或渲染层。 |
| 124 | 用户认为英文名 Pantry「没有闲聊的感觉」，要求把 APP 英文名整体改为 Teahouse（中文名「茶话间」不变） | 英文品牌名 Pantry → Teahouse，范围限"用户与文档可见的名字"（用户拍板）：① UI 文字——关于页 / 首启向导中英文字标、主窗口标题 `<用户名>-🍵Teahouse`；② 构建产物——`productName=Teahouse`（安装路径 `/opt/Teahouse`、`Teahouse.app`、`Teahouse.exe`，NSIS/portable/deb/AppImage/dmg/zip 产物名、mac `CFBundleName/DisplayName`、deb maintainer、Linux `StartupWMClass=teahouse`）；③ 全部 `.md` 文档里的品牌"Pantry"。**保留内部代码标识不动**（用户选定）：`window.pantry` preload 桥、`PantryApi`/`PantryIcon`/`PantryBrandLogo`/`PantryEmojiBlank` 标识符、`pantry-img`/`pantry-sticker`/`pantry-svg` 协议、`PANTRY_*` 环境变量、icon 文件名 `pantry-logo-*` / `pantry.png`、备份扩展名 `.pantry-bak`、npm 包名 `pantry`、appId `com.pantry.app`（保升级连续性）、GitHub 仓库 URL `skyjt/pantry`（仓库暂不改名，避免链接 404）。**零数据迁移**：userData 仍由决议 #60 的 `app.setName('茶话间')` 按中文名派生，与英文名无关；不改协议、存储、IPC。 |
| 125 | 移除会话希望真正删除聊天内容，且操作前二次确认、删除后给一次反悔机会 | 「移除会话」语义由"只移出列表、不删历史"改为"**删除聊天内容**"（用户拍板）：① 右键菜单点「移除会话」先弹**二次确认**弹窗，明示移除后聊天记录将被删除；② 确认后会话立即从列表隐藏（活动会话则关闭聊天区），底部居中浮出「已删除…」提示 +「撤回」按钮，**倒计时 10 秒**；③ 10 秒内点撤回立即恢复，期间不落库删除；④ 超时才真正删除——新增 `MsgRepo.deleteByConv(convId)` 删除该会话全部消息及其 `messages_fts` 全文索引，再 `ConvRepo.remove` 删会话条目。撤回窗口与计时在渲染层 `chatStore`（`pendingRemoval` + 10s 定时器）实现，后端 `removeConversation` 收到调用即真正落库删除。已知取舍：撤回窗口内若退出应用，删除不落库、会话保留（数据安全优先）。不改线上协议、IPC 签名；新增 SQLite 删除语句但无 schema 迁移。 |
| 126 | 置顶会话和普通会话在列表里外观一样，不好区分 | 会话列表中置顶会话整条加**淡灰底色**与普通会话区分。新增 token `--bg-pinned`（浅色 `#EEF0F2` / 深色 `#262B27`，比 `--bg-list` 略深的淡灰，明暗双主题）；`ConvList` 给置顶项加 `.conv.pinned` 背景，且该规则在 `.conv:hover`（`--line`）与 `.conv.active`（茶青实底白字）之前定义，hover 与选中态仍优先覆盖。纯渲染层 + token，不改协议、存储、IPC。 |
| 127 | 聊天顶栏与左侧列表顶栏高度不一致，视觉不统一；输入框高度希望能手动调节 | 两项 UI 优化（taste 原则，限现有设计语言与 token）：① **顶栏等高对齐**——左侧列表搜索栏由约 70px（`padding 34/8` + 28px 字段）改为与聊天头部 `.head` 等高的固定 **84px** + `padding-top 32px`（让出拖拽带）+ 底部分隔线，两栏顶部分隔线跨栏连成一条。② **输入框可拖拽调高**——`ChatPane` 输入区顶部（消息/输入分隔线处）新增 `ns-resize` 手柄（细灰 grip、hover 变深），按住上下拖改 `.input-shell` 高度，clamp 到 48–320px，向上更高、向下更矮，结果写 localStorage 记忆。两项均纯渲染层，用现有 token，不新增 IPC / 协议 / 存储。 |
| 128 | 会话列表右键菜单弹出后，单击聊天栏那一侧不会关闭菜单（bug） | 原来菜单只靠 `ConvList` 的 `.pane @click` 关闭，该 handler 只覆盖列表栏自身 DOM，点到聊天栏（另一个组件）时不触发，菜单关不掉。改为菜单打开期间在 `document` 挂全局监听：`click`（点窗口任意处含聊天栏）、`contextmenu`（在别处再次右键）、`window` 的 `blur`（窗口失焦）任一发生即关闭，菜单关闭时移除监听，组件卸载兜底清理；菜单本体保留 `@click.stop`，点菜单项不被误关。纯渲染层修复，不改协议、存储、IPC。 |
| 129 | 文件消息卡片左右高低不协调（图标/文件名靠上、右侧操作或状态居中，错位） | 根因：`FileCard` 的 `.card` 用 `display: flex` 但未设 `align-items`（默认 `stretch`），左侧图标与文件名/大小是顶部对齐，右侧 `.tail` 却垂直居中；右侧比左侧高（如 3 个堆叠的接收/另存为/拒绝按钮）时文件名浮在顶部、按钮居中，明显错位。且 `.icon` 是 30px 盒子却塞 36px 字形，溢出挤占与文件名的间距。修复：`.card` 加 `align-items: center` 让图标、文件名/大小、右侧操作/状态共享同一垂直中心线；`.icon` 盒子改 36px 与字形等宽，间距恢复正确。纯渲染层 + 现有 token，不改协议、存储、IPC。 |
| 130 | 聊天消息缓存多处线性查重 / 查状态，且文件/图片发送完成时若已切换会话，回填可能依赖错误的当前会话 | `chatStore` 对已加载会话维护内存级 `MessageCache`（`Set<msgId>` + `Map<msgId, MessageView>`），在分页加载、搜索上下文、回到最新、追加新消息、删除会话时同步重建或清理。`msg:status` 事件用 `Map` O(1) 定位本地消息，追加消息用 `Set` 去重，向上加载历史时过滤重复页。文件 / 图片 / 表情发送返回后不再读取当时的 `activeConv`，而是以主进程返回的 `MessageView.convId` 回填，避免发送请求期间切换会话导致乐观消息插入错误列表。纯渲染层优化，不改 IPC / 协议 / SQLite / UI。 |
| 131 | 全仓库扫描后，高频渲染路径仍有若干重复遍历和临时数组分配 | 做一轮跨模块代码级优化：① `splitEmojiText` 按 emoji 首 UTF-16 单元建立候选表，扫描文本时只检查可能匹配的内置 emoji，避免每个位置遍历完整 emoji 表、每段再对完整表做 `indexOf`；② `peersStore.onlineCount`、`ChatPane` 单聊 peer 查找与群在线收件人数改用既有 Map getter / 单次计数循环，减少联系人规模上来后的重复数组分配；③ `GroupPanel` 添加成员候选把群成员数组转 Set 后判断，避免联系人 × 群成员的嵌套 `includes`；④ `FileCard` 将群发传输的完成/失败/活跃/已接收名单收敛为一次统计对象，状态文字、角标和 tooltip 共用同一结果。纯代码优化，不新增 UI，不改 IPC / 协议 / SQLite / 配置。 |
| 132 | 安全扫描报告指出多处信任边界过宽：渲染层路径可直达主进程、图片 offer 只信任声明总大小、发现/ACK 未绑定源地址、备份导入保留外部路径、CI 构建 job 持有写权限 | 按报告做最小安全修复：① `filePick` 返回的本机路径进入按窗口隔离的一次性授权，`fileOffer`/`imgOfferPath`/群聊对应入口必须消费授权；图片路径发送前复制到应用图片目录，`pantry-img`/OCR/另存/表情提取只读取状态完成且消息类型为 image/sticker、路径位于应用管理媒体目录的传输。② 接收 `file-ctl offer` 时以文件清单重新求和，要求等于 `totalSize`，图片/表情自动接收只看复核后的大小。③ `entry/alive/profile` 要求 `profile.nodeId === envelope.from`，已在线节点不接受不同 UDP 源地址重绑定，可靠消息 ACK 必须来自本轮发送目标 IP+UDP 端口。④ 备份导入无归档媒体时不再保留 JSON 中的 `savedPath`/表情 `path`，备份导出也只打包应用管理目录下的媒体。⑤ Release workflow 默认 `contents: read`，构建 job checkout 不持久化凭证，仅发布 job 显式 `contents: write`。版本 0.21.7 → **0.21.8**（安全 bug 修复，按决议 #73 patch +1）。 |
| 133 | 收到对方窗口震动后主窗已唤起并切到发起人单聊，但消息区停在旧位置/历史，没有定位到最新消息 | 修复私聊震动收端"直达最新"未生效的两处根因：① `chatStore` 在 `scroll:'latest'` 入口（震动 / 通知 / 托盘直达、通讯录与搜索进入、回到最新）打开会话时强制重载最新一页，不再复用可能是历史搜索上下文或不完整的内存缓存，确保贴的"底部"就是真正最新消息（含刚落库的"对方发来一次窗口震动"系统提示行）；会话列表前台切换仍走 `restore` 恢复离开位置，决议 #111 行为不变。② 消息区新增"贴底跟随"：用户处于贴底意图（latest 打开 / 自己发消息 / 本就在底部）时，图片、文件卡片等内容异步撑高后用 `ResizeObserver` 继续贴到底部，避免先滚到底、图片加载后又把最新顶出视口；用户一旦向上翻看历史即停止跟随。纯渲染层修复，不改协议、IPC、SQLite 或外网能力。版本 0.21.8 → **0.21.9**（bug 修复，按决议 #73 patch +1）。 |
| 134 | 消息区往上翻历史时缺一个快速回到最新消息的入口；希望滚得足够远（约两屏）才出现，避免频繁打扰 | 把原本仅搜索/跳转到非最新页（`viewingHistory`）时出现的"回到最新"按钮，升级为消息区右下角的悬浮圆形按钮，并扩展触发条件：① 搜索 / 跳转到历史页时照常出现；② 在最新页内向上滚动、距底超过约两屏（2× 消息区可视高）时出现，回到近底即自动隐藏。按钮为白底圆形 + 茶青向下箭头 + 柔和阴影（只用现有 token、明暗双主题），淡入上移动效复用项目既有缓动，`prefers-reduced-motion` 降级；相对消息区右下角定位，不受输入框拖拽高度（决议 #127）影响。点击：历史页走 `backToLatest` 重载最新页，最新页直接滚到底。纯渲染层，不改协议、IPC、SQLite 或外网能力。版本 0.21.9 → **0.21.10**（UI 增强，按决议 #73 patch +1）。 |
| 135 | 输入框复制网页 / 富文本 emoji 后粘贴失败或被当作截图发送；"回到最新"悬浮按钮 hover 时仍会显得半透明 | 两个 UI bug 合修：① 输入框粘贴仍先处理文件管理器复制出来的真实文件；若剪贴板存在 `text/plain`，一律交给原生 textarea 粘贴，避免 Twemoji / 富文本 emoji 同时携带的 `image/png` 被截图分支抢走；只有无文本、无文件路径的图片剪贴板才按截图位图发送。② "回到最新"按钮 hover 显式保持白底与 `opacity: 1`，覆盖进场透明度，不再出现半透明 hover 态。纯渲染层，不改协议、IPC、SQLite 或外网能力。版本 0.21.10 → **0.21.11**（UI bug 修复，按决议 #73 patch +1）。 |
| 136 | 聊天里的表情消息本身右键没有"复制"，所以无法复制后粘贴回输入框发送 | 表情 / 图片消息右键菜单补「复制」：复用已有受限媒体读取通道 `fetchStickerSource(transferId)` 读取应用管理目录下的 image/sticker 源文件，在渲染层用 canvas 转成 `image/png` 写入系统剪贴板。随后在输入框 Ctrl+V 时，现有"无文本图片剪贴板"分支会按粘贴图片发送。该实现不新增主进程读取口、不放宽路径权限、不引依赖；GIF 表情复制为当前帧 PNG，完整动图复用「转发」或表情面板发送。版本 0.21.11 → **0.21.12**（UI bug 修复，按决议 #73 patch +1）。 |
| 137 | #136 在部分 Electron 环境里用 Chromium `ClipboardItem` 写图片剪贴板后仍无法粘贴回输入框 | 保留 `fetchStickerSource(transferId)` 受限读取与渲染层 canvas 转 PNG，但实际写系统图片剪贴板改为经 preload 窄 IPC `clipboard:write-image` 交主进程 `nativeImage.createFromBuffer` + `clipboard.writeImage`，并立刻 `clipboard.readImage()` 读回确认非空后才提示"已复制"。输入框粘贴路径不变，仍由无文本图片剪贴板分支发送；该修复不放宽媒体读取权限、不新增依赖、不改线上协议。版本 0.21.12 → **0.21.13**（UI bug 修复，按决议 #73 patch +1）。 |
| 138 | #137 已能把表情写入系统图片剪贴板，但输入框 `Command+V` 的浏览器 paste 事件仍可能拿不到 Electron 原生图片剪贴板内容，甚至不派发可用 paste 数据 | 输入框粘贴保持原顺序：真实文件路径优先、`text/plain` 交给 textarea、浏览器 `ClipboardEvent.items` 图片继续发送；同时主进程用 `before-input-event` 捕获主窗 `Command/Ctrl+V` 并通知渲染层，渲染层确认当前聚焦在输入框后，经 preload 窄 IPC `clipboard:read-image` 让主进程读取 `clipboard.readImage().toPNG()`，读到非空 PNG 才按粘贴图片发送。主进程若发现剪贴板有文本则返回 null，避免抢走 emoji / 富文本正文；不新增依赖、不改线上协议。版本 0.21.13 → **0.21.14**（UI bug 修复，按决议 #73 patch +1）。 |
| 139 | 用户希望新增“分歧解决”娱乐能力：输入框上方有 `PK` 入口，可选择骰子或猜拳；发送时像随机 GIF 一样先播放动画再落到随机结果，对方可点击同段消息旁边按钮参与；私聊和群聊都要可用 | 做正式结构化消息而不是普通 GIF 图片：① 新增 `msg(kind:"pk")`，主进程用本地随机数在发送瞬间生成结果，线上载荷只带 `game/result`（群聊带 `groupId/groupRev`），发送前用户不知道结果；② UI 入口为表情后、窗口震动前的 `PK` 小图标，浮层并排两个按钮「猜拳」「骰子」，消息气泡播放约 1.5 秒本地动画后定格；③ PK 是在线小娱乐：单聊仅对方在线时可发，群聊仅发给当时在线的其他成员，离线成员不入队、不补发；发送失败可重试同一结果，不重新随机；④ 对方/群成员点击气泡旁按钮时发送一条新的独立 PK 消息，猜拳按钮「我也来」、骰子按钮「掷一下」，允许反复参与，不输出胜负、不做回合聚合、排行榜或押注；⑤ 通知、会话预览、历史搜索不透出结果，导出可展示完整结果；PK 可撤回，但不可复制、不可转发；不发送真实 GIF、不新增文件传输、不引远程资源或依赖，旧版本不做 fallback 文本。版本计划 0.21.14 → **0.22.0**（功能更新，按决议 #73 minor +1）。 |
| 140 | PK 功能已可用，但入口图标偏复杂、浮层和气泡像临时组件，需要按 taste 原则打磨到高可用状态 | 在不改协议、存储、随机规则和投递语义的前提下，只做渲染层 UI 精修：① `PK` 工具栏图标简化为与其他 `PantryIcon` 一致的线性拳头相碰图形，保留小号 `PK` 文字，但减少路径和装饰细节；② 玩法浮层改用同一线性图标体系展示「猜拳」「骰子」，加入清晰 hover / active / focus 状态和 disabled title，不使用 emoji 文字符号充当控件图标；③ PK 气泡收紧尺寸与信息层级，结果区、动画区、参与按钮使用现有 8px 圆角、茶青主色和明暗主题 token，按钮文字不换行；④ 动画只用 transform / opacity，`prefers-reduced-motion` 直接显示最终结果。版本 0.22.0 → **0.22.1**（UI 微调，按决议 #73 patch +1）。 |
| 141 | PK 三件套（入口图标 / 玩法浮层 / 消息气泡）虽经 #140 打磨，但骰子自绘线性骰面与猜拳 Twemoji 彩色手势视觉语言割裂、气泡三段松散、入口 7.5px `PK` 角标在任何屏幕都糊，整体不和谐统一，用户要求重新设计到精致统一 | 在不改协议、存储、随机规则与投递语义前提下对渲染层做系统性视觉重设计（沿用既有 token、8px 圆角、茶青主色、明暗双主题）：① 气泡改为「统一开奖窗」骨架——骰面点阵与猜拳 Twemoji 手势住进同一个柔和展示窗（同尺寸 / 圆角 / 底色 / 内描边），用统一画框消解两种玩法差异，气泡收紧为「玩法标签 → 开奖窗 → 弱化结果文字」三层；② 参与按钮「我也来」/「掷一下」由气泡外侧独立胶囊改为融入气泡底部一体行（带分隔线），消除割裂感，仅他人 PK 显示、离线灰显与提示不变；③ 揭晓动画在 1.5s 轮播定格基础上增加定格瞬间开奖窗轻微 scale 回弹 + 茶青高亮一闪，仍只用 transform / opacity，`prefers-reduced-motion` 直接显示结果；④ 工具条 PK 入口由线性图标 + 右下 7.5px 糊角标改为线性拳碰图标 + 清晰 `PK` 文字并排（调整 #140「保留小号 PK 文字」的角标形态），加宽热区与工具条协调；⑤ 玩法浮层两卡片与气泡开奖窗视觉呼应，统一线性图标体系，保留 hover / active / focus / disabled。版本 0.22.1 → **0.22.2**（UI 微调，按决议 #73 patch +1）。 |
| 142 | #141 落地后用户进一步要求：骰子做出 3D 转动效果、猜拳也 3D 化、浮层猜拳图标（pk-rps）偏丑需重画 | 在不改协议 / 存储 / 随机 / 投递语义前提下继续做渲染层升级：① 骰子开奖窗内改为 **CSS 3D 立方体**——六面各画真实 1–6 点数，`preserve-3d` + `perspective`，投掷时 `requestAnimationFrame` 驱动立方体快速翻滚，定格用 `transition` 弹性减速到结果点数那一面朝前（预置每面朝前角度保证点阵正立）；② 猜拳改 **CSS 3D 翻牌**——正反双面同显当前 Twemoji 手势，绕 Y 轴翻转轮播，定格停在结果手势正面（Twemoji 手势本身不变，仍符合产品约束）；③ `pk-rps` 图标由张开五指手改为「张开剪刀」（两指环 + 交叉刃），辨识度更高、与 `pk-dice` 方块骰协调；④ 仍只用 transform / opacity，历史消息与 `prefers-reduced-motion` 直接定位到结果面、不转动。版本 0.22.2 → **0.22.3**（UI 微调，按决议 #73 patch +1）。 |
| 143 | 用户要求 PK 入口图标改成「拳击手套对碰」的感觉，并去掉并排的 PK 文字 | 纯渲染层微调：① 工具条 `pk` 入口图标由抽象拳碰线性图形改为**两只拳击手套对碰**（圆润拳头 + 拇指弧 + 腕带），辨识度更高；② 去掉并排的 `PK` 文字，按钮回到与其他工具一致的纯图标形态（热区 30×28、图标 18px），tooltip 仍为「PK」（调整 #141 第④点「保留小号 PK 文字」）。不改协议 / 数据 / 浮层与气泡。版本 0.22.3 → **0.22.4**（UI 微调，按决议 #73 patch +1）。 |
| 144 | 用户反馈 PK 入口拳套图标仍偏丑、要求改成 PK 艺术字；且点开玩法浮层后「猜拳 / 骰子」会被左侧会话列表方向裁切遮挡 | 纯渲染层微调：① 工具条 `pk` 入口图标由「拳击手套对碰」改为**斜体 `PK` 字标**（`skewX(-10)` 右倾 + 加粗描边，竞技动感，调整 #143 的拳套形态），纯图标按钮形态与 tooltip「PK」不变；② 修复玩法浮层遮挡——`PK` 按钮紧邻聊天面板左缘，原 `.pk-popover` 居中弹出（`left:50%` + `translateX(-50%)`）使浮层左侧溢出 `.chat` 的 `overflow:hidden` 被裁，改为左对齐按钮 `left:0` 向右展开。不改协议 / 数据 / 随机 / 气泡。版本 0.22.4 → **0.22.5**（UI bug 修复 + 微调，按决议 #73 patch +1）。 |
| 145 | 用户反馈 PK 聊天气泡占高度太高（垂直 4 层堆叠约 170px），顶掉太多聊天消息，要求重新设计显示 UI 更紧凑 | 纯渲染层重设计为**横向卡**（不改协议 / 存储 / 随机 / 投递语义）：原「玩法标签 → 开奖窗 → 结果文字 → 通栏参与按钮」垂直堆叠，改为「左：开奖窗 / 中：玩法标签 + 结果两行 / 右：参与按钮小胶囊」横向排列；开奖窗 62→42px、骰子 3D 立方体 40→28px、猜拳翻牌同步缩小（3D 翻滚/翻牌与定格逻辑不变）；参与按钮由气泡底部分隔线一体行改为右侧胶囊（hover 实底茶青）。气泡实测高度从 ~170px 降到 ~60px（省 ~64%）。版本 0.22.5 → **0.22.6**（UI 微调，按决议 #73 patch +1）。 |
| 146 | 用户反馈：猜拳揭晓动画过程中「出了石头 / 剪刀 / 布」结果文字随翻牌轮播快速跳动，观感乱，希望像骰子「投掷中…」那样固定提示 | 纯渲染层微调：猜拳未定格时结果文字由 `pkResultText(rolling)`（随 90ms 轮播变化）改为固定「出拳中…」，与骰子「投掷中…」对齐；翻牌手势图仍由 `rolling` 轮播（视觉翻动不变），定格后正常显示「出了石头 / 剪刀 / 布」。不改协议 / 随机 / 投递。版本 0.22.6 → **0.22.7**（UI 微调，按决议 #73 patch +1）。 |
| 147 | 用户反馈 PK 消息卡片宽度不一致：结果「出了布」比「出了石头」短一截、动画态「出拳中…」与定格结果宽度忽变，看着难看，要求宽度保持一致 | 纯渲染层微调：PK 气泡由按内容自适应（`max-width:234px`）改为**固定宽度**——他人 PK（含参与按钮）200px、自己 PK 132px，信息列 `flex:1` 撑满、结果文字左对齐、短文字右侧自然留白；固定值取自当前最宽内容，卡片不会变更宽，只消除「短一截」与动画→定格的宽度跳变。不改协议 / 随机 / 投递。版本 0.22.7 → **0.22.8**（UI 微调，按决议 #73 patch +1）。 |
| 148 | 用户反馈 PK 猜拳出「石头」时手势图片不显示，剪刀 / 布正常 | 根因（纯渲染层 bug）：`PkBubble` 把石头映射到 emoji ✊（twemoji 码点 `270a`），但本地 `src/renderer/src/assets/twemoji/` 只内置 `270b`(布) / `270c`(剪刀)、缺 `270a.svg`，于是 `rpsSrc` 取到空串、模板 `v-else-if="rpsSrc"` 整张翻牌卡片不渲染。修复：石头改映射 ✊ → 👊（`1f44a`，项目已内置、同套 `#FFDC5D`+`#EF9645` 画风的拳头），复用既有资源、零新增；剪刀 ✌️ / 布 ✋ 不动。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.8 → **0.22.9**（bug 修复，按决议 #73 patch +1）。 |
| 149 | 用户反馈聊天工具条「窗口震动」图标偏丑、看着像「屏幕发光」 | 纯渲染层图标重绘：原 `nudge` 图标为「显示器矩形 + 支架底座 + 四向放射短线」，放射线易被误读为屏幕发光。改为「带标题栏的应用窗口（竖向圆角矩形 + 顶部标题栏分隔线）+ 左右对称振动弧」——振动弧是通用「震动 / 振动」符号语言（同手机振动模式图标），曲线与放射直线观感迥异、消除发光感，「窗口左右抖动」语义更准。仅改 `PantryIcon.vue` 中 `nudge` 的 SVG path，沿用同套 24 viewBox / stroke 1.6 描边风格，不改功能 / 协议 / 投递。版本 0.22.9 → **0.22.10**（UI 微调，按决议 #73 patch +1）。 |
| 150 | 用户反馈设置页分类、设置项与 UI 混乱、不合理，要求从文案、设置展示、UI 设计整体优化 | 设置页整体重设计（纯渲染层，不改 IPC / 协议 / 存储）：**① 信息架构 8→7 组**——按用户心智而非技术维度重组：「端口」从「安全与高级」归位到「网络」、「发送键」从「消息通知」归位到「聊天与文件」、「通用外观」改名「通用」、「消息通知」改名「通知」只留提醒、「网络连接」改名「网络」、整组删除「安全与高级」杂物抽屉。**② 保存策略统一**——纯偏好（开关 / 主题 / 字体 / 提示音 / 发送键）即时生效、无保存按钮，仅文本输入类（资料 / 端口 / 快捷键 / 文件位置）保留保存按钮，消除「这要不要点保存」的困惑。**③ 展示统一**——二选一偏好（主题、发送键）由下拉改分段控件，与头像样式分段同语言。**④ 删占位**——移除语言 / 监听网卡 / 诊断日志 / 清理离线联系人 4 个置灰占位，消除未完成感。**⑤ 视觉系统化收敛**——建设置页专属标尺（间距 4px 基准台阶、圆角 Shape Lock 容器 8 / 控件 6 / 胶囊 999、字号 4 级阶梯 18/15/13/12），核心布局类 token 化、区块标题 14→15 提层级，清理 `changeTheme`/`changeSendKey` 死代码。版本 0.22.10 → **0.23.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 151 | 用户要求设置改为实时保存、去掉保存按钮：输入或焦点丧失后自动保存，并在设置上覆盖弹出「设置已保存」浮层后慢慢淡出 | 设置全面实时保存（纯渲染层，复用既有保存 IPC，不改协议 / 存储）：**① 失焦自动保存**——资料 4 字段、UDP/TCP 端口失焦(blur)即存，快捷键录制框失焦即存，头像点击、文件目录选定即存；开关 / 主题 / 字体 / 提示音 / 发送键沿用即时生效。**② 去掉全部保存按钮**——删资料 / 文件位置 / 端口 / 快捷键 4 个保存按钮（保留「更改」浏览、「恢复默认」「重置应用设置」等动作按钮）。**③ 防抖与校验**——值未变则跳过、不弹窗；昵称空则复原原值并提示「昵称不能为空」，端口越界则复原并提示「端口需为 1-65535」。**④ 统一 toast**——保存反馈由页头角落 `.notice` 升级为底部居中深色胶囊浮层（Vue `<Transition>` 淡入 + 轻微上移、停留约 1.8s 后淡出，尊重 `prefers-reduced-motion`），文案随操作（「设置已保存」/「端口已保存，重启后生效」等）；网络扫描的停留提示仍留页头 notice。清理 `flashSaveButton` / `savedFlash` 死代码。版本 0.23.0 → **0.24.0**（功能更新，按决议 #73 minor +1）。 |
| 152 | 用户反馈「设置已保存」toast 显示位置 / UI 有严重问题，要求用 taste skill 重新设计 | 修复 + 重设计（纯渲染层 CSS）：**根因**——决议 #151 的 `.toast` 样式块在上一轮编辑中途丢失、从未写入文件，导致 toast `<div>` 无任何样式、作为 `.settings`（`display:flex`）的第三个 flex 子项裸奔（无定位 / 无背景 / 挤在侧栏与内容区旁），即用户看到的「位置严重问题」。**注**：五连（typecheck/build/smoke）抓不住此类纯样式缺失——类未定义不报错、smoke 启动 1.5s 不触发 toast，只能靠真人肉眼发现。**修复 + taste 重设计**：① 补回并重写 `.toast`；② 关键修正水平居中——`left: calc(50% + 87px)`（87 = 侧栏 174 的一半）把中心从视口中线移到右侧内容区中心，不再偏向侧栏；③ 底部上浮（`bottom:22px`，顶部已被拖拽带 / 标题 / 关闭按钮占满）；④ 深色胶囊对比当前主题、`✓` 用 `currentColor` 保证明暗两主题都够对比、茶青味柔影（非纯黑，taste §4.4）；⑤ 淡入 + 上移 / 淡出 + 下移、`z-index:60`、尊重 `prefers-reduced-motion`。版本 0.24.0 → **0.24.1**（bug 修复，按决议 #73 patch +1）。 |
| 153 | 用户要求把 GitHub 项目名改成 Teahouse、不再保留 Pantry（推翻决议 #124 中「仓库名故意保留 pantry」的部分） | GitHub 仓库 `skyjt/pantry` 重命名为 `skyjt/teahouse`（`gh repo rename`，GitHub 自动重定向旧 URL，现有 release 下载链不断）；本地 git remote 同步更新为 teahouse；README / CONTRIBUTING / 关于页源码链接 / handoff 里所有 `skyjt/pantry` 链接改为 `skyjt/teahouse`。**内部代号仍保留 pantry**（决议 #124 技术理由不变、用户不可见）：appId `com.pantry.app`（改了 Win/UOS 会把现有用户升级识别为新应用、装成两个）、`window.pantry` preload 桥、自定义协议 scheme（`pantry-img://` 等）、数据目录、npm 包名。协议报文无 pantry 字面标识，新旧版本互通不受影响。版本 0.24.1 → **0.24.2**（关于页可见链接更新，按决议 #73 patch +1）。 |
| 154 | 用户要求完善 README：写入做项目的初心、标注真机实测通过的平台，并用 shuorenhua 技能整体去 AI 味 | README 重写第一屏（按 README Scene Pack：先说清「是什么、给谁、解决什么」）：① 新增「为什么做这个」初心段——飞鸽这类好用的要收费、内网通只有 Windows 装不上国产系统、iptux 普通人用不明白，所以做一个免费 / 跨平台（Win7 → UOS → Mac）/ 打开就会用的内网 IM；② 平台支持表加「真机实测」列，标注 **Win7 x64 / UOS 20 x64 / macOS 26（Apple Silicon）已真机装好跑通收发**（Debian 仅构建支持、未真机实测）；③ 用 shuorenhua 把 tagline / 介绍 / 特性 / 参与开发去模板感、说人话，技术段（工作原理 / 安全性 / 端口表）按 docs 保守保留术语与系统主语。纯文档，不改代码 / 协议。版本 0.24.2 → **0.24.3**（文档完善，按决议 #73 patch +1）。 |
| 155 | 用户要求品牌 logo 更融入、去掉白卡底板 | 三处 logo（SettingsApp / App / SetupWizard）改纯茶杯图形、移除白卡底板更融入。版本 0.24.4 → **0.24.5**（功能更新；详见 git 提交 b0e0eea）。 |
| 156 | logo 描边有锯齿，要求平滑去锯齿 | 品牌 mark logo 折线描摹拟合为向心贝塞尔曲线、平滑去锯齿。版本 0.24.5 → **0.24.6**（详见 git 提交 9750ebf）。 |
| 157 | logo 平滑后造型走样，要求保留原造型 | mark logo 重描去抖：Douglas-Peucker 简化 + 向心曲线 + 三点正圆，保留原造型。版本 0.24.6 → **0.24.7**（详见 git 提交 9f52825）。 |
| 158 | 用户反馈图片 OCR（Tesseract.js）效率与性能差，提出内嵌 PaddleOCR 小模型；并发现更新的 PP-OCRv6 | 图片 OCR 引擎从 **Tesseract.js 换为 PaddleOCR PP-OCRv6_tiny + onnxruntime-web**（决议 #97 的 Tesseract 方案退役）。**选型**：PP-OCRv6_tiny（det 1.7MB + rec 4.3MB + 字典，合计约 6MB，CTC + DB 结构）实测中文准确率、空格 / 行检测、速度全面优于 PP-OCRv5_mobile，仅破折号偶误识；onnxruntime-web 1.20.1（devDependency，随 bundle 打包、纯 JS/wasm 非 native、单线程主线程跑、`wasmPaths` 同源加载、动态 import 切按需 chunk）。**前后处理**：vendoring `paddleocr.js`（MIT）到 `src/renderer/src/utils/paddleocr/`，自带 resize / 阈值 / 膨胀 / 连通域 + CTC 解码，不需 OpenCV。**资源**：模型入库 `build/ocr/`（git 跟踪，CI 直接可用），`prepare-ocr-assets.mjs` 复制模型 + ort wasm 到 `public/ocr/`；vite 插件删掉 onnxruntime bundle 版 `new URL` 重复 emit 的冗余 wasm。**边界**：对外接口 `recognizeImageText` / `OcrResult` 与 `img:ocr-*` IPC 不变、`ImageViewer` 零改动（仍只用整段文字）；纯本地不联网、CSP `wasm-unsafe-eval` 已允许（bundle 内 protobufjs `inquire` 的 eval 在 try/catch 内被拦即回退，不影响功能）；`public/ocr` 体积 58MB → 18MB。版本 0.24.7 → **0.25.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 159 | 用户反馈首次启动文件默认保存目录「下载/茶话间」不合理：希望跨系统统一放「文档」下的类似目录，且中文目录名兼容性 / 中文支持不好 | 文件默认保存目录由 `app.getPath('downloads')/茶话间` 改为 **`app.getPath('documents')/teahouse`**：① 父目录「下载」→「文档」（各系统都有等价的 Documents，「收到的文件」属长期归档、更合文档语义，下载目录偏临时区）；② 目录名中文「茶话间」→ ASCII「teahouse」（规避中文目录名在部分系统 / 命令行 / 跨平台工具链下的编码与兼容问题，对应用户反馈「中文支持不好」）；③ `getPath('documents')` 跨平台一致（Win=Documents、mac/Linux=~/Documents，Linux 依 XDG 未配置则回退 home），接收时已有 `mkdirSync(recursive)` 自动建目录；④ 仅改默认回退值——用户在向导 / 设置里自选的 `fileDir` 原样保留，首启向导第 3 步与设置页「保存位置」显示读后端 `defaultFileDir` 自动跟随；既有用户若一直用默认目录，升级后新收文件落新目录、旧文件留原处不自动迁移；`index.ts` 两处硬编码回退提取为单一 `defaultFileDir()` helper。不改协议 / 存储 / IPC。版本 0.25.0 → **0.25.1**（优化 / 微调，按决议 #73 patch +1）。 |
| 160 | 用户要求把「网络 → 网段扫描」里的具体网段从标签流改成结构化列表，展示 网段 / 该网段扫描到的有效（在线）节点数 / 操作（删除网段、刷新该网段），操作要有实际功能，UI 贴合整体风格 | 已保存扫描网段由 `.chips` 标签流重设计为**紧凑表格**（用户在 2 个贴合设置页风格的方案里选定）：三列 **网段（CIDR，等宽）｜ 在线（茶青徽标，0 时灰显）｜ 操作（⟳ 刷新该网段 / ✕ 删除该网段，图标按钮）**，网段下方小灰字标来源（本机添加 / 同事同步，承接决议 #114）。**有效节点数** = 实时遍历节点注册表 `PeerRegistry`、统计 `online && IP ∈ CIDR` 的节点数：在零 Electron 依赖的 `cidr.ts` 新增位运算 `ipInCidr(ip, cidr)`（含 `cidr.test.ts`），`ScanRangeItemView` 加只读字段 `nodeCount`，主进程 `scanRangeItems()` 填充。**操作接现有实际能力**：刷新 = `scanRange(cidr)`（重新探测该网段，探测后延迟约 2.5s 重拉设置以更新在线数，因 P2P 探测/应答异步滞后不会秒变）；删除 = 从 `scanRanges` 移除并写忽略记录（`saveAppSettings` 返回新视图即时刷新列表，承接 #16）。纯渲染重组 + 新增只读统计，不改协议 / 存储结构 / 扫描发现机制；ui-design §8 分组表同步。版本 0.25.1 → **0.26.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 161 | 用户对 #160 网段表格的 UI 反馈：① 来源标签从网段下方移到 IP 段后面（如 `166.9.26.0/24（本机）`）；② 操作按钮平时即带淡色（刷新淡绿、删除淡红），不要 hover 才变色；③ 删除要二次确认 | 接 #160 的纯渲染层打磨：① 来源由副行小灰字改为 **CIDR 同行括号后缀**（`网段（本机 / 来自 X）`），`.range-cidr` 由纵向改横向 baseline、CIDR 完整不缩、来源可省略；② 操作按钮**默认即着色**——刷新 `.accent` 淡茶青、删除 `.danger` 淡红（`opacity .55`），hover 升实色（`opacity 1`），取代原「平时灰、hover 才上色」；③ **删除二次确认**——点 ✕ 进确认态，该行就地覆盖「删除该网段？[删除][取消]」层（`confirmingCidr` 状态 + `confirmRemove`），点删除才真移除、取消复原，防误删。纯渲染层，不改数据 / IPC / 协议。版本 0.26.0 → **0.26.1**（UI 微调，按决议 #73 patch +1）。 |
| 162 | 用户反馈：① 主窗口「刷新全局用户」按钮丑；② 网段表格「在线/操作」两列太挤、网段列太宽；要求用 taste skill 优化 | taste 驱动纯渲染层优化（design read：redesign-preserve，茶青/圆角体系锁定、克制）：① **刷新按钮**（决议 #115）——移除按钮下方外挂 34×3 进度直条（导航栏唯一非 40×40 元素、破坏按钮列节奏的「小尾巴」）+ 去掉扫描态 primary-weak 方块底与图标旋转（双动效冗余），进度改由**图标外圈环形**表达（`conic-gradient` + `radial-gradient` closest-side mask 抠茶青细环，inset 2，复用 `scanProgressVisible`/`scanPercent`，完成后淡出），扫描态仅图标茶青 + 进度环、空闲态干净 refresh 与其它导航按钮齐整，尊重 reduced-motion；② **网段表格列宽**（#160/#161）——`grid-template-columns` 由 `1fr 56px 64px` 改 `minmax(0,1fr) 84px 72px`，在线列加宽居中、操作列加宽右对齐，拉开「在线/操作」间距、收窄网段列留白。纯渲染层，不改 JS / 数据 / 协议；ui-design §6/§8 同步。版本 0.26.1 → **0.26.2**（UI 微调，按决议 #73 patch +1）。 |
| 163 | 用户反馈刷新按钮的环形进度跳动，要求平滑动画 | 环形进度由跳变改为平滑增长——根因：conic-gradient 的 `--scan-p` 是未经 `@property` 注册的普通 CSS 变量、默认不可过渡，每次 `scanPercent` 更新角度直接跳。修复（纯渲染层、不改扫描逻辑）：① `@property --scan-p { syntax:'<number>' }` 注册为可插值 typed 属性；② `.scan-ring.visible` 加 `transition: --scan-p 0.4s linear`，扫描中进度匀速平滑增长；③ 用单独 `scanRingPct` ref 驱动环、`refreshAllUsers` 发起扫描前归零，且归零落在环隐藏态（`.scan-ring` 基础规则无 --scan-p 过渡）瞬间完成，避免下次扫描从上次满环倒退回 0；④ reduced-motion 下连同 `.scan-ring.visible` 一并关过渡、进度直接到位。版本 0.26.2 → **0.26.3**（UI 微调，按决议 #73 patch +1）。 |
| 164 | issue #3：发送图片（含截图）接收方正常收到、但发送方显示「传输失败」 | 根因：图片走文件传输通道，发送方消息「成功/失败」只由 offer 控制报文的回程 ACK 决定，与图片数据是否真传完解耦——UDP 丢包时对端收到 offer 即自动接收并经 TCP 拉走整图（接收方正常），但 offer 回程 ACK 丢失致发送方 UDP 重传（1+2+4s）+ TCP 兜底在 7~14s 后全判负，而图片数据通常 1s 内传完，迟到的判负把已传完的消息标成失败。修复（`files.ts` 纯 services 状态机，不改协议 / 数据面）：以数据面真实结果为准——① `served`（数据完整送达）→ 消息标 `sent`；② 收到对方 `accept` → 消息标 `sent`；③ `applyMsgStatus` 终态保护：已 `sent` 不被迟到的 `failed` 覆盖；④ offer 可靠发送失败时若 transfer 已 `accepted`/`done` 则不判失败、不回退卡片（单聊 `offerPaths` + 群聊 `offerGroupPaths` 同改）。补 3 个回归测试。版本 0.26.3 → **0.26.4**（bug 修复，按决议 #73 patch +1）。 |
| 165 | （补登）issue #3 发图片发送方误显「图片传输失败」彻底修复 | 接 #164：真根因是发送方取图被 `managedTransferMediaView` 的 `status==='done'` 拦截（收发不对称、与丢包无关）；修复为发送方(out)本地媒体在 offering 即放行取图、`served` 以 `transferRepo` 为准救回误判、`ImageBubble` 发送方失败以 `msg.status` 为准。已发布 v0.26.5。正文见 §11 v1.41。 |
| 166 | 局域网内同平台节点间，高版本能否让低版本自动同步更新？（用户提出） | 做**纯内网 P2P 自更新**（F-SYS-5 升级落地）：节点互知 platform/ver/caps，发现同平台 + 更高版本 + 声明可作源（caps `upd1`）的在线节点 → 提示「内网有新版」→ 用户确认 → 复用文件传输通道拉安装包（SHA-256 校验）→ 安装重启。**谁先升级谁自然成源、下载保留接力扩散、无管理员 / 无人工放包**。本期 Windows nsis（安装时自留安装器、per-user 静默装）+ Linux deb（运行态 `dpkg-deb` 自重打包、pkexec 授权装）；mac 暂缓。用户确认式（不静默），纯内网零外网、不违反红线 #5（禁的是外网更新检查）。分三步实现见 tech-design §12，协议见 protocol §3/§5/§8。版本 0.26.5 → **0.27.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 167 | 设置页加「检测更新」按钮（用户要求） | 关于页键值区（版本 / 许可 / 源码）下方新增「检测内网更新」按钮：点击主动调既有 `update:check` ipc 查当前在线节点里同平台 + 更高版本 + 可作源（caps `upd1`）者，有则提示「内网有新版 vX（来自 某同事）」、无则「当前已是内网最新版本」。是决议 #166 第一步（被动提示）的主动入口补充，复用既有 ipc、不新增协议、不改主进程。版本 0.27.0 → **0.27.1**（微调，按决议 #73 patch +1）。 |
| 168 | 用户要求用 taste skill 优化「设置 - 关于」里的检测内网更新功能 | 保留 #167 的主动检测语义，不扩展拉包 / 安装：关于页检测区由单行按钮 + 裸结果文案改为一条紧凑状态行，左侧用现有 `PantryIcon` 展示 idle / checking / found / none / error 五态，默认说明「只从已发现的在线同事中查找同平台更新源，不访问外网」，检测中显示明确过程，有结果时显示「发现新版本」或「已是最新」，IPC 异常时给「检测失败，请稍后重试」。按钮改为带 refresh / loader 图标的紧凑动作，动效只旋转 loader 并尊重 `prefers-reduced-motion`。纯渲染层 taste 微调，复用既有 `checkUpdate`，不新增协议 / IPC / 依赖。版本 0.27.2 → **0.27.3**（UI 微调，按决议 #73 patch +1）。 |
| 169 | 局域网自更新第二步继续推进：更新包 offer 需要真正进入文件传输通道 | 补齐 `file-ctl offer` 的 `purpose:"update"` 运行时入口：shared 类型与 codec 白名单允许 `update`，但强制单文件、非群聊、总大小 > 0；服务层收到更新包 offer 后不插入聊天消息、不 bump 会话、不加未读、不显示在普通传输记录，而是登记隐藏传输并自动 accept 到 `userData/data/updates` 临时目录（或测试注入目录）。本轮只做拉包入口与隔离落点，安装包版本核对、平台包格式校验、安装重启仍属 #166 后续。补 codec 与 FilesService 回归测试。版本 0.27.3 → **0.27.4**（功能推进，按决议 #73 patch +1）。 |
| 170 | 局域网自更新第二步继续推进：检测到更新后需要能真正发起请求，并让可用源节点回传已有安装包 | `update{op:"req"}` 纳入 Messenger 可靠控制报文集合（UDP ACK / TCP 控制帧均接受），新增 `update:request` IPC 给 UI 发起“向当前最佳更新源索包”。A 端收到请求后复核同平台、请求方在线且本机版本更高；若本地更新包目录 / release 目录里已有与本机版本匹配的 nsis/deb 安装包，则调用 FilesService 以隐藏 `purpose:"update"` transfer 发回。找不到本地包时静默不回 offer，B 端仍可重试；nsis 安装时自留包、Linux `dpkg-deb` 自重打包、包内版本核对与安装重启仍属后续。版本 0.27.4 → **0.27.5**（功能推进，patch +1）。 |
| 171 | 设置-关于里的检测更新区仍像独立功能卡，和关于页键值信息不够融合 | 采用方案 A“键值行化”：把「检测内网更新」并入 `版本 / 许可 / 源码` 的同一组 `about-row`，左侧统一为「内网更新」标签，右侧第一行显示短状态（未检查 / 正在检测 / 有新版 / 正在请求 / 请求已发出 / 已是最新 / 检测失败），第二行显示简短说明；移除左侧状态图标块和独立卡片感，只保留右侧紧凑动作按钮，found 状态按钮变为茶青弱底「同步更新」。纯渲染层 UI 微调，不改 IPC / 协议 / 主进程。版本 0.27.5 → **0.27.6**（UI 微调，patch +1）。 |
| 172 | 用户反馈「内网更新」不应直接展示「只从已发现的在线同事中查找」这类解释性说明，后续偏开发 / 机制解释的文字应收起 | 设置-关于的「内网更新」行只常驻短状态与动作按钮；主窗口「内网有新版」弹层只常驻版本 / 来源 / 当前版本 / 同步按钮。默认范围说明、纯内网 / 不访问外网 / 索包不静默安装等机制解释改放到圆形 `?` 帮助图标里，鼠标悬停或键盘聚焦时显示自绘 tooltip，不使用原生 `title`。正文仅在发现版本（展示版本与来源）或错误（展示重试提示）等必要反馈时出现。纯渲染层 UI 微调，不改 IPC / 协议 / 主进程。版本 0.27.6 → **0.27.7**（UI 微调，patch +1）。 |
| 173 | 用户反馈设置-关于的 `?` 说明 tooltip 被关于卡片 / 窗口底部裁切 | 修复 #172 的显示 bug：设置-关于 `?` tooltip 改为向上浮出，提升浮层层级，避免落到关于卡片底部裁切区；主窗口更新弹层保持同样的上浮策略。仅改渲染层 CSS，不改 IPC / 协议 / 主进程。版本 0.27.7 → **0.27.8**（UI bug 修复，patch +1）。 |
| 174 | 用户提出内网协作场景中双方都是认识的人，文件接收不应把所有决策都交给接收方；希望发送方可在文件发出后点击「直接发送」，对方在线时免确认接收 | 新增**私聊文件直接发送**：发送者先按普通方式发送私聊文件/文件夹，发送方文件卡片出现后，若接收方在线、版本支持且未关闭直接发送总开关，发送者可点击卡片上的「直接发送」，接收方自动 accept 并保存到 `文件保存位置/发送人名字/`；接收方不再点击「接收」。该能力只服务熟人内网协作的快速传文件场景，不做陌生人白名单式自动接收。接收方保留本地总开关「允许同事直接发送文件」，默认开启；关闭后直接发送退化为普通文件卡片。**群聊文件不允许直接发送**，避免大群同时拉取造成网络 / 磁盘尖峰；离线文件仍提示不在线、不入队。协议新增可兼容旧端的直接发送控制动作与能力位；UI 文案统一使用「直接发送」而非「自动接收」。版本 0.27.8 → **0.28.0**（功能更新，按决议 #73 minor +1）。 |
| 175 | 用户反馈拖动或粘贴文件都没法有效发送 | 修复拖拽 / 粘贴文件路径缺少一次性授权导致 `file:offer` / `img:offer-path` 被主进程拒绝的问题：选择器路径仍由 `file:pick` 授权；拖拽 / 粘贴路径新增 `file:grant-paths` IPC，由 preload 将 Electron `File.path` 解析出的本地路径提交给主进程登记一次性授权，再复用既有普通文件 / 图片发送通道。`file:offer`、`group-file:offer`、`img:offer-path` 仍必须消耗授权，不放宽渲染层任意路径发送。版本 0.28.0 → **0.28.1**（bug 修复，patch +1）。 |
| 176 | 用户反馈直接发送和已保存的文件卡片 UI 变丑，直接发送让卡片变高，完成态文案「已保存到 xx 文件夹」换行 | 用 taste skill 做文件卡片收紧：等待状态不再占右侧一行，改并入大小 meta 行；右侧双动作保持同一行，主按钮为「直接发送」，取消在双动作场景下改为 `x` 图标按钮并保留 tooltip，减少宽高占用；直接接收完成态只显示「已保存本地」，不显示发送人目录名。纯渲染层 UI / 文案微调，不改协议、IPC、传输状态机、保存目录生成或群聊不支持直接发送的规则。版本 0.28.1 → **0.28.2**（UI 微调，patch +1）。 |
| 177 | 用户反馈 `605.8 KB · 等待接收` 仍会被右侧按钮挤断，且发送完成态「已保存到对方电脑」太长 | 文件卡片等待态从 meta 行移到文件名同行的固定状态片：普通等待显示「等待接收」，直接发送中显示短文案「发送中」，大小行只保留大小 / 文件数 / 速率，避免状态被省略号截断。发送方传输完成态统一显示「发送成功」，不区分是否直接发送、不展示保存到对方电脑等长句。纯渲染层 UI / 文案微调，不改协议、IPC、传输状态机或保存语义。版本 0.28.2 → **0.28.3**（UI 微调，patch +1）。 |
| 178 | 用户反馈接收文件卡片仍然很丑，高度太高、不协调 | 普通入站文件的 `offering` 接收态不再把「接收 / 另存为 / 拒绝」三颗按钮纵向堆叠；右侧改为一行动作组：茶青主按钮「接收」+ 文件夹图标「另存为」+ `x` 图标「拒绝接收」。两个图标按钮保留中文 tooltip / aria-label，降低高度同时不丢操作。纯渲染层 UI 微调，不改协议、IPC、传输状态机或直接发送规则。版本 0.28.3 → **0.28.4**（UI 微调，patch +1）。 |
| 179 | 用户明确：不管自动还是手动接收，只要不用「另存为」，文件都应默认保存在 `默认目录/<联系人名称>/` | 统一默认接收落点：`file:accept` 未传另存目录时，服务层用 `文件保存位置/联系人名称/` 作为基础目录；私聊直接发送复用同一默认目录逻辑，不再有单独的 direct-only 保存路径。联系人名称以本地备注优先、其次 peer 昵称，清洗非法路径字符；若是失败重试且已有 `savedPath`，继续沿用原接收目录，避免重试时换目录。点击「另存为」时完全使用用户选择的目录，不再额外套联系人子目录。群聊文件仍不支持直接发送，但手动普通接收按发送人显示名进入联系人子目录。版本 0.28.4 → **0.28.5**（行为微调 / bug 修复，patch +1）。 |
| 180 | 用户反馈使用第三方截图工具截图后粘贴发送，会出现两张一样的图片 | 修复粘贴图片双发送：输入框粘贴仍优先处理真实文件、文本与浏览器 `ClipboardEvent.items` 图片；主进程 `before-input-event` 触发的原生图片剪贴板兜底改为延迟执行，若同一次粘贴已被浏览器 paste 事件消费（文件 / 文本 / 图片任一分支），立即取消兜底。移除渲染层 `keydown Ctrl/Cmd+V` 的立即兜底，避免在 paste 事件前抢先发送。版本 0.28.5 → **0.28.6**（bug 修复，patch +1）。 |
| 181 | 用户要求新增 Debian 10 / UOS 20 的 arm64 构建包 | Linux 发布矩阵新增 **Debian 10 / UOS 20 arm64**：`package.json` 的 Linux deb/AppImage target 同时声明 x64 与 arm64，新增 `dist:linux:arm64`；GitHub Actions 新增独立 arm64 job，在 Debian 10 arm64 容器内 `npm ci`、源码重建 better-sqlite3、跑五连验证、构建 `Teahouse-<version>-linux-arm64.deb` 与 `Teahouse-<version>-linux-arm64.AppImage`，并生成 `SHA256SUMS-debian10-uos20-arm64.txt`；Release notes 与发布资产同步列出 arm64。为避免 Linux x64 / arm64 deb 并存后自更新选错包，`update{op:"req"}` 增加可选 `arch:"x64"|"arm64"`，源端按请求架构匹配本地安装包；旧端缺 arch 时保持兼容。版本 0.28.6 → **0.29.0**（发布能力新增，按决议 #73 minor +1）。 |
| 182 | `v0.29.0` tag 发布流水线里 arm64 job 容器步骤过快结束，上传阶段找不到 arm64 产物 | 修复 arm64 CI 编排：将 Debian 10 arm64 容器内的安装依赖、native 重建、五连验证、打包、GLIBC/deb 校验和 SHA 清单生成从 workflow heredoc 移入 `scripts/ci-linux-arm64.sh`，workflow 负责调用脚本并在容器结束后打印 `release/` 文件列表，若再次路径异常可直接从日志定位。版本 0.29.0 → **0.29.1**（发布链路 bug 修复，patch +1）。 |
| 183 | 用户指出 Linux arm 包应使用远程 arm runner，不应使用 QEMU 模拟 | 纠正 arm64 发布调度：取消 `docker/setup-qemu-action` 与 `docker run --platform linux/arm64`，Linux arm64 job 改为 `runs-on: ubuntu-22.04-arm`，并用 `container: node:18-buster` 在远程 arm64 runner 上直接运行 Debian 10 arm64 容器；继续复用 `scripts/ci-linux-arm64.sh` 完成依赖安装、native 重建、五连验证、打包、GLIBC/deb 校验与 SHA 清单。取消错误方向的 `v0.29.1` 发布流水线，重新以 0.29.2 发布。版本 0.29.1 → **0.29.2**（发布链路修正，patch +1）。 |
| 184 | `v0.29.2` 远程 arm runner 已正确启动，但 deb 阶段 electron-builder 拉取 x86 fpm，且 arm64 job 误触发 x64 目标 | 修复 arm64 打包工具链：Linux target 配置不再在 `package.json` 内声明 x64/arm64 双架构，改由 `dist:linux` 显式构建 `deb:x64 AppImage:x64`，`dist:linux:arm64` 显式构建 `deb:arm64 AppImage:arm64`；arm64 CI 安装 `ruby ruby-dev squashfs-tools` 并 `gem install fpm -v 1.9.3`，打包时设置 `USE_SYSTEM_FPM=true` 与 `USE_SYSTEM_MKSQUASHFS=true`，避免 `app-builder` 下载并执行 x86 打包工具。版本 0.29.2 → **0.29.3**（发布链路修正，patch +1）。 |
| 185 | `v0.29.3` 远程 arm runner 已使用系统 fpm，但 Debian 10 Ruby 2.5 安装 fpm 时解析到不兼容的新版 ffi | 首次修正 arm64 Ruby 打包依赖：`scripts/ci-linux-arm64.sh` 在安装 `fpm 1.9.3` 前先安装 `ffi 1.17.4`，试图避免 RubyGems 解析到要求 Ruby 3+ 的新版 ffi；后续 `v0.29.4` 发布日志确认该版本自身也不兼容 Ruby 2.5，见 #186。版本 0.29.3 → **0.29.4**（发布链路修正，patch +1）。 |
| 186 | `v0.29.4` 远程 arm runner 仍失败：`ffi 1.17.4` 的 RubyGems 元数据要求 Ruby 3+，Debian 10 Ruby 2.5 无法安装 | 修复 arm64 Ruby 依赖版本：arm64 CI 安装 `libffi-dev`，并将预安装 ffi 改为 `gem install ffi -v 1.15.5`，这是更保守的 Ruby 2.5 可用版本；随后再安装 `fpm 1.9.3` 并继续使用系统 fpm / mksquashfs 打包。版本 0.29.4 → **0.29.5**（发布链路修正，patch +1）。 |
| 187 | `v0.29.5` 远程 arm runner 已通过 ffi/fpm，但 AppImage 阶段 Debian 10 系统 `mksquashfs` 不支持 electron-builder 传入的 `-offset` 参数 | 修复 arm64 AppImage 打包工具选择：撤掉 `USE_SYSTEM_MKSQUASHFS=true`，不再强制使用 Debian 10 老版系统 mksquashfs；arm64 job 只强制 `USE_SYSTEM_FPM=true` 解决 deb 阶段 fpm 架构问题，AppImage 阶段回到 electron-builder 的 appimage 工具包。版本 0.29.5 → **0.29.6**（发布链路修正，patch +1）。 |
| 188 | 用户确认方案 A：文件、图片新增撤回；已接收的文件不支持撤回 | 采用**跨端一致媒体消息 ID + 复用现有 recall 控制报文**的方案：`file-ctl offer` 新增 `msgId`，图片 / 普通文件 / 群文件的收发两端使用同一 `messages.id`，撤回仍走 `msg(kind:"recall", targetId)` 和既有 ACK / 重传 / 离线补发。新增能力位 `mrec1`，仅双方新端时展示媒体撤回入口。图片 5 分钟内可撤回，撤回后隐藏气泡并禁止继续通过该消息打开；文件 5 分钟内仅在接收未完成时可撤回，等待 / 传输中文件撤回会取消传输并清理 `.part`，已完成保存的文件不受撤回影响；群文件只在所有收件人均未完成接收时可撤回。表情包不纳入本轮用户入口。版本 0.29.6 → **0.30.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 189 | 用户反馈发送图片后消息区不贴到底部；系统截图后茶话间会退到后面一层 | 修复两处前台体验：自己发送文本 / PK / 图片 / 文件 / 表情等消息成功追加到当前会话时，渲染层 store 显式请求定位到最新页，确保异步图片 / 截图发送后消息区贴底；内置截图框选结束、复制或发送后，主窗口恢复时短暂置顶并释放，避免被系统截图窗口或前一个应用压在后面。版本 0.30.0 → **0.30.1**（bug 修复，patch +1）。 |
| 190 | 用户要求 Excel 表格粘贴后默认以图片展示，发送出去后双方都能在图片和文字之间切换，切换控件放在气泡上沿的小滑块 | 表格粘贴按图片消息发送并支持图片/文字视图切换。输入框粘贴 Excel / WPS / LibreOffice / 网页表格时，默认生成表格图片并走既有图片传输；支持新能力位 `tbl1` 的双方额外传递原始制表符文本，图片气泡上沿显示小型分段滑块「图片 / 文字」，默认图片视图，切到文字视图可选择复制原始 TSV。文字元数据最多 4096B UTF-8，超出时仅截断文字视图并提示，图片仍完整。旧端仍只看普通图片；文字元数据不写 FTS、不改变会话预览、不新增消息类型或 SQLite 迁移。版本 0.30.1 → **0.31.0**（功能更新，按决议 #73 minor +1、patch 归 0）。 |
| 191 | 用户反馈点击小图片打开独立看图窗口时，底部菜单因窗口太小挤在一起、显示很难看 | 图片查看器保留“小图按原始尺寸显示”的原则，但窗口与内容区不再完全由图片尺寸下探到极小值：独立看图窗口设置最小外框尺寸，`img:fit-viewer-window` 初始适配计算给内容区预留不小于 560×360 的可用空间，底部半透明工具条始终有稳定横向与纵向余量；小图仍居中按 100% 显示，不强制放大，不改协议、存储或图片传输。版本 0.31.0 → **0.31.1**（UI bug 修复，patch +1）。 |
| 192 | 用户反馈重新打开软件后点击用户消息没有自动滚到最下面，有时收到消息后打开也不是最新消息 | 采用更接近常见聊天软件的进入策略：用户从会话列表、联系人、搜索联系人、通知 / 托盘 / 震动进入会话时，默认加载最新 50 条并定位到底部；即使本轮内存里已有该会话的历史窗口或旧滚动位置，也以“最新消息”作为显式入口语义。只有历史搜索跳转继续使用 `target` 居中定位目标消息；阅读当前会话历史时收到新消息仍不强拉到底，保留右下角“回到最新”。修复仅改渲染层状态策略，不改协议、IPC、SQLite 或消息存储。版本 0.31.1 → **0.31.2**（UI bug 修复，patch +1）。 |
| 193 | 设置管理密码后群改名 / 添加成员 / 移出成员体验失效 | 群成员面板移除原生 `window.prompt()` 管理密码入口，改为面板内常驻密码输入框，改名、添加成员、移出成员统一读取该输入；缺密码或密码错误走行内失败提示。`group:update` 成功返回后立即把新的 `GroupView` 写入本地 groups store，再由既有 `group:updated` 事件兜底同步。协议、SQLite、密码摘要规则不变。版本 0.31.2 → **0.31.3**（bug 修复，patch +1）。 |
| 194 | 用户希望茶话间兼容内网通：可单独新增内网通 IP 段，扫描出内网通节点，并与内网通通信 | 新增**内网通兼容模式**设计：主协议继续保持自有 UTF-8 JSON；兼容能力走独立 IPMSG / 飞鸽适配器，默认关闭，用户在设置中单独维护兼容 IP / CIDR。首版绑定 `2425/UDP`，扫描目标发送 `BR_ENTRY`，解析内网通 `ANSENTRY`，将兼容联系人独立展示；文本发送使用 `SENDMSG|SENDCHECK`，收到 `RECVMSG` 标记送达；入站 `SENDMSG` 写入独立兼容会话并回执。兼容节点不进入茶话间 `peers` 身份表，不参与 gossip、`scan-ranges`、群聊、自更新、文件传输或离线补发。GBK 解码作为正式实现前提，推荐引入精确锁纯 JS 依赖 `iconv-lite`。详细设计见 [nwt-compat-design.md](nwt-compat-design.md)。 |
| 195 | 用户要求进一步检查内网通图片、文件、震动等协议，并要求内网通用户在茶话间 UI 有特殊标志，PK 等不兼容功能隐藏 | 扩展内网通兼容设计：协议层新增 IPMSG 附件 / 剪贴板图片 / TCP `GETFILEDATA` / `GETDIRFILES` / 信息查询 / 离开状态 / 震动缺口的能力矩阵。实测结论为：`2425/UDP` 文本互通稳定；`ANSENTRY` 不声明文件或图片能力；标准文件与剪贴板图片 offer 会被内网通 ACK 并在“文件传输”页展示，但未捕获到 TCP 拉取，暂列实验能力；`GETINFO` / `GETABSENCEINFO` 无应答；窗口震动没有标准 IPMSG 命令，继续隐藏。UI 侧新增 `内网通` 兼容徽标、会话头部“功能受限”副标题和 `ConversationCapabilities` 门控，兼容会话只保留文本、历史搜索和发送按钮，隐藏 PK、震动、图片、文件、文件夹、直接发送、媒体撤回等入口。**（第一轮结论，第二轮复测见 #196）** |
| 196 | 第二轮复测内网通兼容链路，发现 `SENDMSG` / 文件 offer / 剪贴板图片 offer 均不复现 | 在同一 Parallels Windows VM（`10.211.55.3`）与同一内网通构建（`ShiYeLine.exe` 2021-11-09）上复测：`BR_ENTRY` 纯命令 `0x1` 仍稳定返回 `ANSENTRY`，但**携带任何选项位的 `BR_ENTRY` 不再响应**；`SENDMSG` 无论 `SENDCHECKOPT`/`UTF8OPT`/`CAPUTF8OPT` 组合、ASCII/UTF-8/GBK 正文、空正文、`BR_ENTRY` 后立即发送、先回 `RECVMSG`/`ANSENTRY`、模拟内网通头部格式——一律不返回 `RECVMSG`，消息不进可见 UI；文件 offer 与剪贴板图片 offer 同样无 ACK、无 TCP `GETFILEDATA` 连接。`EnumWindows` 确认内网通进程内为 `pantry-probe` 节点创建了隐藏窗口，但主界面联系人列表未加入该节点，说明存在"联系人准入"前置条件。稳定首版范围降级为仅发现 + 兼容联系人展示 + 能力降级 UI；文本收发、文件 offer、图片 offer、TCP 拉取全部降级为"已映射未闭环"，待 VM 打通入联系人前置条件后再恢复。详细见 [nwt-compat-design.md](nwt-compat-design.md) §2.3 第二轮与 §15 第 6 项。纯文档，不改代码。 |
| 197 | 主界面「刷新全局用户」一点即扫全部网段，容易误触，希望点击后二次确认 | **全量刷新二次确认**（用户拍板）：① 居中模态；② 每次可点先确认；③ 内容为**精简网段摘要**——标题 + 一句「将探测 N 个网段」+ CIDR 列表（最多 4 条，超出「另 N 个」；不堆长说明 / 来源 / 脚注）；④ 茶青「开始扫描」。遮罩 / 取消 / Esc 不扫。设置页单网段 ⟳ 不套本弹窗。纯渲染层。版本 **0.32.0** 初版，**0.32.1** 文案与视觉收紧。详见 [ui-design.md](ui-design.md) §2。 |
| 198 | 群成员上限 50 偏紧，希望支持更大讨论组 | `GROUP_MAX_MEMBERS` **50 → 200**（`shared/protocol.ts` 单一常量）。建群选人、添加成员、codec 入站校验与群内 @ mentions 上限均随之；UI 显示 `成员 n / 200`，满员禁用添加。旧端（仍按 50 校验）收到 >50 人的 `group.info` 会拒绝该元数据——大群需两端均为新版本。版本 **0.32.3**（参数调整，patch +1）。 |
| 199 | 内网通兼容调研周期长，近期不宜占用主开发 | **暂缓实现**。#194/#195/#196 与 [nwt-compat-design.md](nwt-compat-design.md) 仅作设计与实测留档；**代码保持零实现**，不排进「下一步」、不写 `net/compat/`。用户确认重新启动后再推进 §15 取舍与 VM 阻塞项。纯决策落档，不改代码。 |
| 200 | 用户要求对 main 分支全库代码审查，产出可交给低成本模型执行的优化方案 | 建立 [optimization-plan.md](optimization-plan.md) 作为唯一优化待办清单（18 项分级：P0=#198 群上限三处漏改、传输层崩溃/fd 泄漏/背压缺失 → P1=messages 缺 seq 索引、导入 O(n²)、渲染层 500ms 全列表重渲染 → P2/P3 收敛与测试补齐）。执行约定：一项一 commit、按 #73 递增版本、网络层改动必须回环测试、完成后回写该文档状态行。纯文档，不改代码。 |
| 201 | 用户决定不再使用 dev-claude 开发分支 | 日常开发**直接提交 `main`**：本地与远端 `dev-claude` 已删除（内容确认全部并入 main 后清理），不再新起开发分支；打包触发改为 `gh workflow run release.yml --ref main`。CLAUDE.md 分支约定与 optimization-plan §0.3 同步更新。纯流程决策，不改代码。 |
| 202 | OPT-16 发现 OCR 引擎和数据库迁移版本的当前文档描述过期 | 纯文档修正：当前图片 OCR 引擎统一表述为内置 PaddleOCR PP-OCRv6 tiny + onnxruntime-web 本地 wasm；数据库迁移版本不在交接文档写死，统一指向 `src/main/store/migrations.ts` 与 `PRAGMA user_version`。历史决议记录中关于早期 OCR 方案的 Tesseract 引用保留，作为演进脉络。纯文档，不改代码、不递增版本。 |
| 203 | OPT-17 发现主进程图片发送 IPC handler 与渲染层图片扩展名白名单重复 | 收敛图片发送重复逻辑：新增 `shared/media.ts` 作为图片扩展名白名单与默认 `.png` 回退的唯一常量来源；主进程 `img:send-bytes` / `group-img:send-bytes` / `img:offer-path` / `group-img:offer-path` 抽为薄壳，共用目标、文件名、字节、路径校验与 staging helper；ChatPane 使用 shared 常量判断图片路径。校验边界与发送语义保持不变。版本 0.32.18 → **0.32.19**（代码质量优化，patch +1）。 |
| 204 | OPT-18 要求补齐 range-sync 与 peer-registry 专项测试 | 新增 / 扩展网络层专项测试：`peer-registry.test.ts` 覆盖 seed 幂等、未知节点空资料拒绝、在线源地址漂移拒绝、离线换址必须带完整 profile、profileRev 旧值不回退、sweep 判离线、online 事件触发时字段已更新；新增 `range-sync.test.ts` 覆盖节点上线延迟分享、stop 清理定时器、入站 CIDR 过滤 / 去重、自身与 unknown 报文忽略，以及 127.0.0.1 回环同步合法去重网段。测试绑定回环地址并使用 `broadcastTargets: []`，不向真实局域网发包。版本 0.32.19 → **0.32.20**（测试补齐，patch +1）。 |
| 205 | CI 五连验证在 macOS / Debian 挂死：`transfer.test` 多文件用例超时，OPT-4 接收背压引入死锁 | 根因：接收写盘 `write` 返回 false 时 `socket.pause()`，随后本文件 `stream.end()` 走 finish 路径可能**不再 emit `drain`**，socket 永久 pause → 下一文件 `pull-ok` 读不到。修复：文件完成 / fail / succeed 时显式 `resumeSocket()`；发送端 drain 监听去重；`TransferServer.stop` 强制销毁活跃连接并限时收口。补「多文件 + 慢写盘」回归用例。版本 0.32.20 → **0.32.21**（bug 修复，patch +1）。 |
| 206 | arm64 Debian 打包（443s）比 x64（226s）慢近一倍，用户要求优化 CI 链路 | 实测拆解：npm ci 重复源码编译 better-sqlite3 约 45s（`build_from_source` 误挂整个 step，postinstall 编译一遍、显式 rebuild 又一遍）＋ 每次重装 ffi/fpm gem 约 46s ＋ fpm xz 单线程压 deb 约 123s。优化：① `build_from_source` 收窄到 `rebuild:electron` 单命令（npm ci 走 prebuilt，GLIBC 基线仍由显式源码 rebuild + check-native-glibc 保证）；② `actions/cache` 缓存 `/var/lib/gems` 与 fpm 可执行，脚本按版本命中跳过安装；③ **deb 压缩 xz → gz**（用户拍板：打包提速最大单项、x64 job 同受益，deb 体积约 +30% 在内网分发场景可接受）；④ 两个 Linux job 移除无用的 rpm 依赖。arm64 触发时机保持每次 push（用户拍板，日常 push 需全平台 artifact）。预期 arm64 443s → 约 250s。版本 0.32.22 → **0.32.23**（CI 链路优化，patch +1）。 |
| 207 | UOS 用户反馈系统截图工具截图后粘贴发送出现两张同图；报告者本机无法复现（决议 #180 修复后仍复现） | 根因是 #180 的**时间窗方案存在两个竞态口子**，慢机器会跨过窗口：① `onPaste` 图片分支在 `await file.arrayBuffer()`（大截图可达十几 MB、耗时可超 80ms）**之后**才标记"本次粘贴已处理"，IPC 兜底定时器在 await 间隙到点，抑制检查扑空 → 原生剪贴板再发一张；② 主进程繁忙时 `clipboard-paste-image` IPC 迟到超过 300ms 抑制窗 → 兜底重发。两张图字节不同（原始位图 vs Electron `toPNG()` 重编码），故入库为两条消息。修复改为**结构性互斥**、不再赌时间窗：`onPaste` 入口在任何 await 之前同步标记并清除已挂兜底定时器；聊天输入框有焦点时 IPC 兜底直接退出（textarea 焦点下 paste 事件必然派发，`onPaste` 尾部已有显式原生剪贴板兜底覆盖 items 为空的场景），IPC 兜底仅保留给"焦点不在任何输入元素"的场景。版本 0.32.23 → **0.32.24**（bug 修复，patch +1）。需请反馈用户在其 UOS 环境验证。 |
| 208 | 全库审查后，用户确认先完成七项优先修复，并要求每项独立测试、最终合并为一个代码提交 | 本批统一修复：① TCP 控制帧按类型、字段、长度、整数与哈希格式做严格白名单校验，解析失败进入终止态并隔离异常；② 自更新 offer 只接受用户最近一次请求授权的源节点、精确平台/架构包名与单包 **512 MiB** 上限，授权 120 秒过期且成功后消费；③ 版本号校验覆盖 `package.json`、lockfile 根版本、Git tag 与 release artifact 文件名，并进入 CI；④ 文件发送端数据流并发默认 3、连接总数 256、握手超时 15 秒、活跃空闲超时 60 秒；⑤ 会话异步导航引入代次，旧请求结果不得覆盖新会话；⑥ 截图工具条按测量尺寸在视口内翻转与夹紧；⑦ 截图窗口拦截所有导航。七项分别补专项 / 回环测试，最终跑五连验证。版本 0.32.24 → **0.32.25**（安全与稳定性修复，patch +1）。 |
| 209 | 用户认为 #208 跨安全、传输、发布与交互多个子系统，要求提高发布版本辨识度 | 将本批最终发布版本从 0.32.25 调整为 **0.33.0**，按决议 #73 采用 minor +1、patch 归零。#208 的实现提交与历史版本记录保留，当前 package / lockfile 统一从 0.32.25 → 0.33.0；功能、协议字段与兼容边界均不再变化。 |
| 210 | 用户确认继续第二批优化，优先收敛渲染启动成本与 OCR 内存边界 | 保留单 HTML / hash 路由，四个根组件改为动态入口；renderer OCR 结果缓存限制为 16 项 LRU，OCR 引擎初始化失败后允许重试；启用 Vite manifest，并在 build 后校验四个动态入口、独立文件与 **200 KiB** 公共启动闭包。消息虚拟列表继续暂缓。版本 0.33.0 → **0.33.1**（性能优化，patch +1）。 |
| 212 | 用户认为左上角个人信息卡信息层级混乱，要求去掉端口、增加本机 IP 并整体优化 UI | 个人信息卡改为单容器分层布局：顶部突出头像、昵称和本机账户身份；中部突出本机 IPv4；组织与设备名使用次级键值行；节点 ID 作为弱化页脚。移除端口展示，端口配置继续留在设置「网络」。复用既有 `app:info` 返回本机 IPv4，不新增 IPC 通道，不改协议、存储与纯内网边界。版本 0.33.3 → **0.34.0**（可感知 UI 功能更新，minor +1、patch 归 0）。 |
| 213 | 用户要求新增 Windows 32 位 Release 平台，并由 GitHub Actions 打包 | 覆盖历史决议 #20：Windows 7 SP1+ 同时发布 x64 与 **ia32（32 位）** 的 NSIS 安装包、portable 便携版和独立 SHA-256 清单。新增 `dist:win:ia32` 与独立 `windows7-ia32` job；五连验证后将 better-sqlite3 显式重建为 ia32，并校验源码目录及最终包内 PE machine 均为 x86，防止混入 x64 native 模块。Release 产物由 12 个增至 15 个。自更新架构白名单同步加入 ia32 并精确匹配安装包。版本沿用本轮 **0.34.0**，与 #212 合并发布。 |
| 214 | 用户反馈 UOS 20 收到消息并产生未读后，日期 / 音量 / 网络旁的系统托盘图标没有可感知闪烁 | 修复 Linux 托盘未读兜底：保留 `app.setBadgeCount` best effort；闪烁帧改为与常规茶青图标差异明显的红色注意图标，并在每次切帧时创建新的 `NativeImage`，降低 Electron 22 / DDE 对复用图像的刷新缓存影响。闪烁控制器在同一托盘的未读更新时只更新当前帧，不反复重启 800ms 周期；托盘销毁或写图失败时安全停表，未读清零立即恢复常规图标。Windows 继续使用数字角标帧，macOS 行为不变。补 Linux 两周期交替、重复未读更新、销毁和清零回归测试，并把 UOS 实机验收写入打包清单。版本 0.34.0 → **0.34.1**（Linux 兼容性 bug 修复，patch +1）。 |
| 215 | 用户要求建立 `naive_ui_dev` 分支，以 Naive UI 优化并逐步重绘应用 | 采用**混合接入**：精确锁定 `naive-ui@2.43.2`，首批在设置窗口接入 `NConfigProvider`、输入框、选择器、开关、单选按钮组与常规操作按钮；主题覆盖继续以茶青 `#3D8B6B`、现有明暗 token、4px/8px 圆角和系统字体为唯一视觉基线。三栏框架、会话列表、聊天气泡、文件/图片/PK 卡片、品牌 SVG、`PantryIcon`、窗口控制、截图与图片查看器继续自绘。Naive UI 仅进入 renderer 动态入口，不进入 main/preload/shared，不新增外网资源；Provider 不放公共静态 bootstrap，保持四窗口拆包与 200 KiB 启动门禁。当前最新版 2.44.x 要求 Node >=20，违反 Node 16 / CI Node 18 基线，禁止使用。版本 0.34.1 → **0.35.0**（UI 基础能力更新，minor +1、patch 归零）。 |
| 216 | 用户反馈首批组件替换视觉变化不明显，要求参考 Apple Design 并结合 Naive UI，让整个应用获得可感知的美化 | 采用**保留品牌与信息架构的整体视觉升级**：三栏位置、功能入口、茶青主色、圆形头像和自绘图标保持稳定；主窗口改用轻材质导航与列表层、弱茶青会话选中态、浮层感聊天头部、留白更充分的消息区和独立浮起的输入区；设置窗口同步升级侧栏、页头与分组面板层级。动效遵循即时按压反馈、空间来源一致和 150-220ms 克制过渡，全部兼容 `prefers-reduced-motion` / `prefers-reduced-transparency` / `prefers-contrast`。Naive UI 扩展到主窗口动态入口的 `NConfigProvider`、搜索输入与主要操作按钮，主题仍集中映射茶话间 token；消息流、媒体卡片、窗口系统与品牌图标继续使用项目组件。只改 renderer 视觉与标准控件，不改协议、IPC、数据、纯内网和 Electron 22 / Chrome 108 基线。版本 0.35.0 → **0.36.0**（整体 UI 功能更新，minor +1、patch 归零）。 |
| 217 | 用户实测发现 Naive UI 重绘后的主窗口三栏只占上半部分，底部出现大块空白，主界面无法正常使用 | 根因是 `App.vue` 外层新增 `NConfigProvider` 后形成新的 DOM 根容器，而 `.shell { height: 100% }` 失去有明确高度的直接祖先，三栏高度退化为内容高度。主窗口 Provider 使用 Naive UI 官方 `abstract` 模式，只提供主题上下文而不生成布局节点，并增加源码级回归测试锁定该契约；设置窗口继续使用自身 `100vh` 布局。补真实窗口验收：默认 960×640、最小窗口、最大化 / 还原、空态与会话态均铺满可用区。版本 0.36.0 → **0.36.1**（主界面回归修复，patch +1）。 |
| 218 | 用户实测发现设置保存提示向右偏移，且点击截图后整屏只剩灰绿色遮罩、看不到屏幕内容 | 两项回归一并修复：①「设置已保存」toast 改为按设置窗口整体 `left: 50%` 水平居中，不再补偿侧栏宽度；② 决议 #210 将截图根组件改为动态入口后，主进程 `did-finish-load` 单次发送的 `capture:init` 可能早于 `CaptureApp` 订阅而丢失，preload 在页面脚本之前注册事件并缓存最近一次截图初始化数据，渲染层稍后订阅时立即回放；截图窗加载底色固定为黑色，避免截图根挂载前透出应用茶青背景。增加初始化先于订阅、正常订阅与退订回归测试，并以真实 Electron 验收屏幕背景可见、框选工具条可用、Esc 可退出。协议、截图字节、日志与纯内网边界不变。版本 0.36.1 → **0.36.2**（UI bug 修复，patch +1）。 |
| 219 | 代码审查发现决议 #216 给结构面板普遍加了 `backdrop-filter`，在 Win7 禁硬件加速基线上构成软件渲染性能回退风险 | **收敛 `backdrop-filter` 的使用范围到真正压住内容的浮层**（菜单、popover、个人卡、撤回 toast、模态遮罩）。主窗口导航栏 / 列表栏、聊天头部 / 输入区、设置侧栏与分组面板等并排结构面板移除 blur——它们背后只有父容器纯色背景，模糊无视觉收益，却让每帧合成都要重采样，Win7 软渲染与设置页滚动场景开销显著（回退决议 #210 优化方向）。半透明背景与内描边、有色阴影保留，材质观感不变。同时落档事实：`prefers-reduced-transparency` 为 Chrome 118+ 特性，Chrome 108 基线上不生效，相关降级仅作为浮层材质的前向增强保留，不得再据此承诺可访问性降级。版本 0.36.2 → **0.36.3**（性能修复，patch +1）。 |
| 220 | 用户实测 UOS 20 / Win7：主窗口最小化、关到托盘或被其他窗口压住失焦时，收到新消息托盘图标不闪烁 | 根因不在托盘而在未读判定：渲染层对**当前打开会话**的新消息无条件立即置读，未读恒为 0，托盘闪烁（由未读总数驱动）从不启动；用户停留在某会话再离开窗口时症状必现。修复：渲染层引入「窗口注视」判定（页面可见且窗口聚焦，与主进程通知抑制条件 `isFocused && isVisible` 同口径）——未被注视时当前会话新消息保留未读，托盘闪烁 / tooltip / 角标 / macOS Dock 计数照常生效；窗口重新聚焦时自动补置读当前会话，未读清零、闪烁停止。非当前会话的未读行为不变；不改协议、IPC 通道与存储。补窗口聚焦 / 最小化 / 失焦三态与聚焦补置读回归测试。版本 0.36.3 → **0.36.4**（bug 修复，patch +1）。 |
| 221 | 用户反馈内置截图卡顿，Win7 上像是在一张先截好的图片里框选；截图内任务栏还会被真实任务栏顶起，导致整张桌面图纵向变形 | 根因有两层：① Windows 会把普通置顶截图窗约束到 `workArea`，旧实现却把包含任务栏的 `display.bounds` 整屏位图以 `100% 100%` 压进更矮的客户区，造成双层任务栏和纵向缩放；② 框选拖动时同时重绘舞台与选区两份全屏背景，并逐次响应所有 `mousemove`，Win7 禁硬件加速后开销明显。修复：Windows 统一以 `display.workArea` 建截图窗，并依据桌面源实际像素尺寸精确裁掉四边任务栏差值；主进程以 PNG `ArrayBuffer` 注入，移除 data URL 的 base64 与字符串复制成本；渲染层改为单一 `<img>` 图层，选区外使用四块纯色遮罩，鼠标移动以 `requestAnimationFrame` 合并；截图窗口保持隐藏，直到位图解码且首帧布局完成再显示。最终裁剪按图片自然尺寸与视口尺寸分别计算 X/Y 比例，不再假设系统 `scaleFactor` 等于截图实际比例。补底部 / 顶部 / 左侧任务栏与 125% DPI 几何测试、截图窗就绪显示测试及渲染源码约束测试。版本 0.36.4 → **0.36.5**（截图性能与 Win7 显示修复，patch +1）。 |
| 222 | 用户反馈 Win7 / UOS 打开设置后，主窗口与设置窗颜色接近且缺少可靠原生阴影，两个窗口粘在一起、设置边界难以辨认；macOS 原生阴影正常 | 按模态任务层级处理：Windows / Linux 的设置窗设为主窗 modal 子窗，创建成功后向主窗口 renderer 推送 `ui:settings-window-state=true`，主窗显示一层纯色半透明 scrim 并拦截交互；设置窗关闭时推送 false 恢复。浅色遮罩使用约 24% 深灰绿、深色约 38% 黑色，进入 / 退出只做 140ms opacity 过渡；减少动态效果时直接静态切换。设置窗口根容器增加平台无关的 1px 高对比内描边，BrowserWindow 显式 `hasShadow:true`；Win7 / UOS 即便合成器不提供阴影，边界仍清楚。macOS 不发送遮罩状态，沿用原生窗口阴影。无透明窗口、无 blur、无持续重绘。版本 0.36.5 → **0.36.6**（跨平台窗口层级微调，patch +1）。 |
| 223 | 用户要求设置中的「发送键」「主题」与账号资料的「图标头像 / 昵称首字」使用同一种滑块格式，并用太阳 / 月亮表达浅色 / 深色主题 | 三处二选一控件统一为自绘双段滑块：共享浅色底座、移动选中块、茶青选中色和键盘焦点；选中块只以 `transform` 做 160ms 无回弹位移，按下即时缩放，`prefers-reduced-motion` 下取消位移动画。主题段仅显示本地线性太阳 / 月亮图标，通过 `aria-label` 分别暴露「浅色主题」「深色主题」；发送键继续显示 `Enter` / `Ctrl/Cmd+Enter`。移除设置页不再使用的 Naive UI RadioGroup / RadioButton，其他 Naive UI 表单控件与即时保存语义不变。版本 0.36.6 → **0.36.7**（设置 UI 微调，patch +1）。 |
| 224 | 用户反馈发送键滑块中的 `Ctrl/Cmd+Enter` 太长，希望 Control、Command、回车改成图标展示 | 发送键两段改为本地线性键帽图标组：第一段只显示回车键；第二段按当前平台显示修饰键 + 回车——macOS 为 Command，Windows / Linux 为 Control，不再同时堆叠两种平台文字。图形由 `PantryIcon` 内置 `key-command` / `key-control` / `key-enter` 提供，按钮分别用完整 `aria-label` 暴露「Enter 发送」「Command + Enter 发送」或「Control + Enter 发送」。发送键滑块宽度收紧，存储值与输入框行为不变。版本 0.36.7 → **0.36.8**（设置 UI 微调，patch +1）。 |
| 225 | 用户确认项目当前使用 MIT，要求改为 GPL v3 | 自 **0.37.0** 起，项目自身源码与二进制分发统一采用 **GNU General Public License v3.0 only（SPDX：`GPL-3.0-only`）**。根目录 `LICENSE` 使用 GNU 官方 GPLv3 英文全文，`package.json` / 锁文件、README 徽章与许可说明、设置「关于」页同步；打包配置把 `LICENSE` 复制到应用资源目录，确保二进制分发携带许可文本。0.36.8 及更早版本已经授予的 MIT 权利继续有效；`THIRD_PARTY_NOTICES.md` 中 Naive UI、Twemoji 等第三方组件仍按各自许可证分发，不随项目主许可改写。无协议、IPC、存储与网络行为变化。版本 0.36.8 → **0.37.0**（分发策略重大调整，minor +1、patch 归零）。 |
| 226 | 用户认为现有 SVG 绘制的应用图标略显不协调，要求使用生图功能，并选定第一张方案落地查看效果 | 采用用户选定的 **方案 A**：深茶青圆角方形底板、暖白茶杯 / 对话气泡轮廓、琥珀茶汤、双叶与三点消息，使用克制的软质珐琅层次。生成图经洋红色键控去背、透明边缘清理与居中方形规范化后，保存为 1024×1024 RGBA 位图母版；`gen-app-icons.mjs` 从该母版派生 Win7 BMP ICO、macOS ICNS、Linux 16–512px PNG 与窗口图标。应用内关于页 / 空状态 / 首启向导改用同源 512px PNG；Windows / Linux 彩色托盘从位图母版缩至 32px，macOS 菜单栏继续保留单色模板图以满足系统着色与小尺寸清晰度。旧 SVG 套件留作历史源与单色小图标回退，不再作为彩色应用图标母版。无协议、IPC、存储与外网请求变化。版本 0.37.0 → **0.38.0**（品牌视觉功能更新，minor +1、patch 归零）。 |
| 227 | 用户反馈单聊顶部对方名称区域在鼠标悬停时出现大块按钮背景，视觉笨重，希望只保留名字颜色高亮 | 恢复决议 #84 的轻量反馈：私聊头部资料入口继续保留完整点击热区和键盘 `focus-visible` 轮廓；鼠标悬停或资料弹窗打开时，只把昵称文字切换为茶青 `--primary`。移除整块 `--surface-hover` 背景、按下缩放与对应背景 / transform 过渡，头像、在线状态、IP 和头部材质均保持稳定。增加源码级 UI 回归测试，防止后续整体主题调整再次给该宽区域引入背景块。纯 renderer 样式修复，不改资料弹窗、协议、IPC、存储或网络行为。版本 0.38.0 → **0.38.1**（UI 回归修复，patch +1）。 |
| 228 | 用户要求去掉消息气泡顶部高光、统一文件卡与文字气泡四角弧度，并用生图能力重绘 Word / PPT 等文件类型图标 | 三项统一完成：① 文字消息气泡移除 `--highlight-edge` 顶部高光，接收气泡只保留极轻外阴影，自己气泡保持纯色平面；② 文件卡圆角从 8px 调整为与文字气泡完全相同的 14px，四角等弧，不使用方向性尖角；③ 使用内置 ImageGen 生成原创简洁文件类型图标图集，经洋红键色去背和单元格归一化后保存为 4×4、1024×1024 RGBA PNG atlas。图集覆盖 Word、Excel、PPT、PDF、压缩包、图片、音频、视频、文本、代码、安装包、通用文件与文件夹；单图限制为两级主色、粗轮廓、少细节、无投影 / 发光 / 官方商标复刻，在 36px 下优先识别。`FileTypeIcon` 保持原 API，运行时只按扩展名选择 atlas 单元格，不引入新依赖或外网资源。版本 0.38.1 → **0.39.0**（消息与文件视觉功能更新，minor +1、patch 归零）。 |
| 229 | v0.39.0 tag 流水线在 macOS 打包阶段失败：复制 GPLv3 许可文件时报 `EEXIST` | 根因是 `LICENSE` 同时声明于全局 `build.extraResources` 和 `build.mac.extraResources`，electron-builder 合并两层数组后向 `Contents/Resources/LICENSE` 复制两次。保留全局 `extraResources` 的单一许可分发配置，使 Windows、Linux、macOS 三端继续携带 GPLv3 全文；从 macOS 专属资源列表移除重复项，该列表仅保留本地化资源。许可元数据测试新增“macOS 不重复声明 LICENSE”断言，锁定合并配置安全。无运行时、协议、IPC、存储与网络行为变化。版本 0.39.0 → **0.39.1**（发布打包修复，patch +1）。 |
| 230 | 用户反馈消息转发弹窗会被消息列表覆盖，遮罩与对话框层级失效 | 根因是 `ForwardDialog` 挂在 `.chat` 内部，受到聊天根节点 `isolation:isolate` 与 `overflow:hidden` 建立的局部层叠 / 裁剪上下文约束，固定定位仍无法稳定高于消息区其他层。将转发弹窗通过 Vue `Teleport` 挂到 `body`，使用独立全窗遮罩和明确的全局层级；对话框补 `role="dialog"`、`aria-modal`、初始焦点与全局 `Esc` 关闭，遮罩和弹窗只做短 opacity / transform 进场，减少动态效果时关闭动画。新增源码级 UI 回归测试锁定 Teleport、层级和无障碍契约。纯 renderer 修复，不改转发业务、协议、IPC、存储或网络行为。版本 0.39.1 → **0.39.2**（UI bug 修复，patch +1）。 |
| 231 | 面向低性能 UOS 与 Win7 做第二轮代码扫描后，确认软渲染浮层、自动 OCR、图片同步 I/O、启动闭包与超规格位图仍有可收敛开销 | 第一批采用统一的运行时性能画像：主进程把既有 Win7 / Linux 软渲染判定通过 `AppInfo.softwareRendering` 告知各 renderer；这些平台关闭浮层 `backdrop-filter`、收紧阴影，并停用小图自动 OCR，底部「识别文字」手动入口完整保留。图片发送暂存、OCR 图源读取与图片另存改用 `node:fs/promises`，避免大图片同步复制 / 读取阻塞主进程事件循环。构建门禁从公共 200 KiB 启动闭包扩展到四个窗口各自的完整静态 JS / CSS 闭包预算；renderer 品牌图由 512px 降至 256px，文件类型 atlas 由 1024px 降至 512px，显示尺寸与透明 RGBA 语义不变。无协议、数据库、依赖、外网请求与硬件加速基线变化。版本 0.39.2 → **0.39.3**（低性能平台性能微调，patch +1）。 |
| 232 | 用户确认主窗口保留 Naive UI，并要求继续审查可复用控件，同时落实图片惰性加载 | 保留决议 #215/#216 的混合接入与主窗口 `NConfigProvider` / `NInput` / `NButton`；代码审查把 `GroupCreator`、`SetupWizard`、`ProfileCard`、`ForwardDialog` 的普通输入框和主次按钮列为后续复用候选，高密度成员复选框、消息工具栏、右键菜单与聊天输入器继续使用项目组件，本轮不改变可见控件。性能增量覆盖三个可滚动媒体面：聊天图片 / 表情消息、聊天记录搜索缩略图、表情包网格的 `<img>` 增加原生 `loading="lazy"` 与 `decoding="async"`，只在接近视口时取图并避免同步解码阻塞渲染；截图桌面图、独立看图原图、品牌首屏图等即时内容保持 eager。无协议、IPC、存储、依赖与网络变化。版本 0.39.3 → **0.39.4**（图片加载性能微调，patch +1）。 |
| 233 | 用户接受主窗口下一批 Naive UI 复用建议，要求提升一致性与美观 | `GroupCreator` 的搜索、组名、两次管理密码与密码提示切换为主题化 `NInput`，页脚取消 / 上一步 / 下一步 / 创建切换为 `NButton`；`ProfileCard` 的本地备注切换为 `NInput`，保存备注 / 发消息切换为 `NButton`。两个组件继续继承主窗口现有 abstract `NConfigProvider` 与集中主题，不新增 Provider、依赖或静态入口。高密度成员复选框、已选成员标签、对话框结构与资料页信息布局保留轻量实现；字段长度、回车保存、禁用态、创建中 / 保存中状态与业务调用保持。版本 0.39.4 → **0.39.5**（主窗口 UI 一致性微调，patch +1）。 |
| 234 | 用户确认补齐图片像素校验、缩略图缓存与近视口加载优化，并拍板安全阈值 | 内联图片固定要求单边 ≤8192px、总像素 ≤3200 万；PNG/JPEG/GIF/WebP/BMP 在发送落盘前读取文件头验证真实格式、宽高与动画信息，扩展名伪装、无法识别及超限图片统一按普通文件发送，表格文字元数据只随合格图片发送。原图查看、OCR、复制、收藏与 `pantry-img` 读取前复用同一校验，异常图片不得进入 renderer 解码。聊天气泡和记录搜索在接近视口后优先读取 320px WebP 派生缩略图；未命中时由 renderer 使用 `createImageBitmap` 的 resize 选项生成并经受限 IPC 原子写入独立缓存，再切换到 `pantry-thumb`；GIF/APNG/动画 WebP、小于等于 320px 的图片继续直接读取原图。派生缓存上限 128MB，按最近访问时间清理，可随时重建且不进入备份；独立看图始终读取通过校验的原图。无线上协议、数据库、依赖与外网变化。版本 0.39.5 → **0.39.6**（低性能平台图片解码与缓存优化，patch +1）。 |
| 235 | 用户发现点击“发送图片”后可选择任意文件并按普通文件发送 | “发送图片”改用独立图片选择 IPC，系统文件对话框只展示 PNG/JPG/JPEG/GIF/WebP/BMP，并允许多选；主进程对返回路径再次按受支持扩展名白名单过滤，只给合格路径签发一次性授权。渲染层移除该按钮对非图片路径调用普通文件发送的兜底；“发送文件”与“发送文件夹”继续使用原通用选择器。决议 #234 的真实格式、像素门禁以及异常 / 超限图片降级规则保持。无线上协议、数据库、依赖与网络变化。版本 0.39.6 → **0.39.7**（图片选择器范围修复，patch +1）。 |
| 236 | 用户反馈主动取消文件发送后，文件卡错误显示“传输失败” | 发送方主动取消文件传输时，同时把传输与对应消息写入 `canceled` 终态；迟到的 offer ACK 失败、聚合回调或其他异步失败不得再覆盖该终态。发送方文件卡统一显示“发送取消”并使用灰色状态，接收方自行取消或发送方收到对端取消继续显示“已取消”，真实发送错误继续显示“传输失败”。群聊批量发送主动取消后同样显示“发送取消”。消息状态类型新增本地 `canceled`，只影响 IPC / 本地存储展示，不增加数据库迁移；无线上协议、依赖与网络变化。版本 0.39.7 → **0.39.8**（文件取消状态修复，patch +1）。 |
| 237 | 用户要求压缩发起讨论组时的人员信息，组织分组放到姓名右侧且不分行 | 建群第一步的联系人项改为单行紧凑布局：复选框、在线点、姓名、公司 / 部门 / 团队路径、离线标记保持同一行；组织路径使用中点分隔并在剩余空间内省略，完整内容保留悬停标题。成员项最小高度从 40px 收紧到 34px，姓名保留主要层级，离线标记固定在最右侧。搜索、多选上限、已选成员条和 Naive UI 输入 / 按钮保持。纯 renderer 布局微调，无协议、IPC、存储、依赖与网络变化。版本 0.39.8 → **0.39.9**（建群成员列表微调，patch +1）。 |
| 238 | 用户认为设置侧栏顶部的头像 / 昵称摘要重复，并反馈主界面个人信息卡出现偏慢、指针移入卡片后会提前消失 | 设置侧栏移除顶部账号摘要，分组导航从拖拽带下方直接开始；账号资料页中的头像编辑与个人信息表单完整保留。主界面个人信息卡的悬停出现等待从 420ms 收短为 120ms，头像、二者间的透明桥接区与信息卡共同构成连续悬停范围；指针离开整个范围后才收起。卡片继续只响应真实鼠标悬停，不响应启动焦点或键盘焦点。纯 renderer 模板 / CSS 微调，无协议、IPC、存储、依赖与网络变化。版本 0.39.9 → **0.39.10**（UI 交互修复，patch +1）。 |
| 239 | 用户要求为设置侧栏的每个分组选项绘制合适图标并放到文字前 | 7 个设置分组分别使用项目内自绘线性图标：账号资料=身份卡、通用=调节滑杆、通知=通知铃、聊天与文件=带文件页的对话框、网络=局域网连接节点、快捷键=键盘、关于=信息圆标。图标统一 18px、1.6px 圆角描边并继承当前文字色；默认与次级文字同色，悬停随行提高对比，选中时与文字共同变为茶青。图标收进设置窗口动态入口专属组件，避免增加其他窗口的共享图标代码；不增加位图资源、依赖、外网请求或图片解码。版本 0.39.10 → **0.39.11**（设置导航视觉微调，patch +1）。 |
| 240 | 用户要求端口修改增加强提醒，只有明确了解影响的用户确认后才能编辑，确认按钮使用红色高亮 | 设置「网络 → 端口」的 UDP / TCP 输入框默认只读；用户点击或键盘聚焦任一端口时，先弹出居中确认框。文案说明 UDP/TCP 端口共同关联局域网发现、消息和文件传输，修改后需要重启，并确保相关客户端、防火墙和网络策略使用匹配配置；配置不一致可能导致联系人无法发现、消息或文件无法送达。补充强调“只有在明确了解当前网络部署，并已准备同步调整相关配置时，才建议继续”。操作仅有「取消」与红色「确认修改」；取消、遮罩或 Esc 保持原值，确认后只解锁当前端口并自动聚焦，失焦沿用现有范围校验和保存逻辑。纯 renderer 交互保护，无协议、IPC、存储结构、依赖或默认端口变化。版本 0.39.11 → **0.39.12**（重要设置防误改，patch +1）。 |
| 241 | 用户要求群主可分配管理员，并放开普通成员邀请权限 | 讨论组新增显式 `ownerId/adminIds`：群主独占管理员任免，可改群名并移出管理员/普通成员；管理员可改群名、移出普通成员；任意现有成员都可直接邀请联系人入群。管理密码继续兼容，群主/管理员免密码，密码持有者只能改名或移出普通成员。群主退出时按成员顺序优先转让给首位管理员，否则转让给首位剩余成员。成员列表显示群主/管理员徽标，邀请、踢人、退群、任免、转让和改名均生成幂等系统提示。协议增加可选角色字段与 `gr1` 能力位，SQLite v11、备份和 IPC 同步扩展；旧端混用时提示升级但不阻断。版本 0.39.12 → **0.40.0**（功能更新，minor +1、patch 归零）。 |
| 242 | 用户要求群成员添加改用适合大量联系人的搜索多选弹窗 | 群成员面板保留单一「添加成员」入口，点击后打开全局模态弹窗；复用建群选人的紧凑联系人行、备注/昵称/组织/IP/主机名搜索、在线状态、复选框与已选标签。当前群成员从候选中隐藏，可选人数固定为 `200 - 当前群人数`；确认后通过一次 `invite` 操作批量加入并生成一条合并系统提示。弹窗通过 Teleport 脱离聊天区裁剪，支持遮罩、取消、Esc、初始焦点与焦点恢复，提交期间禁止重复操作和关闭；失败时保留选择并显示行内提示。协议、IPC、数据库与依赖保持不变。版本 0.40.0 → **0.41.0**（批量邀请功能更新，minor +1、patch 归零）。 |
| 243 | 用户要求支持自定义用户头像与群头像，并可在应用前预览、拖动和缩放图片 | 新增用户/群共用的圆形头像裁剪弹窗，静态 JPG/PNG/WebP/BMP 经本地裁剪后统一生成 192×192、≤32KiB WebP；源图限制 20MiB、8192px，动态或异常图片拒绝。`Profile/GroupMeta` 增加可选头像 SHA-256，数字头像和默认群图标继续作为回退；新增 `av1` 能力与可靠头像按需取图报文，图片只在纯内网传播并写入受管缓存。群头像权限与改名一致，显示于会话列表、群聊头部和群成员面板，变化写入幂等系统提示。SQLite v12、备份、IPC 与 CSP 同步扩展；旧端继续显示回退头像。版本 0.41.0 → **0.42.0**（功能更新，minor +1、patch 归零）。 |
| 244 | 用户反馈设置的头像编辑区同时出现“图片头像”和“选择图片”，形成两个重复入口 | “图片头像”分段项作为未设置自定义头像时的唯一图片选择入口，点击后直接打开文件选择与裁剪弹窗；已有自定义头像时才在分段项下显示“更换图片 / 恢复默认”，避免同一动作重复展示。图标头像、昵称首字、即时保存、裁剪和同步逻辑保持。纯 renderer 条件展示修复，无协议、IPC、数据库或依赖变化。版本 0.42.0 → **0.42.1**（UI 回归修复，patch +1）。 |
| 245 | 用户要求重整设置头像模式的名称与选项展示 | 三段模式统一为“动物表情 / 昵称首字 / 自定义头像”。选择动物表情时只展示动物表情网格和背景颜色；选择昵称首字时只展示背景颜色，并允许显式选择和同步 10 种背景色；选择自定义头像时只展示一个“上传图片”按钮，点击后进入既有选图与裁剪流程。`Profile.avatar` 复用保留区间 `200..209` 编码昵称首字背景色，旧值 `-1` 继续按昵称散列颜色；不增加 SQLite 字段或 IPC。版本 0.42.1 → **0.43.0**（功能更新，minor +1、patch 归零）。 |
| 246 | 用户反馈自定义头像放大后无法稳定拖动，上传后本人和对方均无法显示，并要求撤回刚发布的版本 | 撤回 v0.43.0 GitHub Release 与标签。裁剪工作区改为窗口级 Pointer Events 跟踪，按 pointerId 维持单次拖动，并在 pointerup / pointercancel / lostpointercapture / 卸载时统一收尾；放大后仍按当前缩放边界移动且不露空白。头像读取 URL 改为固定短主机名加 64 位哈希路径，并在应用就绪前把 `pantry-avatar` 登记为标准安全 scheme，使 Chrome 108 在 Win7 / UOS / macOS 上使用一致的解析规则；本人、联系人和群头像共用同一 URL 生成与严格解析函数，已获取头像通过 `avatar:ready` 继续触发重试。线上头像哈希与 `avatar` 报文、SQLite v12、IPC 和 `av1` 保持兼容。版本 0.43.0 → **0.43.1**（严重回归修复，patch +1）。 |
| 247 | 本机双实例实测确认 #246 未完整修复：放大后仍无法拖动，PNG/JPEG 均生成同一份全透明 WebP，导致本人、联系人和群头像继续显示回退图 | 裁剪拖动改用桌面端稳定的窗口级 `mousedown/mousemove/mouseup`，不再依赖 Pointer Capture；按下后记录起点与当前偏移，移动时继续复用缩放边界，抬起、窗口失焦和卸载统一结束。裁剪几何固定使用主进程已校验的源图宽高，避免 DOM `naturalWidth/naturalHeight` 在解码前以 `0×0` 被 computed 缓存；应用头像时从原始受检字节创建 `ImageBitmap` 并绘制到 192×192 Canvas。编码前读取像素 Alpha，裁剪结果全透明时拒绝保存并保留弹窗。补可见像素、全透明拒绝、位图释放、窗口级鼠标拖动源码约束与双实例 PNG/JPEG 真机回归。线上协议、SQLite v12、IPC、`av1`、受管头像 URL 和缓存结构保持。版本 0.43.1 → **0.43.2**（严重回归修复，patch +1）。 |
| 248 | 自定义头像功能（#243–#247）代码审查发现四处健壮性 / 效率问题，用户确认修复 | 恢复默认群头像的 IPC 失败补上错误提示，不再产生未处理异常；资料保存拒绝声明受管缓存中不存在的新头像哈希，避免全网节点对取不到的头像无休止空请求；头像目录 prune 只清理修改时间超过 1 分钟的陈旧 `.tmp` 残留，不再与正在进行的原子写入竞态；`AvatarStore` 增加已验证哈希内存缓存，`has/resolvePath` 命中后不再重复读盘、解析 WebP 与重算 SHA-256，prune 删除文件时同步失效。无线上协议、SQLite、IPC 结构与依赖变化。版本 0.43.2 → **0.43.3**（健壮性加固，patch +1）。 |
| 249 | 头像审查遗留两项：群头像请求源 ACK 后无文件会静默等 10 秒超时；裁剪最大缩放固定 4 倍与源图分辨率无关，大图裁不出小区域 | 协议 v0.48 为 `avatar` 报文新增无数据提示 `{op:"miss", hash, groupId?}`：来源通过成员/权限校验但无法提供数据时以一次性 UDP 单发告知（不走可靠通道，旧端整包忽略不回 ACK、可靠发送会误判离线）；请求方只接受当前请求源的 miss，群头像立即改试下一个未尝试的在线 `av1` 成员源，用户头像保留请求登记至超时作为冷却、不再放大空请求频率。裁剪弹窗最大缩放改为随源图动态放宽：允许放大到裁剪区域约等于 192px 输出分辨率，小图不放大，加减按钮与滚轮改为 ×1.12 等比步进、滑杆上限同步；输出降采样改经 `createImageBitmap` 裁剪 + 高质量缩放一步完成，消除大图单步 drawImage 锯齿，整数裁剪矩形夹回图内避免采到界外透明像素。触屏拖动（Pointer Events）不做：#246 刚在真机回归撤回，桌面产品收益低风险高。版本 0.43.3 → **0.44.0**（协议与裁剪功能更新，minor +1、patch 归零）。 |
| 250 | 用户搜索 `juntian` 时出现昵称为 `test1` 的联系人，并要求搜索结果显示头像 | 全局搜索的联系人匹配范围收敛为本地备注、昵称、公司、部门、团队与 IP，移除隐藏主机名匹配，避免同一台机器上的多个联调身份因共享主机名产生无关命中；建群与群邀请的成员搜索仍保留主机名字段。联系人结果行新增与通讯录一致的圆形头像，支持数字头像、自定义头像与离线灰显，名称按本地备注优先展示，副行继续显示组织路径或 IP。无协议、IPC、SQLite、依赖与网络变化。版本 0.44.0 → **0.44.1**（搜索 bug 与 UI 修复，patch +1）。 |
| 251 | 用户反馈消息右键菜单中的“撤回（04:55）”换成两行且视觉权重过高 | 撤回操作名与动态状态拆为同一按钮内的左右两列：左侧固定“撤回”，右侧显示小号、弱化、等宽数字 `mm:ss`，不可用原因也放右侧；按钮与复制 / 转发保持相同行高、禁止换行，最后 10 秒才把倒计时提升为危险色。保留 500ms 实时刷新、5 分钟窗口、超时禁用和媒体撤回条件；高频菜单不新增进出或倒计时动画。新增格式化与源码级 UI 回归测试。纯 renderer UI 修复，无协议、IPC、SQLite、依赖与网络变化。版本 0.44.1 → **0.44.2**（UI 微调，patch +1）。 |
| 252 | 用户认为通讯录已有绿 / 灰状态点，无需再显示“离线”文字 | 普通通讯录联系人行以行尾绿色 / 灰色圆点作为明确在线状态标识，离线联系人继续保持头像与姓名灰显、在线优先排序；移除姓名后的“· 离线”副文字及专用样式，减少重复信息并给姓名留出更多横向空间。聊天头部、联系人资料等没有同等行尾状态点或需要明确语义的位置继续显示在线 / 离线文字。新增源码级 UI 回归测试。纯 renderer UI 微调，无协议、IPC、SQLite、依赖与网络变化。版本 0.44.2 → **0.44.3**（UI 微调，patch +1）。 |
| 253 | 用户反馈 Win7 输入法候选窗会固定到屏幕左上角，且最终用户无法通过启动参数自行规避 | Electron 22 所含 Chromium 108 在 Windows 默认启用 TSF，部分 Win7 旧输入法 / 第三方输入法无法从该路径取得有效光标布局，候选窗会回退到左上角。应用启动早期仅在 Windows NT 6.1（Win7 / Server 2008 R2）追加 `--disable-features=TSFImeSupport`，让 Chromium 自动回退到兼容性更好的 IMM32；Win8 及以上 Windows、Linux、macOS 保持原路径。Win7 判定与既有软渲染条件复用，新增纯函数测试覆盖 NT 6.1 生效及其他平台不生效。无 UI、协议、IPC、SQLite、依赖与网络变化。版本 0.44.3 → **0.44.4**（Win7 兼容修复，patch +1）。**（已被 #254 认定无效并撤销）** |
| 254 | v0.44.4 发布后用户实测反馈候选窗仍固定在屏幕左上角（截图为搜狗拼音） | 经核对 Chromium 108 源码（`ui/base/ime/init/input_method_factory.cc`），Windows 输入法路径选择条件为「启用 `TSFImeSupport` **且**系统版本 > Win7」才走 TSF，Win7 恒走 IMM32——#253 追加的 `--disable-features=TSFImeSupport` 在 Win7 上是空操作，故修复无效属必然。本轮撤销该开关（删除 `windows-ime.ts`，Win7 判定保留为 `util/windows-version.ts` 供软渲染条件复用），并加源码级回归测试禁止再以该开关名义修输入法。真实病灶在 IMM32 定位链路（`ImmSetCandidateWindow` + 系统插入符），该链路与 Win7 版 Chrome 相同、多年验证可靠，且同类「候选窗跑屏幕左上角」在 Win7 搜狗侧本身是常见故障（搜狗修复工具 / 兼容候选窗设置可解），需真机对照测试区分应用侧 / 输入法侧后再定向修复（记事本同机对照 + 微软拼音应用内对照）。无 UI、协议、IPC、SQLite、依赖与网络变化。版本 0.44.4 → **0.44.5**（撤销无效修复，patch +1）。 |
| 255 | 承接 #254：多名 Win7 用户反馈候选窗固定屏幕左上角，同机其他软件正常，用户要求按自有分析直接修复 | 结合截图症状定位故障模型：Win7 IMM32 路径要求 Win32 键盘焦点位于顶层窗口，候选窗定位（`ImmSetCandidateWindow` + 系统插入符）、MSAA 光标事件与 WM_IME_* 消息处理均以其为锚点；焦点落入 Chromium 内部子窗口（Chrome Legacy Window，仅转发普通键盘消息、不转发 WM_IME_*）时，打字仍可上屏但候选窗退回系统默认位置（屏幕左上角），且窗口已激活、点击页面不会触发系统重派焦点，故障呈粘性。修复：新增 `util/win7-ime-focus.ts`，主窗口与设置窗口每次获得焦点后延迟 150ms 重走一次原生 show 流程。版本 0.44.5 → **0.44.6**。**（源码复核确认 `show()` 无法保证执行 `SetInitialFocus` 且会重复触发窗口显示生命周期，已被 #256 撤销，未发布）** |
| 256 | 复核 #255 并结合用户新增截图继续修复 Win7 IMM32 候选窗退回应用左上角 | Electron 22 的 BrowserWindow 已提供初始焦点视图，`HWNDMessageHandler::Show` 会先由 `Widget::SetInitialFocus` 聚焦内容视图，成功后跳过兜底 `SetFocus(hwnd)`；同时 `show()` 会无条件激活窗口并通知 `WasShown`，因此 #255 的焦点自愈既不能保证重置输入法焦点，又有重复生命周期副作用。本轮删除主进程 `show()` 自愈，改在主聊天原生 `textarea` 上执行 Win7 专用输入状态刷新：输入框获得焦点，或窗口重新激活且输入框仍为活动元素时，在无 composition 的事件后微任务中保存选区与滚动位置，短暂 `blur()` 后立即 `focus({preventScroll:true})` 并恢复选区/滚动，确保刷新早于下一次键盘输入。原生输入框从 `NONE` 回到可输入状态会让 Chromium 108 重新发布 text-input state 与 selection bounds，`RenderWidgetHostViewAura::OnSelectionBoundsChanged` 随即调用 `InputMethodWinImm32::OnCaretBoundsChanged`，再由 `IMM32Manager` 以真实 caret rect 同步 `ImmSetCandidateWindow` 与临时系统 caret；不再依赖不可达的 HWND 兜底分支。仅 Win7 主聊天输入框启用，设置窗口和其他平台保持原行为；composition 期间跳过，避免中断正在组词。新增纯函数测试覆盖平台门控、禁用/非活动/组词保护、选区与滚动恢复，并加源码级接线回归。无协议、SQLite、依赖与网络变化；`AppInfo` 仅增加本机 `windows7` 布尔标记供 renderer 精确门控。版本 0.44.6 → **0.44.7**（Win7 兼容修复，patch +1）。**（Win7 搜狗真机实测无效，已被 #257 撤销）** |
| 257 | v0.44.7 发布后 Win7 搜狗输入法实测候选窗仍停在应用左上角 | 实测撤销 #256 的 `textarea` blur/focus 刷新及 `AppInfo.windows7` 门控，避免继续保留无效的输入状态扰动。Chromium IMM32 源码显示 `MoveImeWindow()` 在 `GetFocus() != window_handle` 时直接返回，中文输入法还可能忽略 `ImmSetCandidateWindow()` 并读取临时系统 caret；DOM 焦点成立无法证明目标 HWND 已持有 Win32 键盘焦点。本轮在 Win7 主窗口重新激活、主聊天 textarea 获焦两个边界直接调用 `webContents.focus()`，把系统焦点同步到 Chromium 原生视图。排查同时发现字体缩放一直通过 renderer 的 `document.body.style.zoom` 实现，默认 100% 也会写入 `zoom: 1`；该 CSS 坐标空间位于 DOM caret bounds 与 Windows IMM32 映射屏幕坐标的关键路径上。本轮把 100% / 110% / 125% 字体缩放迁到主进程 `webContents.setZoomFactor()`：主窗口和设置窗口创建时先应用当前倍率，renderer 加载完成后按最新设置再次确认，设置更新时对现存的主窗口与设置窗口同步倍率，再向 renderer 广播设置；renderer 只保留主题切换并清除旧的 body inline zoom。截图窗和看图窗维持 1:1 坐标。页面视觉倍率和设置语义保持，缩放坐标由 Chromium WebContents 原生链路统一换算。新增纯函数和窗口接线回归测试，锁定 Win7 门控、原生焦点调用、倍率白名单、销毁保护、窗口创建/加载/更新同步及 renderer 禁止重新写入 body zoom。无协议、SQLite、依赖与网络变化；IPC 仅增加本机窗口焦点同步调用。版本 0.44.7 → **0.44.8**（Win7 搜狗输入法兼容修复，patch +1）。**待 Win7 搜狗真机回归验证**。 |
| 258 | v0.44.8 发布后 Win7 搜狗再次真机实测失败；新增照片显示候选窗精确贴在茶话间客户区左上角，实际 textarea 光标位于窗口右下方 | 照片把失效锚点从“系统屏幕原点”进一步收敛为“应用客户区 `(0,0)`”：目标 WebContents 已获得输入并可正常上屏，#257 的 Win32 焦点假设不成立，撤销 `webContents.focus()`、对应 IPC 与主窗口 focus 接线；#257 已完成的 `webContents.setZoomFactor()` 迁移继续保留，避免恢复 renderer CSS zoom。主聊天 textarea 此前以 `position:absolute; inset:0` 覆盖在固定高度容器中，本轮改为占满容器的正常流定位，并补 `box-sizing:border-box`，让 Blink 直接从实际布局对象计算编辑光标。仅 Win7 在 `compositionstart` / `compositionupdate` 事件内同步交替改变 textarea 左内边距 0–1px 并立即读取布局矩形，避免连续事件被 Vue 渲染批处理合并，触发 Chromium 108 重新布局编辑光标并向 `InputMethodWinImm32::OnCaretBoundsChanged` 发布非零 selection bounds；`compositionend` 立即复位。脉冲不改输入值、选区、DOM 焦点和组词状态，其他系统不产生布局扰动。恢复 `AppInfo.windows7` 只用于 renderer 精确门控，新增纯函数与源码接线测试覆盖平台/活动元素保护、0–1px 交替、结束复位、正常流布局及旧焦点 IPC 完整移除。无协议、SQLite、依赖与网络变化。版本 0.44.8 → **0.44.9**（Win7 搜狗输入法兼容修复，patch +1）。**待 Win7 搜狗真机回归验证**。 |
| 259 | `v0.44.9-beta.1` 在 Win7 搜狗真机仍失败；交叉测试确认同一主窗口的全局搜索正常、独立设置窗口的昵称输入正常、同机 Chrome 正常，只有聊天消息框候选窗落到客户区左上角 | 交叉结果排除 BrowserWindow 无边框、WebContents 焦点、页面缩放和搜狗全局环境，故障范围收敛到聊天自定义多行编辑器。#258 的 composition 几何脉冲实机无效，本轮完整删除。为避免继续猜测具体 CSS，`0.44.9-beta.2` 仅在 Win7 把聊天框切换到基础 textarea 画像：继续复用原生 `v-model`、键盘发送、粘贴和滚动事件，绕开 `PantryEmojiBlank`、Twemoji 镜像层、透明背景、绝对镜像覆盖与 composition 自定义处理；基础控件使用静态正常流、微软雅黑、实体背景和普通内边距。其他系统继续使用完整 emoji 镜像编辑器，#257 的 WebContents 原生缩放继续保留。若真机恢复，后续逐项恢复 Win7 样式以锁定触发项；若仍失败，下一轮对照 `contenteditable` 编辑路径。无协议、SQLite、IPC、依赖与网络变化；`AppInfo.windows7` 继续只作 renderer 门控。版本 **0.44.9-beta.1 → 0.44.9-beta.2**。 |
| 260 | `v0.44.9-beta.2` 经 Win7 搜狗真机确认候选窗已跟随聊天输入光标，但基础 textarea 新增的内边框、独立底色和圆角破坏了原输入区的一体化视觉；同时 Win7 缺少彩色 emoji 字体，关闭 Twemoji 镜像后，草稿中的 emoji 退化成黑色系统字形 | 保留 #259 已验证有效的真实输入层：Win7 textarea 继续使用静态正常流、微软雅黑、实体材质背景、普通内边距，禁止 `PantryEmojiBlank`、透明文字和 composition 自定义处理。视觉上移除诊断用内边框与独立圆角，让实体背景与外层 `--material-strong` 同色，恢复原输入区留白和层级。彩色 emoji 改为与输入坐标解耦的纯展示覆盖层：覆盖层绝对定位且 `pointer-events:none`，使用与真实 textarea 相同的微软雅黑、字号、行高、内边距和滚动偏移；普通文字片段透明，仅 emoji 槽以同色实体背景遮住 Win7 黑色字形并绘制本地 Twemoji SVG。真实 textarea 始终保持不透明文字和系统字体，继续作为 Chromium/IMM32 唯一的 caret 几何来源。其他平台保留既有 `PantryEmojiBlank` + 完整镜像路径。无协议、SQLite、IPC、依赖与网络变化。版本 **0.44.9-beta.2 → 0.44.9-beta.3**。 |
| 261 | `v0.44.9-beta.3` 已恢复 Win7 输入框彩色 emoji，但安全输入层改用系统字体后，emoji 字符槽通常小于消息正文的 `1.3em`：Twemoji 为避免越界被压小，连续输入多个 emoji 时，覆盖图形与原生 caret 的步进还会累计产生视觉偏差 | 在 #259 已验证候选窗正常的安全 textarea 上仅恢复 `PantryEmojiBlank` 字体首位：内置 emoji 基础码点继续使用固定 `1.3em` advance，变体选择符 / ZWJ 保持零宽，普通文字自动回落微软雅黑；Win7 展示覆盖层使用完全相同的字体栈，Twemoji 恰好铺满槽位，输入态尺寸与消息正文一致，连续 emoji 的图形、字符槽和原生 caret 逐个对齐。已验证有效的静态正常流、实体背景、不透明文字、普通内边距和无 composition 自定义处理全部保持；空白字体已隐藏底层系统 emoji，覆盖层移除遮字用实体底片与扩边，避免选区出现色块。其他平台路径不变。无协议、SQLite、IPC、依赖与网络变化。版本 **0.44.9-beta.3 → 0.44.9-beta.4**。 |
| 262 | `v0.44.9-beta.4` Win7 搜狗真机回归：只把 `PantryEmojiBlank` 放回真实 textarea 后，候选窗立即再次退到窗口角落；与 `beta.3` 系统字体 textarea 候选窗正常形成单变量对照，确认空白 WebFont 参与真实编辑排版就是触发项，#261 的 Win7 路径撤销 | Win7 真实编辑控件永久移除 `PantryEmojiBlank` 和 textarea 展示覆盖层，改用系统字体的原生 `contenteditable`：普通文字与组合输入始终由微软雅黑排版，内置 emoji 在编辑 DOM 中渲染为 `contenteditable=false` 的本地 Twemoji 原子节点，节点宽高与消息正文同为 `1.3em`，Chromium 原生 caret 直接落在节点前后，连续 emoji 无累计宽度偏差。编辑器按 UTF-16 文本偏移映射 DOM Selection，支持面板插入、@ 提及、换行、粘贴、发送与滚动；composition 期间只读取浏览器原生 DOM，不重建节点，结束后才将新 emoji 归一为原子节点，避免打断搜狗组词。其他平台继续使用既有 textarea + `PantryEmojiBlank` 镜像。无协议、SQLite、IPC、依赖与网络变化。版本 **0.44.9-beta.4 → 0.44.9-beta.5**。**待 Win7 搜狗真机验证 contenteditable 候选窗定位。** |
| 263 | 用户确认私聊文件和群聊文件都应默认只有 24 小时接收时效，到期后发送方与接收方显示符合各自方向的提示 | 普通文件 offer 增加发送端生成的 `expiresAt`，固定为发送时刻 +24 小时；接收端按信封时间计算剩余窗口并把本地截止时间夹在 24 小时以内，旧 offer 缺字段时从完整收包时刻起执行 24 小时上限。发送端为同一消息只持久化一份源文件 manifest，各点对点 transfer 持久化 `expires_at`；启动时恢复仍有效的出站供流与入站断点上下文，过期或缺少旧上下文的活动记录安全收口。`offering/failed/canceled` 到期转 `expired`，到期前已进入 `accepted` 的当前尝试可完成，失败或重启中断后若已过期则终止。发送端文件卡显示「发送已到期」，接收端显示「文件已过期」，群发卡保留已接收计数；设置传输记录使用同方向文案。图片、表情、自更新包不受该普通文件领取期限影响。协议 v0.49、SQLite v13；版本 **0.44.9-beta.5 → 0.45.0**（统一文件领取时效，minor +1、patch 归零）。 |
| 264 | `v0.45.0` 标签发布时，Windows x64 与 ia32 的源码结构测试因 checkout 使用 CRLF 换行而失败 | 源码结构测试读取 Vue 文件后统一把 CRLF / CR 换行归一为 LF，再执行既有结构断言，保证 macOS、Linux 与 Windows 的验证语义一致。产品代码、协议 v0.49、SQLite v13、依赖与网络行为均无变化；版本 **0.45.0 → 0.45.1**（跨平台测试修复，patch +1）。 |
| 265 | Issue #14：Kylin V10 SP1 内置截图选择“文字”后点击选区没有输入入口；源码复核发现文字工具调用 Electron 22 不支持的 `window.prompt()`，返回空值后静默跳过标注 | 截图层改用自绘单行输入框：点击位置作为文字锚点，输入框在视口内夹紧并自动聚焦；Enter 提交、Esc 取消本次文字，composition / `keyCode 229` 期间不抢候选确认键；发送 / 复制前提交当前非空文字。Canvas 文字改用左上基线和与预览一致的粗体。移除最后一处 `window.prompt()` 并补纯函数、源码约束测试。协议 v0.49、SQLite v13、IPC、依赖与网络行为保持；版本 **0.45.1 → 0.45.2**（截图文字标注修复，patch +1）。 |
| 266 | 用户确认截图工具条用文字显示功能按钮缺乏意义，希望改为直观图标 | 选择、矩形、箭头、文字、马赛克、发送、复制、取消全部改用现有 `PantryIcon` 线性 SVG 体系；新增截图选择、矩形、箭头、马赛克四个本地图标，发送复用纸飞机、文字复用文字选择、复制与取消复用既有图标。可见按钮不再显示功能文字，通过原生悬停提示与 `aria-label` 保留名称，工具按钮增加 `aria-pressed`；工具组和操作组增加细分隔线，并补 hover / active / focus-visible 状态。仅改 renderer 与文档，无协议、SQLite、IPC、依赖或网络变化；版本 **0.45.2 → 0.45.3**（截图工具条视觉微调，patch +1）。 |
| 267 | 用户要求截图工具条的每个按钮在鼠标移入时明确显示提示 | 八个图标按钮统一使用 `data-tooltip` 驱动截图层内的自定义提示，悬停 250ms 后显示，键盘 `focus-visible` 立即显示；提示含深色气泡与方向箭头。工具条距视口顶部不足 48px 时提示自动翻到按钮下方，避免被窗口边缘裁掉。保留 `aria-label`，移除会与自定义气泡重复的原生 `title`。仅改 renderer 与文档，无协议、SQLite、IPC、依赖或网络变化；版本 **0.45.3 → 0.45.4**（截图按钮提示完善，patch +1）。 |
| 268 | Issue #17：macOS v0.45.1 在草稿已有文字、中文输入法仍显示候选时按 Enter，会提前发送候选开始前的旧草稿 | 聊天输入统一维护 composition 活动状态，并在键盘分流最前面检查本地状态、`event.isComposing` 与 `keyCode 229`；命中时直接把按键留给原生输入法，不触发 @ 提及、发送或换行。普通 textarea 与 Win7 contenteditable 都向父层报告 composition 开始 / 结束；候选确认完成、草稿同步后，下一次 Enter 才按用户的发送键设置执行。抽取 renderer 通用 IME 判定并由截图文字输入复用，补纯函数与源码接线回归。协议 v0.49、SQLite v13、IPC、依赖与网络行为保持；版本 **0.45.4 → 0.45.5**（输入法候选确认修复，patch +1）。 |
| 269 | Issue #18：v0.45.1 / v0.45.5 发起讨论组后按钮一直停在“创建中”；在组名输入框内拖选文字并移出弹窗松手时，弹窗还会意外关闭 | 双实例实测确认 `pickedIds` 是 Vue 响应式 Proxy，直接传给 Electron IPC 会抛出 `An object could not be cloned`。建群提交前把成员 ID 复制为普通数组，并以 `try/catch/finally` 保证任何 IPC 失败都恢复按钮、保留已填内容并显示行内错误；提交期间禁止返回、取消与遮罩关闭。遮罩关闭改为同时要求按下和最终点击都发生在遮罩自身，输入框内开始的拖选即使在遮罩上松手也不关闭。补响应式数组快照、异常恢复和遮罩手势源码回归。协议 v0.49、SQLite v13、IPC 契约、依赖与网络行为保持；版本 **0.45.5 → 0.45.6**（建群严重回归修复，patch +1）。 |
| 270 | Issue #19：粘贴任何"含一个制表符 + 两行以上"的文本都会被判成表格并**当场发成图片**，用户要的纯文字发不出去，也没有任何逃生口；用户同时提出希望能自己选发文字还是发图片 | 决议 #190 的表格粘贴收紧为两条口径：HTML 侧要求整段片段有且仅有一张 `<table>` 且表外无实质文字（旧口径匹配任意位置的 `<table>`、又只取第一张表，会把"正文 + 表格"整块吃掉并让图片与文字视图内容不一致）；纯文本侧要求 ≥2 行、每行列数一致、≥2 列且首列非全空（表格软件复制矩形区域列数必然一致，Tab 缩进的代码与日志则不满足）。命中后行为由"直接发送图片"改为**只把原文插入输入框**，输入区上方显示轻提示条「检测到表格 · 发送为图片 · 忽略」，5 秒后自动消失；直接回车即发纯文字。草稿超 4096B 时文案改为「表格过大，只能按图片发送」且提示条不自动消失。用户改动草稿、切换会话、点忽略或发送后提示条即收起，剪贴板现成图片在粘贴事件内取走以免 `DataTransfer` 失效。协议 v0.49、SQLite v13、IPC 契约、依赖与网络行为保持；版本 **0.45.6 → 0.46.0**（表格粘贴交互变更，minor +1、patch 归零）。 |
| 271 | 内网日常协作里大量需求是"我把资料摆好、同事自己来取"，现有文件消息只能一次次点对点推；用户要求做文件分享：可设置自己的共享目录并设读写权限（原决议 #7 把"共享文件柜"列为远期评估） | **共享文件柜转正为正式功能**（§6.10，P1，v0.47），从非目标中移出。形态由用户拍板为「**单目录 + 默认档 + 按人例外**」：每个节点最多一个共享根目录（本机任选、默认未设置=关闭，不随资料广播、真实路径不入协议）；全局默认权限三档「不共享 / 只读 / 可读可传」，默认值为**不共享**；可对个别联系人单独指定权限，既能调高也能调低，**有效权限 = 命中例外则用例外，否则用默认档**。例外按 nodeId 记录，跟身份走，对方改昵称换 IP 不失效；权限判定全部在共享方本机完成。仅私聊一对一，双方都在线才可访问；本期不做群聊共享目录、不做跨节点搜索、不做变更订阅推送、不做在线预览编辑 |
| 272 | 「可读可传」里的"传"如果等同于完整读写，内网无账号体系、误删和被删都无从追责；但每次上传都弹确认又太吵 | 用户拍板"**只能上传新文件**"：写权限 = 允许对方往我的文件柜放东西，内容统一落到**共享根下以上传者名字命名的子目录**（`共享根/上传者显示名/`，名称取本地备注优先、其次昵称并清洗），重名自动加后缀；**不允许删除、改名、覆盖任何已有文件，也不允许上传方指定落点**。落点由接收方本机计算、不进协议（沿用决议 #179"目录名不入协议"的口径），上传方因此无法把文件写到共享根之外。支持文件 / 多文件 / 整夹上传，目录结构保留在该子目录内。上传不需要接收方逐次确认 |
| 273 | 需要一个"点开私聊就能进对方共享目录"的入口，又不能把聊天挤走 | 用户拍板**私聊头部图标按钮 + 右侧覆盖式面板**：私聊头部 `⋯` 左侧新增「文件柜」图标按钮，点击后面板绝对定位覆盖聊天区右侧一整列（复用决议 #67 群信息面板的形态，宽度 320px），盖在消息上方而不挤压布局，可边聊边浏览、关闭即回到聊天。面板内容为面包屑 + 返回上级 + 刷新 + 文件/目录列表（名称、大小、修改时间）+ 底部权限状态条与上传按钮。列表按"目录在前、再按名称"排序，单页 200 条滚动加载，单目录超 5000 条截断提示；`.` 开头的隐藏文件与目录一律不列出。对方未设共享 / 把我关成不共享 / 离线 / 版本不支持时按钮灰显并说明原因（灰显口径沿用决议 #17） |
| 274 | 别人动我的文件柜要不要提醒，提醒多了会刷屏、不提醒又可能很久不知道有人放了东西 | 用户拍板"**只在有人上传时提示**"：上传完成后在与 TA 的私聊里插一条系统提示「张三 上传了 3 个文件到你的文件柜」，点击直接打开本机对应目录；单次上传只汇总为一条。该提示让会话冒泡并**计入未读**，但**不弹桌面通知**，避免与文件消息通知互相刷屏。**单纯浏览、下载不产生任何提示**，也不写聊天记录。系统提示沿用既有 `kind='system'` 形态，不进 FTS。文件柜的上传下载正常进入「传输记录」 |
| 275 | 文件柜要能列目录、下载、上传，但用户明确要求**方案里不许新增端口** | 全部复用现有通道，**零新增端口、零新增数据面**：控制面在既有 UDP 17878 新增 `share` 报文（`list` / `list-ok` / `get` / `deny` 四个 op，可靠投递；单包超 1200B 时走既有 TCP 控制帧兜底），数据面 100% 复用 TCP 17879 的拉取式传输。**下载** = 浏览方发 `share{op:"get"}`，共享方校验权限后回既有 `file-ctl offer` 并带 `purpose:"share-get"`，浏览方自动 accept 后按既有 pull 流拉取；**上传** = 上传方直接发 `file-ctl offer` 带 `purpose:"share-put"`，共享方校验写权限后自动 accept 并拉取，落到 F-SHARE-5 规定的子目录。两类 purpose 均**不入聊天流、不生成文件消息、不套用决议 #263 的 24 小时领取期限**。新增能力位 **`shr1`**；未声明 `shr1` 的旧端按未知类型忽略 `share` 报文，新端对其灰显文件柜入口，并拒收其 `purpose:"share-*"` offer。协议细节见 protocol.md §8.2 |
| 276 | 共享目录是用户任选的真实目录，一旦路径校验有洞就等于把整块磁盘交出去 | 安全边界按 P0 级约束执行：① 网络来的路径一律当相对路径校验，拒绝绝对路径、`..`、盘符与保留字符，解析后**复核真实路径仍在共享根内**，指向共享根之外的符号链接直接跳过；② 目录层级超过 16 层不再展开；③ 同一联系人列目录限流 10 秒 5 次，超限回 `deny{reason:"busy"}`；④ 共享根**禁止选择**用户主目录根、系统盘根与本应用数据目录，设置时即拦截并说明原因；⑤ 日志沿用决议 #6 脱敏口径，只记节点 ID、条目数、字节数，**不记文件名与目录内容**；⑥ 传输仍不加密，沿用内网信任边界（决议 #5）；⑦ 分页快照最多缓存 8 份、存活 60 秒，防止有人靠翻页长期占内存 |
| 277 | 共享根、默认档、按人例外这些新状态存哪里，会不会污染既有表和配置 | 分两处存：**共享根路径与默认权限档**进 `config.json` 的 `fileCabinet: {root, mode}`（与既有设置同批原子写，缺省视为 `{root:'', mode:'off'}`）；**按联系人例外**进 SQLite 新表 `share_grants(node_id PK, mode, updated_ts)`，随迁移升到 **v14**（与 peers 同域、便于连显示名和在线状态渲染设置页表格，也避免把易变的多条记录塞进全量重写的 config）。文件柜的上传下载**复用既有 `transfers` 表**，只在 `files` JSON 里带上 `purpose`，**不新增列、不新增消息表**。系统提示复用 `messages(kind='system')`。服务层新增 `services/share.ts` 与 `store/share-repo.ts`，`net/` 只在 `codec.ts` 加 `share` 白名单，数据面 `transfer.ts` 不动，分层铁律不变 |
| 278 | v0.49.0 交付后的独立复审发现文件柜面板两处会被真实撞到的缺陷：① 滚动翻页失败会把**已经加载好的全部条目整片清空**，只剩一句错误 + 「重试」——而对方的列目录限流是 10 秒 5 次（决议 #276 ⑦），一个目录最多 5000 项、每页 200 条，进目录后连滚 4 页就到配额，**第 6 次请求必定 `busy`**，浏览大目录几乎一定触发，用户看到的是"文件突然全没了"；② 切换会话时面板不收起，直接挂到新对端身上，若新对端离线或未声明 `shr1`，会出现"头部按钮已灰显、面板却开着并报错"的矛盾状态 | ① **首屏失败与翻页失败拆成两个状态**：`failReason` 只服务首屏（进目录 / 刷新 / 面包屑跳转），仍整片替换为居中错误态；新增 `moreFailReason` 只在列表末尾追加一行横排小字 + 「重试」，**已加载条目一律保留**；翻页失败后 `onScroll` 暂停自动续拉，直到用户点重试，避免继续滚动反复撞限流。快照失效（`gone`）继续按原样静默从第 0 页重来。② 会话切换的 watch 里与 `showMembers` 一并置 `showCabinet = false`。**不改协议、不改限流常量、不动主进程**——纯渲染层失败态分层；新增 `ui/file-cabinet-panel.test.ts` 源码结构测试锁住两条行为。协议 v0.50、SQLite v14、IPC 契约、依赖与网络均不变，版本 **0.49.0 → 0.49.1**（bug 修复，patch +1）。 |
| 279 | 同一轮复审的第三条：`share:upload` 在发起前要实测选中内容的文件数与总字节（决议 #272 的 2 GiB 门禁靠它把关），但实现是**主进程里的同步递归遍历**（`statSync` + `readdirSync`）。用户上传一个几万文件的文件夹时，整棵目录树遍历完之前主进程事件循环被占满，**聊天收发、传输进度、托盘全部一起卡住**，而且界面上没有任何"正在测算"的反馈，看起来就是软件假死 | 测算逻辑从 `index.ts` 装配层抽到 `util/upload-measure.ts` 并改为异步：`fs/promises` 的 `stat`/`readdir`、显式待处理队列代替递归（深目录不吃调用栈）、**每批 64 条并发**——批内并发让 libuv 线程池跑满、速度接近同步版，批与批之间的 `await` 保证事件循环能处理别的活。深度上限 32、"任一条目读不到就整体判失败"、"空目录返回 null"、设备文件跳过等既有语义逐条保持。抽到 util 层后可被 vitest 直接调用，补 8 例真实临时目录测试（含跨批 200 条与超 32 层）。`index.ts` 随之移除 `readdirSync` 依赖、调用点改 `await`。协议 v0.50、SQLite v14、IPC 契约、依赖与网络均不变，版本 **0.49.1 → 0.49.2**（性能修复，patch +1）。 |
| 280 | 复审 C 级里 `services/share.ts` 的三条：① `ShareDownloadGate` 按 peerId 直接覆盖授权——用户先点「下载」、返回后再点「另存为」选了别的目录，此时第一批 offer 才到货，会消费掉第二次登记的落点，**先发出的文件落进了后选的目录**，对用户就是"文件找不到"；② `resolveWithinRoot` 每次调用都重新 `realpathSync(root)`，而 `listShareDirectory` 对每个符号链接条目都要调它复核归属，符号链接多的目录会把共享根反复解析几十上百次；③ `listShareDirectory` 是**读满 5000 条就 break、之后才排序**，超上限的目录展示的其实是文件系统返回顺序的前 5000 条，同一目录换台机器、甚至换个时刻看到的内容都可能不同，"目录在前"的承诺也在截断边界上失效 | ① 授权改为**句柄化 FIFO**：`begin()` 返回 token，同一对端允许多条在途并按登记顺序配对；`cancel(token)` / `isPending(token)` 只作用于本次，不再误伤同对端的其他在途授权；在途上限 32 条防异常堆积。调用点相应改用句柄，超时分支由「consume 探测」改为「isPending + cancel」，语义更直白。② 抽出内部 `resolveUnderRealRoot(realRoot, rel)`，共享根的 realpath 由调用方算一次传入，`resolveWithinRoot` 保持原签名不变。③ 改为**先按全量条目排序再截断**：`readdirSync` 带 `withFileTypes`，用 Dirent 类型作排序键（落选条目一次 stat 都不用付，同时省掉逐条 `lstatSync`），截断后再对入选条目按 stat 出的真实 `isDir` 排一次，保证符号链接指向目录时也排在前面。`truncated` 语义随之从"读满即截断"变为"候选数超过上限"，更准确。新增 4 例单测（FIFO 配对 / 撤销只影响本句柄 / isPending 生命周期 / 5001 条目录的排序截断）。**新发现但本轮未处理**：`listShareDirectory` 全程同步，5000 条目录 = 5000 次 `statSync`，且由对端浏览触发——别人翻我的文件柜就能让我的主进程卡一下，异步化需连 `handleList` 一起改，已记入 handoff §4。协议 v0.50、SQLite v14、IPC 契约、依赖与网络均不变，版本 **0.49.2 → 0.49.3**（bug 修复 + 性能，patch +1）。 |
| 281 | 复审 B 级的界面细节与一致性六条：① 字节格式化有三份实现且**精度不一致**（`FileCabinetPanel` 用「12 MB」，`SettingsApp` 与 `stores/transfers` 用「12.3 MB」），同一个大小在不同界面上长得不一样；② 文件柜截断提示把 `SHARE_DIR_MAX_ENTRIES` 写死成「5000」，改常量文案就说谎；③ 面板「全选」实际只勾选已加载的条目，旁边却写着 total 项，语义误导；④ 列表行文件名 ellipsis 截断后没有 `title`，长名无法查看全称（`FileCard` / `GroupPanel` 都有）；⑤ `CaptureApp.vue` 散落 8 处茶青字面量（`#3d8b6b` / `#327b5e`），违反「颜色只用 tokens.css 变量」；⑥ `@dragleave` 挂在面板根节点，拖过内部子元素时反复触发再被 dragover 点亮，虚线框抖动 | ① 新增 `renderer/utils/format.ts` 统一 `formatBytes`，规则定为**字节不带小数、其余单位 ≥10 取整、<10 保留一位**（既不让「1 MB」丢精度，也不让「1234.6 MB」撑爆行宽）；三处旧实现删除，`stores/transfers` 的 `fmtBytes` 一并撤销、`FileCard` 改引用新函数。② 截断文案改常量插值。③ 还有下一页时标签显示「选择已加载」，全部取回后才是「全选」。④ 行名补 `:title`。⑤ 6 处 CSS 字面量改 `var(--primary)`；canvas 标注色从 `--primary` 读一次并缓存（截图窗不调 `applyAppearance`，取值恒为浅色档，导出 PNG 与屏幕预览始终同色）；主按钮按下态改 `filter: brightness(0.9)`，沿用 `ConvList` / `App` 既有惯例，不再另造字面量。工具选中态的 `rgba(61,139,107,0.85)` **保留**——tokens 没有等价档位（`--primary-weak` 仅 12%），换过去会让选中态几乎消失。⑥ `dragleave` 改判 `relatedTarget` 是否仍在面板内，拖出窗口（null）才熄灭。新增 `utils/format.test.ts` 5 例与两组源码结构断言。协议 v0.50、SQLite v14、IPC 契约、依赖与网络均不变，版本 **0.49.3 → 0.49.4**（细节修复，patch +1）。 |
| 282 | 复审 C 级最后一条：`FilesService` 里 `offerUpdatePackage` / `offerSharePaths` / `offerSharePut` 三个方法，从 `randomUUID()` 到失败回滚的后半段（登记 `transfers` 行、写 `outgoing` 出站上下文、`sendOfferPackets`、"发失败但对方已接上就不算失败"的兜底）**一字不差地各写了一遍**，共约 120 行。以后再改传输语义（比如新增一个隐藏 purpose、调整失败判定）很容易漏改其中一处 | 抽出私有 `offerHidden(peerId, prepared, msgIdPrefix, blob)` 承载共同收尾，三个入口只保留各自的前置校验与差异参数：自更新走 `prepareUpdatePackage` + `update:` 前缀 + blob 带 `savedPath`；文件柜下行走 `prepareOutgoing` + `share:` 前缀 + `purpose:"share-get"`；文件柜上行额外校验 `shr1` 能力位与 2 GiB 上限后走 `purpose:"share-put"`。`files.ts` 净减 46 行，既有 48 例 `files.test.ts` 全部通过，**行为零变化**（纯结构重构，无新增测试断言）。协议 v0.50、SQLite v14、IPC 契约、依赖与网络均不变，版本 **0.49.4 → 0.49.5**（重构，patch +1）。 |
| 283 | 文件共享是每天都用的功能，位置却和它的使用频率完全不匹配：**我的柜子设置埋在设置窗「聊天与文件」的第三组**，浏览同事的柜子**只能从私聊头部**进——想拿点东西得先想起是谁发过、再翻到那个会话。用户要求把它提出来放到导航栏设置按钮上方，单独做一个界面，并把"看对方共享的文件"这套 UI 好好设计一遍 | 用户逐条拍板（设计稿过稿）：① **入口**放导航栏底部工具组最上一格（更新提示之下、刷新之上），21px 与相邻工具按钮同量级，**不加角标**——有人上传已在私聊冒泡计未读；② **独立窗口**而非主窗页签（1000×680、最小 860×560、单实例、沿用设置窗的无标题栏形态），左栏 280px = 我的柜子摘要卡 + 支持文件柜的同事列表（带搜索、在线在上离线灰显），右栏 = 文件浏览器或我的柜子管理页，**两者都要**（不只做设置搬家）；③ **私聊内 320px 面板保留**，两个入口并存——边聊边拿是刚需，面板同步按新样式重画并新增「在文件柜窗口打开」；④ 浏览视图 **详情列表 + 可切网格**，手感对齐系统文件管理器（单击选中、**双击进目录**、Ctrl/Shift 多选、Ctrl+A、空格、Backspace、F5、右键下载/另存为/复制名称，没有删除改名）；⑤ 排序**固定"目录在前再按名称"不可切**（分页取回，本地重排会让翻页错乱）、**不做缩略图**（要加协议取图报文，且会把翻目录变成成片图片传输）；⑥ 权限徽标放右栏头部不放左侧列表（有效权限只有列过一次目录才知道，批量探测会撞对方 10 秒 5 次的限流）；⑦ 设置窗的「我的文件柜」整组**移走不留重复入口**，原位只留一行指路；⑧ 新增「最近有人放进来」汇总列表（读既有 `transfers` 里 `purpose='share-put'` 的记录，按人按次列最近 10 条、点击打开本机目录）。**协议 v0.50、SQLite v14、`services/share.ts`、权限判定与安全边界一律不动**，纯前台入口与界面重组；新增 `ui:open-cabinet`、`share:recent-uploads` 两个 IPC。版本 **0.49.5 → 0.50.0**（功能更新，minor +1、patch 归 0）。 |
| 284 | 决议 #283 落地后用户实际用下来提出：**不要再弹一个窗口**，就在原有主界面里实现 | 用户拍板改形态：文件柜**不再是独立窗口**，而是主窗导航栏的**第三个页签**——主窗本来就是「导航 + 列表栏 + 内容区」，文件柜正好占后两格（列表栏 = 我的柜子摘要 + 同事列表，内容区 = 浏览器 / 我的柜子管理页），多开一个窗口只是把同一件事拆到两处，还多一套窗口生命周期。实现上删掉 `windows/cabinet-window.ts` 与 `#/cabinet` 第五渲染入口（回到四入口契约，决议 #210），界面拆成 `components/CabinetList.vue` + `components/CabinetPane.vue`，共享状态收进 `stores/cabinet.ts`；`ui:open-cabinet` 语义由"开窗"改为"**显示主窗并切到文件柜页签**"（设置窗的「打开文件柜」、私聊面板的「在文件柜里打开」都走它）。**功能、交互与视觉一条没减**。副作用：NSelect 等表单控件随文件柜进入主窗静态闭包，`App.vue` 预算由 640/96 KiB 抬到 800/116 KiB——不改用 App 内动态 import，因为那会让 Rollup 把 App 的 facade 并进匿名共享块、manifest 里丢掉 `src/App.vue` 动态入口，四入口门禁随之失效；公共启动闭包（200 KiB 门禁）不受影响，仍为 80 KiB。协议 v0.50、SQLite v14、`services/share.ts` 与依赖均不变，版本 **0.50.0 → 0.51.0**（功能形态调整，minor +1）。 |
| 285 | 用户要求在现有中文基础上增加英文 README 与各类文档，并完成发版 | 中文既有路径继续承担需求、协议、界面与技术设计主事实源；新增 `README.en.md`、贡献/开发/第三方声明英文版，以及 `docs/en/` 当前有效规范，所有入口提供中英双向切换。新增 `docs/README.md` 文档导航和 `check:docs` 覆盖/链接校验，Release 说明采用中英双语标题与下载指引。应用界面多语言仍按既有 P2 远期项管理，本轮运行时、协议、IPC、SQLite、依赖与网络行为保持。版本 **0.51.0 → 0.51.1**（文档与发布微调，patch +1）。 |
| 286 | Issue #29 / #31：部分 Kylin、UOS/Huawei 设备点击内置截图无响应，另有 UKUI 下截图仍带主窗口 | Wayland 不再按 `XDG_SESSION_TYPE` 提前返回：Electron 22 启动时合并启用 `WebRTCPipeWireCapturer`，截图仍实际探测 `desktopCapturer`；无法取源时才降级。隐藏主窗口改为等待 `hide` 信号、Linux 合成器退场时间并最终复核不可见，复核失败则不抓屏。空源、空图和异常统一恢复可见主窗并通过 `capture:failed` 在应用内显示 7 秒可关闭提示；原本隐藏的快捷键调用优先用系统通知，通知不可用再显示主窗，所有失败都给出系统截图 + `Ctrl+V` 退路。协议 v0.50、SQLite v14、依赖与网络行为不变；版本 **0.51.1 → 0.51.2**（Linux 截图兼容修复，patch +1）。 |
| 287 | Issue #30：表情只能从聊天图片收藏，多行网格会重叠，群聊不能发送收藏表情 | 表情面板新增系统图片多选导入，导入路径使用独立、按窗口隔离的一次性授权并复用静图 WebP / GIF 收藏管线；固定高网格按正方形条目实际高度生成行，超出后滚动；群聊表情复用既有 `purpose:"sticker"` 群媒体传输，仅投递给当时在线成员。协议 v0.50、SQLite v14、依赖均不变；版本 **0.51.2 → 0.52.0**（用户可见功能，minor +1、patch 归零）。 |
| 288 | 群聊引用回复 | 群文本携带可选源消息 ID，本地群内解析引用摘要并保留不可用目标退路；协议 v0.51、SQLite v15，版本 0.53.0；详见本文件 v2.65 变更记录。 |
| 289 | ARM64 Wayland 截图防崩溃 | 原生屏幕枚举前复用系统截图粘贴提示，其他平台保持能力探测；版本 0.53.1；详见本文件 v2.66 变更记录。 |
| 290 | 群简介与群公告 | 群主、管理员及正确管理密码持有者可设置和清空简介 / 公告，旧端缺省保留本地值，备份完整往返；版本 0.54.0；详见本文件 v2.67 变更记录。 |
| 291 | PR #39 复审修复 | 简介 / 公告独立鉴权，单次操作禁止夹带，弹窗复用管理密码路径；版本 0.54.1；详见本文件 v2.68 变更记录。跨版本全量快照补齐按 #292 执行。 |
| 292 | 发布前复核发现接收端缺中间 rev 时，最新全量群快照会被误判为一次夹带操作 | 本地 IPC 仍只允许单操作，相邻 rev 仍拒绝夹带。跨版本快照中，rev 差须覆盖变化的文本字段数及既有可识别结构操作数；简介 / 公告先独立验证管理权限，再复用原有成员、群名、角色、头像权限矩阵。不得用高 rev 绕过权限或接受未知结构组合。补齐 `need/info` 跳版本及越权回归测试；协议 v0.51、SQLite v16、界面及依赖不变，版本 **0.54.1 → 0.54.2**。 |
| 293 | Issue #38：沿用桌面习惯，按 Esc 隐藏主窗口并保留可配置快捷键 | 仅主窗口的普通 Esc 进入隐藏；输入法候选、修饰键、长按重复及已消费事件让行。已有模态弹窗优先，补齐建群、会话菜单/移除确认、聊天浮层及文件柜选择的 Esc 消费，避免一次按键同时收起浮层和隐藏窗口。增加仅主窗口可调用的隐藏 IPC，托盘不可用时最小化；关闭偏好与全局快捷键保持原值。协议、存储和依赖不变，版本 **0.54.2 → 0.54.3**（键盘交互微调，patch +1）。 |
| 294 | OPT-23：会话菜单视口避让 | 用户确认执行 2026-09 评审第一批。会话菜单按实际尺寸夹紧到当前 CSS 视口内，保留 8px 边距，打开期间随窗口调整重新定位；保持置顶、免打扰、移除确认、10 秒撤回和 Esc 消费顺序。既有页面缩放、协议、存储、依赖及各平台兼容分支不变，版本 **0.54.3 → 0.54.4**。 |
| 295 | OPT-24：全局搜索请求清理 | 保留 200ms 防抖；关键词改变或面板卸载即清理未启动计时器并使旧请求回调失效。仅当前请求可以写入结果、失败和完成状态；等待与失败使用现有占位样式，重新输入可重试。搜索范围、分类、排序、条数、点击跳转及输入法路径保持，版本 **0.54.4 → 0.54.5**。 |
| 296 | OPT-25：引用与传输读取去重 | 引用复用所属会话的既有消息 ID 索引，保持追加、裁剪、重载及撤回状态的响应式更新；页外消息与传输状态按 ID 合并进行中读取，完成、缺失、失败均释放，失败沿用缺失退路，后续可重试。已到达的实时传输状态优先于早先读取结果；不增加常驻历史结果缓存，不修改协议、数据库、传输状态机和兼容分支。版本 **0.54.5 → 0.54.6**。 |
| 297 | OPT-26：全局搜索减少重复 FTS | 用户确认启动评审第二批。单次 MATCH 分别计算既有 text/pk 计数与排序时间、全部匹配类型的最新 seq，再按现有索引读取摘要；重复 seq 等非唯一结果回退旧查询。保持混合类型摘要、排序/上限、切词、LIKE 转义及旧库兼容，不新增迁移或依赖，版本 **0.54.6 → 0.54.7**。 |

| 298 | OPT-27：闲置会话与传输缓存回收 | 根据 100 会话持续后台消息探针，闲置会话按最近使用顺序回收整组消息及索引，上限 10 个且合计 3000 条；当前会话与 10 秒移除撤回项豁免。会话重新进入仍从主进程加载最新页或指定历史上下文；失效分页不能复活已淘汰缓存。传输保留 offering/accepted 与被卡片引用的状态，未被引用的终态最多 200 条，结束即释放测速样本。引用按组件生命周期计数，迟到读取仍让实时推送优先。仅释放 renderer 投影，未读、转发、重试、持久数据和兼容路径保持，版本 **0.54.7 → 0.54.8**。 |

| 299 | OPT-28：缩略图生成并发约束 | 近视口静态缩略图从缓存检查到读取、缩小解码和写回共用有界队列，硬件渲染最多 4 路，软件或尚未确定的渲染配置最多 2 路。同一 transferId 的展示共享任务；离开近视口、重新绑定或卸载时释放该处需求，没有剩余需求的排队项丢弃，已开始项完成后释放槽位。已完成 URL 复用现有 512 项 LRU 工具；不淘汰进行中的共享任务。保留 cache 参数、动图/小图、失败原图退路、480px 观察范围及独立查看/OCR/像素门禁。无协议、IPC、数据库或依赖变化，版本 **0.54.8 → 0.54.9**。 |
| 300 | GitHub Actions 打包耗时优化 | Linux 安装拆为禁用自动生命周期的 npm ci、显式恢复 electron/esbuild/protobufjs/vue-demi 安装钩子、一次 Debian 10 源码重建 better-sqlite3；保留重建后及最终包 GLIBC 2.28 检查。五平台每个 job 只构建一次应用，后续冒烟与 electron-builder 复用该产物；本地 smoke/dist 脚本仍自行构建。补 Linux npm 下载缓存及五平台 Electron/打包工具下载缓存，按平台与架构隔离；安装包 artifact 关闭重复压缩。保持 main/manual/tag 触发、五平台全量验证、15 个发布资产、Win7/Electron 22 与 Debian 10/UOS20 兼容基线。版本 **0.54.9 → 0.54.10**。 |
| 301 | OPT-29：列表辅助文字对比度 | 用户确认按评审方案执行 UI 小批次。三类列表中需要阅读的辅助文字局部改用现有 `--text-2`，覆盖会话时间/摘要/计数、通讯录 IP/计数/离线昵称、搜索分类/摘要/组织路径/离线说明，以及空态与搜索状态。保留离线灰显和 #252 状态点、禁用语义、全局 token、布局、字号与缩放；不触及平台兼容分支。版本 **0.54.10 → 0.54.11**。 |
| 302 | OPT-30：列表键盘可达性 | 会话、通讯录分组/联系人和三类全局搜索结果的行入口使用原生 `button type="button"`，由浏览器提供 Tab 顺序及 Enter/Space 激活；局部 `focus-visible` 茶青内描边标记焦点。Enter/Space 与既有单击等价：会话打开、分组展开/收起、联系人资料展示、搜索跳转；通讯录双击开聊、会话右键菜单及 Esc 消费保持。分组提供展开状态、会话提供当前位置、联系人可访问名称包含在线状态。保留布局、字体继承、省略与减少动态效果；不增加全局快捷键或修改 IME/平台分支。版本 **0.54.11 → 0.54.12**。 |
| 303 | 聊天看图时需要在同一会话连续浏览 | 图片查看器画布两侧增加上一张 / 下一张按钮，按本地 messages.seq 查询完整会话图片历史，首末边界置灰不循环；过滤表情、普通文件、撤回与不可用媒体，按消息去重群发传输。复用受管媒体校验与原查看窗口，切图保留窗口位置尺寸并重置图片和 OCR 状态，方向键平移保持。新增本地只读导航 IPC，不改线上协议、库表、依赖。版本 **0.54.12 → 0.55.0**。 |
| 304 | 用户要求像微信一样在图片上直接选字复制，并控制低配信创 CPU 占用；已确认手动触发、缓存复用 | 全平台改手动按需 OCR；透明行文字层随原图缩放旋转，以原生选区完成部分/跨行选择、快捷键和按钮复制，移除独立结果框。单个本地 Worker 承载现有单线程 PaddleOCR，切图/关闭取消未完成推理；复用输入尺寸、模型与有界缓存，修正整行 token 缓存长度边界，禁止多图并行与自动识别。文字定位使用去掉裁剪扩边的检测框，每行节点数与行数成正比；特殊排版可能存在字位拟合偏差。无协议、库表、依赖或 CSP 放宽，版本 **0.55.0 → 0.56.0**。 |

| 305 | 用户实测图片文字与选区高亮尺寸不一致，跨行拖选时光标在文本与抓手之间切换 | 撤销 #304 把检测收缩轮廓当成完整字区的处理，输出恢复既有扩边框；按浏览器真实文字选择盒校正透明行文本的位置及双轴缩放，使原生选区贴合 OCR 行框；仅布局建立时度量，缩放与拖选时复用。非Shift文字起拖先清旧选区再由原生行为定位新锚点，避免进入文本拖放；保留Shift扩选与双击选词。文字区开始的拖选手势在经过行间/图内空白时保持文本光标，抬起、取消、失焦、隐藏或切图时释放；空白起拖仍平移图片。保留原生部分/跨行选区、一行一个节点、手动识别、缓存与单线程 Worker。版本 **0.56.0 → 0.56.1**。 |

| 306 | Win7 / UOS 内置 emoji 选区复制丢失，UOS 开启 NumLock 后小键盘 2468 仍移动光标 | 内置 emoji 的公共展示组件保留真实 Unicode 文本，以透明文本和本地 SVG 叠放恢复原生选区复制；右键整条复制继续使用消息原文。Win7 原子表情编辑器复制 / 剪切按现有 UTF-16 选区读取草稿，保留换行和变体选择符。Linux 的可编辑 input / textarea 在 NumLock 明确开启、物理键为 Numpad0–9、逻辑键为对应数字或导航值时使用原生 insertText；只在插入成功后消费事件，保留输入事件、撤销、maxlength 和选区替换。NumLock 关闭或状态缺失、独立方向键、修饰键、IME composition、只读 / 禁用控件均交还原生处理；Windows / macOS 不安装该兼容处理。真实 UOS 输入法事件链仍须目标机复测。无协议、库表、IPC、依赖变更，版本 **0.56.1 → 0.56.2**。 |
| 307 | 用户确认开发英文模式，支持后续多语言扩展 | 内置简体中文与英文；设置「通用」可即时切换并持久保存，所有窗口同步。新安装中文系统使用中文、其余使用英文；已有配置缺语言字段时保留中文。覆盖首次向导、主窗、设置、文件柜、截图、看图、托盘、通知、应用弹窗与错误提示。用户消息、昵称、群名、文件名保持原文；历史系统提示保持原样，新系统提示附带本地结构化模板与参数，切换语言后重新呈现。语言资源全部本地打包，不新增依赖、外网访问或线上协议；版本 **0.56.2 → 0.57.0**。 |

### 远程桌面查看方案（2026-09-16）

| # | 背景 | 决议 |
|---|---|---|
| 308 | 用户希望先完成远程初步协助，接受降低画质，只看屏幕且无需加密；本轮要求准备文档和细节 | 将一对一只读桌面查看从 #7 远期评估中拆出，进入设计阶段。逐次请求/同意/选屏、持续共享提示、随时停止、锁屏/断线结束；以低帧率 JPEG 经既有 TCP 端口传输，不加密、不新增端口或依赖，不实现控制/音频/录屏。协议、分层、异常与验收先落档；独立查看窗口及精确画质参数保留为建议，平台支持以目标机验证为准。**本轮仅文档，代码未实现，包版本保持 0.57.0；实现轮再按 #53/#73 递增 minor。** |
| 309 | 用户考虑纯内网条件，希望桌面查看更流畅，指定 10 帧/秒 | 将默认目标确定为 **10 帧/秒**，替代 #308 的初始帧率试验建议。最短采样间隔改为 100ms；JPEG 字节预算调整为最多 5 MiB/秒，与 512 KiB 单帧上限 × 10 帧匹配，按需消耗。保留单帧在途、编码/网络背压与超时，实际负载不足时降帧、不补发旧帧；验收记录实际帧率与 CPU。分辨率/压缩质量仍待试验，其他范围不变。**仅文档，应用保持 v0.57.0。** |
| 310 | 用户在四档建议后确认开始开发 | 本轮实现只看屏幕的一对一协助，默认自动（10/5/3 帧），并提供节省资源 3、标准 5、流畅 10 手动档；档位仅在本次会话保留、画质一致。按独立查看窗口方案接入，共享方每次明确同意、持续置顶停止入口。版本递增为 **0.58.0**；平台能力须通过本机采集/锁屏前置条件检测，真实 Win7/UOS/麒麟验证另记，不扩大到控制、音频或录制。 |
| 311 | 用户要求复审多平台、国产平台性能及 UI | v0.58.1 修正协助选屏操作区溢出，统一紧凑工具栏与共享状态小窗；保留既有四档、逐次同意和只读范围。低档同步约束采集帧率，查看端采用原生位图交接与显式释放，Linux 锁屏探测不阻塞聊天启动并支持失效后重新检测。保持 Electron 22 / Node 16 / Chrome 108、单帧背压与目标机实测边界。 |
| 312 | 用户要求贴边小窗、协助聊天记录及发起二次确认 | v0.59.0：共享提示收为 320×56 DIP 贴边条，继续使用同一受限窗口及媒体流；请求先确认再发送。每次请求生成一张本地系统卡片，保留请求与终态、实际连接起止和持续时间；只在生命周期变化时写库，帧流和档位不写库。协议与逐次授权保持，异常退出如实标注未知时间。 |
| 313 | 用户要求协助卡片与普通聊天消息一样按发起方排列 | v0.59.1：自己发起的卡片靠右，对方发起的卡片靠左，复用普通消息的行布局和间距；拒绝、取消、结束及重新加载历史均保持发起方方向。沿用已有 isMine 字段，无协议或存储变更；普通系统提示仍居中。 |
| 314 | 用户要求一次协助只保留发起方一侧的一张卡片，并缩小占用空间 | v0.59.2：同一会话从请求、开始到结束持续原位更新，禁止额外追加开始/结束卡片；每次新请求才产生新卡片。两行摘要显示名称与当前状态、时间与实际持续时长；完整起止时间和原因放在悬停及无障碍说明中。复用左右气泡配色，移除方向说明、时间表格和聊天顶部重复状态条；发起失败提示保留。无逐秒计时器、协议或存储变更。 |

| 315 | 用户确认诊断包方案并要求设计、开发 | v0.60.0：设置→关于增加“诊断与反馈”，支持导出本地 ZIP、复制脱敏环境信息、打开导出文件所在文件夹；用户自行附到 Issue，无上传。常态记录启动、异常退出、网络监听、消息状态、文件传输关键阶段、截图及协助生命周期；按操作 ID 串联，禁止消息/文件/图片/剪贴板内容、凭据和原始错误文本入日志。日志异步批量写入，最近 7 天且总量最多 10 MiB，队列、单条、重复事件均限额；不逐包、逐帧或逐进度记录。默认匿名化节点/IP、排除用户名及路径；可单次勾选附带本次运行的真实网络地址（默认关闭）。ZIP 包含摘要、环境/当前状态、近期日志；上一轮未正常退出留下标记。原生崩溃只提供退出线索，不承诺完整堆栈；无内存转储、常驻性能监控和自动修复。补充故障 Issue 模板和无法启动时的日志位置。 |

## 10. 待定问题清单

**内网通兼容（#199 暂缓）**：整块能力冻结为长期待办；§15 产品取舍与 #196 的 VM 阻塞项**暂停决策**，无需追问用户，直到用户明确要求恢复。

**远程桌面查看（#310）**：独立窗口、默认自动与 3/5/10 手动档已经实现。剩余目标机验收：Win7 x64/ia32、UOS/麒麟各桌面的持续采集、指针、文字可读性、锁屏与性能；macOS 物理屏幕权限。合成页面的 Electron 自测不替代这些结论。

补充候选：是否采用 §6.11.1 的「自动 / 节省资源 / 标准 / 流畅」四档，并默认自动、不记忆上次档位，待用户确认；自动调节的初始阈值须经过性能试验。

## 11. 变更记录

- 2026-06-10 v0.1 初稿。定稿决策：主场景=中大型办公内网（≤500 节点）；离线消息=发送端暂存补发；v1 含图片、内置截图、群聊；UI=单窗口左列表右聊天。
- 2026-06-10 v0.2 第一轮 8 项待定全部形成决议（见第 9 节）；协议取舍确定为"借鉴机制、自有报文"；分组模型确定为公司/部门两级树形。
- 2026-06-10 v0.3 第三轮（UI 轮）：三栏布局、分组升为三级（公司▸部门▸团队）、联系人持久保留+资料自动刷新（F-DISC-7 新增）、全局搜索覆盖聊天记录与文件、聊天导出升 P1、设置页范围定稿；新增 [ui-design.md](ui-design.md) 并互链。
- 2026-06-10 v0.4 第四轮（补充轮）：规模升至 1000 节点；新增 F-DISC-8 在线二次校验、F-MSG-7 表情与表情包；离线加副文字；导出增迁移备份包并支持导入（身份映射，换电脑不丢数据）。
- 2026-06-10 v0.5 决议 #20：放弃 32 位系统支持，Windows 仅 x64（Win7 须 64 位版）。
- 2026-06-10 v0.6 决议 #21：远期支持本地开放接口（AI 读取/接管聊天），新增 6.8 节立下安全边界；当前版本不实现。
- 2026-06-10 v0.7 第七轮（查漏轮，决议 #22）：新增 F-SYS-6 首次启动向导、F-DISC-9 备注名、F-MSG-8 转发与引用、群内 @、投屏勿扰、明文存储与日志脱敏声明；协议侧同步开机风暴对策。
- 2026-06-11 v0.8 第八轮（v0.4 撤回轮，决议 #23）：F-MSG-6 明确为 2 分钟内撤回自己的文本 / 群文本消息，原消息隐藏、系统提示留痕，指令走可靠投递与离线补发；图片、文件、表情撤回留后续扩展。
- 2026-06-11 v0.9 第九轮（P1 设置默认值轮，决议 #24）：明确自启、关窗行为、截图/唤起快捷键、提示音、通知预览、深色主题、字体缩放的默认策略，作为设置页落地依据。
- 2026-06-11 v0.10 P1 本地交付候选：多选群发、链接点击、转发、群内 @、截图标注、断点续传、传输记录、深色主题、端口设置、HTML/TXT/备份包导出导入落地；迁移备份包纳入联系人、会话、群、表情、传输记录与图片/表情媒体，导入继续执行旧"我"到本机身份映射。
- 2026-06-11 v0.11 第十轮（UI 图标与群管理轮，决议 #25-#27）：系统 UI 图标统一为自绘线性 SVG；主窗口标题改为 `<用户名>-🍵Teahouse`；讨论组新增创建 IP 与可选管理密码，群管理从“任何成员可操作”收紧为“密码或创建 IP”。
- 2026-06-12 v0.12 第十一轮（头像模板与设置图标修正，决议 #28）：头像改为 20 个亲和动物 emoji 图标 + 背景色模板，保留昵称首字模式；设置入口图标改为明确齿轮。
- 2026-06-12 v0.13 第十二轮（左侧导航图标修正，决议 #29）：只重画主窗口左侧聊天/通讯录图标，导航栏仍保持既有 56px 茶青侧栏。
- 2026-06-12 v0.14 第十三轮（讨论组创建搜索与密码提示，决议 #30）：建群改为“搜索选人 → 下一步设置”的两步弹窗；管理密码需二次确认，并新增随群元数据同步的密码提示。
- 2026-06-12 v0.15 第十四轮（设置页信息架构重排，决议 #31）：设置页改为常见桌面软件式左侧分组 + 右侧分组面板；重排顺序并补齐说明、空态、禁用态与统一保存反馈。
- 2026-06-12 v0.16 第十五轮（群聊媒体轮，决议 #32）：群聊图片/文件复用单聊点对点文件通道，发送端按在线成员逐个 offer，收端按 `groupId/groupRev` 入群会话；离线群成员不做文件补发队列。
- 2026-06-12 v0.17 第十五轮补充（决议 #33）：群聊图片内联上限收紧为 10MB；超限图片按普通文件发送，接收端手动接收后才开始传输，单聊图片仍保持 20MB。
- 2026-06-12 v0.18 第十六轮（决议 #34）：群聊/私聊输入区工具条最右侧新增会话内历史搜索入口，支持关键词、图片、文件、日期范围筛选，并跳转高亮命中消息。
- 2026-06-12 v0.19 第十七轮（决议 #35）：会话内历史搜索弹窗改为设置页尺度的居中面板，图片命中直接展示缩略图。
- 2026-06-12 v0.20 第十八轮（决议 #36）：会话内历史搜索打开后默认显示最近记录；日期筛选改为连续点击日期的日历范围选择。
- 2026-06-12 v0.21 第十九轮（决议 #37）：私聊头部用户名字区域可打开对方资料弹窗，并在弹窗内修改本地备注。
- 2026-06-12 v0.22 第二十轮（决议 #38）：输入框 placeholder/hint 统一改用更浅的 `--text-placeholder`，降低辅助提示的视觉权重。
- 2026-06-12 v0.23 第二十一轮（决议 #39）：品牌 logo 确定为“茶杯气泡”统一标识，补菜单栏单色、彩色小 logo 与大图标三件套。
- 2026-06-12 v0.24 第二十二轮（决议 #40）：通讯录双击联系人直接进入单聊，单击联系人右侧资料卡重设计为完整资料页。
- 2026-06-12 v0.25 第二十三轮（决议 #41）：左侧聊天/通讯录主入口图标放大到 25px，侧栏宽度、按钮热区和设置入口尺寸不变。
- 2026-06-12 v0.26 第二十四轮（决议 #42）：托盘 / 菜单栏未读总数跨平台刷新，macOS 显示菜单栏数字与 Dock 角标，Windows 使用任务栏 overlay 数字并闪烁托盘，Linux 以系统角标尝试 + 闪烁兜底。
- 2026-06-12 v0.27 第二十五轮（决议 #43）：菜单栏 / 托盘单色 logo 缩至 32px 画布约 82% 内容区，彩色小 logo 与大图标保持原比例。
- 2026-06-12 v0.28 第二十六轮（决议 #44）：新增 GitHub Actions 发布矩阵；main/manual 产出 artifact，`v*` tag 自动创建/更新 Release；Linux 由 Debian 10 容器编译 deb/AppImage，Windows 构建 Win7 x64 安装版与便携版。
- 2026-06-12 v0.29 第二十七轮（决议 #45/#46）：Win7 / UOS 真实平台兼容修正，头像和内置 emoji 子集改为本地 SVG 渲染；短文本 UDP 可靠投递失败后增加 TCP 控制帧兜底。
- 2026-06-12 v0.30 第二十八轮（决议 #47）：头像和内置 emoji 兼容渲染从临时自绘图形改为 Twemoji 本地 SVG 子集，并补 CC-BY 4.0 署名。
- 2026-06-12 v0.31 第二十九轮（决议 #48）：输入框编辑态补同套 Twemoji 本地 SVG 镜像渲染，避免 Win7 / UOS 输入时仍显示方框。
- 2026-06-12 v0.32 第三十轮（决议 #49/#50）：主窗与设置窗改沉浸式无标题栏（mac 红绿灯 / Win·Linux 自绘窗口控制按钮 + 顶部隐形拖拽带）；设置页头像编辑器重排为大预览 + 分段切换 + 图标网格 + 色板；并修正输入框 emoji 镜像层按实际字体逐字符测量对齐，消除表情越多光标偏移越大的问题。
- 2026-06-12 v0.33 第三十一轮（决议 #51/#52）：mac 主窗红绿灯移至列表栏顶部（x=68），不再横跨导航栏分界线；UOS 点击失效判定为 Linux CSS 拖拽区不可靠，Linux 改 JS 拖拽（主进程光标跟随）并保留回退系统标题栏预案。
- 2026-06-12 v0.34 决议 #53：每轮迭代递增 `package.json` patch 版本号（UOS dpkg 同版本拒装），规则入 AGENTS.md 提交约定；本轮版本补齐为 0.5.18。
- 2026-06-12 v0.35 决议 #54：修正 UOS20 glibc 2.28 native 模块打包基线，关闭 electron-builder 二次 native rebuild，并新增最终包 GLIBC 上限检查；本轮版本升至 0.5.19。
- 2026-06-12 v0.36 决议 #55：Linux 默认禁用硬件加速（消除 UOS GPU 报错噪音）；同轮修正 emoji 三处渲染尺寸统一（面板/输入框/消息，ui-design v0.38）与 Win7 聊天区顶部不可拖（聊天头部 no-drag 残留挖掉拖拽带）；版本升至 0.5.20。
- 2026-06-12 v0.37 第三十二轮（决议 #56–#59，Win7/UOS 桌面体验修复轮）：输入框 emoji 改等宽空白字形字体终局方案（对齐与尺寸矛盾消解）；快捷键录制式设置 + 注册状态反馈 + 恢复默认；Linux 桌面多尺寸图标 / 窗口显式 icon / Win·Linux 彩色托盘；修复渲染层 window.close() 绕过 close 事件导致"关闭进托盘"失效。版本随三个增量升至 0.5.23。
- 2026-06-13 v0.38 决议 #60：安装路径全 ASCII（productName=Teahouse，显示名保持「茶话间」，userData 经 app.setName 零迁移）；修复 UOS deb 安装硬链接报错（窗口图标独立文件 + USE_HARD_LINKS=false + CI deb 归档校验）。版本 0.5.24。
- 2026-06-13 v0.39 第三十三轮（决议 #61）：修 Win7 输入框首行遮挡（textarea + 镜像同步顶部留白）与滚动条闪烁抽动（全局自绘滚动条）。版本 0.5.25。
- 2026-06-13 v0.40 决议 #62：取消消息群发（注释 App.vue 入口 + MassSender 留痕），改用讨论组。版本 0.5.26。
- 2026-06-13 v0.41 决议 #63：撤回窗口 2→5 分钟；右键"撤回"实时倒计时「撤回（mm:ss）」，超时变灰「撤回（超时）」。版本 0.5.27。
- 2026-06-13 v0.42 决议 #64：logo 重设计为饱满扁平茶杯气泡，新增 gen-app-icons.mjs 重生成 png/ico/icns + linux 全套图标，PantryBrandLogo 同步。版本 0.5.28。
- 2026-06-13 v0.43 决议 #65：消息显示时间接收侧时钟偏移矫正（新增 net/peer-clock.ts，复用 envelope.ts 零协议改动，排序仍靠 seq）。版本 0.5.29。
- 2026-06-13 v0.44 第三十四轮（决议 #66）：Windows 通知应用名设 AUMID（不再显示 electron.app.Teahouse）；群消息通知标题=群名、正文「发送人：内容」。版本 0.5.30。
- 2026-06-13 v0.45 决议 #67：群信息面板改绝对定位覆盖右侧整列（不挤压消息）+ 成员紧凑头像。版本 0.5.31。
- 2026-06-13 v0.46 决议 #68：导航栏 56→68px，mac 红绿灯回左上角 (14,18)，废弃 #51 列表栏顶部方案。版本 0.5.32。
- 2026-06-13 v0.47 决议 #69：CI 新增 macOS arm64 打包 job，Release 三平台齐发（Win7/Debian·UOS/mac arm64）。版本 0.5.33。
- 2026-06-13 v0.48 决议 #70：导航栏底色茶青深色改中性浅灰（--rail-bg，微信式），红绿灯落浅色更自然，图标选中茶青高亮。版本 0.5.34。
- 2026-06-13 v0.49 决议 #71：会话列表选中态改实心茶青底 + 白字（微信式），hover 淡灰区分。版本 0.5.35。
- 2026-06-13 v0.50 决议 #68 微调：mac 红绿灯坐标 (14,18) → (10,12) 更贴左上角，更协调。版本 0.5.36。
- 2026-06-13 v0.51 决议 #68 再微调：mac 红绿灯 (10,12) → (9,7)，y 上提贴合窗口圆角视觉中心。版本 0.6.1。
- 2026-06-13 v0.52 决议 #72：dev 模式 mac Dock 用 app.dock.setIcon 补茶杯图标（打包版仍靠 icns）。版本 0.6.2。
- 2026-06-13 v0.53 决议 #73：版本号递增规则细化——功能 minor +1 归零 patch、bug/微调 patch +1。版本 0.6.3（本条为微调，patch +1）。
- 2026-06-13 **v0.6.0 里程碑**：本轮 UI 大改（微信式三栏配色：浅灰导航栏 + 选中态实心茶青；红绿灯归位；群信息面板覆盖整列；logo 重设计）+ 跨平台体验修复（Win7 输入框/滚动条、Win11 通知、UOS 安装/图标、撤回 5 分钟倒计时、消息时间时钟矫正）+ 三平台打包（含 macOS arm64）累积达里程碑级，版本由 0.5.x 拉至 **0.6.0** 标志 v0.6 起始；分支 `feat/v0.6-ui-revamp`。后续仍按决议 #53 每增量 patch +1。
- 2026-06-13 v0.54 第三十五轮·增量 A（决议 #74，会话内历史搜索体验优化）：① 搜索结果文本/摘要走 Twemoji 彩色 SVG 渲染（复用 CompatEmoji），修 Win7 不显示、UOS 黑白线描；② 跳转后按会话 `lastTs` 判定窗口是否已含最新消息，修「回到最新」按钮在最新位置/关闭搜索后误显；③ 结果信息密度提升：文本命中去掉「文本消息」小标题与前置类型图标、摘要升为主体（可换行两行），文件命中保留类型图标 + 文件名，图片命中保留缩略图；④ 修 Win7 结果列表末行被裁（弹窗高度约束 + 结果区底部留白）；⑤ 图片命中改「单击放大看图（复用大图查看器）/ 双击定位」，文本·文件保持单击定位。版本 0.6.3 → **0.7.0**（含看大图新功能，按决议 #73 minor +1 归零 patch）。
- 2026-06-13 v0.55 第三十五轮·增量 B（决议 #75，文件消息卡片）：① 群聊文件发送进度由整体进度条改为「已接收 / 总数」x/x 计数，hover x/x 展开已接收成员名单（移出消失）——`TransferView` 暴露 `peerId`（复用 transfers.peer_id 现成字段，零协议 / 迁移改动）；② 文件卡按扩展名显示彩色类型图标（新增 FileTypeIcon：Word 蓝 / Excel 绿 / PPT 橙 / PDF 红 / 压缩包 / 图片 / 音频 / 视频 / 文本 / 代码 / 安装包 + 通用兜底），一眼辨识文件类型。彩色文件徽标属"内容标识"，是单色系统图标基线（ui-design §10）的明示例外。版本 0.7.0 → **0.8.0**（功能更新，按决议 #73 minor +1 归零 patch）。
- 2026-06-13 v0.56 第三十五轮·增量 C（决议 #76，复制文件粘贴发送）：输入框 Ctrl+V 除截图位图外，支持直接粘贴从文件管理器复制的文件（Electron 为剪贴板 File 注入真实路径，复用拖拽同机制 `file.path`，零主进程改动）；单张图片文件按图片发、其余按文件发，与拖拽行为一致。版本 0.8.0 → **0.9.0**（功能更新，按决议 #73 minor +1 归零 patch）。
- 2026-06-13 v0.57 第三十六轮（决议 #77，应用图标底色改纯白）：dock / 任务栏 / 安装包应用图标背板由浅青 `#EBF4EF` 改纯白 `#FFFFFF`（用户反馈浅青偏绿、不通用、不够清爽），茶杯 logo 造型与配色不变；重生成 png/ico/icns + Linux 全尺寸 + 窗口图标。窗口 `backgroundColor` 不属图标范畴，保持不变。版本 0.9.0 → **0.9.1**（微调，按决议 #73 patch +1）。
- 2026-06-13 v0.58 第三十七轮（决议 #78，强制直连·禁代理）：纯内网 IP 工具不应走任何系统/环境代理——启动早期 `app.commandLine.appendSwitch('no-proxy-server')` 全局禁用代理，不开放任何代理配置项（AGENTS 红线 #5 同步加固）。版本 0.9.1 → **0.9.2**（加固，按决议 #73 patch +1）。
- 2026-06-13 v0.59 决议 #79（修 UOS 开机自启重复提示）：Linux autostart desktop 文件写入前先比对内容，相同则跳过；此前 `applyAutoLaunch` 每次启动无条件重写，触发 UOS「茶话间 正在设置开机自启动」提示。版本 0.9.2 → **0.9.3**（bug 修复，按决议 #73 patch +1）。
- 2026-06-14 v0.60 决议 #80（删设置页两个占位开关）：「聊天与文件」页移除「每次询问保存位置」（始终 `disabled`、功能从未实现）与「图片自动接收」（恒为 `checked disabled`、易误导成可关闭）两个占位开关——前者无对应实现，后者所述「20MB 内图片自动入缓存」是 F-MSG-3 的既有行为而非可配置项，留着只会让用户以为能改其实不能。纯前端 UI 删除，不涉协议/存储/IPC；图片自动入缓存功能与 20MB 上限（决议 #2、protocol §2）保持不变。ui-design §8 设置项表同步移除「图片自动接收策略」。版本 0.9.3 → **0.9.4**（微调，按决议 #73 patch +1）。
- 2026-06-14 v0.61 决议 #81（在线/离线状态视觉补齐与顶部头部重设计）：① 会话列表单聊头像右下角补「在线/离线状态点」（绿 `--online`／灰 `--offline`，描窗口底色细边）——ui-design §3 早已规定「头像（带在线小圆点）」、此前未实现，本轮补齐；离线只显灰点、头像保持彩色（用户拍板）。② 聊天顶部单聊头部由「昵称 + 右侧孤立『● 在线』字符标签」改为「对方头像（带同款状态点）+ 昵称 / 在线·IP 两行」，与会话列表统一视觉语言，删去不协调的字符圆点标签（ui-design §6 同步）。`AvatarMark` 新增可选 `online` prop（根改为定位容器、内容裁切移入 `.avatar-face`，状态点得以露出圆形外缘），不传则不显示、其余调用方零影响；纯前端，不涉协议/存储。版本 0.9.4 → **0.10.0**（功能更新，按决议 #73 minor +1 归零 patch）。
- 2026-06-14 v0.62 决议 #82（在线状态展示一致性 + 桌面细节优化）：① macOS 菜单栏单色图标 retina 发糊 → 改按 @2x 解释同一张 32px 图（逻辑 16pt、物理 32px 像素对齐，`tray.ts` createTrayBaseIcon），清晰不糊。② 群成员在线展示由「整头像离线灰显」（决议 #67）统一为「绿/灰状态点」——与私聊/会话列表同一视觉，头像始终彩色，在线判定 `peersStore.byId(id).online`、自己恒在线（GroupPanel 成员列表 + 添加列表）。③ 修私聊顶部副标题「在线 · IP」中 IP 被 ellipsis 截断：标题区 max-width 340 → 460px。④ 主界面右侧空态去掉版本号/运行时技术信息，改为随机名人名言（24 条本地内置、每次打开随机一条、零外网，新增 `utils/quotes.ts`）。⑤ 复核左上角自己头像本就不挂在线状态点（导航栏头像不传 `online`），维持「只展示头像」。版本 0.10.0 → **0.11.0**（功能更新含空态名言，按决议 #73 minor +1 归零 patch）。
- 2026-06-14 v0.63 决议 #83（群成员列表中"自己"的显示）：群信息面板里自己原显示裸「我」+ 默认头像（`peersStore` 不含自己，`byId(selfId)` 取不到真实头像）；改为「<昵称>（我）」+ 自己的真实头像——`chatStore` 启动时从 `getSettings` 缓存 `selfNick`/`selfAvatar` 并订阅设置更新，GroupPanel 据此渲染自己的名字与头像。另复核：左上角导航栏自己头像从未传 `online`（git 可证 App.vue 历史无 `:online`），用户截图所见灰点来自一个与当前代码对不上的旧构建，重新打包安装即消除。版本 0.11.0 → **0.11.1**（微调，按决议 #73 patch +1）。
- 2026-06-14 v0.64 决议 #84（自己一律不显示在线状态点）：决议 #83 给群成员里的"自己"加了恒绿在线点；按"自己永远在线、状态点对自己无意义"的原则，群成员列表里自己改为不显示状态点（只头像 +「昵称（我）」），其余成员仍显示绿/灰点（GroupPanel `:online` 对 selfId 传 `undefined`）。复核：左上角导航栏自己头像本就无状态点（git 证 App.vue 无 `:online`、无伪元素圆点、tokens.css 也无），用户所见灰点确为旧构建残留，装新包即消失。版本 0.11.1 → **0.11.2**（微调，按决议 #73 patch +1）。
- 2026-06-14 v0.65 决议 #85（根治左上角自己头像灰点——Vue Boolean prop 转型陷阱）：**订正 #83/#84 的误判**——此前两轮反复断言"左上角灰点是旧构建残留、代码无问题"，实为误诊；bug 一直在代码里。真根因：`AvatarMark` 的状态点 prop 声明为 `online?: boolean`，导航栏自己头像（及设置页/PeerList/ProfileCard 等）不传该 prop，但 **Vue 对声明为 Boolean 类型的 prop 在未传时会按布尔转型规则强制置为 `false`（而非 `undefined`）**，于是模板 `v-if="online !== undefined"` 恒成立、冒出一个 `is-offline` 灰点——这正是用户在左上角看到、装新包也不消失的灰点。修复：prop 改为字符串联合类型 `presence?: 'online' | 'offline'`（字符串 prop 未传保持 `undefined`），模板判定改 `v-if="presence"`；显式需要状态点的调用方（ChatPane 顶部、ConvList、GroupPanel 成员/添加列表）传 `presence`，不需要的（App 导航栏、SettingsApp、PeerList、ProfileCard、消息气泡头像）不传即不显示。纯前端改动，不涉协议/存储/IPC；ui-design 颜色 token `--online/offline` 不变。版本 0.11.2 → **0.11.3**（bug 修复，按决议 #73 patch +1）。
- 2026-06-14 v0.66 决议 #86（公开仓库与发布说明整理）：仓库与用户文档统一指向 `skyjt/pantry`，公开仓库 README / Release 说明面向外部用户，旧内部仓库名从交接与打包指引中移除；版本 0.11.3 → **0.11.4**（发布整理，按决议 #73 patch +1）。
- 2026-06-15 v0.67 决议 #87/#88（群改名提示与 IP 完整展示）：群名变化写入幂等系统提示，改名人显示「你把群名…」，其他成员显示「备注/昵称把群名…」；私聊顶部 IP 副标题取消 ellipsis，保证内网 IP 完整可见。版本 0.11.4 → **0.12.0**（含用户可见群改名提示，按决议 #73 minor +1 归零 patch）。
- 2026-06-15 v0.68 大文件传输 0B 延迟修复：修复发送端收到 accept/pull 后先完整预读大文件计算 SHA-256、再开始写 TCP 数据的问题；首次拉取改为边发送边计算哈希，10GB 级文件不再在接受后长时间停留 0B。版本 0.12.0 → **0.12.1**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.69 决议 #93：内置图片查看器补齐缩放、适应窗口、原始大小、旋转、拖拽平移、滚轮缩放、快捷键、另存为与关闭；顶部浮动工具条沿用茶青单主色和明暗主题，不新增 IPC / 协议 / 存储。版本 0.13.3 → **0.14.0**（功能更新，按决议 #73 minor +1 归零 patch）。
- 2026-06-15 v0.70 决议 #94：图片查看器从主窗全屏覆盖层改为独立普通窗口，主窗口可继续聊天/搜索；聊天气泡和历史搜索图片点击只发起 `img:open-viewer`，图片窗口复用 `#/image-viewer` 入口并保留缩放/旋转/平移能力。版本 0.14.0 → **0.14.1**（体验修正，按决议 #73 patch +1）。
- 2026-06-15 v0.73 决议 #97：图片查看器新增本地 OCR 拖选复制文字。小图片自动识别，大图片手动触发；识别结果只在当前图片窗口内存态叠加选择框，不写库、不联网、不做“大爆炸”分词面板。版本 0.14.3 → **0.15.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-15 v0.74 决议 #98：OCR 拖选从文字块粒度细化为 symbol/字符粒度，并把可选文字区域鼠标改为文本选择光标；复制时按词边界恢复英文空格、中文连续拼接。版本 0.15.0 → **0.15.1**（体验微调，按决议 #73 patch +1）。
- 2026-06-15 v0.71 决议 #95：图片窗口标题栏默认显示图片文件名；内部菜单改为底部半透明覆盖条并移除内部关闭按钮；初始窗口/图片尺寸按屏幕工作区 70% 阈值计算，小图原始尺寸、大图等比缩放并显示比例。版本 0.14.1 → **0.14.2**（体验修正，按决议 #73 patch +1）。
- 2026-06-15 v0.72 决议 #96：修复 #95 后续反馈的大图空白问题：图片显示改为显式宽高缩放，布局盒子与视觉尺寸一致；图片窗口最小内容尺寸降到系统允许最低，并补充多尺寸图片截图验证。版本 0.14.2 → **0.14.3**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.75 决议 #99：图片双击放大改为以鼠标位置为锚点，OCR 结果按图片缓存并在组件层先查缓存，避免缩放或再次点击识别触发重复 OCR。版本 0.15.1 → **0.15.2**（体验修正，按决议 #73 patch +1）。
- 2026-06-15 v0.76 决议 #100：修复 #99 实测问题：图片平面改为显式中心定位，补双击锚点几何单测；OCR 层只让文字 token/复制按钮接收事件，无文字区域继续拖拽图片。版本 0.15.2 → **0.15.3**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.77 决议 #101/#102：OCR 拖选取消可见矩形框选层，改为起止字符范围选择；未选中文字不显示淡框，选中态只保留类文本选择底色；OCR 结果缓存上移到主进程会话级内存，图片重新打开后可直接恢复文字选择层。版本 0.15.3 → **0.15.4**（体验修复，按决议 #73 patch +1）。
- 2026-06-15 v0.78 决议 #103：OCR 文字拖选改为光标边界半开区间选择，按字符中心线判断起止边界，修复从目标位置开始仍把前一个字符选进来的问题。版本 0.15.4 → **0.15.5**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.79 决议 #104：按用户给定参考图刷新品牌 logo 系统为线性茶杯气泡 + 双叶 + 三点消息，重绘三件套 SVG、托盘/未读图标生成逻辑和应用内 `PantryBrandLogo`，并重生成 png/ico/icns/Linux/window 图标。版本 0.15.5 → **0.15.6**（品牌视觉微调，按决议 #73 patch +1）。
- 2026-06-15 v0.80 决议 #105：取消图片上的 OCR 覆盖拖选层，点击识别后打开独立文本结果窗，用户在 textarea 内用系统原生选区复制；保留主进程会话级 OCR 缓存和底部“复制全部文字”。版本 0.15.6 → **0.15.7**（体验修正，按决议 #73 patch +1）。
- 2026-06-15 v0.81 决议 #106：按用户反馈订正品牌 logo 杯身轮廓，从偏方的圆角杯改为参考图式下缘半圆茶杯气泡；同步 SVG、应用内组件、托盘/未读生成图和全套打包图标。版本 0.15.7 → **0.15.8**（品牌视觉微调，按决议 #73 patch +1）。
- 2026-06-15 v0.82 决议 #107：接入用户提供的 SVG 品牌套件，应用内组件直接加载 SVG，打包图标和托盘/未读图标均从 SVG 源生成，不再手写茶杯轮廓。版本 0.15.8 → **0.15.9**（品牌视觉微调，按决议 #73 patch +1）。
- 2026-06-15 v0.83 决议 #88 后续修正：私聊头部资料按钮宽度改为随内容区响应式拉宽，副标题移除 ellipsis / overflow 截断，保证「在线 · IP」完整显示。版本 0.15.9 → **0.15.10**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.84 私聊头部 hover 修正：保留名字/IP 区域的响应式宽度，但移除 hover/active 整块灰底，只用标题文字茶青作为轻量反馈。版本 0.15.10 → **0.15.11**（UI 微调，按决议 #73 patch +1）。
- 2026-06-15 v0.85 决议 #108：UOS20 桌面通知摘要对 emoji 降级为 `[表情]`，并在 Linux / Windows 通知上显式传入应用图标，避免通知中心显示系统默认图标。版本 0.15.11 → **0.15.12**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.86 决议 #109：私聊新增窗口震动即时提醒，协议为 `msg(kind:"nudge")`；不写聊天记录、不离线补发，发送/接收均按同一对端 60 秒最多 2 次且两次至少间隔 15 秒限流。版本 0.15.12 → **0.16.0**（功能更新，按决议 #73 minor +1 归零 patch）。
- 2026-06-15 v0.87 决议 #110：私聊震动收端自动定位到发起人的单聊，并在收发两端插入本地系统提示行；仍不离线补发、不写 FTS，限流规则不变。版本 0.16.0 → **0.16.1**（体验修正，按决议 #73 patch +1）。
- 2026-06-15 v0.88 决议 #111：修复切换会话时消息区滚动位置上下跳动；同一前台会话切换恢复离开位置，首次打开/后台直达/主窗隐藏后重新点开会话默认到最新，当前会话收新消息不再无条件把阅读历史的用户拉到底。版本 0.16.1 → **0.16.2**（bug 修复，按决议 #73 patch +1）。
- 2026-06-15 v0.89 决议 #112/#113：免打扰会话未读数量灰显，免打扰屏蔽入站震动置前/震动动作；Windows 收到非免打扰震动时短暂置前，修复窗口仅被遮挡时不弹到最上方；群元数据新增 `creatorId` 并追加 SQLite v9 迁移，修复宿主机多网卡/虚拟机网络下无密码群改名 `group.info` 被创建 IP 校验误拒的问题。版本 0.16.2 → **0.16.3**（bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v0.90 决议 #114：新增扫描范围低频自动分享与受控后台扫描。`scan-ranges` 只同步 CIDR 记录，收端自动入配置并标注来源；自动扫描延迟 30–90 分钟、同网段 12 小时去重、在线规模大时抽样参与，用户移除后写忽略表。版本 0.16.3 → **0.17.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-16 v0.91 决议 #115：主界面左侧设置按钮上方新增“刷新全局用户”入口，点击后扫描所有已保存网段并显示进度条；进度以已发出探测地址数推进，运行中禁止并发启动。版本 0.17.0 → **0.18.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-16 v0.92 决议 #116：左侧导航按钮新增延迟淡入中文 tooltip；自己头像悬停显示只读个人信息卡，移出收起。版本 0.18.0 → **0.19.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-16 v0.93 决议 #117：自己头像信息卡修正为仅鼠标悬停触发，避免启动焦点误弹；信息卡改为头像标题区 + 分组资料区，提升可读性。版本 0.19.0 → **0.19.1**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v0.94 决议 #118：左侧导航 tooltip 取消菱形箭头和位移动画，改为单体圆角标签 + 短透明度显隐，降低显示/消失突兀感。版本 0.19.1 → **0.19.2**（UI 微调，按决议 #73 patch +1）。
- 2026-06-16 v0.95 决议 #119：左侧导航 tooltip 改为 pointermove 后计时触发，移除 CSS hover/focus 直接显示，修复打开软件即弹“聊天”提示。版本 0.19.2 → **0.19.3**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v0.96 决议 #120：修复 Windows Release workflow 五连验证失败不阻断发布的问题，并让通知图标路径按目标平台稳定生成。版本 0.19.3 → **0.19.4**（发布链路 bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v0.97 决议 #121：启动后释放左侧导航按钮焦点，点击后也 blur，修复全新启动仍显示黄色焦点框。版本 0.19.4 → **0.19.5**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v0.98 决议 #122：taste 细节 UI 精修轮（首启向导真实 logo + 修正过期文案、私聊资料浮层对称状态点、设置页分隔符统一为 ·、全局搜索分类标题改间距分隔、表情包空态两行化）。版本 0.19.5 → **0.19.6**（微调，按决议 #73 patch +1）。
- 2026-06-16 v0.99 决议 #123：设置窗弹出位置由屏幕中心改为主窗几何中心，跟随主窗所在显示器并夹取工作区防越界。版本 0.19.6 → **0.19.7**（微调，按决议 #73 patch +1）。
- 2026-06-16 v1.00 决议 #124：英文品牌名 Pantry → Teahouse（中文「茶话间」不变）。UI 字标 / 主窗标题 / 构建产物（productName、安装路径、产物名、mac bundle、deb maintainer、StartupWMClass）/ 全部文档改 Teahouse；内部代码标识（window.pantry、协议、组件名、字体、包名、appId、仓库 URL）保持不变，userData 仍按「茶话间」派生、聊天数据零迁移。版本 0.19.7 → **0.20.0**（品牌整体更名，按决议 #73 minor +1）。
- 2026-06-16 v1.01 决议 #125：「移除会话」改为删除聊天内容——右键先二次确认，删除后底部浮条 10 秒内可撤回，超时才落库删除（新增 `MsgRepo.deleteByConv` 删消息 + 全文索引，再删会话条目）。撤回窗口与计时在渲染层实现，不改协议 / IPC 签名 / schema。版本 0.20.0 → **0.21.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-16 v1.02 决议 #124 收尾：补改 #124 漏掉的非 src/.md 处——`release.yml` 产物名 glob / 发布说明 / Release 标题 / `Pantry.app` xattr 由 Pantry 改 Teahouse（保留小写 `pantry-` CI 桶名与 `skyjt/pantry`），品牌 SVG `aria-label` 改「Teahouse icon」。修复 v0.21.0 发版因 `Pantry-*` glob 匹配不到 `Teahouse-*` 产物而失败。版本 0.21.0 → **0.21.1**（发布链路修复，按决议 #73 patch +1）。
- 2026-06-16 v1.03 决议 #126：会话列表置顶项整条加淡灰底色 `--bg-pinned`（明暗双主题），与普通会话区分；`.conv.pinned` 定义在 hover/选中态之前，二者仍优先覆盖。版本 0.21.1 → **0.21.2**（UI 微调，按决议 #73 patch +1）。
- 2026-06-16 v1.04 决议 #127：① 左侧列表搜索栏与聊天头部等高 84px + 底部分隔线，两栏顶栏对齐；② 聊天输入框高度可拖拽调节（输入区顶部 ns-resize 手柄，48–320px，localStorage 记忆）。纯渲染层。版本 0.21.2 → **0.21.3**（UI 微调，按决议 #73 patch +1）。
- 2026-06-16 v1.05 决议 #128：修复会话列表右键菜单弹出后单击聊天栏不关闭的 bug——菜单打开期间挂 document `click`/`contextmenu` 与 window `blur` 全局监听，关闭即移除。版本 0.21.3 → **0.21.4**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-16 v1.06 决议 #129：文件消息卡片 `.card` 加 `align-items: center`，图标/文件名/右侧操作共享垂直中心线，修左右高低错位；`.icon` 盒子改 36px 与字形等宽。版本 0.21.4 → **0.21.5**（UI 微调，按决议 #73 patch +1）。
- 2026-06-17 v1.07 决议 #130：渲染层 `chatStore` 为已加载会话维护消息 ID 索引与状态索引，减少追加/状态事件的线性扫描；文件 / 图片 / 表情发送完成后按返回的 `MessageView.convId` 回填，避免发送期间切换会话造成本地列表回填错位。版本 0.21.5 → **0.21.6**（代码优化与 bug 风险收敛，按决议 #73 patch +1）。
- 2026-06-17 v1.08 决议 #131：全仓库扫描高频路径后，优化内置 emoji 切分、peer 查找 / 在线计数、群添加候选、文件卡片传输统计，减少重复遍历和临时数组分配。版本 0.21.6 → **0.21.7**（代码优化，按决议 #73 patch +1）。
- 2026-06-17 v1.09 决议 #132：按安全扫描报告修复路径授权、媒体读取、offer 大小复核、发现源地址绑定、UDP ACK 目标绑定、备份导入外部路径保留和 Release workflow 权限过宽。版本 0.21.7 → **0.21.8**（安全 bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.10 决议 #133：修复私聊震动收端唤起后消息区未定位到最新——`scroll:'latest'` 入口重载最新页（不复用历史搜索窗口或不完整缓存），消息区用 `ResizeObserver` 贴底跟随图片 / 文件卡片等异步撑高内容；`restore` 切换恢复离开位置不变（决议 #111）。版本 0.21.8 → **0.21.9**（bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.11 决议 #134：消息区"回到最新"升级为右下角悬浮圆按钮，触发条件扩展为"搜索 / 跳转到历史页 或 在最新页上滚超约两屏"，近底自动隐藏；白底圆形 + 茶青箭头 + 柔和阴影、淡入上移动效、reduced-motion 降级，相对消息区右下定位不受输入框拖拽高度影响。版本 0.21.9 → **0.21.10**（UI 增强，按决议 #73 patch +1）。
- 2026-06-17 v1.12 决议 #135：修复输入框粘贴富文本 emoji 被剪贴板 `image/png` 副本抢走的问题，`text/plain` 交回原生 textarea 粘贴，截图位图仍在无文本时发送；"回到最新"悬浮按钮 hover 强制白底不透明。版本 0.21.10 → **0.21.11**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.13 决议 #136：表情 / 图片消息右键菜单新增「复制」，通过既有 `fetchStickerSource` 受限读取源图，渲染层转 PNG 写入系统剪贴板；粘贴到输入框后沿用无文本图片粘贴发送。版本 0.21.11 → **0.21.12**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.14 决议 #137：表情 / 图片消息复制改由主进程 Electron 原生剪贴板写入并读回校验，修复 `ClipboardItem` 写入后仍无法粘贴回输入框的问题。版本 0.21.12 → **0.21.13**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.15 决议 #138：输入框 Command+V 增加主进程图片剪贴板读取兜底，修复 Electron 原生图片剪贴板未出现在浏览器 paste event 时无法粘贴发送的问题。版本 0.21.13 → **0.21.14**（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-17 v1.16 决议 #139：正式设计 PK 分歧解决功能。输入区新增 `PK` 小图标入口，玩法为「猜拳」「骰子」；发送结构化 `msg(kind:"pk")` 而不是真实 GIF，主进程随机生成结果，渲染层播放约 1.5 秒本地动画。PK 只面向在线成员即时投递，不走离线补发；对方可点气泡旁按钮反复发新的 PK 消息，不做胜负输出。通知 / 会话预览 / 历史搜索不透结果，导出可展示完整结果；PK 可撤回，但不可复制、不可转发；不新增外网资源或依赖。实现版本计划 **0.22.0**。
- 2026-06-17 v1.17 决议 #140：PK UI 高可用打磨。入口图标简化为同一线性系统下的拳头相碰图形；玩法浮层改用线性「猜拳 / 骰子」图标并补 hover / active / focus / disabled 状态；PK 气泡收紧尺寸、层级和参与按钮，动画只用 transform / opacity，`prefers-reduced-motion` 直接显示结果。版本 **0.22.1**。
- 2026-06-18 v1.18 决议 #141：PK 三件套系统性视觉重设计。气泡改「统一开奖窗」骨架——骰面点阵与猜拳 Twemoji 手势住进同尺寸 / 圆角 / 底色 / 内描边的同一展示窗，收紧为「玩法标签 → 开奖窗 → 弱化结果」三层；参与按钮「我也来」/「掷一下」融入气泡底部一体行；揭晓定格增加开奖窗 scale 回弹 + 茶青高亮一闪（仍只 transform / opacity，尊重 `prefers-reduced-motion`）；工具条入口改线性拳碰图标 + 清晰 `PK` 文字并排（去 7.5px 糊角标）；玩法浮层与开奖窗视觉呼应。不改协议、存储、随机与投递语义。版本 0.22.1 → **0.22.2**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.19 决议 #142：PK 开奖动画 3D 化。骰子开奖窗内改为 CSS 3D 立方体（六面真实点数，rAF 翻滚 + transition 弹性落定到结果面）；猜拳改 CSS 3D 翻牌（正反双面同显 Twemoji 手势，绕 Y 翻转轮播，停在结果手势正面）；`pk-rps` 图标改「张开剪刀」。仍只 transform / opacity，历史与 reduced-motion 直接定位结果面、不转动。不改协议 / 存储 / 随机 / 投递语义。版本 0.22.2 → **0.22.3**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.20 决议 #143：PK 入口图标改为「拳击手套对碰」（圆润拳头 + 拇指 + 腕带），并去掉并排的 `PK` 文字，按钮回到与其他工具一致的纯图标形态（30×28、图标 18px），tooltip 仍为「PK」。纯渲染层，不改浮层与气泡。版本 0.22.3 → **0.22.4**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.21 决议 #144：PK 入口图标改为**斜体 `PK` 字标**（`skewX(-10)` 右倾 + 加粗描边）取代拳套；并修复点开玩法浮层后「猜拳 / 骰子」被聊天面板左缘裁切——浮层由居中弹出（`left:50%` + `translateX(-50%)`）改为左对齐按钮 `left:0` 向右展开（避开 `.chat` 的 `overflow:hidden`）。纯渲染层，不改协议 / 气泡。版本 0.22.4 → **0.22.5**（UI bug 修复 + 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.22 决议 #145：PK 聊天气泡重设计为**横向卡**瘦身——原垂直「标签 / 开奖窗 / 结果 / 通栏按钮」改为「开奖窗左 + 标签&结果两行中 + 参与按钮胶囊右」横向排列，开奖窗 62→42px、骰子立方体 40→28px、猜拳翻牌同步缩小（3D 动画逻辑不变），实测高度 ~170px → ~60px。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.5 → **0.22.6**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.23 决议 #146：猜拳揭晓动画过程中结果文字由随轮播跳动的「出了石头 / 剪刀 / 布」改为固定「出拳中…」（与骰子「投掷中…」对齐），翻牌手势图仍轮播，定格后正常显示结果。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.6 → **0.22.7**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.24 决议 #147：PK 消息卡片改固定宽度（他人 200px / 自己 132px），信息列 flex:1 撑满、结果左对齐留白，消除「出了布」比「出了石头」短一截、动画态与定格态宽度跳变。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.7 → **0.22.8**（UI 微调，按决议 #73 patch +1）。
- 2026-06-18 v1.25 决议 #148：修复 PK 猜拳「石头」手势图片不显示（剪刀 / 布正常）——根因本地 twemoji 缺 `270a.svg`（✊），`rpsSrc` 取空致翻牌卡片 `v-else-if` 不渲染；改石头映射 ✊ → 👊（`1f44a`，已内置同套画风），零新增资源，剪刀 / 布不动。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.8 → **0.22.9**（bug 修复，按决议 #73 patch +1）。
- 2026-06-19 v1.26 决议 #149：聊天工具条「窗口震动」图标重绘——原「显示器 + 底座 + 放射线」易被误读为屏幕发光，改为「带标题栏的应用窗口 + 左右对称振动弧」（通用振动符号），消除发光感、语义更准。仅改 `PantryIcon.vue` 的 `nudge` path，纯渲染层。版本 0.22.9 → **0.22.10**（UI 微调，按决议 #73 patch +1）。
- 2026-06-19 v1.27 决议 #150：设置页整体重设计——信息架构 8→7 组（端口归网络、发送键归聊天、删「安全与高级」、「通用外观」→「通用」、「消息通知」→「通知」只留提醒），保存策略统一为「偏好即时生效 / 输入类留保存」，主题与发送键改分段控件，删 4 个置灰占位，视觉收敛到设置页专属 token 标尺（间距台阶 / 圆角 Shape Lock / 字号 4 级 / 区块标题 14→15）。纯渲染层，不改 IPC / 协议 / 存储；ui-design §8 分组表同步更新。版本 0.22.10 → **0.23.0**（功能更新，按决议 #73 minor +1、patch 归 0）。
- 2026-06-19 v1.28 决议 #151：设置全面实时保存——文本输入（资料 / 端口 / 快捷键）失焦即存、头像 / 文件目录选定即存、偏好沿用即时生效；删全部保存按钮；值未变跳过、校验不过复原并提示；保存反馈升级为底部居中「设置已保存」胶囊 toast（淡入 + 上移、约 1.8s 后淡出）。纯渲染层，复用既有保存 IPC。版本 0.23.0 → **0.24.0**（功能更新，按决议 #73 minor +1）。
- 2026-06-19 v1.29 决议 #152：修复「设置已保存」toast 显示错乱——根因 #151 的 `.toast` 样式块在编辑中途丢失、从未入文件，toast div 无样式裸奔在 `.settings` flex 布局里（五连抓不住纯样式缺失，靠真人发现）。补回并 taste 重写：`left:calc(50% + 87px)` 居中于内容区（补偿 174px 侧栏）、底部上浮、深色胶囊 + `currentColor` 对勾 + 茶青柔影、淡入上移 / 淡出下移、尊重 reduced-motion。版本 0.24.0 → **0.24.1**（bug 修复，按决议 #73 patch +1）。
- 2026-06-20 v1.30 决议 #153：GitHub 仓库 `skyjt/pantry` → `skyjt/teahouse`（重命名 + 本地 remote + README / CONTRIBUTING / 关于页 / handoff 链接全改 teahouse；GitHub 自动重定向旧 URL，现有下载链不断）。内部代号保留 pantry（appId `com.pantry.app` 改了破坏升级、`window.pantry` / 协议 scheme / 数据目录用户不可见），协议无 pantry 标识、互通不受影响。版本 0.24.1 → **0.24.2**（patch +1）。
- 2026-06-20 v1.31 决议 #154：完善 README——新增「为什么做这个」初心段（飞鸽收费 / 内网通只有 Windows / iptux 难用 → 做免费跨平台好用的内网 IM）；平台表加「真机实测」列，标注 Win7 x64 / UOS 20 x64 / macOS 26 已真机跑通收发（Debian 仅构建支持）；用 shuorenhua 把第一屏去 AI 味、说人话，技术段保守保留术语。纯文档，不改代码。版本 0.24.2 → **0.24.3**（patch +1）。
- 2026-06-20 v1.32 决议 #155：品牌 logo 展示形态优化——关于页 / 主界面空状态 / 首启向导三处 `PantryBrandLogo` 由 icon 变体（白卡圆角底板 + drop-shadow，决议 #92/#107）改为 color 变体纯茶杯图形（`pantry-mark.svg`），消除白卡在纯白背景上的阴影方块与深色背景上的突兀白块，logo 直接融入页面背景。仅改 `variant`、尺寸不变，icon/mono variant 与 SVG 套件保留。关联 #90/#107/#122，ui-design §8 同步。（其间 0.24.4 为死代码清理 chore，无决议。）版本 0.24.4 → **0.24.5**（UI 微调，patch +1）。
- 2026-06-20 v1.33 决议 #156：品牌 mark logo 描边平滑去锯齿——`pantry-mark.svg` 的茶杯轮廓原是位图描摹（trace）出的 331 段直线折线逼近曲线，放大 / 软件渲染（Win7/UOS 禁硬件加速）下折线棱角呈锯齿；经闭合向心（centripetal）Catmull-Rom 样条拟合为约 338 段三次贝塞尔曲线（8 个子路径：杯身 / 内壁 / 双叶 / 三点），边缘顺滑、锯齿消除，造型与识别度不变、贴合原轮廓不过冲（注：标准 Catmull-Rom 在疏密不均的描摹点上会过冲、把轮廓吹胖扭曲，更难看；故改用向心参数化按点距加权拟合）。仅平滑应用内显示用的 src mark（关于页 / 空态 / 向导经 color 变体引用，决议 #155），未被引用的 `build/icons` 打包图标 svg 不动。与决议 #107 一致——仍以 SVG 套件为源，只把折线版优化为曲线版，非手写新路径。版本 0.24.5 → **0.24.6**（资源微调，patch +1）。
- 2026-06-20 v1.34 决议 #157：mark logo 进一步重描去抖（接 #156）——#156 向心平滑仍忠实复现了描摹点的高频抖动，线条放大仍「高高低低」；改用 Douglas-Peucker 简化先抓造型主干转折点、丢弃抖动噪声点（杯身/叶/把轮廓约 280→76 点），再向心 Catmull-Rom 连成干净曲线，三个省略号点改用正圆 `<circle>`（统一圆心 y / 等径 / 均匀间距）。保留原茶杯造型，线条顺滑、点正圆，svg 4.98KB。已知瑕疵（用户知情接受、待后续设计精修）：杯把根「肩」、气泡尾尖、叶根等不同部件交接处衔接略生硬——原 logo 分件描摹、衔接靠手绘毛糙掩盖，干净化后凸显，属设计层面细节，简化/平滑算法补不上。期间也试过 Figma 几何重绘（造型偏生硬走样，弃用）。版本 0.24.6 → **0.24.7**（资源微调，patch +1）。
- 2026-06-25 v1.35 决议 #159：文件默认保存目录由「下载/茶话间」改为「文档/teahouse」。① 父目录「下载」→「文档」——各系统都有等价的 Documents（Win=Documents、mac/Linux=~/Documents），「同事发来要长期留存的文件」放文档比放偏临时的下载目录更合归档语义；② 目录名中文「茶话间」→ ASCII「teahouse」——规避中文目录名在部分系统 / 命令行 / 跨平台工具链下的编码与兼容问题（对应用户反馈「中文支持不好」）；③ `app.getPath('documents')` 跨平台取系统文档目录（Linux 依 `XDG_DOCUMENTS_DIR`，未配置回退 home），接收文件时 `transfer.ts` 已有 `mkdirSync(recursive)`、目录不存在自动建、无需预置；④ 仅改「默认回退值」——用户曾在首启向导 / 设置里自选过的 `fileDir` 原样保留不动；首启向导第 3 步与设置页「保存位置」均显示后端 `settingsView.defaultFileDir`、自动跟随更新；既有用户若一直沿用默认目录，升级后新收文件入新目录、已收旧文件留在原「下载/茶话间」不自动搬迁（不擅动用户磁盘）；`index.ts` 两处硬编码（`getSaveDir` 回退、`settingsView.defaultFileDir`）提取为单一 `defaultFileDir()` helper、防下次再漏改一处。不改协议 / 存储 / IPC。版本 0.25.0 → **0.25.1**（优化 / 微调，按决议 #73 patch +1）。
- 2026-06-25 v1.36 决议 #160：「网络 → 网段扫描」已保存网段由可移除标签（`.chips`）重设计为**紧凑表格**——三列「网段 / 在线 / 操作」列对齐：① **网段**列等宽字体显示 CIDR，下方小灰字标来源（「本机添加」/「同事同步」，承接 #114）；② **在线**列茶青圆角徽标显示「有效节点数」= 当前在线且 IP 属于该网段的同事数（0 时徽标灰显），实时统计自节点注册表 `PeerRegistry`（`online && ipInCidr(ip, cidr)`）——为此在零 Electron 依赖的 `cidr.ts` 新增位运算 `ipInCidr(ip, cidr)`（含 `cidr.test.ts` 测试），`ScanRangeItemView` 增只读字段 `nodeCount`，主进程 `scanRangeItems()` 填充；③ **操作**列两枚图标按钮接现有实际能力——⟳ 刷新该网段 = `scanRange(cidr)` 重新发探测（探测后延迟约 2.5s `reload()` 重拉设置，让新上线同事计入在线数，因 P2P 探测/应答异步滞后、不会秒变），✕ 删除该网段 = 从 `scanRanges` 移除并写忽略记录（`saveAppSettings` 返回新视图即时刷新列表，承接 #16）。纯渲染重组 + 新增只读统计，不改协议 / 存储结构 / 扫描与发现机制；ui-design §8 分组表「网段以标签展示」一句同步改为表格。版本 0.25.1 → **0.26.0**（功能更新，按决议 #73 minor +1、patch 归 0）。
- 2026-06-25 v1.37 决议 #161：接 #160 对网段扫描表格的 3 项 UI 打磨（纯渲染层，不改数据 / IPC）——① **来源标签移到网段后**：由 CIDR 下方副行小灰字改为同行括号后缀（`166.9.26.0/24（本机）`/`（来自 张三）`），`.range-cidr` 从纵向 column 改横向 baseline、CIDR 完整不缩、来源可省略；② **操作按钮平时即淡色**：刷新按钮加 `.accent` 默认淡茶青、删除 `.danger` 默认淡红（`opacity:.55`），hover 升至实色（`opacity:1`），取代决议 #160 的「平时灰、移上去才变色」，让两个动作的意图色平时就可见；③ **删除二次确认**：点 ✕ 不再直接删，而是该行就地弹出覆盖层「删除该网段？[删除][取消]」（`confirmingCidr` ref + `confirmRemove`，层 `position:absolute inset:0` 盖住操作区），点「删除」才真正从 `scanRanges` 移除、点「取消」复原，避免误删网段。版本 0.26.0 → **0.26.1**（UI 微调，按决议 #73 patch +1）。
- 2026-06-25 v1.38 决议 #162：用 taste skill 优化两处 UI（纯渲染层，不改 JS / 数据 / 协议）——① **主窗口「刷新全局用户」按钮**（决议 #115）：移除按钮下方外挂的 34×3 进度直条（导航栏里唯一非 40×40 的元素、像个破坏按钮列节奏的「小尾巴」），去掉扫描态 primary-weak 方块底 + 图标旋转动画（与进度条双动效冗余）；进度改为**环绕图标的环形进度**（`conic-gradient(primary 已扫%, line 0)` + `radial-gradient closest-side` mask 抠出细环，`inset:2` 贴在 40×40 按钮内，`--scan-p` 绑 `scanPercent`、`.visible` 跟 `scanProgressVisible` 含完成后停留再淡出），扫描态仅「图标茶青 + 外圈进度环」、空闲态就是干净的 refresh 图标与聊天/设置按钮完全齐整；尊重 `prefers-reduced-motion`（环 opacity 过渡关闭）。② **设置页网段扫描表格列宽**（决议 #160/#161）：`grid-template-columns` 由 `1fr 56px 64px` 改 `minmax(0,1fr) 84px 72px`，在线列加宽 + 徽标居中、操作列加宽 + 按钮右对齐贴边、表头「操作」右对齐，拉开原先挨太近的「在线/操作」两列、同时收窄网段列过宽的留白。版本 0.26.1 → **0.26.2**（UI 微调，按决议 #73 patch +1）。
- 2026-06-25 v1.39 决议 #163：刷新按钮环形进度（决议 #162）由跳变改为**平滑动画**。根因：conic-gradient 角度用的 `--scan-p` 是未经 `@property` 注册的普通自定义属性，CSS 默认不对它插值，所以每次扫描进度（`scanPercent`=已发出/总地址）更新、环就直接跳到新角度。修复（纯渲染层、不改扫描逻辑）：① `@property --scan-p { syntax:'<number>'; inherits:false; initial-value:0 }` 注册成可过渡的 typed 数值属性；② `.scan-ring.visible`（扫描中）加 `transition: --scan-p 0.4s linear`，进度环匀速平滑增长；③ 新增 `scanRingPct` ref 专门驱动环（不再直接绑 `scanPercent`），`refreshAllUsers` 在发起扫描前把它归 0——此刻环处隐藏态（`.scan-ring` 基础规则无 --scan-p 过渡）、归零瞬间完成，规避「上次扫完满环 → 这次从 100% 倒退回 0」的回退动画；`applyScanProgress` 每次进度更新同步 `scanRingPct = scanPercent`；④ `prefers-reduced-motion` 下连 `.scan-ring.visible` 一并 `transition:none`、进度直接到位不动画。版本 0.26.2 → **0.26.3**（UI 微调，按决议 #73 patch +1）。
- 2026-06-26 v1.40 决议 #164：修复 issue #3「发图片 → 接收方收到、发送方显示传输失败」。**根因**：图片走文件传输通道，发送方那条消息的成功/失败状态目前只由 offer 控制报文的回程 ACK 决定，跟图片数据是否真的传完是解耦的；有 UDP 丢包时——对端其实第一次就收到 offer、自动接收并通过 TCP 把整张图拉走（所以接收方一切正常），但 offer 的回程 ACK 丢了，发送方 UDP 重传（1+2+4s）加 TCP 兜底全部判负（7~14s），而局域网图片数据通常 1s 内就传完，迟到的判负把已经传完的这条消息标成了「失败」。**修复**（`files.ts`，纯 services 状态机，不动协议 / 数据面 / 扫描发现）：以数据面真实结果为准——① TCP 供流完成的 `served` 事件 → `applyMsgStatus('sent')`（数据完整送达即已发送）；② 收到对方 `accept` 控制报文 → `applyMsgStatus('sent')`（对方已接受即已送达）；③ `applyMsgStatus` 加终态保护：消息一旦为 `sent`，迟到的 `failed` 不再覆盖（与既有 `recalled` 保护并列）；④ offer 可靠发送回调 `ok=false` 时先查 transfer 状态，已 `accepted`/`done` 就不再 `finish('failed')`、不翻消息失败（单聊 `offerPaths` 与群聊 `offerGroupPaths` 各改一处）。新增 3 个 `files.test.ts` 回归用例（offer ACK 丢失但 served / accept 均判 sent、offer 失败且无送达迹象仍判 failed）。版本 0.26.3 → **0.26.4**（bug 修复，按决议 #73 patch +1）。
- 2026-06-26 v1.41 决议 #165：接 #164 彻底修 issue #3「发图片 → 接收方正常、发送方显示『图片传输失败』」——#164 只修了 `files.ts` 的**消息状态机时序**，没触及发送方**取图被拒**这个直接致因，故未根治。**这次的真根因（三处叠加）**：① **发送方取图卡 `done`**：渲染层经 `pantry-img://<transferId>` 自定义协议取图，主进程 `managedTransferMediaView` 一律要求 `transfer.status==='done'` 才放行；但发送方的图自始就在本地受管目录（`imagesDir/out/`），`ImageBubble` 对自己发的图 `ready=(isMine && savedPath)` 一就绪便立即请求 `pantry-img`，此刻传输多半还在 `offering/accepted`，取图被拒 → `<img>` 触发 `@error` → `broken=true` → 显示「图片传输失败」，且 `broken` 一旦置真不随后续 `done` 自愈。这**与丢包无关**、是收发不对称所致（接收方 `ready=(status==='done')`、请求时已 `done` 故正常），丢包只是让 `done` 更晚、令 broken 必现。② **`served` 救援不可恢复**：#164 的 `served→sent` 依赖发送态 `outgoing` 仍在，但若 offer 判负（`sendReliable` 返回 false）抢先 `finish('failed')` 删了 `outgoing`，迟到的 `served` 因 `if(!out)return` 救不回，`transfer` 永久卡 `failed`（#164 三个测试都只测了「served/accept 先、判负后」，漏了逆序）。③ **`ImageBubble` 失败判定脱节**：发送方也用 `transfer.status` 判失败，与 #164 守护的 `msg.status`（终态保护）两套真相脱节。**修复**（仍不动协议 / 数据面 / 发现扫描）：① `managedTransferMediaView` 仅对接收方（`direction==='in'`）要求 `done`，发送方（`out`）本地媒体在 `offering` 即放行取图——消除 broken 主因；② `files.ts` 的 `served` 改以 `transferRepo` row 为准、不依赖 `outgoing` 存活，能把被迟到判负误标的 `failed` 救回 `done`+`sent`（数据完整送达是最权威成功信号）；③ `ImageBubble` 发送方失败以 `msg.status==='failed'` 为准、接收方维持看 `transfer` 结果。新增 1 个 `files.test.ts` 回归用例（offer 判负先标 failed、`served` 后到 → 救回 done/sent，正是 #164 的测试盲区）。版本 0.26.4 → **0.26.5**（bug 修复，按决议 #73 patch +1）。
- 2026-06-26 v1.42 决议 #166：局域网 P2P 自更新（F-SYS-5 升级落地，分三步实现）。纯内网、零服务器、无中心的点对点更新——节点互知 platform/ver/caps，发现**同平台 + 版本更高 + 声明可作更新源（caps `upd1`）**的在线节点 → 提示「内网有新版 vX.Y.Z（来自 某同事）」→ 用户确认 → 复用文件传输通道拉安装包（沿用 TCP DoneFrame 的 SHA-256 完整性校验）→ 安装并重启；**谁先升级谁自然成源**（下载过的节点保留包、接力扩散），无「管理员」角色、无人工放包。本期形态：Windows nsis（安装时自留一份安装器到数据目录、更新 per-user 静默装免 UAC）+ Linux deb（运行态 `dpkg-deb` 把自身重打包成 deb、更新经 pkexec 系统授权装）；mac dmg/zip 因形态识别 + 签名 / Gatekeeper 成本暂缓。用户确认式（不静默），安全 = 用户确认 + SHA-256 + 同平台严格匹配 + 包内版本核对 + 内网信任边界（决议 #5），**纯内网零外网、不违反红线 #5**（红线禁的是外网更新检查 / CDN）。本轮交付**第一步·发现与提示**：caps 能力位 `upd1`、运行形态自检、`profile.ver` 投影到 `PeerView`、同平台 semver 比对择源、主界面「内网有新版」提示入口；拉包（第二步）与安装重启（第三步）见 tech-design §12 后续里程碑。协议见 protocol §3/§5/§8，模块 / 里程碑见 tech-design §3/§12，UI 见 ui-design §7.6。版本 0.26.5 → **0.27.0**（功能更新，按决议 #73 minor +1、patch 归 0）。
- 2026-06-26 v1.43 决议 #167：设置页关于组新增「检测内网更新」按钮——键值区（版本 / 许可 / 源码）下方，点击主动调既有 `update:check` ipc 查当前可用更新源，有则就地显示「内网有新版 vX（来自 某同事）」、无则「当前已是内网最新版本」；与主界面被动提示（决议 #166 / §7.6）互补，复用 `checkUpdate`、不新增协议 / 不改主进程。版本 0.27.0 → **0.27.1**（微调，按决议 #73 patch +1）。
- 2026-06-26 v1.44 决议 #166（第二步·part1 协议地基）：实现自更新拉包的请求报文——shared/protocol 新增 `MSG_TYPES.update` + `UpdateReqPayload`（`{op:'req', platform}`）；codec 入站白名单校验（op 必须 `req`、platform 必须合法 win/mac/linux，未知字段忽略），补 codec 单测（合法往返 + op/platform/缺字段坏报文拒绝）。收发集成、A 侧备包（Linux `dpkg-deb` 自重打包 / Windows nsis 自留安装器）、B 侧拉取校验为后续 part（多数需真机打包环境验证）。版本 0.27.1 → **0.27.2**（推进，按决议 #73 patch +1）。
- 2026-06-27 v1.45 决议 #168：用 taste skill 优化设置 - 关于里的检测内网更新区：由一行按钮 + 结果文案改为紧凑状态行，补 idle / checking / found / none / error 五态、内网范围说明、失败提示和带图标的检测按钮；只改渲染层与文案，复用 `checkUpdate`，不新增协议 / IPC / 依赖。版本 0.27.2 → **0.27.3**（UI 微调，按决议 #73 patch +1）。
- 2026-06-27 v1.46 决议 #169：局域网自更新第二步继续推进——`file-ctl offer` 支持 `purpose:"update"`，codec 强制单文件 / 非群聊 / 正大小；FilesService 收到更新包 offer 后跳过聊天消息、会话 bump、未读与普通传输记录，自动 accept 到 `userData/data/updates` 临时目录。安装包格式与版本核对、安装重启仍留 #166 后续。版本 0.27.3 → **0.27.4**（功能推进，patch +1）。
- 2026-06-27 v1.47 决议 #170：局域网自更新第二步继续推进——`update{op:"req"}` 纳入 Messenger 可靠控制报文集合，新增 `update:request` IPC；UI 可在发现新版后向当前最佳更新源发起索包。源节点收到请求后复核同平台、请求方在线、本机版本更高，若本地更新包目录 / release 目录已有匹配本机版本的 nsis/deb 安装包，则用隐藏 `purpose:"update"` transfer 发回；无包时按协议静默不回 offer、允许重试。nsis 自留包、deb 自重打包、包内版本核对和安装重启仍留后续。版本 0.27.4 → **0.27.5**（功能推进，patch +1）。
- 2026-06-27 v1.48 决议 #171：设置-关于检测更新区按用户选定的方案 A 改为键值行化：并入版本 / 许可 / 源码同组，移除左侧状态图标块和独立卡片感，右侧用短状态 + 小说明 + 紧凑动作按钮表达；found 状态按钮为茶青弱底「同步更新」。纯渲染层 UI 微调，不改 IPC / 协议 / 主进程。版本 0.27.5 → **0.27.6**（UI 微调，patch +1）。
- 2026-06-28 v1.49 决议 #172：「内网更新」相关界面去掉默认可见的解释性说明；设置-关于行与主窗口新版弹层都把“只从已发现的在线同事中查找同平台更新源、不访问外网、同步更新只发起索包且不静默安装”等机制说明放进圆形 `?` 帮助图标 tooltip；正文只保留短状态、动作按钮，以及发现版本 / 错误等必要反馈。纯渲染层 UI 微调，不改 IPC / 协议 / 主进程。版本 0.27.6 → **0.27.7**（UI 微调，patch +1）。
- 2026-06-28 v1.50 决议 #173：修复设置-关于 `?` 说明 tooltip 被关于卡片 / 窗口底部裁切的问题；tooltip 改为向上浮出并提升层级，避免落进卡片底部裁切区。纯渲染层 CSS 修复，不改 IPC / 协议 / 主进程。版本 0.27.7 → **0.27.8**（UI bug 修复，patch +1）。
- 2026-06-28 v1.51 决议 #174：新增并实现私聊文件「直接发送」。发送方先普通发送私聊文件/文件夹，文件卡片出现后可点击「直接发送」；接收方在线且支持该能力时免确认自动拉取，默认保存到 `文件保存位置/发送人名字/`；发送人目录名本地生成并清洗，重名避让不覆盖。接收方保留默认开启的总开关「允许同事直接发送文件」，关闭后退化为普通文件卡片。群聊文件明确不允许直接发送，离线文件仍不入队。协议/UI/技术文档同步，版本 0.27.8 → **0.28.0**。
- 2026-06-28 v1.52 决议 #175：修复拖拽 / 粘贴文件无法有效发送。新增 `file:grant-paths` IPC 给拖拽 / 粘贴路径补一次性授权，再复用既有文件与图片发送通道；主进程发送 IPC 仍要求消耗授权，不放宽任意路径发送。版本 0.28.0 → **0.28.1**。
- 2026-06-28 v1.53 决议 #176：用 taste skill 收紧文件消息卡片 UI。直接发送入口保留在发送后的卡片上，但等待状态不再占右侧一行，改并入大小 meta；「直接发送」与取消动作保持同一行，取消在双动作场景下用小图标按钮，避免卡片因新增按钮变高；接收方直接发送完成态文案改为「已保存本地」，不再显示「已保存到 xx 文件夹」。纯渲染层 UI / 文案微调，不改协议、IPC、传输状态机或保存目录规则。版本 0.28.1 → **0.28.2**。
- 2026-06-28 v1.54 决议 #177：继续收紧文件卡片短状态文案。「等待接收」不再放进 meta 行，改为文件名同行固定状态片，避免被右侧按钮挤成「等待…」；发送方完成态统一显示「发送成功」，不再显示「已保存到对方电脑」等长句。版本 0.28.2 → **0.28.3**。
- 2026-06-28 v1.55 决议 #178：接收态文件卡片从纵向三按钮改为一行动作组：主按钮「接收」+ 文件夹图标另存 + `x` 图标拒绝，压低卡片高度并保持中文 tooltip。版本 0.28.3 → **0.28.4**。
- 2026-06-28 v1.56 决议 #179：统一默认文件接收目录。普通手动「接收」与私聊「直接发送」自动接收，在未点「另存为」时都保存到 `文件保存位置/联系人名称/`；「另存为」完全使用用户选择目录；失败重试沿用已记住的原接收目录。版本 0.28.4 → **0.28.5**。
- 2026-06-28 v1.57 决议 #180：修复第三方截图工具粘贴后重复发送同一张图片。主进程原生剪贴板兜底延迟执行，并在浏览器 paste 事件已处理文件 / 文本 / 图片时取消；渲染层不再在 keydown 阶段抢先读剪贴板。版本 0.28.5 → **0.28.6**。
- 2026-06-28 v1.58 决议 #181：新增 Debian 10 / UOS 20 arm64 发布包。Linux CI 矩阵增加 arm64 Debian 10 容器 job，产出 arm64 deb/AppImage 与 SHA-256 清单；自更新请求增加可选架构字段，源端按架构匹配本地安装包，避免 x64/arm64 deb 混用。版本 0.28.6 → **0.29.0**。
- 2026-06-28 v1.59 决议 #182：修复 arm64 发布流水线未把产物落到 `release/` 的问题。新增 `scripts/ci-linux-arm64.sh` 承载容器内完整验证/打包脚本，workflow 改为调用该脚本并打印产物列表。版本 0.29.0 → **0.29.1**。
- 2026-06-28 v1.60 决议 #183：Linux arm64 发布 job 取消 QEMU，改用 GitHub 远程 `ubuntu-22.04-arm` runner + `node:18-buster` Debian 10 arm64 容器直接打包。版本 0.29.1 → **0.29.2**。
- 2026-06-28 v1.61 决议 #184：修复远程 arm runner 上 electron-builder 拉取 x86 打包工具与误打 x64 target 的问题。Linux dist 脚本显式限定 `deb/AppImage` 架构，arm64 CI 改用系统 fpm / mksquashfs。版本 0.29.2 → **0.29.3**。
- 2026-06-28 v1.62 决议 #185：首次修复 Debian 10 arm64 容器内 Ruby 2.5 安装 fpm 时解析到不兼容新版 ffi 的问题，arm64 CI 先安装 `ffi 1.17.4` 再安装系统 `fpm 1.9.3`；后续发布日志确认该 ffi 版本仍要求 Ruby 3+，由 #186 继续修正。版本 0.29.3 → **0.29.4**。
- 2026-06-28 v1.63 决议 #186：修复 `ffi 1.17.4` 仍不兼容 Debian 10 Ruby 2.5 的问题。arm64 CI 补 `libffi-dev`，改为安装 `ffi 1.15.5` 后再安装 `fpm 1.9.3`。版本 0.29.4 → **0.29.5**。
- 2026-06-28 v1.64 决议 #187：修复 Debian 10 系统 `mksquashfs` 不支持 AppImage `-offset` 参数的问题。arm64 CI 撤掉 `USE_SYSTEM_MKSQUASHFS=true`，只保留系统 fpm。版本 0.29.5 → **0.29.6**。
- 2026-06-30 v1.65 决议 #188：实现媒体撤回方案 A。`file-ctl offer` 新增跨端一致 `msgId`，图片 / 文件撤回复用现有 `msg(kind:"recall")`；图片 5 分钟内可撤回，文件仅未接收完成时可撤回，已接收完成文件不支持撤回；群文件须所有收件人均未完成接收才可撤回。版本 0.29.6 → **0.30.0**。
- 2026-06-30 v1.66 决议 #189：修复发送图片 / 截图后的前台体验。自己发送消息成功追加到当前会话时显式请求定位到最新页，避免图片等异步发送后消息区停在旧位置；截图框选结束后主窗口短暂置顶再释放，避免退到其他窗口后面。版本 0.30.0 → **0.30.1**。
- 2026-07-02 v1.67 决议 #190：表格粘贴按图片消息发送并支持图片/文字视图切换。输入框粘贴 Excel / WPS / LibreOffice / 网页表格时，默认生成表格图片并走既有图片传输；支持新能力位 `tbl1` 的双方额外传递原始制表符文本，图片气泡上沿显示小型分段滑块「图片 / 文字」，默认图片视图，切到文字视图可选择复制原始 TSV。文字元数据最多 4096B UTF-8，超出时仅截断文字视图并提示，图片仍完整。旧端仍只看普通图片；文字元数据不写 FTS、不改变会话预览、不新增消息类型或 SQLite 迁移。版本 0.30.1 → **0.31.0**。
- 2026-07-02 v1.68 决议 #191：修复小图片打开独立看图窗口时底部工具条挤压的问题。图片窗口保留小图 100% 原始尺寸显示，但 BrowserWindow 与初始 fit 内容区设置最小空间，确保底部菜单不被压缩到糊在一起；不改协议、存储或图片传输。版本 0.31.0 → **0.31.1**。
- 2026-07-02 v1.69 决议 #192：修复会话打开后不在最新消息的问题。会话列表 / 联系人 / 通知 / 托盘 / 震动等用户进入入口默认重载最新页并滚到底部，不再复用历史窗口缓存或旧 scrollTop；历史搜索跳转仍定位目标消息，当前会话内阅读历史时收到新消息不强制打断。版本 0.31.1 → **0.31.2**。
- 2026-07-03 v1.70 决议 #193：修复设置管理密码后群改名 / 群管理体验失效的问题。根因集中在群成员面板依赖原生 `window.prompt()` 输入管理密码，沙盒 Electron 窗口里该入口不可控且失败后只弹阻塞提示；同时渲染层没有主动使用 `group:update` 返回值刷新本地组视图。改为在群成员面板内常驻显示管理密码输入框，改名、添加成员、移出成员统一读取该输入；缺密码或密码错误走面板内行内提示，成功后立即把返回的 `GroupView` 写入 groups store，再由既有 `group:updated` 事件兜底同步。协议、SQLite、密码摘要规则不变。版本 0.31.2 → **0.31.3**。
- 2026-07-08 v1.71 决议 #194：新增内网通兼容模式设计文档。基于 VM 实测确认内网通 `2425/UDP` 的 IPMSG / 飞鸽互通通道可完成发现与文本回执；设计确定兼容模式默认关闭、单独配置 IP 段、独立适配器、独立联系人 / 会话、首版只做发现与普通文本收发，文件传输和内网通私有协议留后续评估。纯文档，不改代码。
- 2026-07-08 v1.72 决议 #195：扩展内网通兼容协议与 UI 设计。补充 IPMSG 文件 / 剪贴板图片 / TCP 拉取 / 信息查询 / 离开状态 / 震动能力矩阵；记录 VM 实测中标准文件和剪贴板图片 offer 会被内网通 ACK 并在文件传输页展示，但 TCP 拉取未闭环，暂列实验能力；UI 设计要求内网通联系人和会话显示兼容标志，并通过 `ConversationCapabilities` 隐藏 PK、震动、图片、文件、文件夹、直接发送和媒体撤回等不兼容入口。纯文档，不改代码。
- 2026-07-08 v1.73 决议 #196：第二轮复测内网通兼容链路。在同一 VM 与同一内网通构建上复测发现：`BR_ENTRY` 纯命令仍稳定返回 `ANSENTRY`，但携带选项位的 `BR_ENTRY` 不响应；`SENDMSG` 系列（文本 / 文件 offer / 剪贴板图片 offer）一律无 `RECVMSG` 回执、消息不进可见 UI；`EnumWindows` 确认内网通进程内为茶话间节点创建了隐藏窗口但未加入联系人列表，存在"联系人准入"前置条件。稳定首版范围降级为仅发现 + 兼容联系人展示 + 能力降级 UI，文本 / 文件 / 图片全部降级为"已映射未闭环"。更新 [nwt-compat-design.md](nwt-compat-design.md) §2.3 / §4 / §5.4 / §15 与本文件第 10 节。纯文档，不改代码。
- 2026-07-09 v1.74 决议 #197：主界面「刷新全局用户」全量网段扫描增加点击后二次确认。用户拍板：居中模态、每次可点均确认、弹框展示网段列表摘要（>6 折叠）、茶青「开始扫描」确认钮。扩展 F-DISC-2b；ui-design / tech-design 同步交互与实现边界。实现落 `App.vue`：`askRefreshAllUsers` → 确认弹窗 → `scanAllRanges`；遮罩 / 取消 / Esc 不扫。版本 0.31.3 → **0.32.0**。
- 2026-07-09 v1.75 决议 #198：讨论组成员上限 50 → **200**。改 `GROUP_MAX_MEMBERS` 与 F-MSG-4 / protocol 常量表；群面板与建群 UI 同步满员门控。版本 0.32.2 → **0.32.3**。
- 2026-07-09 v1.76 决议 #199：内网通兼容**暂缓实现**——设计保留、代码不写、不排近期；handoff「下一步」去掉实现准备项。纯文档。
- 2026-07-09 v1.77 决议 #200：全库代码审查落档 [optimization-plan.md](optimization-plan.md)，18 项分级优化待办（P0 群 200 上限漏改 / 传输层健壮性 → P3 文档漂移），交低成本代理逐项执行。纯文档。
- 2026-07-09 v1.78 决议 #201：弃用 dev-claude 开发分支，日常开发直接提交 main（本地与远端分支已删除、内容已全部并入）；CLAUDE.md 与 optimization-plan.md 工作流程同步更新。纯文档。
- 2026-07-09 v1.79 决议 #198 补齐：IPC 建群 / 改群 / 群内 @ 与备份导入统一使用 `GROUP_MAX_MEMBERS=200`，修复 65 人以上建群静默失败、批量成员变更或 @ 被旧上限截断、大群备份导入被截断到 50 人的问题。版本 0.32.3 → **0.32.4**。
- 2026-07-09 v1.80 决议 #202：修正文档漂移。当前图片 OCR 引擎统一写为内置 PaddleOCR PP-OCRv6 tiny + onnxruntime-web 本地 wasm；数据库迁移版本改为引用 `migrations.ts` / `PRAGMA user_version`，避免交接文档继续手写旧迁移号。纯文档，不递增版本。
- 2026-07-09 v1.81 决议 #203：完成 OPT-17 图片发送 IPC 重复逻辑收敛。图片扩展名白名单提到 `shared/media.ts`，主进程四个图片发送 handler 共用校验与 staging helper，渲染层 ChatPane 共用同一后缀常量。版本 0.32.18 → **0.32.19**（代码质量优化，patch +1）。
- 2026-07-09 v1.82 决议 #204：完成 OPT-18 网络层专项测试补齐。扩展 `peer-registry.test.ts` 覆盖地址漂移防护、离线换址、profileRev、sweep 与 online 事件时序；新增 `range-sync.test.ts` 覆盖延迟分享、stop 清理定时器、CIDR 过滤 / 去重和 127.0.0.1 回环同步。版本 0.32.19 → **0.32.20**（测试补齐，patch +1）。
- 2026-07-09 v1.83 决议 #205：修复 OPT-4 接收端写盘背压在多文件传输时的死锁——`stream.end()` 可能吞掉 `drain`，socket 永久 pause 导致下一文件卡死；CI macOS/Debian 五连验证超时即此根因。版本 0.32.20 → **0.32.21**（bug 修复，patch +1）。
- 2026-07-09 v1.84 决议 #205 补充：验收复核发现 v0.32.21 的「多文件 + 慢写盘」回归用例依赖回环时序，快盘机器上对修复前代码也能通过、锁不住死锁；补一条**确定性**回归用例——手工帧服务端把 `pull-ok + 全部裸流 + done` 单包写出 + 1 字节水位慢写流，逼出「done 与尾部数据同批 + 末尾 write 返回 false」的死锁条件，已验证修复前必超时失败、修复后必通过。纯测试，版本 0.32.21 → **0.32.22**（测试补齐，patch +1）。
- 2026-07-09 v1.85 决议 #206：CI 打包链路优化。arm64 job 消除 npm ci 阶段的重复 better-sqlite3 源码编译、gem（ffi/fpm）按版本缓存、deb 压缩 xz → gz（用户拍板，x64 同步受益）、两个 Linux job 移除无用 rpm；arm64 触发保持每次 push（用户拍板）。版本 0.32.22 → **0.32.23**（CI 优化，patch +1）。
- 2026-07-09 v1.86 决议 #207：修复系统截图粘贴双发（#180 时间窗方案残留竞态）。`onPaste` 入口同步 mark；input/textarea 焦点下不调度 IPC 兜底；兜底 await 后校验粘贴代次。版本 0.32.23 → **0.32.24**（bug 修复，patch +1）。
- 2026-07-10 v1.87 决议 #208：落地全库审查的七项优先修复——TCP 入站帧严格校验与异常隔离、自更新请求授权和 512 MiB 上限、版本 / tag / artifact 一致性 CI 门禁、文件传输资源预算、会话导航竞态防护、截图工具条视口定位、截图窗口导航拦截。每项独立测试，最终合并为一个代码提交。版本 0.32.24 → **0.32.25**。
- 2026-07-10 v1.88 决议 #209：用户将 #208 批次的最终发布版本调整为 **0.33.0**，反映本次同时覆盖安全、传输资源、发布门禁与交互稳定性的改动规模。package / lockfile 由 0.32.25 → **0.33.0**，无新增行为变化。
- 2026-07-10 v1.89 决议 #210：第二批优化聚焦渲染启动成本与 OCR 内存边界。四个根组件按 hash 动态加载，OCR 结果缓存限制为 16 项 LRU 且初始化失败可重试；构建增加 manifest 与 200 KiB 公共启动闭包门禁。协议与用户行为保持不变，版本 0.33.0 → **0.33.1**。
- 2026-07-10 v1.90 决议 #211：修复群聊接收文件「进度条长期 0、取消后无法重新下载」。三项改动：① 发送端数据流排队（并发预算 3 路满载）时向支持 `tw1` 的接收端发 TCP `wait` 帧并每 20 秒保活，接收端卡片显示「排队等待发送方」而不是假死的 0 速度；② 接收端拉取增加 60 秒空闲超时，静默断网/对端假死不再永久冻结进度条，超时判失败后可按既有 `.part` 断点续传重试；③ 接收方取消改为可恢复——保留 `.part` 与本地传输上下文，发送方收到对端 cancel 后保留供流授权（自己主动取消才作废），取消后的入站文件卡显示「重新下载」按钮，断点续传重新拉取；对旧版本对端（无 `tw1`）不提供重新下载入口，行为同旧版。协议细节见 protocol.md §8/§9 v0.41。版本 0.33.1 → **0.33.2**（bug 修复，patch +1）。
- 2026-07-10 v1.91 决议 #211 补充：用户反馈「群聊点接收后再取消不显示重新下载」，按用户操作序列补一条精确复现回归用例（群 offer 带 groupId/msgId → accept → 拉取悬挂在真实静默供流端 → 传输中 cancel），验证 0.33.2 接收端行为正确：状态回 canceled、retryable 保持 true、可再次 accept。代码无缺陷可修，判定反馈场景为对端未升级（`tw1` 双端要求，按设计隐藏按钮）或跨重启（上下文仅会话内保留）。纯测试，版本 0.33.2 → **0.33.3**（patch +1）。
- 2026-07-13 v1.92 决议 #212：重设计左上角个人信息卡。去掉端口，补充本机 IPv4；头像 / 昵称、本机 IP、组织 / 设备、节点 ID 形成四级信息层次，取消卡片内部多层等权小卡片。`app:info` 只扩展返回字段，协议与存储不变。版本 0.33.3 → **0.34.0**（功能更新，minor +1、patch 归 0）。
- 2026-07-13 v1.93 决议 #213：覆盖历史决议 #20，新增 Windows ia32（32 位）发布平台。GitHub Actions 独立完成五连验证、better-sqlite3 x86 重建与 PE 架构校验，产出 NSIS / portable / SHA-256 三项资产；自更新协议与包名匹配同步支持 ia32。版本沿用 **0.34.0**，与 #212 合并发布。
- 2026-07-13 v1.94 决议 #214：修复 UOS 20 系统托盘未读闪烁不可感知。Linux 改用高对比红色注意帧与常规茶青图标交替，每次切帧重建 `NativeImage`；重复未读更新不再反复重置周期，托盘销毁 / 写图失败安全停表，清零恢复常规图标。补齐 Linux 定时器与生命周期回归测试及 UOS 实机验收项。版本 0.34.0 → **0.34.1**。
- 2026-07-13 v1.95 决议 #215：在 `naive_ui_dev` 分支启动 Naive UI 混合接入。精确锁定 Node 16 兼容候选 `naive-ui@2.43.2`，首批迁移设置窗口的标准表单与常规操作控件；茶话间核心 IM 组件、品牌和窗口系统继续自绘。Provider 留在设置动态入口内，维持纯内网、Chrome 108 与公共启动闭包门禁。版本 0.34.1 → **0.35.0**。
- 2026-07-13 v1.96 决议 #216：参考 Apple Design 对主窗口与设置窗口做整体视觉升级。主界面采用轻材质分区、弱茶青选中态、浮层式头部和输入区；Naive UI 扩展到主窗口搜索、主要按钮及 Provider，保留自绘消息、媒体、图标与窗口交互。补 reduced-motion / reduced-transparency / prefers-contrast 降级。版本 0.35.0 → **0.36.0**。
- 2026-07-13 v1.97 决议 #217：修复 Naive UI `NConfigProvider` 布局节点打断高度链、导致主窗口三栏只占上半区和底部大块空白的严重回归。主窗口 Provider 改用 `abstract` 模式并补源码回归测试；验收覆盖默认尺寸、最小窗口、最大化 / 还原、空态和会话态。版本 0.36.0 → **0.36.1**。
- 2026-07-13 v1.98 决议 #218：修复「设置已保存」toast 向右偏移与截图窗口丢失 `capture:init` 后只显示灰绿色遮罩。toast 改为设置窗口整体居中；preload 提前监听并缓存截图初始化数据供动态 `CaptureApp` 订阅时回放，截图窗口加载底色固定为黑色。补事件时序回归测试与真实 Electron 截图交互验收。版本 0.36.1 → **0.36.2**。
- 2026-07-13 v1.99 决议 #219：`backdrop-filter` 收敛到真正压住内容的浮层；主窗口三栏结构面板、聊天头部 / 输入区、设置侧栏与分组面板移除无视觉收益的 blur，消除 Win7 禁硬件加速下的软渲染逐帧开销。落档 `prefers-reduced-transparency` 为 Chrome 118+ 特性、108 基线上不生效的事实。版本 0.36.2 → **0.36.3**。
- 2026-07-13 v2.00 决议 #220：修复主窗口最小化 / 关到托盘 / 失焦时收到当前会话新消息托盘不闪烁——渲染层原先无条件即时置读当前会话，未读恒为 0。改为「窗口可见且聚焦」才即时置读，离开窗口期间保留未读驱动托盘闪烁与角标，重新聚焦自动补置读。版本 0.36.3 → **0.36.4**。
- 2026-07-13 v2.01 决议 #221：修复 Win7 截图卡顿、双层任务栏与画面变形。Windows 框选范围改用工作区并按桌面源真实像素裁掉任务栏；渲染层改为单桌面位图 + 四块纯色遮罩、动画帧合并拖动，位图首帧就绪后再显示窗口；最终裁剪按图片自然尺寸分别换算 X/Y。版本 0.36.4 → **0.36.5**。
- 2026-07-13 v2.02 决议 #222：Windows / Linux 设置窗改为主窗模态子窗，打开时主界面覆盖静态暗色 scrim 并拦截交互，关闭后恢复；设置窗补平台无关的 1px 高对比内描边并显式请求系统阴影。macOS 沿用原生窗口阴影、不增加主窗遮罩。版本 0.36.5 → **0.36.6**。
- 2026-07-13 v2.03 决议 #223：账号资料、主题与发送键统一为同一双段滑块；主题用太阳 / 月亮线性图标表达浅色 / 深色，选中块做短距离无回弹位移并兼容减少动态效果。版本 0.36.6 → **0.36.7**。
- 2026-07-13 v2.04 决议 #224：发送键滑块改为平台自适应的本地键帽图标组；macOS 显示 Command + 回车，Windows / Linux 显示 Control + 回车，滑块宽度同步收紧。版本 0.36.7 → **0.36.8**。
- 2026-07-14 v2.05 决议 #225：项目主许可从 MIT 调整为 `GPL-3.0-only`，自 0.37.0 起适用；历史 MIT 授权继续有效，第三方许可保持独立，安装包资源目录随附 GPLv3 全文。版本 0.36.8 → **0.37.0**。
- 2026-07-14 v2.06 决议 #226：落地用户选定的生图方案 A，以透明 1024px PNG 作为彩色应用图标母版，派生 Win7 / macOS / Linux 平台图标、窗口图标、彩色托盘和应用内品牌展示；macOS 菜单栏保留单色模板图。版本 0.37.0 → **0.38.0**。
- 2026-07-14 v2.07 决议 #227：修复私聊头部资料入口 hover 回归，移除宽区域整块背景与按下缩放，只保留昵称茶青高亮和键盘焦点轮廓。版本 0.38.0 → **0.38.1**。
- 2026-07-14 v2.08 决议 #228：文字气泡移除顶部高光，文件卡与文字气泡统一为四角 14px；文件类型图标切换为内置 ImageGen 生成、透明归一化的 4×4 PNG atlas。版本 0.38.1 → **0.39.0**。
- 2026-07-14 v2.09 决议 #229：修复 macOS 打包时全局与平台专属 `extraResources` 重复复制 `LICENSE` 导致的 `EEXIST`；许可证只在全局配置声明一次，三端分发语义保持。版本 0.39.0 → **0.39.1**。
- 2026-07-14 v2.10 决议 #230：修复转发弹窗被消息列表覆盖；弹窗通过 Teleport 进入 renderer 全局顶层，补独立遮罩、明确层级、初始焦点、Esc 关闭和 reduced-motion 降级。版本 0.39.1 → **0.39.2**。
- 2026-07-14 v2.11 决议 #231：低性能平台第一批优化。Win7 / Linux renderer 接收软渲染画像并关闭浮层磨砂、收紧阴影，小图 OCR 改为手动触发；图片暂存、OCR 图源读取和另存异步化；四窗口增加完整静态闭包预算，renderer 品牌图与文件类型 atlas 分别降至 256px / 512px。版本 0.39.2 → **0.39.3**。
- 2026-07-14 v2.12 决议 #232：主窗口保留 Naive UI 混合接入并完成可复用控件审查；聊天图片 / 表情消息、记录搜索缩略图和表情包网格启用原生惰性加载与异步解码。版本 0.39.3 → **0.39.4**。
- 2026-07-14 v2.13 决议 #233：`GroupCreator` 与 `ProfileCard` 的普通输入框、主次操作接入主窗口现有 Naive UI 主题；高密度成员选择与专用结构继续保持轻量实现。版本 0.39.4 → **0.39.5**。
- 2026-07-14 v2.14 决议 #234：内联图片增加 8192px 单边 / 3200 万总像素与真实格式校验，超限或异常图片转普通文件；聊天流和记录搜索增加 320px WebP、128MB LRU 派生缩略图缓存并保持近视口加载。版本 0.39.5 → **0.39.6**。
- 2026-07-14 v2.15 决议 #235：发送图片按钮改用专用图片选择 IPC，文件对话框与主进程返回值均限制 PNG/JPG/JPEG/GIF/WebP/BMP；渲染层取消非图片转普通文件的选择器兜底。版本 0.39.6 → **0.39.7**。
- 2026-07-14 v2.16 决议 #236：发送方主动取消文件传输时写入受保护的 `canceled` 消息终态，迟到异步失败不再覆盖；发送文件卡显示“发送取消”，真实错误继续显示“传输失败”。版本 0.39.7 → **0.39.8**。
- 2026-07-14 v2.17 决议 #237：发起讨论组的联系人项压缩为 34px 单行布局，姓名后直接显示公司 / 部门 / 团队路径，长组织信息在右侧省略。版本 0.39.8 → **0.39.9**。
- 2026-07-14 v2.18 决议 #238：设置侧栏移除重复的账号摘要；主界面个人信息卡改为 120ms 出现，并让头像、桥接区与卡片形成连续悬停范围，离开整个范围后收起。版本 0.39.9 → **0.39.10**。
- 2026-07-14 v2.19 决议 #239：设置侧栏 7 个分组增加对应的 18px 自绘线性图标，统一继承文字色并在选中时使用茶青。版本 0.39.10 → **0.39.11**。
- 2026-07-14 v2.20 决议 #240：UDP / TCP 端口默认锁定，点击后先显示网络中断风险提示；取消保持原值，红色“确认修改”只解锁当前端口，失焦继续校验并保存。版本 0.39.11 → **0.39.12**。
- 2026-07-14 v2.21 决议 #241：讨论组新增群主/管理员/普通成员角色；群主任免管理员，管理员可改名与移出普通成员，所有成员均可直接邀请联系人；群主退出自动转让，角色与成员变化写入系统提示。协议、SQLite v11、备份、IPC 与群成员面板同步扩展。版本 0.39.12 → **0.40.0**。
- 2026-07-14 v2.22 决议 #242：群成员添加改为全局搜索多选弹窗，复用建群联系人选择能力，隐藏已有成员并按剩余名额批量邀请；失败保留选择，提交期间阻止重复操作。版本 0.40.0 → **0.41.0**。
- 2026-07-14 v2.23 决议 #243：新增自定义用户与群头像，共用本地圆形裁剪弹窗；头像以可选哈希声明并经 `av1` 在局域网按需获取，旧端使用数字头像或默认群图标回退。协议 v0.44、SQLite v12、备份、IPC 与全部头像展示位同步扩展。版本 0.41.0 → **0.42.0**。
- 2026-07-14 v2.24 决议 #244：设置头像编辑区收敛图片入口；未设置自定义头像时只保留“图片头像”分段项，已有图片后才显示“更换图片 / 恢复默认”。版本 0.42.0 → **0.42.1**。
- 2026-07-14 v2.25 决议 #245：设置头像模式改为“动物表情 / 昵称首字 / 自定义头像”，三种模式仅展示各自选项；昵称首字支持显式背景色并复用 `avatar=200..209` 同步。协议 v0.45，版本 0.42.1 → **0.43.0**。
- 2026-07-14 v2.26 决议 #246：撤回 v0.43.0 Release 与标签；修复头像裁剪放大后拖动中断，并把 `pantry-avatar` 登记为标准安全 scheme、资源地址改为固定短主机名 + 哈希路径，使三平台稳定读取本人、联系人和群头像。协议 v0.46，版本 0.43.0 → **0.43.1**。
- 2026-07-15 v2.27 决议 #247：裁剪改为窗口级鼠标拖动；头像输出从原始字节创建 `ImageBitmap` 后绘制，并在 WebP 编码前拒绝全透明像素结果。协议 v0.47 仅记录本地处理收紧，版本 0.43.1 → **0.43.2**。
- 2026-07-15 v2.28 决议 #248：自定义头像链路健壮性加固——恢复群头像失败补提示、资料保存校验头像哈希本地存在、prune 跳过新鲜 `.tmp`、`AvatarStore` 增加已验证哈希内存缓存。无协议与存储变化，版本 0.43.2 → **0.43.3**。
- 2026-07-15 v2.29 决议 #249：协议 v0.48 为 `avatar` 新增尽力而为 `miss` 提示，群头像取图源无文件时立即故障转移到下一在线成员；裁剪最大缩放随源图分辨率动态放宽至 192px 输出粒度，等比步进，大图降采样改走 `createImageBitmap` 高质量缩放。版本 0.43.3 → **0.44.0**。
- 2026-07-15 v2.30 决议 #250：全局联系人搜索移除隐藏主机名匹配，避免同机多身份被共同主机名误命中；结果行新增圆形头像、自定义头像支持与离线灰显，并按备注优先展示名称。版本 0.44.0 → **0.44.1**。
- 2026-07-15 v2.31 决议 #251：消息右键菜单撤回项改为单行左右布局，操作名固定在左、倒计时或不可用原因弱化放右；最后 10 秒才增强危险色，与复制 / 转发保持等高。版本 0.44.1 → **0.44.2**。
- 2026-07-15 v2.32 决议 #252：通讯录联系人行移除姓名后的“· 离线”文字，保留行尾绿 / 灰状态点与离线灰显，减少重复信息。版本 0.44.2 → **0.44.3**。
- 2026-07-15 v2.33 决议 #253：Win7 启动早期关闭 Chromium `TSFImeSupport` 并回退 IMM32，修复部分旧输入法 / 第三方输入法候选窗固定左上角；其他平台保持现状。版本 0.44.3 → **0.44.4**。（已被 #254 撤销）
- 2026-07-16 v2.34 决议 #254：核实 Chromium 108 在 Win7 恒走 IMM32，#253 的 TSF 开关系空操作，予以撤销；Win7 判定收敛到 `util/windows-version.ts`，候选窗左上角问题转入真机对照定位（应用侧 / 输入法侧）。版本 0.44.4 → **0.44.5**。
- 2026-07-16 v2.35 决议 #255：按"Win32 焦点落入内部子窗口导致 IMM32 输入法整链失效"故障模型实施 Win7 输入法焦点自愈——主窗口/设置窗口获得焦点后延迟重走原生 show 流程钉回键盘焦点；仅 Win7 生效。版本 0.44.5 → **0.44.6**，待 Win7 真机回归。
- 2026-07-16 v2.36 决议 #256：源码复核撤销 #255 的主进程 `show()` 自愈；Win7 主聊天 `textarea` 在安全焦点边界执行输入客户端 blur/focus 刷新，触发 Chromium IMM32 强制重取 caret bounds，并完整恢复选区与滚动，composition 期间跳过。版本 0.44.6 → **0.44.7**。（Win7 搜狗真机实测无效，已被 #257 撤销）
- 2026-07-16 v2.37 决议 #257：Win7 搜狗实测否定 #256，撤销 textarea blur/focus 与 `AppInfo.windows7`；主窗口激活和 textarea 获焦时用 `webContents.focus()` 满足 Chromium IMM32 的 Win32 焦点句柄门槛，字体缩放从 renderer `body.style.zoom` 迁到主进程 `webContents.setZoomFactor()` 统一坐标换算。版本 0.44.7 → **0.44.8**，待 Win7 搜狗真机回归。
- 2026-07-16 v2.38 决议 #258：v0.44.8 Win7 搜狗真机照片确认候选窗锚在应用客户区 `(0,0)`，撤销无效的 `webContents.focus()` 与 IPC；textarea 改为正常流布局，仅 Win7 在 composition 期间以 0–1px 几何脉冲强制 Chromium 重算并发布 caret bounds。保留 #257 的原生页面缩放迁移。版本 0.44.8 → **0.44.9**，待 Win7 搜狗真机回归。
- 2026-07-16 v2.39 决议 #259：交叉测试确认全局搜索、设置昵称与同机 Chrome 输入均正常，故障仅限聊天自定义 textarea；撤销 #258 无效的 composition 几何脉冲，Win7 聊天框改用无自定义字体、无 emoji 镜像、无透明覆盖的基础 textarea 做单变量分支验证。版本 **0.44.9-beta.1 → 0.44.9-beta.2**，待 Win7 搜狗真机回归。
- 2026-07-16 v2.40 决议 #260：Win7 搜狗已验证基础 textarea 候选窗定位正常；移除诊断用内边框和异色底，保留微软雅黑实体输入层，并新增不参与输入布局的 Twemoji 展示覆盖层遮换黑色系统 emoji。版本 **0.44.9-beta.2 → 0.44.9-beta.3**。
- 2026-07-16 v2.41 决议 #261：Win7 安全 textarea 与 Twemoji 覆盖层恢复 `PantryEmojiBlank` 的 `1.3em` 等宽表情槽，输入态表情尺寸重新对齐消息正文，连续表情与原生 caret 按相同槽宽步进；安全布局和实体输入层保持。版本 **0.44.9-beta.3 → 0.44.9-beta.4**。
- 2026-07-16 v2.42 决议 #262：Win7 真机单变量回归确认真实 textarea 使用 `PantryEmojiBlank` 会让搜狗候选窗再次失位，撤销 #261 的 Win7 字体路径；Win7 改用系统字体 contenteditable，并以内联 `1.3em` Twemoji 原子节点承载表情和原生 caret。版本 **0.44.9-beta.4 → 0.44.9-beta.5**。
- 2026-07-21 v2.43 决议 #263：私聊与群聊普通文件统一增加发送后 24 小时领取期限；到期后发送方显示「发送已到期」、接收方显示「文件已过期」，关闭接收/重试入口。发送端持久化截止时间与共享源文件 manifest，协议 v0.49、SQLite v13；版本 **0.44.9-beta.5 → 0.45.0**。
- 2026-07-21 v2.44 决议 #264：Windows 标签构建暴露源码结构测试对 CRLF 敏感；测试读取 Vue 源码后先统一换行，再执行既有断言。产品行为不变，版本 **0.45.0 → 0.45.1**。
- 2026-07-21 v2.45 决议 #265：修复 Kylin V10 SP1 截图文字工具无输入入口。删除 Electron 22 不支持的 `window.prompt()`，改为截图层内原地单行输入框；Enter / Esc 与中文 composition 安全分流，发送 / 复制前提交非空文字，预览和 PNG 统一左上基线。协议 v0.49、SQLite v13、IPC、依赖与网络均不变，版本 **0.45.1 → 0.45.2**。
- 2026-07-21 v2.46 决议 #266：截图工具条八个功能按钮统一改为本地线性 SVG 图标，去掉可见按钮文字；保留中文悬停提示、无障碍名称与工具选中状态，工具组和操作组以分隔线区分。协议 v0.49、SQLite v13、IPC、依赖与网络均不变，版本 **0.45.2 → 0.45.3**。
- 2026-07-21 v2.47 决议 #267：截图工具条八个按钮增加自定义悬停 / 键盘焦点提示；顶部空间不足时提示自动翻到按钮下方，移除重复的原生 `title` 并保留 `aria-label`。协议 v0.49、SQLite v13、IPC、依赖与网络均不变，版本 **0.45.3 → 0.45.4**。
- 2026-07-21 v2.48 决议 #268：修复 macOS 聊天输入法候选确认时 Enter 提前发送旧草稿；textarea 与 Win7 contenteditable 统一上报 composition 状态，键盘分流在发送 / 换行 / @ 提及前拦截活动 composition、`isComposing` 和 `keyCode 229`。协议 v0.49、SQLite v13、IPC、依赖与网络均不变，版本 **0.45.4 → 0.45.5**。
- 2026-07-22 v2.49 决议 #269：修复 Issue #18 建群成员响应式数组无法经 Electron IPC 克隆后永久停在“创建中”的问题；提交改传普通数组并以异常收尾恢复按钮、显示行内错误。建群遮罩只接受从遮罩自身开始并结束的点击，输入框拖选移出不再误关弹窗；提交期间锁定返回、取消和遮罩关闭。协议 v0.49、SQLite v13、IPC 契约、依赖与网络均不变，版本 **0.45.5 → 0.45.6**。
- 2026-07-25 v2.50 决议 #270：修复 Issue #19 表格粘贴误判并取消自动发送。表格判定收紧为"整段确实就是一张表"（HTML 侧唯一 `<table>` 且表外无实质文字，纯文本侧 ≥2 行、列数一致、≥2 列、首列非全空）；命中后只把原文插入输入框并显示「检测到表格 · 发送为图片 · 忽略」轻提示条，直接回车即发纯文字，草稿超 4096B 时改提示只能按图片发送且不自动消失。协议 v0.49、SQLite v13、IPC、依赖与网络均不变，版本 **0.45.6 → 0.46.0**。
- 2026-07-25 v2.51 决议 #271–#277（共享文件柜需求轮，**本轮只落档、不动代码**）：新增 §6.10 共享文件柜（F-SHARE-1–8，P1，v0.47），并把它从 §3 非目标（原决议 #7"远期评估"）中移出。形态为「单目录 + 默认档（不共享/只读/可读可传）+ 按人例外」，写权限限死为"只能上传新文件、落到共享根下上传者同名子目录、不可删改覆盖"；入口为私聊头部「文件柜」按钮 + 右侧覆盖式面板；只有上传会在私聊插系统提示（冒泡计未读、不弹桌面通知），浏览下载静默。**不新增任何端口**：控制面复用 UDP 17878 新增 `share` 报文与能力位 `shr1`，数据面 100% 复用 TCP 17879 拉取式传输的 `purpose:"share-get"|"share-put"`，两者不入聊天流、不套 24 小时领取期限。存储为 `config.fileCabinet` + SQLite **v14** 新表 `share_grants`，传输记录复用 `transfers`。协议见 protocol.md §8.2、模块与库表见 tech-design.md §3/§4/§5、界面见 ui-design.md §5/§8。版本号待实现轮按决议 #73 递增到 **0.47.0**。

- 2026-07-25 v2.52 决议 #271/#276/#277 落地第 ① 步「我的文件柜」：共享根与默认权限档进 `config.fileCabinet`（缺省 `{root:"", mode:"off"}`，老配置升级后一律不共享），按联系人例外进 SQLite **v14** 新表 `share_grants`（只存与默认档不同的行，恢复"跟随默认"即删行）。共享根拒绝文件系统 / 盘符根、用户主目录根，以及与应用数据目录相互包含的路径，选定后再复核目录存在且可读。设置页「聊天与文件」新增「我的文件柜」一组（目录选择 / 打开 / 停止共享、三段默认权限滑块、按联系人例外表格）。**本步不发任何报文、不声明 `shr1`**，协议 v0.50 文本不变；浏览下载（②）与上传（③）按 handoff §4 续做。版本 **0.46.0 → 0.47.0**。

- 2026-07-25 v2.53 决议 #273/#275/#276 落地第 ② 步「浏览与下载」：私聊头部新增「文件柜」按钮（离线 / 未开共享 / 版本不支持时灰显并悬停说明原因），点开在聊天右侧盖出 320px 面板，可逐级进子目录、面包屑返回、滚动分页（每页 200 条）、勾选后下载或另存为，进度就地显示且不进聊天流。协议侧 `share` 报文与 `purpose:"share-get"` 打通，**不新增端口**。安全边界按 F-SHARE-7 全部落地：两层路径校验 + `realpath` 越界复核、隐藏文件不列、10 秒 5 次限流、60 秒一次性下载授权、日志只记条目数。**上传（F-SHARE-5）尚未接入**，本版对端只会看到「只读」。版本 **0.47.0 → 0.48.0**。

- 2026-07-25 v2.54 决议 #272/#274 落地第 ③ 步「上传」，共享文件柜三步全部完成：面板底栏出现「上传到 TA 的文件柜」「上传文件夹」，也可直接把文件拖进面板；上传统一落到对方 `共享根/我的名字/`，重名加后缀，不能删改覆盖对方已有文件（F-SHARE-5 全部口径落地）。对方落盘完成后在私聊看到一条「XX 上传了 N 个文件到你的文件柜」系统提示，点击直接打开该目录，会话冒泡计未读但不弹桌面通知（F-SHARE-6）。同时拆除第 ② 步的临时收口：`perm` 恢复如实回报，设置里选「可读可传」现在对同事真正生效。版本 **0.48.0 → 0.49.0**。

> 注：原 v1.42 / 决议 #166 曾登记一条「issue #3 残留·发送方图片卡『接收中…』」修复（v0.26.6），经核查其描述的 `chat.ts` 代码（`messages.find(fileRef)` ensure）在真实代码中不存在、根因（`transfers.byId` 登记不全）亦不成立——`transfers.byId` 由 `onTransferUpdated` 事件实时填充、`ImageBubble` 组件自身 `ensure` 懒加载，登记不会漏。该条无对应代码、未发布，#166 号已改记本功能（issue #3 实际由 #165 / v0.26.5 修复，用户已实测通过）。

- 2026-07-26 v2.55 决议 #278：v0.49.0 交付后独立复审的 A 级修复。文件柜面板把首屏失败与翻页失败拆成两个状态——首屏失败仍整片替换为错误态，滚动翻页失败只在列表末尾追加一行小字 + 「重试」并保留已加载条目，失败后暂停自动续拉；对方列目录限流 10 秒 5 次，原实现下浏览大目录连滚几页就会把整屏条目清空。切换会话时文件柜面板与群信息面板一起收起，消除"按钮已灰、面板还开着报错"的矛盾态。协议 v0.50、SQLite v14、IPC、依赖与网络均不变，版本 **0.49.0 → 0.49.1**。
- 2026-07-26 v2.56 决议 #279：文件柜上传的体量测算改为异步。原实现是主进程里的同步递归遍历，上传几万文件的文件夹时会占满事件循环、让聊天与传输一起卡住；现抽到 `util/upload-measure.ts`，用 `fs/promises` + 显式队列 + 每批 64 条并发，既不阻塞界面又保持原有的深度上限与失败语义，并补 8 例单元测试。协议 v0.50、SQLite v14、IPC、依赖与网络均不变，版本 **0.49.1 → 0.49.2**。
- 2026-07-26 v2.57 决议 #280：`services/share.ts` 三条修正。下载授权改句柄化 FIFO，同一对端多次下载不再互相顶掉落点（原实现下"先下载再另存为"会让第一批文件落进后选的目录）；共享根 realpath 由调用方算一次传入，列目录不再逐条重解析；目录列举改为先按全量排序再截断，超 5000 项的目录不再展示文件系统返回顺序的随机子集。协议 v0.50、SQLite v14、IPC、依赖与网络均不变，版本 **0.49.2 → 0.49.3**。
- 2026-07-26 v2.58 决议 #281：界面细节与一致性六条。字节格式化收敛为 `utils/format.ts` 一份实现（字节不带小数、其余单位 ≥10 取整、<10 保留一位），文件柜截断提示改常量插值，「全选」在还有下一页时改称「选择已加载」，列表行补长名 tooltip，截图窗 8 处茶青字面量改走 `--primary` 与既有 brightness 惯例，拖拽高亮不再因经过子元素而抖动。协议 v0.50、SQLite v14、IPC、依赖与网络均不变，版本 **0.49.3 → 0.49.4**。
- 2026-07-26 v2.59 决议 #282：`FilesService` 三个隐藏传输入口（自更新包 / 文件柜下行 / 文件柜上行）的重复收尾抽为私有 `offerHidden`，各入口只保留前置校验与差异参数，净减 46 行、行为零变化。至此 v0.49.0 复审提出的 12 条全部处理完毕。协议 v0.50、SQLite v14、IPC、依赖与网络均不变，版本 **0.49.4 → 0.49.5**。
- 2026-07-26 v2.60 决议 #283：文件柜从设置里搬出来成为一等入口。§6.10 新增 **F-SHARE-9**（导航栏底部工具组新增「文件柜」按钮 + 1000×680 独立窗口：左栏我的柜子摘要卡与支持文件柜的同事列表、右栏文件浏览器 / 我的柜子管理页；单击选中、双击进目录、多选与右键、详情列表与网格双视图、排序固定不可切、不做缩略图），F-SHARE-1 的设置入口迁到该窗口，F-SHARE-3 改为两个入口并存，F-SHARE-6 增「最近有人放进来」汇总列表（数据取自既有传输记录）。**不动协议、不动库表、不动权限与安全边界**，纯前台重组：协议 v0.50、SQLite v14 均不变，新增 `ui:open-cabinet` 与 `share:recent-uploads` 两个 IPC。版本 **0.49.5 → 0.50.0**（功能更新，minor +1、patch 归 0）。
- 2026-07-27 v2.61 决议 #284：文件柜形态由独立窗口改为**主窗第三个页签**——列表栏放我的柜子摘要与同事列表，内容区放浏览器 / 我的柜子管理页。删除 `windows/cabinet-window.ts` 与 `#/cabinet` 渲染入口（回到四入口契约），界面拆为 `CabinetList` + `CabinetPane` 两个组件、状态收进 `stores/cabinet.ts`，`ui:open-cabinet` 改为「显示主窗 + 切页签（可带 peerId 定位）」。功能与交互与 #283 完全一致，只是不再弹窗。`App.vue` 静态闭包预算随之抬到 800/116 KiB，公共启动闭包不变。协议 v0.50、SQLite v14、库表与依赖均不变，版本 **0.50.0 → 0.51.0**。
- 2026-08-10 v2.62 决议 #285：建立简体中文 + 英文文档体系。新增根目录英文 README、贡献/开发/第三方说明、`docs/en/` 当前规范与 `docs/README.md` 导航；中英入口双向互链，`check:docs` 校验覆盖和链接，GitHub Release 增加双语标题与下载指引。应用界面语言、协议 v0.50、SQLite v14、IPC、依赖与网络均保持，版本 **0.51.0 → 0.51.1**。
- 2026-08-26 v2.63 决议 #286：修复 Issue #29 / #31 的 Linux 截图兼容。Wayland 启用 Electron 22 PipeWire capturer 后实际探测屏幕源，不再提前拒绝；主窗口隐藏等待事件与合成器退场并复核不可见；空源、空图和异常通过应用内提示或系统通知明确给出系统截图 + `Ctrl+V` 退路。协议 v0.50、SQLite v14、依赖与网络均不变，版本 **0.51.1 → 0.51.2**。
- 2026-08-27 v2.64 决议 #287：修复 Issue #30。表情面板新增受限的本地图片多选导入；收藏网格改为按正方形实际高度生成行并保持 200px 内容区滚动；群聊表情复用现有群媒体 offer，只发给在线成员。协议 v0.50、SQLite v14、依赖与网络均不变，版本 **0.51.2 → 0.52.0**。
- 2026-08-29 v2.65 决议 #288：群聊引用回复功能落地。F-MSG-8 升级为已实现 P2 功能：发送侧仅携带源消息 ID（`replyTo`），codec 拒绝非法参数、伪造对象；接收侧在本地群会话内查询源消息生成 senderName + 首行文本摘要写入 `ReplyMeta`，渲染层显示引用缩略信息栏，点击跳转定位到源消息；目标消息不存在时接收正常，渲染层显示"原消息不可用"提示。协议升 v0.51，SQLite 升 v15，版本 **0.52.0 → 0.53.0**。
- 2026-08-29 v2.66 决议 #289：修复 Issue #34 的麒麟 ARM64 Wayland 截图闪退。Electron 22 在该组合枚举屏幕源可能触发进程级原生崩溃，应用无法用 JS 异常处理恢复；统一截图入口在隐藏主窗和调用 `desktopCapturer` 前改走既有失败提示，保留系统截图后 `Ctrl+V` 发送退路。x64 Wayland、ARM64 X11 和其他平台继续使用原截图链路。协议 v0.51、SQLite v15、依赖与网络均不变，版本 **0.53.0 → 0.53.1**。
- 2026-08-31 v2.67 决议 #290：群简介与群公告。F-MSG-4 补充：群主、管理员和持有正确管理密码的普通成员可在群成员面板设置、清空群简介（≤ 200 字符）与群公告（≤ 1024 字符）；空内容不展示；设置后随 `group.info` 全网广播，所有成员同步；旧端缺省字段保留本地已知值，首次接收默认空串；迁移备份完整保留两字段；修改写入幂等系统提示。codec 入站执行可选字段长度白名单校验，远端变更执行单操作权限隔离；SQLite v16 迁移追加两列。协议 v0.51、SQLite v16，版本 **0.53.1 → 0.54.0**。
- 2026-09-05 v2.68 决议 #291：PR #39 复审修复。`group.info` 的简介与公告字段继续保持可选，旧端缺省时保留本地已知值；远端一次只允许修改简介或公告之一，权限独立校验为群主、管理员或正确管理密码，拒绝普通成员借邀请、改名等操作夹带文本。群成员面板统一复用一套弹窗和既有密码更新路径，备份导入导出完整保留两字段。协议 v0.51、SQLite v16、依赖与端口均不变，版本 **0.54.0 → 0.54.1**。
- 2026-09-05 v2.69 决议 #292：区分单次群管理操作与跨版本全量快照；在版本差足够且文本与结构权限均通过时补齐累积变化，保留普通成员越权及相邻版本夹带拒绝。补录已有 #288–#291 决议索引。协议 v0.51、SQLite v16，版本 **0.54.1 → 0.54.2**。
- 2026-09-05 v2.70 决议 #293：完善 Issue #38 的主窗口 Esc 隐藏操作，输入法、弹窗、菜单与文件柜选择优先消费按键；隐藏独立于关闭偏好，托盘不可用时最小化。保留现有可配置全局快捷键，版本 **0.54.2 → 0.54.3**。
- 2026-09-05 v2.71 决议 #294：OPT-23 修复窗口边缘会话菜单裁切，保留全部菜单操作及兼容路径；版本 **0.54.3 → 0.54.4**。
- 2026-09-05 v2.72 决议 #295：OPT-24 修复全局搜索竞态、卸载后定时器和失败状态，保留查询语义；版本 **0.54.4 → 0.54.5**。
- 2026-09-05 v2.73 决议 #296：OPT-25 收敛重复引用遍历与重复状态 IPC，保留消息和传输状态语义；版本 **0.54.5 → 0.54.6**。
- 2026-09-05 v2.74 决议 #297：OPT-26 收敛全局搜索重复 FTS，保留旧查询语义与异常历史数据退路；版本 **0.54.6 → 0.54.7**。
- 2026-09-05 v2.75 决议 #298：OPT-27 为闲置会话和传输投影设回收边界，保护当前阅读、撤回窗口及有效传输，版本 **0.54.7 → 0.54.8**。
- 2026-09-05 v2.76 决议 #299：OPT-28 限制缩略图冷缓存流水线并发并取消无人使用的排队项；版本 **0.54.8 → 0.54.9**。

- 2026-09-05 v2.77 决议 #300：消除 Linux native 和五平台应用重复编译，补下载缓存、关闭 artifact 重压缩，保留全部发布门禁；版本 **0.54.9 → 0.54.10**。
- 2026-09-05 v2.78 决议 #301：UI 小批次 OPT-29 局部提高列表辅助文字对比度，沿用既有布局与兼容路径；版本 **0.54.10 → 0.54.11**。
- 2026-09-05 v2.79 决议 #302：OPT-30 使用原生按钮补列表键盘入口与局部焦点语义，保留原有鼠标交互与平台路径；版本 **0.54.11 → 0.54.12**。

- 2026-09-06 v2.80 决议 #303：新增当前会话图片前后切换，完整本地历史按消息顺序浏览，首末箭头置灰并跳过不可用媒体；版本 **0.54.12 → 0.55.0**。

- 2026-09-06 v2.81 决议 #304：用户确认手动识别与缓存复用，改图片内原生选字复制、单 Worker 可取消推理，移除独立结果框；版本 **0.55.0 → 0.56.0**。

- 2026-09-06 v2.82 决议 #305：校正图片原生文字选区尺寸与位置，跨行拖选期间保持文本光标；版本 **0.56.0 → 0.56.1**。

- 2026-09-07 v2.83 决议 #306：恢复内置 emoji 的原生文本复制，补齐 Win7 编辑器复制 / 剪切，并为 Linux NumLock 数字输入加受限兼容；版本 **0.56.1 → 0.56.2**。

- 2026-09-09 v2.84 决议 #307：新增简体中文 / English 模式、全窗口即时切换与本地结构化系统提示，版本 **0.56.2 → 0.57.0**。

- 2026-09-16 v2.85 决议 #308：新增 §6.11 远程桌面查看，明确一对一、单屏、每次同意、低帧率 JPEG/TCP 明文方案及数据/平台边界；同步协议、UI、技术与交接文档的方案和未决项。**仅设计落档，应用保持 v0.57.0，尚未实现或发布。**

- 2026-09-16 v2.86 决议 #309：默认目标确定为 10 帧/秒，同步 100ms 采样间隔、5 MiB/秒 JPEG 预算及实际帧率/负载验收；压缩、分辨率与平台验证仍待完成。**仅文档，应用保持 v0.57.0。**

- 2026-09-16 方案补充（待确认）：按用户后续讨论增加 §6.11.1 四档候选与自动调节思路；与已确认的 #309 分开标记，尚未确定默认模式或实现。

- 2026-09-16 v2.87 决议 #310：实现远程桌面查看、独立窗口、默认自动及 3/5/10 手动档；完成逐次选屏授权、单帧背压、终态清理、Linux 锁屏接口探测及双语 UI。新增回环/状态/边界测试和真实 Electron 合成画面自测；目标机权限与性能验收单独保留。版本 **0.57.0 → 0.58.0**。

- 2026-09-17 v2.88 决议 #311：复审只读屏幕协助的多平台适配、低配负载与紧凑 UI；范围和四档默认保持，应用 **0.58.0 → 0.58.1**。本地验证结果与物理平台验收分别记录。

- 2026-09-17 v2.89 决议 #312：新增屏幕协助聊天卡片与发送确认，共享提示贴边缩小；应用 **0.59.0**。

- 2026-09-17 v2.90 决议 #313：协助聊天卡片按请求发起方左右排列，沿用普通消息布局；应用 **0.59.1**。

- 2026-09-17 v2.91 决议 #314：一次协助一张两行摘要卡片，开始/结束原位更新，收起重复信息；应用 **0.59.2**。

- 2026-09-17 v2.92 决议 #315：诊断与反馈范围、脱敏边界、日志预算与本地附件交付流程落档，应用 **0.60.0**。
