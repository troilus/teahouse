# 茶话间（Teahouse）UI / 交互设计文档

> [简体中文](ui-design.md) · [English](en/ui-design.md)

| | |
|---|---|
| 状态 | v1.98；v0.60.0 诊断与反馈（#315） |
| 日期 | 2026-09-17 |
| 关系 | 功能依据 [requirements.md](requirements.md)；本文是**界面与交互的唯一事实来源** |

## 1. 设计基调

**以稳定桌面 IM 为骨架，吸收 Apple Design 的材质、空间一致性和即时反馈原则**：三栏结构保持熟悉，层级依靠留白、材质重量与克制色彩建立。"安静、可靠、精致"落到具体手段上：

- **一个主题色**点缀全局（按钮、选中态、自己的气泡底色），其余全部中性灰阶；主题色定为**茶青 `#3D8B6B`**（决议 #1）。
- **系统字体栈**：`PingFang SC / Microsoft YaHei / Noto Sans CJK`，正文 14px，绝不用衬线、不用艺术字。
- **图标统一为项目内自绘线性 SVG**（1.6px 描边、圆角端点、继承当前文字色），不混用拟物/彩色图标，不用 emoji 充当系统图标；设置图标必须画成清晰齿轮，不使用放射状“太阳”隐喻。Win7 / UOS 缺字平台不可靠，头像和内置 emoji 子集的可见形态使用 Twemoji 本地 SVG 子集兼容渲染，输入框编辑态也必须覆盖，协议与复制仍保留原始 emoji 字符。
- **品牌 logo 使用统一的“茶杯气泡”标识**：杯身兼具对话气泡轮廓，右侧保留杯把，左下伸出对话尾。**v0.15.9 起以用户提供的 SVG 套件作为唯一品牌源（决议 #107）**：任务栏/Dock、标准 logo、小尺寸 logo、菜单 logo、macOS 菜单栏 outline、深浅背景和横向 logo 都保留为本地 SVG；应用内 `PantryBrandLogo` 直接加载 SVG，托盘和打包图标也从 SVG 光栅化生成，不再手写茶杯轮廓。历史背景：v0.5.28 曾改为饱满实心填充风（决议 #64），v0.15.6/#104 与 v0.15.8/#106 为参考图手绘过渡版。
- **动效克制**：150-220ms 缓动，按压在 pointer-down 阶段立即缩放到 0.97-0.98；只用于反馈、层级和状态变化，不做自动循环过场。动效路径与触发源保持空间一致，允许用户立即反向操作。
- **材质与形状（决议 #216，#219/#231 修订）**：结构层使用轻半透明背景、柔和内描边和有色阴影表达层级；导航 / 列表材质较厚，聊天头部 / 输入区材质较轻。磨砂（`backdrop-filter`）只用于真正压住内容的浮层（菜单 / popover / 模态遮罩 / toast）；Win7 / Linux 运行时画像为软渲染时，这些浮层统一使用实色表面、关闭磨砂并收紧阴影。容器 12-16px、控件 8-10px、胶囊 999px，按用途固定，不混用随机圆角。
- **窗口铺满契约（决议 #217）**：主窗口 Naive UI Provider 使用 `abstract` 模式，只提供主题上下文、不生成布局 DOM；动态根与 `.shell` 继续保持完整的 100% 高度链。默认 960×640、最小尺寸和最大化 / 还原后，导航、列表、内容区都必须铺满窗口可用高度，不得以内容高度收缩或在底部留下空白。
- 深色主题 v0.4（roadmap），实现时颜色全部走 token 变量，先把底子留好。
- **组件底座（决议 #215/#216/#232/#233）**：Naive UI 承载标准表单、搜索输入与常规操作反馈。设置窗口使用 Input / Select / Switch / Button / Provider；主窗口使用 Provider、搜索 Input 与主要 Button，`GroupCreator` 的普通文本输入和页脚操作、`ProfileCard` 的备注输入和资料页操作同步接入现有主题。`SetupWizard`、`ForwardDialog` 继续列为后续普通表单复用候选；高密度成员复选框、已选成员标签、消息工具栏、右键菜单、聊天输入器保留项目实现。主题覆盖集中映射茶青、明暗 token、8-16px 形状标尺、系统字体和克制动效。三栏结构、会话 / 联系人列表、聊天气泡、文件 / 图片 / PK 卡片、品牌与系统图标、窗口控制、截图及图片查看器保留项目实现；不引入 vfonts、xicons、远程字体或在线素材。
- **emoji 复制与小键盘输入（决议 #306）**：聊天与搜索中的内置表情继续显示 1.3em 本地 SVG，选中后以原始 Unicode 复制，连续表情和文字间不额外插入换行；右键复制整条消息。Win7 编辑器支持含表情的部分复制 / 剪切。Linux 文本输入框在 NumLock 开启时按数字输入小键盘 0–9，关闭后保留导航；组合键、输入法组词、只读和禁用状态保持原有行为。

## 2. 主窗口：三栏结构

```
┌──────┬──────────────────────┬────────────────────────────────────────┐
│  我  │ [搜索]        [+]   │ (头像) 张三 · 在线 · 某某科技/研发部/后端组 ⋯ │
│      ├──────────────────────┼────────────────────────────────────────┤
│ 聊天 │ ● 张三        10:24 │              ┌──────────────────────┐   │
│ 聊天 │   好的，收到    (2)  │              │ 需求文档发我一下？     │   │
│      │ 讨论组          9:15 │              └──────────────────────┘   │
│ 通讯 │   李四: [图片]       │   ┌────────────────────┐               │
│ 通讯 │ ○ 王五        昨天  │   │ [文件] 需求文档v3.docx │            │
│ 录   │   [文件] 设计稿.zip  │   │ 2.4 MB   已送达      │               │
│      │                      │   └────────────────────┘               │
│      │                      ├────────────────────────────────────────┤
│ 设置 │                      │  表情 截图 图片 文件                     │
│      │                      │  输入消息…                              │
└──────┴──────────────────────┴────────────────────────────────────────┘
  68px      272px（可拖拽）              自适应，≥560px
```

| 栏 | 内容 | 规格 |
|---|---|---|
| ① 导航栏 | 上：我的头像（点击=编辑资料）→ 聊天 → 通讯录；下：文件柜（决议 #283）→ 刷新全局用户 → 设置 | 68px 固定（决议 #68，为容纳 mac 红绿灯由 56px 加宽）；**中性浅灰底（决议 #70，微信式，`--rail-bg`，右细线分隔列表栏）——图标默认深灰、选中/hover 茶青高亮**，让 mac 红绿灯落在浅色上自然；聊天/通讯录主入口图标 25px，底部工具组（文件柜 / 刷新 / 设置）图标 21px；图标带未读总角标 |
| ② 列表栏 | 随导航切换：会话列表 / 通讯录；顶部搜索框常驻 | 272px 固定；后续恢复拖拽时范围 220-340px |
| ③ 内容区 | 聊天界面 / 联系人资料卡 / 空状态 | ≥560px；未选会话时显示品牌 logo 空状态 |

- 窗口最小 960×640，记忆上次大小与位置；关闭按钮默认最小化到托盘（设置可改）。
- **主窗口 Esc（决议 #293）**：无修饰键的单次 Esc 隐藏主窗；不改变关闭按钮偏好与设置中的全局快捷键。输入法组词期间、长按重复及已消费按键不触发。弹窗 / 菜单 / 表情与 @ 选择等临时浮层优先收起，提交忙态的弹窗保持等待；文件柜先关闭右键菜单或取消选择，下一次 Esc 才隐藏窗口。首启向导与打开的设置窗阻止主窗隐藏。托盘不可用时改为最小化以保留恢复入口；图片、截图和设置窗口保持独立键盘行为。
- **沉浸式无标题栏**（决议 #49/#51/#52）：主窗与设置窗不显示系统标题栏，三栏内容直通窗口顶部。macOS 保留原生红绿灯，**主窗红绿灯在导航栏左上角 (9,7)**（贴合窗口圆角视觉中心）（决议 #68：导航栏由 56px 加宽至 68px 容纳标准红绿灯，回到微信式三栏左上角位置；此前决议 #51 曾临时移到列表栏顶部 x=68，与 UI 不协调已废弃）；设置窗红绿灯在侧栏左上 (12,10)。Windows / Linux 在右上角自绘窗口控制按钮（最小化 / 最大化·还原 / 关闭，关闭悬停红底白字）。窗口顶部 32px 为隐形拖拽带（可拖动、双击最大化还原）：macOS / Windows 用系统 / CSS 拖拽区，**Linux 因 CSS 拖拽区不可靠改为 JS 拖拽**（决议 #52，对用户行为无差异）。导航栏头像、列表搜索框、聊天头部内容均从拖拽带下方开始；全屏弹层（向导、转发、图片查看器等）层级高于拖拽带，交互不受影响。
- **文件柜按钮（决议 #283/#284）**：导航栏底部工具组最上一格（更新提示之下、刷新之上），`cabinet` 图标 21px，点击**在主界面内切到文件柜页签**（形态见 §8.2），选中态与聊天 / 通讯录同一套；不加角标，未设共享目录也可点。
- **刷新全局用户按钮（决议 #115 / #162 / #197）**：放在导航栏底部工具组、设置按钮正上方，使用 `refresh` 线性图标。仅当设置里至少有一个扫描网段时可点；**可点状态下每次点击先弹出居中二次确认**（决议 #197），用户确认后才启动全量扫描。扫描中按钮进入加载态并禁用，图标外圈显示环形进度（决议 #162，由原下挂直条改为环绕图标的 conic 细环），进度代表“已发出探测地址 / 总地址”。完成后短暂停留再淡出，避免把左侧栏变成常驻状态面板。
- **全量刷新二次确认弹窗（决议 #197/#216）**：布局骨架对齐「移除会话」确认（决议 #125）——`position:fixed` 半透明遮罩 + 居中圆角卡片（约 **300px**、16px 圆角），`role="dialog"` + `aria-modal`。**信息克制**：无长说明 / 来源 / 脚注；顶部用**暖色警示圆标**（`warning` 三角叹号，琥珀系，非危险红）表达「留意代价」，标题居中。**不**用危险红主按钮。

  | 元素 | 规格 |
  |---|---|
  | 警示标 | 居中 48px 圆：暖琥珀底 + 线性 `warning` 图标（提醒而非恐吓） |
  | 标题 | `刷新全局用户`（15px 半粗，居中） |
  | 摘要 | 仅一句：`将探测 N 个网段` |
  | 网段列表 | 只列 CIDR（等宽），浅底圆角行；最多 **4** 条；超出 `另 N 个` |
  | 操作 | 顶部分隔线后右对齐：`取消` + 茶青 `开始扫描` |
  | 关闭 | 遮罩 / 取消 / **Esc** → 不扫；「开始扫描」→ 既有 `scanAllRanges` |
  | 边界 | 无网段或扫描中：按钮禁用不弹窗。设置页单网段 ⟳ 不使用本弹窗 |

  动效克制：短淡入；`prefers-reduced-motion` 关闭。不引入组件库、不用系统 `confirm()`。
- **左侧导航悬停说明（决议 #116/#117/#118/#119/#212/#216/#238）**：聊天、通讯录、刷新全局用户、设置按钮均使用自绘中文 tooltip；tooltip 不得由 CSS `:hover` / `:focus-visible` 直接触发，必须在鼠标于按钮上真实移动后再计时约半秒显示，pointer 离开立即清理，避免应用启动、默认焦点或静止鼠标位置导致误弹。不使用原生 `title`，避免系统黄框和设计语言冲突。tooltip 采用单体圆角深色标签，不做箭头、不做位移，显隐只保留短透明度变化，避免显示/消失时出现“菱形 + 矩形”的拆件感。自己头像悬停时不显示普通 tooltip，而是在头像右侧显示个人信息卡；信息卡在真实鼠标进入头像 120ms 后出现，启动焦点或键盘焦点不得误弹。头像、到卡片之间的透明桥接区和卡片自身属于同一个悬停范围，指针进入卡片后继续保持，离开整个范围才收起。个人信息卡采用单一外层容器：顶部以大头像、昵称和「本机账户」建立主身份层；中部以淡茶青区域重点展示本机 IPv4；组织与设备名采用无底色键值行；节点 ID 收进最弱层级页脚。端口不在个人信息卡展示，仍可在设置「网络」中查看和修改。卡片保留单行截断、16px 圆角、克制阴影以及浅色 / 深色主题等价层级。

## 3. 列表栏 · 会话列表（导航=聊天）

- 条目：头像（带在线小圆点）、名称、最后消息摘要、时间、未读角标；免打扰会话仍显示未读数量，但角标改为灰色，区别于普通红色强提醒。
- **选中态**（决议 #71，视觉由 #216 更新）：当前会话使用弱茶青材质底、左侧 3px 茶青内强调线和主文字高对比；时间与摘要继续使用次级文字，未读角标保持语义红。该方案减少整条实色块的视觉重量，同时保持当前位置清晰。hover 使用更浅的中性材质。
- 摘要规则：`李四: [图片]`、`[文件] 设计稿.zip`、`[PK] 猜拳` / `[PK] 骰子`（不展示结果）、草稿优先显示红字 `[草稿] …`。
- 排序：置顶在前，其余按最后消息时间倒序（微信同款）；**置顶会话整条加淡灰底色**（`--bg-pinned`，决议 #126），与普通会话一眼区分，hover / 选中态仍优先覆盖。
- 右键菜单：置顶 / 免打扰 / 移除会话（**决议 #125**：点击先弹二次确认弹窗，说明「移除后聊天记录将被删除」；确认后会话立即从列表移除并删除聊天内容，但底部居中浮出「已删除与『X』的聊天记录」+「撤回」按钮，**倒计时 10 秒**；10 秒内点「撤回」立刻恢复（期间未落库），超时才真正删除聊天记录（消息 + 全文索引 + 会话条目）。早期"只移出列表、不删历史"的行为已废弃）。
- 顶部 `[+]`：发起讨论组（多选联系人，见 §7.1）。
- **菜单定位（决议 #294）**：显示后按实际宽高夹紧到 CSS 视口内，四边保留 8px；打开期间随窗口调整重新定位。页面字体缩放仍通过 `webContents` 实现，菜单操作及 Esc 优先级保持。
- **列表键盘入口（决议 #302 / OPT-30）**：会话行使用原生按钮，Tab 聚焦，Enter/Space 打开；当前会话用 `aria-current` 标识。三类列表均以 2px 茶青内描边显示键盘焦点，不占布局、不覆盖既有选中强调线。

## 4. 列表栏 · 通讯录（导航=通讯录）

```
[搜索]
▾ 讨论组
   # 产品讨论组 (8人)
▾ 某某科技 (23/41 在线)
   ▾ 研发部 (12/18)
      ▾ 后端组 (5/7)
         ● 张三  Windows  ← 在线
         ● 李四  macOS
         ○ 王五            ← 灰显 + 灰色状态点，沉底
      ▸ 前端组 (4/6)
   ▸ 设计部 (3/8)
▸ 未分组 (2/5)
```

- **三级树：公司 ▸ 部门 ▸ 团队 ▸ 成员**，逐级折叠，每级标题显示（在线/总数）；资料字段空缺的层级自动跳过，全空归入"未分组"。
- 组内排序：在线优先，再按拼音；普通联系人行以行尾绿色 / 灰色圆点表达在线状态，离线者头像与昵称继续灰显，昵称旁不重复显示“离线”（决议 #17/#252）。
- **历史联系人持久保留**：凡是出现过的节点永久留在通讯录（离线灰显），重启不丢。不提供手动删除（对方还在网内时删了也会回来）；"清理 90 天未上线联系人"为后续版本能力（决议 #150 起不再摆置灰占位，做好再加）。
- **资料自动刷新（防"机器换人"）**：对方上线时、改资料时即时更新；在线期间心跳每 30s 携带资料版本号，发现版本变化立刻拉取最新资料——机器没换、用的人换了昵称/部门，最迟半分钟内通讯录就纠正过来。
- 单击成员 → 内容区显示**联系人资料页**：顶部为圆形头像、昵称/备注、在线状态与组织路径；中部按“备注 / 详细信息”分组展示本地备注输入、IP、主机、平台、最近在线时间；底部提供「保存备注」「发消息」动作。资料页使用内容区完整页面布局，靠留白和细分割线组织信息，不使用突兀的居中小浮卡。双击成员 → 直接进入该联系人的单聊界面（复用打开会话探活，决议 #40）。
- 键盘（决议 #302）：分组和联系人行均可 Tab 聚焦，Enter/Space 分别切换折叠和展示资料；继续沿用资料页「发消息」按钮开聊。分组公布 `aria-expanded`，联系人可访问名称含显示名、在线状态和 IP；不新增方向键树形导航或全局监听。
- 资料页可**设置备注名**（仅本机生效，列表与搜索优先按备注显示，决议 #22/#40）。
- 顶部固定「讨论组」分组，列出已加入的组。
- 开启内网通兼容模式后，通讯录增加固定「内网通兼容」分组（决议 #194/#195），位于讨论组之后、组织树之前。条目显示兼容昵称 / 主机名、`内网通 · IP:端口` 副文字和在线状态点；昵称右侧显示小型描边徽标「内网通」。离线条目灰显并显示「离线」。兼容联系人不展示公司 / 部门 / 团队资料，也不进入普通组织树；单击显示简版资料，双击打开兼容会话。

## 5. 内容区 · 聊天界面

**头部**：单聊在头部显示对方头像（右下角在线/离线状态点）+ 昵称，昵称下方以小字显示在线/离线状态 · 对方 IP（在线茶绿、离线灰，决议 #81）。IP 是内网排障关键信息，副标题必须单行完整展示，不使用省略号截断（决议 #88）。点击头像/昵称这块用户名字区域，在头部下方弹出紧凑资料框，展示头像、备注/昵称、组织、IP、主机、平台、在线状态，并可直接修改对方备注。备注仅本机可见，保存后会同步影响会话列表、通讯录和搜索显示。**该资料入口的鼠标反馈只改变昵称文字颜色为茶青，不铺整块 hover 背景、不做按下缩放；完整热区与键盘焦点轮廓继续保留（决议 #84/#227）。**群聊显示圆形群头像 + 组名 + 人数；没有自定义图片时沿用成员群组图标。聊天头部与左侧列表搜索栏**等高（84px）**、顶部都让出 32px 拖拽带，底部分隔线跨栏连成一条，视觉统一（决议 #127）。**打开会话即触发一次探活**，约 2s 内以真实结果刷新状态，离线则显示灰字"离线"——防止对着"假在线"的人发消息（F-DISC-8）。右侧 `⋯` 打开会话设置（置顶/免打扰/查看成员/清空本会话记录）。群聊点人数 → 右侧弹出群信息面板（含「+ 添加成员」）。**面板覆盖右侧一整列**（绝对定位、从顶到底、盖在消息之上而非挤压消息，决议 #67）；成员每项显示紧凑圆形头像 + 在线/离线状态点（绿/灰，头像始终彩色，与私聊/会话列表一致；决议 #82 由原「离线灰显」改）。**私聊头部在 `⋯` 左侧再放一个「文件柜」图标按钮（决议 #273）**，点击打开对方的共享文件柜面板（形态见下方「文件柜面板」）；按钮在对方离线、未设共享目录、把我关成"不共享"或版本不支持（未声明 `shr1`）时灰显，鼠标悬停以自绘 tooltip 说明具体原因（「对方离线」/「对方未开启文件柜」/「对方未对你开放」/「对方版本不支持」），灰显口径与决议 #17 一致。群聊头部不出现该按钮。

**文件柜面板（决议 #273）**：形态复用群信息面板——绝对定位覆盖聊天区右侧一整列（相对 `.chat`，top/bottom 0、左边框 + 阴影、z 高于消息、顶部留 32px 拖拽带），**宽度 320px**（比群信息面板的 248px 宽，容纳文件名与大小两列），盖在消息之上而非挤压布局，可边聊边浏览；同一时间只显示一个右侧面板，打开文件柜会收起会话设置类浮层。自上而下四段：① **头部**——26px 圆头像 + 标题「XX 的文件柜」+ 权限徽标（「只读」/「可上传」，决议 #283 由底栏上移）+ 「在文件柜里打开」图标按钮（带着当前对端切到 §8.2 的文件柜页签）+ 关闭 `✕`；② **面包屑行**——`文件柜 / 设计稿 / 2026`，可点任意一级跳回，左侧「返回上级」箭头按钮，右侧「刷新」图标按钮（重新从第 0 页拉取）；③ **列表**——每行 36px（决议 #283 由 40px 收紧），左侧 20px 文件类型图标（复用现有 `file-types` 图标集，目录用文件夹图标），中间名称单行省略，右侧小字显示大小（面板宽度放不下修改时间列，目录不显示大小）；行 hover 淡灰底，**单击选中、双击进目录**（决议 #283 起与窗口版一致，原「单击目录即进入」取消），行首在 hover 或已勾选时显示复选框；顶部可全选当前页；键盘与右键菜单同 §8.2。列表按"目录在前、再按名称"排序，滚动到底自动加载下一页（每页 200 条），加载中显示骨架行；`.` 开头的隐藏项不出现。空目录显示居中空态「这个文件夹是空的」；单目录被截断时列表底部显示 `--text-3` 小字「目录内容过多，仅显示前 5000 项」。④ **底栏**——左侧选中摘要（权限徽标已上移到头部，决议 #283），右侧主按钮：勾选了条目时为茶青实心「下载（N）」，未勾选且我有写权限时为「上传到 TA 的文件柜」，无写权限时该按钮不出现。**上传按钮的语义在任何层级都一样**（永远上传到对方文件柜里以我命名的子目录，与当前浏览到哪一层无关），按钮下方常驻一行 `--text-3` 小字说明「文件会放进 TA 文件柜的『我的名字』文件夹里」，避免用户误以为传到当前目录。面板也接受直接把文件 / 文件夹拖进来上传（有写权限时高亮拖拽区，无权限时显示禁止态）。下载与上传的进度、速率、取消复用现有文件卡的状态语义，就地显示在底栏上方的一条进度行里，**不进聊天流**。请求超时或被拒时，列表区就地显示原因与「重试」（如「对方已关闭文件柜」「操作太频繁，请稍后再试」）。**首屏与翻页的失败态必须分开（决议 #278）**：首屏（进目录 / 刷新 / 面包屑跳转）失败才把列表整片替换成居中错误态；**滚动翻页失败只在列表末尾追加一行横排小字 + 「重试」，已经加载出来的条目一律保留**——对方的列目录限流是 10 秒 5 次，连滚几页撞上 `busy` 是常态，清空列表会让人误以为文件没了。翻页失败后暂停自动续拉，直到用户点「重试」，避免继续滚动反复撞限流。**切换会话时面板一并收起**：留着会直接挂到新对端身上，若新对端离线或不支持，会出现「头部按钮已灰、面板却开着报错」的矛盾状态。

**兼容会话头部（决议 #194/#195）**：内网通兼容联系人打开后仍复用聊天内容区，但标题旁显示「内网通」徽标，头部副标题固定为「内网通兼容 · IP:端口 · 功能受限」，右侧菜单只保留置顶、免打扰、清空本会话记录和查看兼容资料。资料框展示昵称、登录用户、主机名、IP、端口、分组、编码来源、最近发现时间和兼容能力，不出现组织资料、版本升级、窗口震动、PK、群聊、自更新或文件直发入口。

**消息区**：

- 从会话列表、联系人、搜索联系人、通知 / 托盘 / 震动等入口打开会话时，消息区默认加载最新 50 条并滚到最新消息；重新打开软件或从托盘恢复后也按最新入口处理。历史搜索结果跳转仍居中定位目标消息，并显示“回到最新”。当前会话新增消息时，仅当用户本来贴近底部或消息是自己发送时自动贴底，避免阅读历史时被打断（决议 #111/#133/#192）。
- 气泡：对方靠左使用轻浮起表面，自己靠右使用主题色浅底；文字气泡与文件卡统一为四角等弧的 14px 圆角。文字气泡不绘制顶部高光或内高光，对方气泡只保留极轻外阴影，自己气泡保持纯色平面（决议 #228）。连续消息间距保持紧凑。群聊中对方消息在左侧显示发送者圆形头像，私聊消息不在气泡旁显示头像。
- 时间分隔：间隔 >5 分钟插一条灰色小字时间；跨天插日期。
- 文本消息：链接自动识别可点；仅气泡本身可右键=复制 / **转发**（选目标会话，P1）/ 撤回（自己的、5 分钟内；撤回项与复制 / 转发等高并保持单行，左侧固定「撤回」，右侧以小号等宽数字显示 `mm:ss`，超时或不可用原因也放右侧并整体灰显；最后 10 秒才增强倒计时危险色，决议 #63/#251），消息行空白区域不触发菜单；右键菜单靠近窗口边缘时自动避让，不被裁切。转发目标弹窗经 Teleport 挂到 renderer 全局顶层，静态暗色遮罩覆盖完整主窗口，弹窗始终高于消息列表、聊天头部、输入区和局部浮层；打开后焦点进入弹窗，遮罩 / 取消 / `Esc` 均可退出（决议 #230）。
- PK 消息：气泡本身右键仅保留「撤回」（自己的、5 分钟内，倒计时规则同文本）；不提供复制、转发或添加到表情。撤回后只隐藏这一条 PK 气泡，不影响别人之后另发的 PK 消息。
- **选区对齐与手势光标（决议 #305）**：原生高亮盒按检测后恢复完整文字范围的行框校正尺寸和位置，避免仅覆盖文字中央的收缩区域，随原图缩放与旋转。非Shift起拖先清除本层旧选区，支持连续重新选字；保留Shift扩选与双击选词。从文字按下开始拖选后，经过行间和图片空白仍显示文本光标；手势结束、取消、窗口失焦或文字层隐藏/卸载后恢复正常光标，空白处开始拖动继续平移图片。
- **图片内文字选择（决议 #304，覆盖下文旧 OCR 浮窗说明）**：首次点击底部“识别文字”后才加载模型并计算；识别中显示进度，再次点击可取消。完成后在原图对应行覆盖透明原生文本，平时不重复绘制文字或边框，选中时显示茶青底色；文字区用文本光标并允许部分/跨行拖选，空白区继续平移图片。缩放/旋转仅变换文字层父级，保持图文对齐；选区可 Ctrl/Cmd+C 或点击底部“复制所选文字”，复制全部入口继续保留。按钮的 pointerdown 不清空原生选区；Esc 先清除选区，再关闭图片窗口。缓存命中直接恢复文字层且不运行 OCR；空结果/失败留底部状态及重试入口。移除 OCR 独立结果框及对应样式；识别完成按钮可切换文字选择层的显隐，以便拖动文字密集的图片。
- **图片连续浏览（决议 #303）**：打开聊天图片后，暗色画布左右居中各显示一个 44px 原生箭头按钮，使用现有 chevron 图标、视觉 token 与 focus-visible 焦点轮廓；分别标注“上一张 / 下一张”。按同一会话完整本地消息顺序浏览，边界按钮置灰且不循环；查询期间按钮禁用，失败显示行内提示并保留当前图。新图适应现有窗口，重置旋转、平移与 OCR 文字选区，窗口保持原位置和尺寸；方向键仍用于平移。箭头独立于拖拽画布和文字选择层，不触发拖拽、双击缩放或底部工具。
- 图片消息：等比缩略图（最大 280×280）内嵌显示，点击打开**独立图片窗口**（决议 #94/#95/#96/#97/#98/#99/#100/#101/#102/#103/#105/#158/#191），不在主聊天窗内强制覆盖，用户可继续聊天、搜索或拖动主窗口。图片窗口为普通可移动/可缩放桌面窗口，系统标题栏默认显示图片文件名并承担关闭能力；内容区为暗色画布，图片按初始规则居中展示：若原图宽高均小于当前屏幕工作区 70%，以 100% 原始尺寸打开；若宽或高超过 70% 阈值，则按先到阈值的边等比缩放到 70% 内并显示缩放比例。小图仍不强制放大，但窗口外框与内容区设最小尺寸，内容区至少保留 560×360 的暗色画布空间，避免底部工具条因图片过小被压缩到拥挤重叠。渲染时 `<img>` 的显示宽高必须显式等于 `naturalSize * zoom`，图片平面用窗口中心 `50% + offset` 定位，`transform` 只负责自身居中与旋转，避免大图/极端比例图片出现布局盒子与视觉尺寸不一致或溢出后贴左上。操作菜单为**底部半透明浮层**，覆盖在图片窗口下方，包含缩放比例、缩小 / 放大 / 适应窗口 / 原始大小 / 左旋 / 右旋 / OCR 识别或查看结果 / 复制全部 / 另存为，不再放内部关闭按钮；按钮 32px 热区、8px 圆角、茶青 active 态，OCR 识别中以同一菜单内的短状态文案展示进度。支持鼠标滚轮缩放、拖拽平移、双击在适应窗口与原始大小间切换（从适应放大到原始大小时以鼠标所在图片坐标为锚点），`Esc` 关闭，`+/-` 缩放，`0` 原始大小，`F` 适应窗口，`R` 旋转；加载失败时在中央显示轻量错误态。OCR 按决议 #304 手动触发，结果直接叠加到原图供选择与复制；同一图片优先复用缓存。图片窗口使用 `pantry-img://transferId` 显示图片，另存为复用主进程保存对话框并挂在图片窗口上，不暴露本地路径；OCR 字节通过受限 `transferId` 读取，交给内置 PaddleOCR PP-OCRv6 tiny + onnxruntime-web 本地 wasm 识别，不展示本地路径。右键菜单含**「复制」/「转发」/「添加到表情」/「撤回」**；「撤回」仅自己的图片、发送后 5 分钟内、对端支持媒体撤回时可用，右侧状态按 #251 显示剩余时间或「不可用」；撤回后图片气泡隐藏，并禁止继续通过该消息打开图片。复制经主进程 Electron 原生剪贴板写入系统图片剪贴板，粘贴到输入框后按图片发送（决议 #137）。添加成功/失败以紧贴消息侧边、垂直居中的小胶囊反馈：对方消息显示在气泡右侧，自己消息镜像到气泡左侧。群聊图片显示在群会话内，发送端一条气泡汇总给在线成员的多条点对点传输；群聊图片超过 10MB 时退化为普通文件卡片，接收者手动接收。
- **OCR 性能边界（决议 #304）**：所有平台手动启动识别，单个本地 Worker 内单线程执行，切图/关闭终止未完成任务；软件渲染画像继续减少磨砂与阴影，文字层用原生选区，不进行每帧或逐字命中扫描。
- **滚动媒体加载（决议 #232/#234）**：聊天图片 / 表情消息、记录搜索缩略图和表情包网格只在接近滚动视口时加载，并使用异步图片解码。静态聊天图片与记录搜索图片优先读取 320px WebP 派生缩略图；首次接近视口且缓存未命中时显示原有加载占位，由 renderer 按原图宽高直接请求缩小解码、写入缓存后展示，避免 `<img>` 持有全尺寸解码面。GIF、APNG、动画 WebP 和最长边 ≤320px 的小图直接读取原图，保留动画与清晰度。进入视口后的点击、右键菜单、加载失败占位与打开原图行为保持；独立看图窗口、截图画面和首屏品牌图保持立即加载。
- **图片像素防护（决议 #234）**：内联图片单边上限 8192px、总像素上限 3200 万。发送时发现真实格式不支持、扩展名伪装、文件头损坏或像素超限，自动以普通文件卡发送；原图查看、OCR、复制、收藏和缩略图生成前均复核同一条件。用户点击图片仍打开通过校验的原图；派生缩略图只服务聊天流与搜索结果，不替换原始文件。
- **发送图片选择器（决议 #235）**：输入区“发送图片”按钮打开专用多选对话框，只展示 PNG/JPG/JPEG/GIF/WebP/BMP；普通文件与文件夹分别由相邻的“发送文件”“发送文件夹”按钮选择。图片选择器返回后仍由主进程复核扩展名并执行决议 #234 的真实格式 / 像素门禁，异常或超限内容按既定规则显示为文件卡。
- **文件主动取消状态（决议 #236）**：发送方点击文件卡“取消”后，单聊与群聊文件卡使用灰色状态显示“发送取消”；接收方自行取消下载，以及发送方收到对端取消时继续显示“已取消”。主动取消进入终态后不再变成红色“传输失败”，真实传输错误仍使用红色失败状态。消息行的发送状态图标同步使用灰色取消标记，避免与错误状态混淆。
- **表格粘贴提示条（决议 #270）**：输入框识别出表格后**不再自动发送**，原文照常插入草稿，输入区在 @ 提及面板与输入框之间插入一条 32px 高的轻提示条：左侧 16px `table` 图标 + `--text-2` 文案「检测到表格」，右侧「发送为图片」文字按钮（茶青 `--primary`）与 14px `x` 忽略按钮。提示条底色 `--bg-list`、圆角 8px、`--line` 描边，不遮挡输入框也不改变输入框高度。默认 5 秒后自动消失；草稿超过单条文本上限时文案改为「表格过大，只能按图片发送」并**不自动消失**，避免草稿既发不出去、图片入口又已消失。用户改动草稿、切换会话、点忽略或点「发送为图片」后立即收起；点「发送为图片」会把刚插入的那段文本从草稿里摘掉再发图片，草稿中原有的其它内容保留。对方离线时「发送为图片」置灰（与工具条图片按钮同一门禁）。淡入使用 120ms opacity，`prefers-reduced-motion` 下直接显示。
- **表格图片消息（决议 #190）**：从 Excel / WPS / LibreOffice / 网页表格粘贴出的表格经上述提示条确认后作为图片消息发送；若该图片带 `tableText`，气泡内容上沿显示一个小型分段滑块「图片 / 文字」。滑块不覆盖图片，位于内容上方 4px，自己的消息右对齐、对方消息左对齐；整体高度约 24px，底色使用 `--bg-list` / 深色对应 token，选中滑块为茶青 `--primary`，选中文字白色，未选中文字用 `--text-2`。默认选中「图片」并展示原缩略图；切到「文字」后，同一气泡下方改为只读等宽文本视图，保留制表符和换行，支持鼠标选择与复制，内容高度设上限并内部滚动，避免撑高聊天流。若 `tableTextTruncated` 为 true，文字块上方以 `--text-3` 小字显示「文字视图已截断，图片完整」，提示不进入可复制 TSV 区域。图片查看器点击只在图片视图生效；文字视图不打开看图器。滑块切换只影响本机显示，不发网络报文、不改变消息状态。滑块位移与内容切换使用 150ms transform / opacity，`prefers-reduced-motion` 下直接切换。
- 表情包消息：固定 120px 高度渲染（不随原图大小变化），右键可「复制」/「转发」或「添加到表情」收藏对方发的表情；复制经主进程 Electron 原生剪贴板写入系统图片剪贴板，粘贴到输入框后按图片发送（GIF 表情复制为当前帧 PNG，完整动图复用转发或表情面板发送），添加反馈沿用图片消息的侧边胶囊。
- **文件消息卡片**（微信式）：

```
┌─────────────────────────┐      状态流转：
│ [彩色类型图标] 设计稿.zip│      等待对方接收 → 传输中(进度条+速率+取消)
│          48.2 MB         │      → 已完成(打开 / 打开所在文件夹)
│          6.2 MB/s        │      → 已取消 / 失败(重试)
└─────────────────────────┘
```

- **文件图标按扩展名彩色区分类型**（决议 #75/#228）：Word / Excel / PPT / PDF / 压缩包 / 图片 / 音频 / 视频 / 文本 / 代码 / 安装包、文件夹分别使用同一张本地透明 PNG atlas 的对应单元格，其余回退通用文件图标。图标为 ImageGen 生成后归一化的原创简洁图形：两级主色、粗轮廓、少细节、无投影 / 发光，36px 下仍能辨识；彩色徽标属“内容标识”，不受单色系统控件图标基线（§10）约束。
- **群聊文件接收进度**（决议 #75）：不再用整体进度条，改为「已接收 / 总数」x/x 计数（如 `10/20`）；鼠标移到 x/x 上浮出已接收成员名单，移出即收起。单聊文件仍用进度条 + 速率。
- **普通接收态文件卡（决议 #178）**：对方发来的普通文件在 `offering` 状态下，右侧不再纵向堆叠「接收 / 另存为 / 拒绝」。操作改为一行动作组：茶青主按钮「接收」，旁边两个 26px 图标按钮分别为文件夹图标「另存为」和 `x` 图标「拒绝接收」，两者必须有中文 tooltip / aria-label。卡片高度应接近普通完成态文件卡，不因接收动作变成高卡片。
- **私聊直接发送文件卡片（决议 #174/#176/#177）**：仅私聊支持，且必须先走普通文件发送，让发送方与接收方都看到普通文件卡。发送方自己的私聊普通文件卡在 `offering` 状态、文件信息已送达、对方在线且支持 `fd1` 时，在右侧操作区显示「直接发送」按钮；按钮使用茶青主按钮样式，离线 / 旧版本 / 尚未送达时置灰并给短提示。卡片保持紧凑三栏：等待状态不放进 meta 行，改为文件名同行固定状态片（「等待接收」/「发送中」），大小行只显示大小 / 文件数 / 速率，避免被右侧按钮挤成省略号；「直接发送」与取消动作同一行，取消在双动作场景下用小图标按钮。发送方完成态统一显示「发送成功」。接收方收到直接发送请求时，卡片不显示「接收 / 另存为 / 拒绝」，直接进入「接收中」状态；完成后只显示「已保存本地」，右侧动作保留「打开 / 打开所在文件夹」，不展示完整本地路径或发送人目录名。失败时发送方显示「直接发送失败」；接收方失败仍显示「继续」用于断点续传。群聊文件卡永远不展示「直接发送」按钮，也永远不进入直接发送状态。
- **文件撤回（决议 #188/#251）**：自己的文件卡右键菜单新增「撤回」，右侧单独显示 `mm:ss` 或不可用原因；仅发送后 5 分钟内、对端支持媒体撤回、且接收未完成时可用。等待接收、已接受但传输中、直接发送传输中、失败/取消但未完成落盘的文件可撤回；点击后卡片隐藏并插入撤回系统提示，接收方若正在拉取则取消传输、清理未完成 `.part`，不再展示「继续」。接收方已完成保存后，发送方菜单不再提供撤回或置灰显示右侧「已接收」；迟到撤回不删除本地已保存文件。群聊文件卡只有所有在线收件人的接收状态均未完成时才显示可用撤回；任一成员已完成接收后，整条群文件卡撤回入口置灰并在右侧显示「部分已接收」。

- **PK 消息气泡（决议 #139/#140/#141/#142/#145）**：用于“猜拳 / 骰子”分歧解决，单聊与群聊一致。沿用既有气泡底色、8px 圆角、茶青主色与明暗主题 token，不做卡片嵌套；气泡为**横向卡**（决议 #145/#147，从原垂直三层 ~170px 瘦身到约 60px 高，避免顶掉过多聊天消息；卡片**固定宽度**——他人含参与按钮 200px、自己 132px，信息列 `flex:1` 撑满、结果文字左对齐、短文字右侧留白——避免不同结果「出了布 / 出了石头」或动画态「出拳中…」与定格态之间宽度忽长忽短）：**左侧**是固定 42px 的「统一开奖窗」——一个柔和下沉底色 + 内描边的圆角展示窗，骰子与猜拳的揭晓都在这一个等尺寸 / 圆角 / 底色的窗内进行，用统一画框消解差异：骰子是一枚 **CSS 3D 立方体**（28px），六面各画真实 1–6 点数，投掷时立方体翻滚、定格时转到结果点数那一面朝前；猜拳是一张 **3D 翻牌**（28px），正反双面同显本地 Twemoji 原色手势（石头 `👊`、剪刀 `✌️`、布 `✋`；石头用 `👊`(1f44a) 而非 `✊`(270a)——本地缺 `270a.svg` 资源，决议 #148），翻转轮播后停在结果手势正面。**右侧**为信息列：上行茶青小标题（同套线性图标 + 「猜拳」或「骰子」），下行是口语化结果文字——猜拳「出了石头 / 剪刀 / 布」，骰子「掷出 4 点」，不显示谁赢谁输。新发 / 新收消息播放一次约 1.5s 揭晓动画——立方体翻滚 / 卡片翻转快速进行后弹性减速落定到载荷结果，定格瞬间开奖窗闪一下茶青高亮；揭晓过程中结果文字固定为「投掷中…」（骰子）/「出拳中…」（猜拳），不随翻牌轮播跳动，定格后才显示「掷出 N 点」/「出了石头 / 剪刀 / 布」（决议 #146）。动画只用 transform / opacity，历史加载、翻页、重开会话与 `prefers-reduced-motion` 下直接显示最终结果（立方体 / 卡片直接定位到结果面，不转动）。收到他人的 PK 时，信息列右侧紧跟一颗参与按钮小胶囊（猜拳「我也来」、骰子「掷一下」，hover 实底茶青）；自己的 PK 不显示参与按钮。按钮有 hover / active / focus-visible / disabled 状态，文字不换行；点击发送一条新的独立 PK 消息，原按钮保留，允许反复点击。若当前没有可参与的在线对象（单聊对方离线，或群里只有自己在线），按钮保留但灰显，hover 提示「PK 只能和在线的人玩」。群聊不做排行榜或大结算面板。

- 发送状态：发送中=自绘小转圈 → 已送达=自绘对勾；失败=红色 ! 点击重发；**对方离线=自绘时钟图标**，气泡下灰字"对方上线后自动送达"。
- 兼容会话发送状态：发送中 → 已送达（收到内网通 `RECVMSG` ACK）→ 失败；对方离线或超时直接显示失败并可重发，不显示「上线后自动送达」，因为兼容模式首版不做离线队列。若消息带 IPMSG 附件 offer，首版只在调试 / 实验文件视图中呈现，不改变普通文本气泡状态。
- 系统提示行（居中灰小字）：撤回提示、窗口震动提示、群成员变更、群名变更等，尽量少，不刷屏。消息撤回后原气泡从本地消息流中隐藏，只保留"你撤回了一条消息 / 对方撤回了一条消息"提示；图片和未完成文件撤回沿用同一提示，不额外解释文件保存规则。窗口震动提示为"你发送了一次窗口震动 / 对方发来一次窗口震动"（决议 #110）。群名变更提示格式为「你把群名「旧名」改成了「新名」」或「备注/昵称把群名「旧名」改成了「新名」」（决议 #87）。

**输入区**：工具条（表情 / PK / 窗口震动 / 截图 / 图片 / 文件 / 文件夹 / 历史搜索，全部用自绘线性 SVG 或明确文字符号）+ 多行输入框 + 发送。`PK` 入口放在表情之后、窗口震动之前，按钮热区与其他工具一致，由斜体 `PK` 字标组成（`skewX(-10)` 右倾 + 加粗描边、竞技动感，不带额外文字），热区与其他工具一致，tooltip 为「PK」。点击后在工具条上方弹出小浮层（左对齐按钮向右展开，避免浮层左侧被聊天面板 `overflow:hidden` 裁切），包含两个并排大按钮：与气泡开奖窗视觉呼应，同套线性图标在上、文字在下，文字分别为「猜拳」「骰子」。单聊对方离线、或群聊没有其他在线成员时，玩法按钮灰显，tooltip / 短反馈为「PK 只能和在线的人玩」；选择成功后立即关闭浮层并发送结构化 PK 消息，发送前不预览结果。私聊窗口震动入口仅在单聊且对方在线时可用，点击后发送一次即时震动；限流失败以工具条旁的轻量短文案反馈，发送成功则同时在消息区插入系统提示行。收端收到震动后唤起/震动主窗口，自动切到发起人的单聊，并在消息区插入系统提示行；若该单聊已设免打扰，只保留系统提示行，不唤起、不置前、不震动主窗口；不额外弹阻塞式对话框。历史搜索入口固定在工具条最右侧；工具条图标悬停约半秒后以淡入浮层显示中文名称，禁用态也保留名称提示。输入框占位 hint 使用比时间戳更浅一阶的 `--text-placeholder`，只起辅助提醒作用，不与已输入正文竞争。输入框编辑态与消息展示态使用同一套 Twemoji 本地 SVG 兼容渲染：当草稿包含内置 emoji 子集时，textarea 上方显示镜像渲染层，底层仍保留原生输入、选区、粘贴和发送行为。`0.44.9-beta.2` 的 Win7 输入法诊断画像暂时绕开自定义空白字体与 emoji 镜像，使用微软雅黑、实体背景和普通内边距的基础 textarea；输入、选区、粘贴、发送和可调高度保持，用于判断搜狗候选窗失位是否由聊天自定义编辑样式链触发（决议 #259）。群聊中图片/文件/文件夹按钮在至少一位其他群成员在线时可用，发送后只投递给在线成员；离线成员不显示为待补发。剪贴板里的截图位图、以及从文件管理器复制的文件，均可直接 Ctrl+V / Command+V 粘贴到输入框发送（决议 #76）：单张图片文件按图片发送，其余按文件发送，与拖拽一致；若剪贴板同时包含文本和图片副本（如网页 / 富文本 emoji），文本优先走原生粘贴；无文本时先读浏览器 paste 图片项，仍为空则从主进程读取 Electron 原生图片剪贴板发送（决议 #135/#138）。表格粘贴是文本优先规则的受控例外：若剪贴板 HTML 含 `<table>`，或纯文本至少两行且含制表符，则识别为表格，默认生成表格图片并发送；同时提取原始 `text/plain` 作为文字视图元数据。文件路径仍优先于表格识别，普通富文本 emoji 不满足表格条件时仍按原规则粘贴文本。主进程原生图片兜底必须晚于浏览器 paste 分流，并在同次 paste 已处理文件 / 文本 / 图片时取消，避免第三方截图工具同时暴露两路图片导致重复发送（决议 #180）。**输入框高度可手动拖拽调节（决议 #127）**：输入区顶部（消息/输入分隔线）有一条细灰手柄，hover 变深、`ns-resize` 光标，按住上下拖动改变输入框高度（约束 48–320px），向上拖更高、向下拖更矮；高度用本地存储记忆，纯渲染层、不影响工具条与发送行为。

**Win7 输入框补充（决议 #260）**：`0.44.9-beta.2` 已经 Win7 搜狗真机确认候选窗定位正常。继续保留静态正常流、微软雅黑和实体材质背景的真实 textarea，不使用 `PantryEmojiBlank`、透明文字或 composition 自定义处理；诊断版内边框、独立底色和内层圆角全部移除，视觉与外层输入卡片融为一体。Win7 彩色 emoji 由不参与布局和命中的展示覆盖层绘制，普通文字透明透出真实 textarea，仅 emoji 槽以同色背景遮住黑色系统字形并显示本地 Twemoji；覆盖层字号、行高、内边距和滚动与真实输入层严格一致。

**Win7 表情尺寸与光标补充（决议 #261）**：安全 textarea 和展示覆盖层恢复相同的 `PantryEmojiBlank` 字体首位，仅让内置 emoji 使用与消息正文一致的 `1.3em` 固定槽宽，普通文字继续回落微软雅黑。Twemoji 图形恰好填满槽位；连续输入多个 emoji 时，图形、字符槽和原生 caret 每次都按相同宽度前进。真实 textarea 继续保持静态正常流、实体背景和不透明文字，不恢复绝对输入覆盖、透明文字或 composition 自定义处理；空白字形已经隐藏系统黑色 emoji，展示层不再绘制遮字底片。

**Win7 原子表情编辑器补充（决议 #262）**：`beta.4` 真机确认真实 textarea 只要重新使用 `PantryEmojiBlank`，搜狗候选窗就会再次退到窗口角落，因此 #261 的 Win7 字体路径撤销。Win7 输入区改用微软雅黑系统字体的原生 contenteditable，普通文字、组合文字和候选窗 caret 都走 Chromium 默认编辑链；内置 emoji 直接显示为不可拆分的本地 Twemoji 节点，宽高固定 `1.3em`，与发送后的表情一致，连续节点之间的原生 caret 按节点实际宽度移动。输入卡留白、材质、字号和行高保持；composition 过程中不替换 DOM，避免中断搜狗组词。其他平台的 textarea 镜像编辑器保持。

**聊天候选确认保护（决议 #268）**：所有平台的聊天编辑器都上报 composition 开始 / 结束。composition 活动期间，以及键盘事件仍带 `isComposing` 或 `keyCode 229` 时，键盘分流不打开 @ 提及、不发送消息、不插入换行，也不阻止原生输入法处理按键。第一次 Enter 只确认当前候选；候选文字完成同步后，下一次 Enter 才按“Enter 发送”或“Ctrl/Cmd+Enter 发送”设置执行。该规则同时适用于普通 textarea 与 Win7 系统字体 contenteditable。

- **截图标注工具条（决议 #208）**：框选完成后优先贴在选区下方 8px，空间不足时翻到选区上方 8px；工具条宽于选区时尽量与选区右缘对齐，其余情况贴选区左缘，并在视口四周保留 8px 安全边距。定位使用工具条实际测量宽高，窗口缩放时即时重算；视口小于工具条时仍从安全边距起放置，避免产生负坐标或完全不可操作。
- **截图文字标注（决议 #265）**：选中“文字”后点击选区，在点击位置显示茶青描边的单行输入框并立即聚焦；输入框靠近屏幕边缘时整体夹在 8px 安全边距内，文字锚点仍保留原点击位置。Enter 提交并显示绿色粗体预览，Esc 只取消本次文字，发送 / 复制会先提交当前非空文字。中文输入法选词期间的 Enter 留给 composition，不触发提交或发送截图；输入框与截图窗口使用系统字体，不叠加自定义空白字体。
- **截图工具条图标化（决议 #266）**：尺寸信息保留为紧凑数字；重新框选、矩形、箭头、文字、马赛克、发送、复制、取消八个功能按钮只显示 17px 线性图标，按钮命中区统一为 30×30px。工具组与操作组之间放一条 18px 半透明竖线；当前工具使用茶青底，发送使用实色茶青，取消仅在 hover 时呈克制红色。所有按钮保留中文悬停提示、键盘焦点环和无障碍名称，工具按钮暴露选中状态；图标全部来自本地 `PantryIcon`，禁止 emoji、字符拼图或外部资源。
- **截图按钮提示（决议 #267）**：鼠标停在任一功能按钮约 250ms 后显示对应中文气泡，键盘焦点进入按钮时立即显示；气泡使用 12px 系统字体、近黑背景、白字和小方向箭头，不遮挡图标。默认显示在按钮上方，工具条顶部坐标小于 48px 时整组提示翻到按钮下方；移出或失焦立即淡出。每个按钮使用独立 `data-tooltip`，可见提示与 `aria-label` 文案一致；不叠加浏览器原生 `title`，避免出现双重提示。

- **截图初始化可见性（决议 #218）**：截图窗出现时必须已能接收并展示桌面截图，动态入口晚于 `did-finish-load` 挂载时由 preload 回放最近一次初始化数据；加载间隙使用纯黑底色，不得透出茶青页面背景或形成整屏灰绿色遮罩。验收需实际完成「打开截图 → 看见桌面 → 拖拽框选 → 工具条出现 → Esc 退出」。

- **截图工作区与流畅度（决议 #221）**：Windows 框选蒙层只覆盖系统工作区，任务栏保留为真实系统界面且不进入可选截图范围；桌面画面与蒙层客户区必须等比例、同边界，禁止把含任务栏的整屏图片纵向压缩。蒙层显示前先完成桌面位图解码与首帧布局，用户看到的第一帧就是可框选桌面。框选时页面只保留一份桌面图，选区外由上 / 右 / 下 / 左四块半透明纯色遮罩变暗；鼠标移动按屏幕刷新帧合并，Win7 软渲染下不得随高频鼠标事件重复绘制第二份全屏背景。马赛克编辑态使用静态网格预览，确认时再生成真实像素化结果，不使用实时模糊。
- **截图失败反馈（决议 #286）**：点击截图后若屏幕源不可用、返回空图、主窗口无法完全隐藏或主进程抛错，主窗口恢复并在顶部居中显示 7 秒可关闭提示条（警示图标 + 明确原因/退路，`role=status`、`aria-live=assertive`）；提示不依赖系统通知，Chrome 108 下只使用既有主题变量。原本隐藏主窗时用全局快捷键触发失败，则优先以系统通知提示而不擅自打断；通知不可用时才显示主窗。所有退路统一说明可用系统截图后在聊天框 `Ctrl+V`。
- **ARM64 Wayland 截图退路（决议 #289）**：该平台组合在 Electron 22 枚举屏幕源前直接显示上述失败提示，不隐藏主窗、不打开框选窗；用户使用系统截图后仍可在聊天框 `Ctrl+V` 发送。x64 Wayland、ARM64 X11 与其他平台入口不变。

- **输入区材质升级（决议 #216）**：输入区从贴底平面改为离窗口边缘 14px 的浮起材质容器，使用 16px 圆角、内描边和轻阴影；工具按钮按下立即缩放，发送按钮使用主色实底突出。减弱透明度偏好下使用不透明表面，高对比偏好下增加清晰边框。

- **文件发送入口（决议 #174）**：聊天工具条的「文件」和「文件夹」按钮保持现有普通发送路径，点击后直接打开系统选择器，不在发送前弹「直接发送」菜单。私聊发送成功后，若符合条件，再在发送方文件卡片上出现「直接发送」按钮；群聊文件 / 文件夹入口始终只走普通发送。

- **兼容会话输入区（决议 #194/#195）**：由 `ConversationCapabilities` 统一控制，只显示文本输入框、历史搜索和发送按钮；表情、表情包、PK、窗口震动、截图、图片、文件、文件夹、直接发送、媒体撤回入口全部隐藏。空输入不可发送；粘贴图片 / 文件时只保留文本内容，若剪贴板没有文本则给轻量提示「内网通兼容会话暂不支持发送图片或文件」。发送失败时气泡保留失败态，用户可修改后重发。

- 表情面板双页签：**emoji** / **我的表情包**。emoji 页显示 Twemoji 本地 SVG 图标，点击仍向输入框插入对应 UTF-8 emoji 字符；输入框编辑态与消息气泡正文展示时对内置 emoji 子集替换为同套 SVG，避免 Win7 / UOS 显示方框。表情包来源：聊天里任意图片右键「添加到表情」，或点面板头部「导入」通过系统图片多选框添加；**保存时自动压缩**（静图最长边 512px 转 WebP，GIF 动图 ≤2MB 原样保留）。面板内右键删除、按钮排序；点击即发送，群聊仅在至少一位其他成员在线时可用。面板不会因鼠标移出自动关闭，仅在点击面板与表情按钮以外区域时关闭；固定高度内容区的每一行按表情实际宽度保留正方形高度，超出后纵向滚动，切换页签或表情包为空时弹出框尺寸保持稳定。
- Enter 发送、Ctrl/Cmd+Enter 换行（设置可互换）。
- 群聊中输入 `@` 弹出成员选择浮层，被 @ 者收到加强通知（会话列表标"[有人@我]"红字，P1，决议 #22）。
- 粘贴图片 → 输入框出现预览块再发送；**拖文件/文件夹到聊天区任意位置即普通发送**（拖入时全区高亮蒙层提示）。私聊拖入文件不提供直接发送预选；发送成功后若符合条件，由发送方文件卡片显示「直接发送」按钮。群聊拖入文件只走普通发送，不出现直接发送入口。

## 6. 全局搜索（微信式）

两个列表页顶部的搜索框是同一套组件，聚焦后弹出结果浮层，分类展示：

| 分类 | 范围 | 说明 |
|---|---|---|
| 联系人 | **全量**（含离线、含未分组） | 按备注、昵称、公司、部门、团队、IP 匹配；隐藏主机名不参与全局搜索。结果行显示圆形头像、备注优先名称、离线标记，以及组织路径或 IP（决议 #250） |
| 讨论组 | 组名、成员名 | |
| 聊天记录 | 消息正文关键词 | 按会话聚合："与张三的聊天 · 3 条相关"，点击 → 进入会话并定位高亮该条 |
| 文件 | 收/发过的文件名 | 结果项可「打开」或「定位到聊天」 |

中文按字匹配、连续词组优先（基于本地索引，秒出结果；实现细节归技术设计）。

全局搜索保留 200ms 防抖；等待期间显示「正在搜索…」，失败显示「搜索失败，请重新输入关键词」，使用现有占位样式。关键词变化及离开面板时清理旧任务，只显示当前关键词的结果，重新输入可重试（决议 #295）。

三类结果行均使用原生按钮（决议 #302），Tab 聚焦、Enter/Space 复用原有点击跳转；焦点只在键盘操作时显示茶青内描边，鼠标悬停及结果省略样式保持。显式覆盖原生按钮的默认子项对齐，文件名使用独立文字容器，长名称/摘要显示省略号，避免横向溢出。

### 6.1 会话内历史搜索

群聊与私聊的输入区工具条最右侧显示“历史搜索”图标按钮。点击后弹出设置页尺度的居中面板，范围固定为当前会话；再次点击、切换会话、点击面板外遮罩或右上角关闭按钮均可关闭。面板宽高参考设置页的桌面软件感，优先保证筛选区、日期区、结果区稳定可扫描，不遮挡输入框内部控件。

面板内容：左侧为关键词输入框、“全部 / 图片 / 文件”分段筛选与连续日期范围选择，右侧为结果列表。打开面板时即显示当前会话最近记录；未输入关键词时按当前类型与日期筛选直接列出匹配记录。日期筛选使用单月日历组件：第一次点击选起始日，第二次点击结束日并高亮连续区间；再次点击可重新选择范围，清空筛选会一并清除日期。

**结果项信息密度（决议 #74）**：以快速扫读为先。文本命中**直接以摘要为主体**（可换行至两行），不再显示“文本消息”小标题与前置类型图标；文件命中显示类型图标 + 文件名；图片命中必须展示图片缩略图而不是只显示图片名，缩略图不可用时才回退为图片占位。所有结果项底部显示发送者与时间。摘要中的内置 emoji 与消息展示一致，走 Twemoji 本地 SVG 彩色渲染，Win7 / UOS 不再出现方框或黑白线描。**命中交互**：文本与文件命中单击即跳转到对应历史位置并短暂高亮；**图片命中单击放大查看（复用大图查看器、不离开搜索面板），双击才跳转定位**，便于快速浏览图片而不丢失搜索上下文。

## 7. 关键流程

### 7.1 发起讨论组（参考微信：从单聊加人）

单聊头部 `⋯` →「添加成员」，或会话列表 `[+]` → 弹出**两步建群弹窗**。

第 1 步是联系人快速搜索与多选：顶部输入框用于按备注/昵称/公司/部门/团队/IP 即时过滤联系人，下面是紧凑单行结果列表（复选框 + 在线点 + 姓名 + 右侧组织路径 + 离线标记）。公司 / 部门 / 团队以中点连接，始终与姓名同一行，空间不足时只省略组织路径并通过悬停标题查看完整内容；成员项最小高度 34px。底部显示已选人数与已选成员条。几百人规模下不要求用户靠滚动定位联系人（决议 #237）。

第 2 步是组设置：组名可留空，默认使用"张三、李四、王五的讨论组"；管理密码可留空，若填写则必须再输入一次确认一致，并可填写密码提示。建群者成为群主；群主和管理员管理时免密码，普通成员可输入管理密码执行改名或移出普通成员。

**建群提交与关闭手势（决议 #269）**：点击“创建”后保留当前组名、密码提示和成员选择，按钮显示“创建中”，返回、取消和遮罩关闭暂时锁定。成功后进入新群会话；参数校验或 IPC 异常时恢复按钮并在设置页显示行内错误，用户可以原地重试。点击遮罩关闭必须从遮罩空白处按下并在遮罩空白处结束；从组名等输入框开始拖选文字，即使指针移到弹窗外再松手，也保持弹窗与已填内容。

群成员面板按成员原顺序展示，并在姓名后显示小型「群主」「管理员」徽标。所有在群成员都能看到「添加成员」；点击后打开全局模态弹窗，沿用建群第一步的紧凑单行联系人、搜索和多选交互。搜索覆盖备注、昵称、公司、部门、团队、IP 与主机名；已有成员不进入候选，在线与离线联系人均可选择，已选项跨搜索结果保留并显示可移除标签。弹窗明确显示已选人数与群剩余名额，一次确认批量邀请；失败时保留选择并就地提示，提交期间禁止重复确认、遮罩关闭与 Esc 关闭。弹窗通过 Teleport 挂到 `body`，打开时聚焦搜索框，关闭后把焦点还给「添加成员」按钮（决议 #242）。

群成员面板顶部显示 64px 圆形群头像、群名和成员数（决议 #243）。群主、管理员及持有正确管理密码的普通成员可点击头像上的编辑按钮，选择本地图片后进入共用头像裁剪弹窗；有管理密码的普通成员沿用面板内密码输入与行内错误。自定义群头像同步显示在会话列表、群聊头部和该面板；恢复默认后立即回到成员群组图标。修改和恢复期间保留裁剪状态，成功后关闭并显示群系统提示。

群简介与群公告显示在群成员面板的群头像下方；空内容不占位置（决议 #290）。群主、管理员及持有正确管理密码的普通成员可通过面板底部入口打开同一套文本编辑弹窗。简介上限 200 字，公告上限 1024 字；已有内容清空后仍可保存，保存空串即恢复未设置状态。密码成员复用面板内管理密码与行内错误，成功后立即刷新本地群视图。弹窗通过 Teleport 挂到 `body`，支持遮罩、取消与 `Esc` 关闭，打开时聚焦文本框，关闭后恢复原按钮焦点；提交期间禁止重复保存和关闭。

群主可用盾牌按钮设置/取消管理员，并可移出管理员或普通成员；管理员只对普通成员显示移出按钮。群内存在未声明 `gr1` 的旧版本成员时，面板显示不阻断操作的升级提示。邀请、踢人、退群、任免管理员、改名和群主自动转让均写入居中的系统提示。

### 7.2 接收文件

对方发文件 → 我方聊天里出现文件卡片（右侧一行动作：「接收」主按钮 + 文件夹图标另存 + `x` 拒绝）→ 点击「接收」时默认保存到 `文件保存位置/联系人名称/`，点击文件夹图标「另存为」时保存到用户选择的目录 → 接收后显示进度 → 完成后卡片变为「打开 / 打开所在文件夹」。接收中若发送方并发预算已满而排队（决议 #211），进度行以「已传 / 总量 · 排队等待发送方」代替速率，不显示假 0 速度。接收方点「取消」后（对端为新版本时）卡片显示「已取消」徽标 + 「重新下载」按钮，点击按已收 `.part` 断点续传重新拉取；对端为旧版本或发送方主动取消 / 撤回时不提供重新下载。传输失败仍显示「继续」断点续传；领取期限内即使应用重启，也会从持久化上下文恢复该入口，缺少有效上下文时不显示不可用按钮。私聊直接发送自动接收也使用同一默认联系人子目录。群聊文件卡片同样出现在群会话里，发送端一条卡片汇总各在线成员的接收进度；接收方手动接收群文件时按发送人显示名进入联系人子目录。**单聊 ≤20MB 图片、群聊 ≤10MB 图片免确认**，直接以图片形式展示；群聊超过 10MB 的图片按普通文件卡片展示并手动接收。会话不在前台时走通知 + 角标。

**文件领取期限（决议 #263）**：普通私聊文件与群聊文件统一只可在发送后 24 小时内首次接收、继续或重新下载。到期后所有接收操作消失，发送方卡片显示灰色「发送已到期」，接收方显示灰色「文件已过期」。截止前已经开始的当前传输保留进度并允许完成，截止后失败或重启中断则转为过期。群发卡片继续展示「已接收 x/y」和成员名单，领取窗口结束且仍有成员未完成时右侧显示「发送已到期」。私聊直接发送也受同一期限约束；图片、表情和隐藏更新包不展示普通文件过期状态。

发送方撤回文件 → 若接收方尚未完成保存，双方隐藏文件卡并显示撤回系统提示；接收方传输中则自动取消并清理未完成临时文件。若接收方已经完成保存，发送方不再能撤回该文件，接收方本地文件保持可用。

### 7.3 托盘 / 菜单栏与通知（点击直达会话）

- 新消息：托盘 / 菜单栏 tooltip 始终显示未读总数；macOS 菜单栏图标旁显示未读数字，并同步 Dock 角标；Windows 使用任务栏 overlay 数字，同时托盘图标在原图标与带数字角标图标之间闪烁；Linux 尝试系统角标（Unity 支持时生效），默认让常规茶青图标与高对比红色注意图标每 800ms 交替，确保 UOS/DDE 的小尺寸系统托盘中也能看清变化（决议 #214）。未读清零后所有数字与闪烁停止并恢复常规图标。**未读判定（决议 #220）**：只有窗口可见且聚焦时，当前打开会话的新消息才即时置读；最小化、关到托盘或被其他窗口压住失焦期间，当前会话的新消息同样计入未读并触发托盘闪烁 / 角标，重新聚焦窗口后当前会话未读自动清零、闪烁停止。
- 桌面通知摘要必须兼容 Win7 / UOS 缺字环境：通知正文不渲染 emoji 图形，文本里的内置 emoji / 常见 emoji 统一显示为 `[表情]`；表情包通知显示 `[表情]`，图片显示 `[图片]`，文件显示 `[文件] 文件名`，PK 显示 `PK · 猜拳` 或 `PK · 骰子` 且不展示结果。Linux / Windows 通知显式带真实应用图标，避免 UOS 通知中心显示系统默认图标（决议 #108/#139）。
- **点击通知或托盘 → 主窗弹出并直接定位到对应会话**（用户原话需求）。
- 托盘右键：打开主窗 / 全局免打扰开关 / 退出。

### 7.4 首次启动向导（决议 #22）

首次打开必经三步，防止全网一片"未命名"：① 昵称（必填，默认取系统用户名）→ ② 公司 / 部门 / 团队（选填，可跳过）→ ③ 文件保存目录确认 → 进主界面。中途退出则下次启动重新进入向导。向导首屏顶部使用真实品牌标识（`PantryBrandLogo` color 变体纯图形 + `茶话间 Teahouse` 中英文字标，与关于页 / 空状态一致，决议 #122/#155），不使用纯文字占位。第三步文案说明该目录用于存放收到的文件，不写"功能即将到来"——文件收发已是核心能力。

### 7.5 给离线的人发消息

正常输入发送 → 气泡时钟态 + 会话内一次性灰字提示"对方当前离线，将在其上线后自动送达"→ 对方上线后自动补发，气泡转 ✓。

### 7.6 局域网自更新（决议 #166）

主界面发现内网有**同平台、版本更高、可作更新源**的在线节点时，给一个**不打扰的提示入口**——主导航栏 / 列表栏顶部一枚淡茶青「内网有新版」徽标（带小红点），不弹窗打断当前操作；无更可用更新源时完全不显示。点开 → **更新弹窗**：显示新版本号、来源同事昵称、当前版本，操作「同步更新」；内网范围、不访问外网、索包不静默安装等机制说明收进圆形 `?` 帮助图标，hover / focus 时显示自绘 tooltip，不作为正文常驻。点「同步更新」→ 向该节点发起内网索包请求；当前实现仅提示请求中 / 已发出 / 暂不可用，不展示普通文件传输卡片。后续进度形态：后台从该节点拉取（复用文件传输的进度展示）→ 拉完校验通过提示「即将重启更新」→ 自动安装重启（Windows 静默完成、Linux 弹一次系统授权框）。失败给明确文案并可重试，不影响继续聊天。整套提示遵循 tokens 茶青单主色 / 8px 圆角 / 明暗主题 / `prefers-reduced-motion`。本期覆盖 Windows 安装版与 Linux deb，mac 暂缓。

### 7.7 内网通兼容模式（决议 #194/#195）

用户在「设置 → 网络」打开内网通兼容开关，填写独立 IP 段或手动节点，点击「扫描兼容节点」。扫描期间只在设置页网络组显示进度，不占用左侧主导航的全局刷新按钮；发现节点后立即进入通讯录「内网通兼容」分组，并在联系人名称和会话头部显示「内网通」徽标。用户双击兼容联系人进入兼容会话后，可发送普通文本；收到兼容节点主动消息时，若联系人尚未在列表中出现，则以临时兼容联系人加入分组并创建会话。兼容模式状态异常时使用明确短文案：`2425/UDP 被占用`、`兼容模式未开启`、`未配置兼容 IP 段`、`未发现兼容节点`。IPMSG 文件 / 剪贴板图片 offer 在首版不开放普通发送入口；若后续实验开关启用，统一渲染为“兼容文件”卡片，操作只保留接收、另存为和拒绝。

### 7.8 共享文件柜（决议 #271–#277、#283）

**开自己的文件柜**：导航栏「文件柜」→ 列表栏「我的文件柜」→ 「选择目录」（拦截主目录根 / 系统盘根 / 应用数据目录并说明原因）→ 选默认权限档（不共享 / 只读 / 可读可传，默认**不共享**）→ 需要时给个别同事加例外。全部即时保存（决议 #150/#151），底部弹「设置已保存」胶囊。（v0.50 前该入口在「设置 → 聊天与文件 → 我的文件柜」，决议 #283 迁出。）

**去别人的文件柜**：两条路——① 导航栏「文件柜」→ 列表栏点同事 → 内容区浏览器；② 私聊 → 头部「文件柜」按钮 → 右侧 320px 面板。然后逐级双击进子目录 → 勾选文件 / 子目录 → 「下载 N 项」→ 进度就地显示，默认落到 `文件保存位置/文件柜-对方名称/`，也可「另存为」。面板头部的「在文件柜里打开」可把当前对端带进文件柜页签。

**往别人的文件柜放东西**：底栏「上传到 TA 的柜子」（仅有写权限时出现），或直接把文件 / 文件夹拖进浏览区 → 文件进入对方 `共享根/我的名字/` → 对方在与我的私聊里看到一条系统提示「我 上传了 N 个文件到你的文件柜」，点击可打开该目录；对方也能在自己的「我的文件柜」页看到「最近有人放进来」里的这一条（决议 #283）。**只有上传会提示；别人浏览、下载我的文件柜全程静默**（决议 #274）。

**拿不到时的表现**：对方离线 / 未开文件柜 / 未对我开放 / 版本过旧，入口按钮灰显并在悬停时说明原因，绝不给出"能点但点了没反应"的入口。

<a id="remote-view"></a>

### 7.9 远程桌面查看（决议 #310，v0.58.0）

功能范围以 requirements §6.11 为准。采用独立、非模态协助窗口，共享方每次同意并选屏，查看方可继续使用主窗聊天。

#### 7.9.1 发起与授权

- 私聊头部工具区增加「查看屏幕」及本地屏幕图标；群聊不展示。旧版本没有 `rv1` 时隐藏；对方仅有接收能力、本机角色不可用、离线或忙碌时禁用并显示具体文字原因。离线保持灰显与「离线」副文字，不能只靠颜色。
- 查看方点击「查看屏幕」先弹出说明和目标姓名/IP，说明对方同意并选屏后开始、仅看画面、双方可随时结束；按钮「取消 / 发送请求」，默认焦点在取消，Tab 限于弹窗，Esc 取消（保留输入法保护）。确认前不调用请求 IPC、不发网络请求、不生成记录；发送期间防重复。已有同一对象的会话直接定位窗口。
- 发送后双方私聊各保留同一会话的一张协助卡片：请求、开始、结束只更新这张卡片，不能追加开始/结束消息。再次发起新的请求才生成下一张。取消、超时、忙碌、断线等均保留；异常退出如实显示中断、未知结束时间和时长。沿用系统记录不增加未读，不提供转发/撤回；切换聊天不影响会话（#314）。
- 卡片按请求发起方排列：自己靠右并沿用自己的茶青气泡底色，对方靠左并沿用对方气泡底色；使用普通消息行间距与最大宽度约束，上限收为 **260px**。常见状态约两行：屏幕图标、名称和简短状态；次行显示请求时间（接通后为开始时间），已接通并结束时显示「时长 MM:SS / H:MM:SS」，其他阶段显示「仅查看」。不另列方向说明或时间表格，默认字号下目标高度约 64px；长日期、翻译或字号放大时自然换行，关键状态不截断。完整请求/起止时间、时长与结束原因通过原生悬停说明和无障碍名称保留。无逐秒计时器和持续动画。状态更新、拒绝、结束和历史重载不改变方向；不显示送达勾选，时间分隔线与其他系统提示仍居中。聊天顶部仅保留请求失败提示，正常会话状态统一在卡片呈现（#313/#314）。
- 共享方收到请求显示「{姓名}（{IP}）想查看你的屏幕」，说明「仅查看本次选择的屏幕，你可以随时停止」，提供「选择屏幕」「拒绝」。未进入选屏时不枚举源、不开始采集。姓名来自本机联系人展示规则，用户名称不翻译。
- 选择屏幕后展示本地缩略图与屏幕名称，用户显式选择，再点「开始共享」；多屏不默认替用户同意主屏。系统授权如被拒绝或取消，保留明确失败原因，双方均能退出流程。进入系统授权窗口不延长协议请求期限。
- 请求直接打开可见且置顶的共享确认窗口，用 `showInactive` 保留当前输入焦点，主窗隐藏也能看到；展示请求不开始采集。重复/限流请求不重复打开窗口。

#### 7.9.2 查看与持续共享提示

| 角色 | 形态 | 必备内容与行为 |
|---|---|---|
| 查看方 | 独立、可调整大小的非模态窗口 | 窗口标题「屏幕协助」，工具栏显示对方姓名及 IP；等比画面、适应窗口、100%、结束；100% 指收到的图像像素。大图可本地平移/滚动，任何鼠标/键盘事件都不转发远端。初始尺寸适配本机工作区，允许最大化；无 OCR、保存或录制入口。 |
| 共享方 | 同一协助入口的紧凑状态窗口，持续置顶且不抢输入焦点 | 320×56 DIP 贴边条显示「正在共享屏幕」、查看者姓名和明显的「停止共享」；完整姓名/IP/已选屏幕可悬停查看。开始前展示等待连接状态；此窗口不允许通过最小化藏掉停止入口，关闭即停止，主聊天窗口继续可用。 |

源屏幕上的共享提示可以出现在传出的画面里；首版不承诺跨平台排除某个窗口。显示共享方鼠标指针的实际效果需在采集验证中检查，禁止用查看方的本地指针冒充远端操作位置。

主窗隐藏/切换聊天不结束独立会话；查看窗最小化维持低帧率接收，恢复时显示最新画面。关闭协助窗、按「结束/停止共享」立即终止；Esc 尊重输入法和选择控件，其他位置结束协助，不能沿用主窗隐藏规则把共享悄悄留在后台。结束无需再次确认。

**流畅度控件**：查看方工具栏提供「自动」「节省资源（3 帧/秒）」「标准（5 帧/秒）」「流畅（10 帧/秒）」，默认自动。旁边显示「目标 N 帧/秒」，该数字不代表实测吞吐。切换影响后续帧、不重连；各档共用画质，每次新会话恢复自动。查看窗最小化时暂用 3 帧目标。

**复审收敛（#311）**：沿用茶青、现有字体与控件 token，查看窗口用紧凑身份栏、流畅度选择和缩放分段按钮，把空间留给画面；窄窗允许控件换行，状态文字不挤掉结束入口。请求阶段使用「取消」，开始共享前按钮仍为「拒绝 / 选择屏幕 / 开始共享」。选屏列表单独滚动，确认按钮固定在窗口底部，键盘选中态清楚可见；系统授权准备中有明确反馈。#312 将共享准备/进行中提示收为 **320×56 DIP** 单行紧凑条，停靠已选显示器工作区右上边缘（无可匹配 display_id 时使用当前显示器），保留两行身份/状态及清楚的停止按钮。共享窗口从确认阶段即使用自绘拖拽标题区，缩窗不创建新 renderer、不重建采集；不允许最大化/最小化，准备后固定尺寸，用户可拖动避让。工作区变化重新贴边，所有位置尊重工作区与系统 DPI；查看方保留原生窗口。无毛玻璃、持续动画、额外图标库。Esc 复用现有输入法保护（含 keyCode 229）。

#### 7.9.3 状态反馈与无障碍

| 状态 | 用户可见反馈 |
|---|---|
| requesting / awaiting-consent | 等待同意 / 收到查看请求；双方可取消或拒绝 |
| preparing | 正在准备屏幕，可能需要系统授权；可取消 |
| connecting | 对方已同意，正在连接；无画面时展示等待区域 |
| active | 查看方显示实时画面；共享方持续显示查看者与停止按钮 |
| 当前帧超过 2 秒未更新 | 画面上方明确提示「画面暂未更新」，读到新帧才移除；超过协议期限进入结束态 |
| ended | 销毁协助窗口和画面，在对应私聊卡片保留本地化终态、请求时间及实际连接起止/时长；重新发起须经过说明确认和新的同意流程 |

界面不展示 token、seq、协议字段或底层错误堆栈。请求/状态使用适度的可访问播报；视频帧更新不逐帧播报。按钮有文字/可访问名称与 Tab/Enter/Space 操作，确认流程使用独立原生窗口内的焦点顺序；Esc 尊重输入法与已经消费的事件。字号/颜色/圆角使用既有 token，保留减少动态效果支持；本地验收中英文切换与最小窗口；各目标机高 DPI 另行验证。

## 8. 设置页与文件柜页

### 8.1 设置页

独立设置窗口（640×480，微信式），左侧分组导航，右侧为「页头说明 + 分组面板 + 设置行」。设置窗弹出时居中到主窗几何中心（决议 #123），而非屏幕中心——主窗被拖到副屏或角落时设置窗跟随主窗，并夹取到主窗所在显示器工作区防止越界。设置窗同主窗为沉浸式无标题栏（决议 #49）：顶部 32px 隐形拖拽带，macOS 红绿灯内嵌，Windows / Linux 右上角仅自绘关闭按钮。设置页不按实现先后排列，而按用户心智与常见桌面软件规范组织：先身份与外观，再通知、聊天文件、网络，最后快捷键和关于（**决议 #150 重组为 7 组**：端口归「网络」、发送键归「聊天与文件」、去掉「安全与高级」杂物抽屉）。每个设置行包含标题、说明、控件。**保存策略统一（决议 #150 / #151）**：所有设置**即时保存**——偏好（开关 / 主题 / 字体 / 提示音 / 发送键）调整即生效，文本输入（资料 / 端口 / 快捷键 / 文件位置）失焦(blur)即存、头像与目录选定即存；**不再有任何保存按钮**（仅保留「更改」浏览、「恢复默认」「重置应用设置」等动作按钮）。每次保存后底部居中弹出「设置已保存」深色胶囊 toast，淡入 + 轻微上移、停留约 1.8s 后淡出（决议 #151，尊重 `prefers-reduced-motion`）；值未变则跳过不弹，昵称空 / 端口越界则复原原值并提示原因。二选一偏好（主题、发送键）用分段控件而非下拉。视觉沿用 4px 间距台阶与 4 级字号阶梯；决议 #216 将形状标尺升级为面板 14px、标准控件 8-10px、胶囊 999px。未实现能力不摆置灰占位，做好再加。

**头像编辑器**（决议 #50/#243/#244/#245）：左侧 64px 圆形大预览 + 当前样式小字（如「自定义头像」/「动物表情 · 茶青」/「昵称首字 · 湖蓝」）；右侧顶部按「动物表情 / 昵称首字 / 自定义头像」排列三段切换。动物表情模式在下方展示 10 列 × 2 行的动物网格和 10 色背景色板；昵称首字模式隐藏动物网格，只展示同一背景色板，选色立即保存；自定义头像模式隐藏动物和色板，只展示一个「上传图片」按钮，已有自定义头像时继续使用该按钮更换图片。切换到动物表情或昵称首字会立即应用相应预设并清除自定义图片；切换到自定义头像只改变编辑面板，上传裁剪成功后才应用。取消选择或裁剪保持原头像。

**头像裁剪弹窗**（决议 #243/#246/#247/#249）：用户和群共用，通过 Teleport 挂到 `body`。中心工作区以圆形遮罩显示最终头像范围，图片默认居中覆盖；鼠标或触控板拖动调整位置，滚轮、缩放滑杆及加减按钮调整大小，重置恢复初始居中。拖动从工作区按下开始后由窗口级鼠标事件持续跟踪，放大、移出工作区或短暂丢失局部命中时仍可继续调整，抬起、窗口失焦或卸载立即结束。最小缩放始终填满圆形范围；最大缩放随源图动态放宽——允许一直放大到裁剪区域约等于 192px 输出分辨率（如 8192px 源图约 42 倍），小于输出分辨率的源图不放大，缩放步进按当前倍率等比（×1.12），拖动边界不露空白；固定输出 192×192 WebP，大图降采样经 `createImageBitmap` 高质量缩放避免锯齿（决议 #249）。应用时直接从原始受检图片字节解码位图，编码前拒绝全透明裁剪结果，失败保留位置与缩放并显示行内错误。弹窗打开时聚焦关闭/缩放控制，支持遮罩、取消与 Esc 关闭并恢复触发按钮焦点；应用期间锁定关闭和重复提交。静态 JPG/PNG/WebP/BMP 可用，GIF、APNG、动态 WebP、源文件超过 20MiB、单边超过 8192px 及解码失败均给出明确提示。所有颜色、圆角、阴影和字号使用现有 token，减少动态效果时取消缩放进场。

**设置侧栏**（决议 #238/#239）：顶部拖拽带下方直接排列「账号资料 / 通用 / 通知 / 聊天与文件 / 网络 / 快捷键 / 关于」分组导航，不重复展示当前头像、昵称或公司摘要；完整身份编辑只出现在右侧「账号资料」页。每项文字前放置 18px 自绘线性图标，语义依次为身份卡、调节滑杆、通知铃、聊天文件、局域网节点、键盘、信息圆标。图标统一使用 1.6px 圆角描边并继承行文字颜色，默认次级灰、悬停提高对比、选中随文字变为茶青；图标与文字间距 9px，整行继续保持 38px 高度和左侧选中强调线。

**端口修改确认**（决议 #240）：网络页 UDP / TCP 端口输入框默认显示当前值并保持只读；点击或键盘聚焦后弹出居中风险确认框，标题按入口显示「确认修改 UDP 端口？」或「确认修改 TCP 端口？」。正文固定为：「UDP/TCP 端口共同用于局域网发现、消息和文件传输。修改后需要重启应用，并确保相关客户端、防火墙及网络策略使用匹配配置；配置不一致可能导致联系人无法发现、消息或文件无法送达。」下方强调：「只有在你明确了解当前网络部署，并已准备同步调整相关配置时，才建议继续。」按钮从左到右为「取消」和红色高亮「确认修改」，初始焦点落在安全区域；Esc、遮罩和取消均关闭提示并保持原值。确认后仅当前输入框进入可编辑状态并自动聚焦 / 全选，失焦后恢复锁定并按既有 1–65535 校验保存。

**分段偏好滑块（决议 #223/#245）**：账号资料的三段头像模式、通用的二段主题、聊天与文件的二段发送键共用同一视觉结构。底座使用列表层中性色，选中项以窗口表面、轻阴影和茶青前景浮起；按钮按下即时轻微缩放，键盘焦点显示清晰茶青外环。主题滑块保持紧凑，只显示本地线性太阳 / 月亮图标，分别代表浅色 / 深色，并保留完整无障碍名称；发送键显示 `Enter` / `Ctrl/Cmd+Enter`。减少动态效果时取消位移动画。

**发送键图标化（决议 #224）**：发送键滑块不再显示长串平台缩写。第一段用单个回车键帽图标；第二段由两个键帽与一个弱化加号组成，macOS 显示 Command + 回车，Windows / Linux 显示 Control + 回车。键帽使用 22px 紧凑方形、5px 圆角、1px 内描边，图标保持本地线性风格；整组宽度从约 198px 收紧到约 134px。按钮的 `aria-label` 保留平台化完整语义，视觉和读屏结果一致。

**材质升级（决议 #216）**：侧栏使用较厚的半透明结构材质，页头移除贯穿硬分割线，分组面板使用 14px 圆角、柔和内描边与有色阴影；导航选中态使用弱茶青底和左侧强调线。Naive UI 控件统一采用 8-10px 控件圆角，并保留即时保存行为。

**保存提示定位（决议 #218）**：「设置已保存」toast 按整个 640×480 设置窗口水平居中，使用 `left: 50%` 配合自身半宽位移；不得继续补偿侧栏宽度而偏到右侧内容区中心。

| 分组 | 设置项 |
|---|---|
| 账号资料 | 头像（编辑器版式见上：大预览 + 分段切换 + 20 个 Twemoji 图标网格 + 背景色板）、昵称、公司、部门、团队 |
| 通用 | 开机自启（默认开）；关闭主窗口行为：最小化到托盘（默认）/退出；主题（双段滑块，太阳=浅色、月亮=深色）；字体缩放 100%/110%/125% |
| 通知 | 新消息系统通知开关；通知是否显示消息内容（隐私遮罩）；提示音（默认关，三款可选） |
| 聊天与文件 | 文件保存位置（路径 + 更改，说明不用「另存为」时保存到 `文件保存位置/联系人名称/`）；允许同事直接发送文件（开关，默认开，说明直接发送同样使用联系人子目录，仅私聊生效）；**我的文件柜指路行（决议 #283）**：共享目录 / 默认权限 / 按联系人例外这套设置**已移到主界面的文件柜页签**，此处只留一行说明 + 「打开文件柜」按钮（点击显示主窗并切过去），不再重复摆控件（避免两处都能改造成状态错觉）；发送键（平台化键帽图标双段滑块）；聊天记录导出（会话 + 时间范围 + HTML/TXT/迁移备份包）；导入备份包；传输记录空态/列表 |
| 网络 | 手动节点 IP（可移除标签展示）；跨网段 CIDR 扫描；已保存扫描网段以**紧凑表格**展示（网段 / 在线节点数 / 操作[⟳ 刷新该网段·✕ 删除该网段]三列，网段后括号标来源（本机 / 来自某同事），在线数=茶青徽标 0 时灰显；操作按钮平时即带淡意图色——刷新淡茶青·删除淡红、hover 加深，删除点 ✕ 后该行就地二次确认才移除，决议 #160/#161）；移除同步来的网段后本机忽略该 CIDR，避免自动同步反复加回；UDP/TCP 端口（改后提示全员需一致并重启生效）；内网通兼容模式（开关，默认关）+ 兼容端口 2425 + 独立兼容 IP 段表（网段 / 最近扫描 / 发现节点数 / 操作[扫描·删除]）+ 手动兼容节点 + 状态行（未开启 / 监听中 / 端口被占 / 扫描中 / 最近发现 N 个节点）。实验文件 / 图片开关首版不展示，完成内网通 TCP 拉取闭环后再放到高级折叠区 |
| 快捷键 | 截图、显示/隐藏主窗：**录制式输入**（聚焦后直接按组合键填入，Esc/退格清空 = 禁用，决议 #57）；注册失败（被系统占用，如 UOS 深度截图占 Ctrl+Alt+A）在设置行下方红字提示；「恢复默认」一键回出厂组合；截图时隐藏茶话间窗口开关 |
| 关于 | **单卡片重设计**（决议 #90 信息分层 + #91 视觉重设计）：① 顶部居中品牌标识——**纯茶杯图形标识**（`PantryBrandLogo` color 变体 = `pantry-mark.svg`，无白卡底板 / 无阴影，直接融入页面背景，决议 #155；早期为 icon 变体白卡图标 + drop-shadow，决议 #92 改掉「茶」字占位）、`茶话间 Teahouse`、定位语、shield 信任徽条「无服务器·无遥测·数据不出局域网」；② 中部键值行——版本（等宽字体）、许可（`GPL-3.0-only`，决议 #225）、源码 GitHub 链接（茶青真链接 + external 图标，点击经系统浏览器打开）、**内网更新**（决议 #167/#168/#170/#171/#172/#173）：同组 `about-row`，左侧标签「内网更新」，右侧第一行短状态（未检查 / 正在检测 / 有新版 / 正在请求 / 请求已发出 / 已是最新 / 检测失败）+ 圆形 `?` 帮助图标 + 紧凑动作按钮；「只从已发现的在线同事中查找同平台更新源、不访问外网、同步更新只发起索包且不静默安装」等解释性说明只在鼠标悬停或键盘聚焦 `?` 时显示自绘 tooltip，不常驻正文、不使用原生 `title`。tooltip 必须向上浮出并处于高层级，避免被关于卡片底部或窗口边界裁切。第二行仅在有新版时显示版本与来源、失败时显示重试提示等必要反馈；有更新时按钮切为茶青弱底「同步更新」，点击后进入 requesting / requested 状态。该处只负责检测与索包，不承诺安装已完成，不再使用独立状态卡或左侧图标块；③ 底部「更多信息」全宽折叠按钮展开灰底开发者托盘——运行时内核（Electron/Chromium/Node）、本机节点 ID、Twemoji 图形署名。展开就地渲染（Chrome 108 无原生 popover），带淡入且尊重 `prefers-reduced-motion` |

**生成式品牌图标（决议 #226，覆盖上表关于页旧 `pantry-mark.svg` 描述）**：应用内关于页、主窗口空状态与首启向导统一展示方案 A 的圆角方形位图图标。图标为深茶青底、暖白茶杯气泡、琥珀茶汤、双叶与三点消息，保留柔和材质层次；分别以 60px、92px、52px 容器 `object-fit: contain` 呈现，不叠加额外白卡、描边或投影。Windows / Linux 彩色托盘使用同源 32px 版本；macOS 菜单栏仍用单色模板图，避免系统深浅菜单栏下失去对比。

**设置窗口层级（决议 #222）**：Windows / Linux 把设置视为模态任务。点击设置后，主窗口完整覆盖一层不可交互的暗色 scrim：浅色主题用约 24% 的深灰绿色，深色主题用约 38% 黑色，140ms 淡入；设置窗关闭时按同一路径淡出。遮罩不做模糊、缩放或循环动画。设置窗自身保持 640×480，在最外层增加 1px 实色内描边：浅色使用约 26% 深灰绿，深色使用约 18% 白色，保证 Win7 关闭 DWM 阴影或 UOS 合成器阴影较弱时仍能看清四边。macOS 已有稳定原生阴影，主窗口不增加 scrim。`prefers-reduced-motion` 下取消过渡，直接切换明暗状态。

### 8.2 文件柜页（主窗第三个页签，决议 #283/#284）

文件共享是高频功能，入口从设置窗第三组提到导航栏。**形态是主窗的第三个页签，不是独立窗口**（决议 #284）——主窗本来就是「导航 + 列表栏 + 内容区」，文件柜正好占后两格；多开一个窗口只是把同一件事拆到两处，还多一套窗口生命周期。**入口**：导航栏底部工具组最上一格（更新提示之下、刷新之上），`cabinet` 线性图标 21px（柜体 + 一道分层线 + 两个抽屉把手，1.6px 描边、继承文字色），沿用既有 `rail-hint` 悬停气泡「文件柜」与聊天 / 通讯录同款选中态；**不加未读角标**（有人上传已在私聊冒泡计未读，同一件事不提醒两次）；未设共享目录时按钮照常可点，进去是引导设置的空态。

**页面骨架**：沿用主窗三栏，切到本页签时——**列表栏（272px）**换成文件柜列表，**内容区**换成文件浏览器或「我的文件柜」管理页；导航栏不动。列表栏顶部让出 36px 拖拽带（与会话列表搜索栏同口径，决议 #127），本页签**不显示全局搜索框**（文件柜自带「搜索同事」，全局搜索在这里没有意义）；内容区头部 52px + 32px 拖拽带 = 84px，与聊天头部等高。

**列表栏**分两段：① **「我的文件柜」摘要卡**——12px 圆角、`--material-strong` 底 + 细描边，卡内三行键值（共享目录路径 / 默认权限 / 单独设过的同事 N 人），选中时改茶青弱底 + 茶青描边；未设目录时整卡换成一句「未开启」+ 「选择目录」主按钮。② **同事的文件柜列表**——分组标题行（「同事的文件柜」+ 计数）、搜索框（人多时过滤），每行 50px：32px 圆头像带在线/离线状态点 + 显示名 + 副标题（在线给部门 · IP、离线给「离线 · 最后在线 …」、旧版本给「版本较旧，不支持文件柜」）。列表只收**通讯录里声明 `shr1` 的联系人**，在线在前、离线灰显在后（决议 #17 口径）。离线与旧版本条目**仍可点**，右栏就地说明原因并给「重试」——不做点不动的死按钮。**权限徽标不在列表里**：有效权限由对方当场判定、只有列过一次目录才知道，为渲染列表批量探测会撞对方 10 秒 5 次的列目录限流。

**内容区 · 浏览器**自上而下五段：① **头部 52px**——32px 圆头像 + 「XX 的文件柜」+ 副标题（在线状态 · IP · 共 N 项）；右侧依次是权限徽标（「只读」灰底 /「可上传」茶青弱底）、列表/网格分段控件、刷新图标。② **面包屑行 40px**——返回上级箭头 + `文件柜 / 设计稿 / 2026`（任意一级可点）。③ **表头 28px**——全选复选框 + 名称 / 大小 / 修改时间（右对齐，`--text-3` 11.5px）；还有下一页时全选标签为「选择已加载」（决议 #281）。④ **列表区**——详情行 40px：复选框（未勾选时只在 hover 显示）、22px 文件类型图标（复用 `file-types` atlas，目录用文件夹图标）、名称（单行省略 + `title` 全名）、大小与修改时间（`tabular-nums` 等宽数字，目录不显示大小）；hover 淡灰底、选中茶青弱底；滚到底自动取下一页（每页 200 条）并显示骨架行。⑤ **底栏两行**——上行左「已选 N 项 · 合计大小」，右「另存为…」「上传到 TA 的柜子」（仅有写权限）「下载 N 项」（茶青实心，未勾选时不出现）；下行常驻 `--text-3` 小字讲清落点（下载到 `文件保存位置/文件柜-对方名称/`、上传进对方柜子里以我命名的文件夹），**避免误以为传到当前目录**。传输进度、速率、剩余、取消就地显示在底栏上方的一条进度行里（细进度条 + 一行文字），**不进聊天流**。

**网格视图**：卡片 108px 高、5 列自适应，42px 大图标 + 两行文件名居中截断，选中为茶青 1.5px 描边 + 左上勾选框。视图偏好记在本机界面偏好里（与共享设置无关），私聊内 320px 面板恒为列表（宽度放不下网格）。两种视图都**不做缩略图预览**（要给协议加取图报文，且会把"翻个目录"变成成片图片传输），也**不提供排序切换**（列表分页取回，本地重排会让翻页顺序错乱）。

**浏览手感对齐系统文件管理器**：单击选中、**双击进目录**（原「单击目录即跳走」取消）、`Ctrl`/`⌘` 加减选、`Shift` 选范围、`↑`/`↓` 移动、`Ctrl`/`⌘`+`A` 全选已加载、`空格` 勾选、`Enter` 进目录或下载单个、`Backspace`（mac 亦支持 `⌘`+`↑`）返回上级、`F5` / `⌘`+`R` 刷新、`Esc` 取消选中；右键菜单只有**下载 / 另存为… / 复制文件名**，**没有删除与重命名**（协议层就不提供改动别人文件的能力，决议 #272）。有写权限时整个浏览区接受拖入上传（茶青虚线高亮 + 「松手就放进 XX 的柜子」+ 落点小字），无权限显示禁止态说明。

**内容区 · 我的文件柜页**：头部（柜子图标 + 「我的文件柜」+ 副标题「正在共享 · 同事按下面的权限访问」+ 右侧「停止共享」文字按钮），下面四张卡片——① **共享目录**（路径行 + 「打开」「更改…」，一句说明"同事看不到它在你磁盘上的位置"）；② **默认权限**（三段滑块「不共享 / 只读 / 可读可传」，下方随档位切换说明文案，选到"可读可传"补一句"只能放新文件，不能删改覆盖"）；③ **单独设过的同事**（紧凑三列表格：头像 + 显示名（离线灰显）/ 权限下拉 / ✕ 移除即恢复跟随默认；表格下一行「可搜索联系人下拉 + 添加」，新例外默认「只读」；无例外时空态「所有同事都按默认权限」）；④ **最近有人放进来**（决议 #283 新增，读既有传输记录里 `purpose='share-put'` 的条目，按人按次列最近 10 条：头像 + 「XX 放进来 N 个文件 · 合计大小」+ 右侧时间，点任意一条打开本机对应目录；一句说明"别人只是浏览、下载不会出现在这里"）。全部即时保存并弹既有「设置已保存」胶囊（决议 #150/#151），共享根被拒时就地红字说明原因。

**空态与失败态**（口径与私聊面板一致，决议 #278）：还没开柜子 → 引导态「你还没有开文件柜」+ 「选择共享目录」；对方关柜 / 未对我开放 → 首屏整片替换为居中错误态 + 「重试」；**翻页失败只在列表末尾追加一行小字 + 「重试」，已加载条目一律保留**并暂停自动续拉；空目录 →「这个文件夹是空的」（有写权限时补「可以把文件拖进来」）；截断 → 底部小字「目录内容过多，仅显示前 5000 项」（常量插值）；对方离线 / 版本过旧 → 说明原因并点明"文件柜要双方都开着软件才能访问"。

## 9. 视觉规范（基础 token）

列表辅助文字（决议 #301 / OPT-29）：会话时间、摘要、计数及空态；通讯录计数、IP、离线昵称及空态；全局搜索分类、摘要、组织路径、离线说明和状态提示统一局部使用 `--text-2`。在线主文字继续使用 `--text-1`，离线头像与状态点仍灰显（#252），禁用态沿用原样。全局 token、字体、行高、列表布局与缩放保持。

| token | 草案值 | 用途 |
|---|---|---|
| `--primary` | `#3D8B6B` 茶青 | 按钮、选中态、自己气泡底（10% 透明度衬底） |
| `--text-1 / 2 / 3` | #17211C / #58645E / #87918C | 主文字 / 次要 / 时间戳 |
| `--text-placeholder` | #B8B8B8 | 输入框占位 hint，低于 `--text-3`，避免看起来像真实输入 |
| `--bg-window / list / chat` | #F9FBFA / #F1F5F3 / #EDF2EF | 窗体 / 列表栏 / 聊天区底；深色主题保持近黑茶灰 |
| `--bg-pinned` | #EEF0F2（深色 #262B27） | 置顶会话整条底色，比 `--bg-list` 略深的淡灰，与普通会话区分（决议 #126） |
| `--bubble-peer / mine` | #FFFFFF / primary 12% | 气泡 |
| `--line` | rgba(40, 66, 53, 0.10) | 分割线与内描边（1px，能省则省） |
| `--material-bar / panel / strong` | 主题相关半透明色 | 顶部栏、列表层与浮起输入区；reduced-transparency 下切换实色 |
| `--scrim` | 主题相关半透明深色 | 模态弹窗遮罩 |
| `--surface-hover / selected` | 中性 5% / 茶青 12% | hover 与当前会话选中态 |
| `--online / offline` | #2BA245 / #C2C2C2 | 在线状态点 |
| `--danger / badge` | #E5484D / #FA5151 | 错误 / 未读角标 |
| `--font-xs / sm / md / lg` | 12 / 13 / 14 / 16px | 时间戳 / 辅助 / 正文 / 标题 |
| 间距 | 4 的倍数 | |
| `--radius-control / panel / pill` | 8 / 12 / 999px | 控件 / 面板 / 胶囊；按组件角色固定 |
| 头像 | **圆形**；20 个 Twemoji 本地 SVG 动物图标 + 背景色模板 | 列表、聊天、资料卡统一（决议 #2/#45/#47）；协议仍只传头像编号，编号由图标下标与背景色下标组合，兼容旧“emoji 下标”编号语义 |
| 系统图标 | 自绘 SVG / 1.6px 描边 / 常规 16 或 18px | 按钮、工具栏、文件卡、状态位统一；不使用 emoji；左侧聊天/通讯录主入口为 25px，设置入口为 21px，不改变侧栏容器 |
| 内置 emoji 尺寸 | 消息正文与输入框统一 **1.3em**（随正文字号）；表情面板格子 16px 字号（图标约 21px） | 三处渲染同一视觉尺度（Twemoji 本地 SVG）；输入框 textarea 与镜像层 font-family 首位放置等宽空白字形字体 `PantryEmojiBlank`（决议 #56）——内置 emoji 字符在输入框内三平台都精确占 1.3em，镜像图标恰好满槽，光标逐字符对齐、连续输入不重叠 |
| 品牌 logo | 线性茶杯气泡 + 双叶 + 三点消息 / 单色、彩色小标、大图标三件套 | 菜单栏单色模板在 32px 画布内约 82% 内容区，界面小 logo、空状态、关于页和打包大图标保持同一轮廓；不使用文字艺术字、不引入远程图片 |
| 托盘图标 | macOS 单色 template；Windows / Linux 彩色填充版（茶青圆角块 + 白色茶杯线条，决议 #58） | 深浅任务栏背景下辨识度一致；未读闪烁帧同步彩色化，Windows overlay 数字角标不变 |
| 图标提示 | 11px / 深灰底白字 / 4px 圆角 / 约半秒延迟淡入 | 仅用于图标按钮的中文名称说明 |

## 10. 决议记录（2026-06-10，审美轮）

| # | 问题 | 决议 |
|---|---|---|
| 1 | 主题色 | **茶青 `#3D8B6B`** |
| 2 | 头像形状 | **圆形**（用户选定，区别于微信的方头像，更柔和现代） |
| 3 | 设置页形态 | **独立小窗**（640×480，左侧分组导航，§8 按此） |
| 4 | 提示音 | **默认静音**：仅托盘闪烁 + 角标；内置 3 款（水滴/木鱼/叮咚）可在设置中开启并试听 |
| 5 | 品牌 logo | **茶杯气泡统一标识**：菜单栏单色、彩色小 logo、大图标共享同一简约轮廓，色彩沿用茶青 `#3D8B6B`，辅以少量暖茶色强调茶水线 |
| 6 | 菜单栏 logo 尺寸 | **单色菜单栏 logo 缩小留边**：32px 画布内标识内容区约 82%，避免系统菜单栏 / 托盘视觉权重过大；彩色小 logo 与大图标不受影响 |
| 7 | Win7 / UOS emoji 兼容 | **头像与内置 emoji 子集使用 Twemoji 本地 SVG 子集**：可见图形不再依赖系统彩色 emoji 字体；emoji 面板、输入框编辑态、消息正文走同一套本地 SVG 渲染；发送、复制、存储仍保留原始 UTF-8 字符。 |
| 8 | logo "有点丑"、细描线单薄 | 重设计为饱满扁平茶杯气泡（决议 #64）：茶青实心杯身 = 对话气泡（左下气泡尾）+ 暖茶汤面 + 杯把 + 双蒸汽；icon / small / mono 三件套与空状态 `PantryBrandLogo` 同造型，茶杯隐喻与茶青配色不变。 |
| 9 | 新参考图要求 logo 更清爽、更像 IM 品牌 | 品牌系统刷新为线性茶杯气泡 + 双叶 + 三点消息（决议 #104）：大图标白色圆角背板 + 柔和阴影；彩色小标用茶青/浅叶绿渐变；托盘 Win/Linux 继续茶青圆角块 + 白色线标，macOS 菜单栏保持单色 template。 |
| 10 | #104 初稿杯身偏方，不像参考图茶杯 | 订正为下缘半圆茶杯气泡（决议 #106）：杯口上沿保留稳定水平感，杯身下缘连续圆弧下坠；对话尾和杯把作为线性轮廓接入，保留三点与双叶。所有展示位置必须优先读作“茶杯 + 聊天”，而不是方形容器。 |
| 11 | 用户提供 SVG 套件，要求按文件接入 | 以 `pantry-svg-icons.zip` 为唯一品牌源（决议 #107）：应用内标准 logo 使用 `icon-standard-logo.svg` / 小尺寸使用 `icon-small-16-64px.svg`，打包和 Dock/任务栏使用 `icon-taskbar-dock-logo.svg`，Win/Linux 托盘使用 `icon-menu-logo.svg`，macOS 菜单栏使用 `icon-macos-menubar-outline.svg`。 |
| 12 | UOS 通知 emoji 显示方框、通知图标为系统默认图标 | 通知正文采用系统通知安全摘要：emoji 降级为 `[表情]`，媒体消息用文本占位；Linux / Windows 通知显式使用真实 app 图标，macOS 继续使用系统 app 图标（决议 #108）。 |
| 13 | 私聊震动需要有入口但不能刷屏骚扰 | 单聊输入区在表情后放“窗口震动”图标按钮；仅对方在线时可点，群聊不显示。限流失败在工具条旁显示“太频繁，稍后再试”等短反馈；发送成功与收端收到均在消息流插入居中系统提示行。收端执行窗口短抖，并自动切到发起人的单聊。发送/接收限流参数以 requirements #109/#110/#112 / protocol v0.25 为准。 |
| 14 | 切换聊天时消息区位置跳动，但新开/后台直达应看最新 | 消息区滚动位置按会话记忆：前台在会话间切换时恢复离开位置，停在底部的会话回来仍贴最新；首次打开、通知/托盘/震动直达、主窗隐藏后恢复再点开会话、以及“回到最新”都滚到最新。当前会话新增消息不再无条件贴底，只在本来接近底部或自己发送时跟随。“回到最新”入口升级为消息区右下角悬浮圆按钮（决议 #134）：搜索/跳转到历史页、或在最新页向上滚动距底超约两屏时出现，回到近底自动隐藏；hover 保持白底不透明，仅茶青描边和阴影增强（决议 #135）。 |
| 15 | 免打扰会话的未读与震动仍然太强提醒 | 免打扰会话的未读数量继续显示，但角标使用灰色；免打扰单聊收到震动时不唤起、不置前、不震动主窗口，只在消息流保留系统提示行。非免打扰震动在 Windows 上即使主窗仅被其他窗口遮挡，也应短暂置前后再执行震动。 |
| 16 | 自动同步来的扫描网段需要可解释、可删除 | 网络连接页的网段标签追加来源短文案：本机新增显示「本机」，远端同步显示「来自 昵称/备注」。用户点击移除后立即从列表消失，并写入本机忽略记录；后续同事再次分享相同 CIDR 不自动加回，除非用户自己重新手动扫描/保存该 CIDR。 |
| 17 | PK 功能要像“随机 GIF”一样有趣，但还要能让对方点击参与 | 输入区新增拳击手套 / 拳头相碰语义的 `PK` 小图标，弹出并排「猜拳」「骰子」玩法浮层；发送结构化 PK 消息，由主进程随机结果，渲染层播放约 1.5s 本地动画。PK 只面向在线成员即时投递，不走离线补发；他人的 PK 气泡旁显示「我也来 / 掷一下」按钮，点击后发新的独立 PK 消息，可反复参与，不输出胜负。通知、会话预览、历史搜索不透结果；PK 可撤回，但不可复制 / 转发；不发送真实 GIF、不引远程资源、不做排行榜或押注。 |
| 18 | PK 三件套虽经 #17/#140 打磨仍不和谐统一：骰子线性骰面与猜拳彩色手势割裂、气泡三段松散、入口 7.5px 角标糊 | 渲染层系统性重设计（沿用 8px 圆角 / 茶青 / 明暗 token，不改协议与投递语义）：气泡改「统一开奖窗」骨架，骰面点阵与 Twemoji 手势住进同尺寸 / 圆角 / 底色 / 内描边的同一展示窗，收紧为「玩法标签 → 开奖窗 → 弱化结果」三层；参与按钮「我也来 / 掷一下」融入气泡底部一体行；揭晓定格加开奖窗 scale 回弹 + 茶青高亮一闪（仍只 transform / opacity）；工具条入口改线性拳碰图标 + 清晰 `PK` 文字并排（去糊角标）；玩法浮层与开奖窗视觉呼应。 |
| 19 | #17/#18 后用户要求骰子做 3D 转动、猜拳也 3D、浮层猜拳图标偏丑 | 渲染层继续升级（不改协议与投递语义）：骰子开奖窗内改 CSS 3D 立方体（六面真实点数，rAF 翻滚 + transition 弹性落定到结果面，预置角度保证点阵正立）；猜拳改 CSS 3D 翻牌（正反双面同显 Twemoji 手势，绕 Y 翻转轮播，停结果手势正面）；`pk-rps` 图标改「张开剪刀」（指环 + 交叉刃）。仍只 transform / opacity，历史与 reduced-motion 直接定位结果面。 |
| 20 | 用户要求 PK 入口图标改成拳击手套对碰、去掉 PK 文字 | 工具条 `pk` 图标改为两只拳击手套对碰（圆润拳头 + 拇指弧 + 腕带）；去掉并排 `PK` 文字，按钮回纯图标形态（30×28、图标 18px），tooltip 仍「PK」。不改浮层与气泡。 |
| 21 | 用户反馈拳套图标仍偏丑、要求 PK 艺术字；且点开玩法浮层后「猜拳 / 骰子」被左侧会话列表方向裁切 | 工具条 `pk` 图标改为斜体 `PK` 字标（`skewX(-10)` 右倾 + 加粗描边）取代拳套；玩法浮层由居中弹出改为左对齐按钮向右展开，避开 `.chat` 的 `overflow:hidden` 左缘裁切。不改气泡与随机语义。 |
| 22 | 用户反馈 PK 气泡占高度太高、顶掉太多聊天消息，要求重新设计更紧凑 | PK 气泡由垂直「标签 / 开奖窗 / 结果 / 通栏按钮」（~170px）重设计为**横向卡**（~60px）：开奖窗左 + 标签&结果两行 + 参与按钮胶囊右；开奖窗 62→42px、骰子立方体 40→28px、猜拳翻牌同步缩小，3D 动画与定格逻辑不变。 |
| 23 | 用户反馈猜拳揭晓动画中「出了 xx」文字随翻牌乱跳，希望固定提示 | 猜拳未定格时结果文字由随 90ms 轮播变化改为固定「出拳中…」（与骰子「投掷中…」对齐）；翻牌手势图仍轮播，定格后显示「出了石头 / 剪刀 / 布」。 |
| 24 | 用户反馈 PK 卡片宽度不一致（「出了布」比「出了石头」短、动画态与定格态忽长忽短） | PK 气泡改固定宽度（他人 200px / 自己 132px），信息列 flex:1 撑满、结果文字左对齐、短文字右侧留白；固定值取自最宽内容，卡片不变更宽。 |
| 25 | 用户反馈 PK 猜拳出「石头」时手势图不显示（剪刀 / 布正常） | 根因：石头映射到 ✊（twemoji `270a`），但本地 `assets/twemoji/` 缺 `270a.svg`（仅内置 `270b` 布 / `270c` 剪刀），`rpsSrc` 取空字符串、翻牌卡片 `v-else-if="rpsSrc"` 整体不渲染。改石头映射 ✊ → 👊（`1f44a`，项目已内置、同套画风的拳头），复用既有资源、零新增；剪刀 ✌️ / 布 ✋ 不动。 |
| 26 | 用户反馈「窗口震动」工具条图标偏丑、看着像屏幕发光 | `nudge` 图标由「显示器 + 支架底座 + 四向放射短线」（放射线像发光）重绘为「带标题栏的应用窗口 + 左右对称振动弧」（通用振动符号），消除发光感、「窗口抖动」语义更准；沿用 24 viewBox / stroke 1.6 描边，仅改 SVG path。 |
| 27 | 用户反馈设置页分类、设置项、UI 混乱不合理，要求整体重设计 | 设置页整体重设计（决议 #150）：① 信息架构 8→7 组——端口归「网络」、发送键归「聊天与文件」、「通用外观」改名「通用」、「消息通知」只留提醒、删「安全与高级」杂物抽屉；② 保存策略统一——纯偏好即时生效、仅输入类留保存按钮；③ 二选一偏好（主题/发送键）改分段控件；④ 删语言/监听网卡/诊断日志/清理联系人 4 个置灰占位；⑤ 视觉收敛到设置页专属标尺（间距 4px 台阶 / 圆角 8·6·999 / 字号 18·15·13·12），区块标题 14→15 提层级。 |
| 28 | 用户要求设置实时保存、去保存按钮，保存后弹「设置已保存」浮层并淡出 | 设置全面实时保存（决议 #151）：文本输入（资料 / 端口 / 快捷键）失焦即存、头像 / 文件目录选定即存、偏好沿用即时生效；删全部保存按钮（保留浏览 / 恢复默认 / 重置）；值未变跳过、昵称空 / 端口越界复原并提示；保存反馈由页头 notice 升级为底部居中深色胶囊 toast，淡入 + 轻微上移、约 1.8s 后淡出，尊重 reduced-motion。 |
| 29 | 用户反馈「设置已保存」toast 显示位置 / UI 有严重问题 | 根因：决议 #151 的 `.toast` 样式块编辑中途丢失、从未入文件，toast div 无样式作为 `.settings` flex 第三子项裸奔。补回并 taste 重写：`left:calc(50% + 87px)` 居中于右侧内容区（补偿 174px 侧栏，不偏侧栏）、底部上浮（顶部被拖拽带 / 标题 / 关闭按钮占满）、深色胶囊 + `currentColor` 对勾（明暗两主题都够对比）+ 茶青味柔影（非纯黑）、淡入上移 / 淡出下移、尊重 reduced-motion。 |
| 30 | 内网协作场景下，发送者需要一个「直接发送」入口，在线同事免确认保存；群聊不允许直接发送 | 私聊文件 / 文件夹按钮与拖拽入口保持普通发送，不在发送前选择模式。普通文件卡片出现在发送方消息区后，若该卡仍在等待接收、文件信息已送达、对方在线且版本支持 `fd1`，右侧操作区显示「直接发送」按钮；对方离线、旧版本或尚未送达时置灰并给短提示。点击后接收方免确认拉取，完成后卡片显示「已保存本地」，不暴露完整本地路径。群聊文件按钮、拖拽和群文件卡都不展示直接发送入口；设置页「聊天与文件」新增「允许同事直接发送文件」总开关，默认开。 |
| 31 | 用户反馈「直接发送 / 已保存」文件卡变高、文案换行，聊天流占用过多 | 文件卡保持高密度横向卡：等待 / 直接发送中状态并入大小 meta；右侧操作区双动作只占一行，主按钮为「直接发送」，取消用 `x` 图标小按钮并保留 tooltip；直接接收完成态统一显示短文案「已保存本地」，不显示发送人目录名。 |
| 32 | 用户反馈「等待接收」在 meta 行仍被截断，发送完成态文案也太长 | 「等待接收」改为文件名同行固定状态片，meta 只保留大小等不会影响理解的信息；发送方完成态统一为「发送成功」。 |
| 33 | 用户反馈接收态文件卡右侧三按钮堆叠后卡片太高、不协调 | 接收态右侧改一行动作组：主按钮「接收」+ 文件夹图标「另存为」+ `x` 图标「拒绝接收」，压低卡片高度并保留中文 tooltip。 |
| 34 | 用户明确默认接收目录规则：自动 / 手动接收只要不用「另存为」，都应进入默认目录下的联系人子目录 | 文件卡仍保留「接收」与文件夹图标「另存为」的清晰区分：点击「接收」使用 `文件保存位置/联系人名称/`，点击文件夹图标才打开目录选择器并直接使用用户选择的目录。直接发送自动接收复用同一默认规则；完成态继续用短文案，不展示具体路径。 |
| 35 | 用户反馈第三方截图工具截图后粘贴，会发送两张一样的图片 | 输入框图片粘贴只能由一条路径消费：浏览器 paste 事件能拿到图片时立即发送并取消原生兜底；浏览器 paste 没有文件 / 文本 / 图片时，才延迟读取主进程原生图片剪贴板。渲染层不得在 keydown 阶段抢先发送图片。 |
| 36 | 文件、图片新增撤回；已接收文件不支持撤回 | 图片右键菜单新增「撤回（mm:ss）」，自己的图片 5 分钟内可撤回，撤回后隐藏气泡并禁止通过该消息打开。文件卡右键菜单新增「撤回」，仅自己的文件、5 分钟内、对端支持媒体撤回且接收未完成时可用；已接收完成的文件显示不可撤回，群文件任一成员完成接收后整条置灰。 |
| 37 | 用户要求 Excel 表格粘贴后默认以图片展示，但消息上可在图片和文字之间切换；切换控件希望是气泡上沿的小滑块 | 表格粘贴消息仍按图片消息发送，默认显示表格图片；带 `tableText` 的图片气泡上沿显示小型分段滑块「图片 / 文字」，不覆盖图片，自己的消息右对齐、对方消息左对齐。选中项用茶青滑块，文字视图显示原始制表符文本并允许选择复制；若文字视图被截断，显示轻提示且不污染可复制 TSV。切换只影响本机显示。 |
| 38 | 用户反馈小图片点击放大后，独立看图窗口太小，底部菜单挤在一起很难看 | 看图窗口保留小图原始尺寸显示，但窗口与内容区设置最小空间：内容区至少 560×360，底部工具条始终有足够横向余量；小图居中显示，不为撑窗口而拉伸图片。 |
| 39 | 用户反馈重新打开软件或收到消息后点击会话，不会自动显示最新消息 | 会话列表 / 联系人 / 通知 / 托盘 / 震动进入会话统一视为“看最新”：重载最新 50 条并滚到底部。只有历史搜索跳转保留目标消息居中；当前会话里用户主动翻历史时，新消息不强制打断阅读。 |
| 40 | 内网通兼容模式需要能单独配置 IP 段、发现兼容节点并通信，同时不污染普通聊天能力 | 设置页网络组新增默认关闭的「内网通兼容模式」，使用独立兼容端口、独立 IP 段和手动兼容节点；通讯录新增「内网通兼容」分组；兼容会话只支持普通文本、ACK 状态和历史搜索，隐藏图片、文件、表情、PK、窗口震动等茶话间主协议能力。 |
| 41 | 内网通联系人需要明显标识，且文件、图片、震动、PK 等能力要按协议实测结果降级 | 联系人名称与会话头部显示「内网通」徽标，副标题增加「功能受限」；输入区由 `ConversationCapabilities` 驱动，只保留文本、历史搜索、发送按钮，隐藏表情包、PK、震动、截图、图片、文件、文件夹、直接发送和媒体撤回。IPMSG 文件 / 剪贴板图片 offer 先作为实验“兼容文件”卡片处理，未完成 TCP 拉取闭环前不开放普通入口；震动无标准 IPMSG 命令，兼容会话持续隐藏。 |
| 42 | 用户选定第一张生图方案并要求落地 | 决议 #226 覆盖 #107 的彩色品牌源：应用图标与应用内品牌展示改用同源透明 PNG 母版；Windows / Linux 彩色托盘同源缩放；macOS 菜单栏继续使用 #107 的单色模板 SVG。 |
| 43 | 用户要求消息 / 文件气泡更协调并重绘文件类型图标 | 决议 #228：文字气泡取消顶部高光；文字气泡与文件卡统一四角 14px；文件类型图标使用本地透明 PNG atlas，保持简洁和小尺寸辨识。 |
| 44 | 用户反馈点击消息转发后，弹窗被消息列表覆盖 | 决议 #230：转发弹窗脱离聊天区局部层叠上下文，Teleport 到 renderer 全局顶层；使用完整遮罩、明确层级、初始焦点和 Esc 退出，短进场动画支持 reduced-motion 降级。 |

当前无未决项。

## 11. 变更记录

- 2026-07-21 v1.64 决议 #265：截图文字标注改为选区内点击后原地输入；输入框自动聚焦并夹紧在视口内，Enter 提交、Esc 取消本次文字，中文 composition 期间不抢确认键，发送 / 复制前提交非空内容。版本 **0.45.1 → 0.45.2**。
- 2026-07-21 v1.65 决议 #266：截图工具条功能按钮改用统一线性 SVG 图标，去掉可见文字；统一 30×30px 命中区，补齐工具选中、hover、focus-visible、取消警示状态，并以分隔线区分工具与操作。版本 **0.45.2 → 0.45.3**。
- 2026-07-21 v1.66 决议 #267：截图工具条八个图标按钮增加自定义中文提示，悬停延迟显示、键盘聚焦立即显示，顶部空间不足时自动翻到按钮下方；移除重复的原生提示。版本 **0.45.3 → 0.45.4**。

- 2026-06-10 v0.1 初稿：三栏结构、会话/通讯录分离、三级通讯录树、聊天界面与文件卡片、微信式全局搜索、群聊发起/收文件/托盘直达流程、设置页 8 分组、视觉 token。
- 2026-06-10 v0.2 审美四项决议：主题色茶青 `#3D8B6B`、圆形头像、设置独立小窗、默认静音（托盘闪烁+角标）。UI 基线就此确定。
- 2026-06-10 v0.3 第四轮补充：离线加"离线"副文字、会话头部打开即探活、表情面板双页签（emoji/表情包）与「添加到表情」、导出双格式 + 导入（身份映射）。
- 2026-06-10 v0.4 查漏轮（决议 #22）：新增 7.4 首次启动向导（原 7.4 顺延为 7.5）、资料卡备注名、消息右键转发、群内 @ 浮层与加强通知、截图隐藏主窗开关。
- 2026-06-11 v0.5 撤回交互落地：自己的消息 2 分钟内右键可撤回；撤回后隐藏原气泡，消息区插入居中系统提示行。
- 2026-06-11 v0.6 P1 交互落地：设置页补头像/主题/字体/通知预览/快捷键/端口/导出范围/传输记录；聊天补链接点击、转发、多选群发、群内 @；截图补矩形/箭头/文字/马赛克标注。
- 2026-06-11 v0.7 图标与群管理落地：系统图标统一改为项目内自绘线性 SVG，移除 UI 控件里的 emoji 图标；发起讨论组增加可选管理密码，群成员面板按创建 IP / 管理密码显示管理入口。
- 2026-06-11 v0.8 聊天右键与图标提示修正：输入区工具图标增加延迟淡入中文名称提示；消息右键只响应实际内容区域，空白行不触发；聊天与图片右键菜单靠边自动避让。
- 2026-06-12 v0.9 头像与设置图标修正：设置入口图标重画为明确齿轮；头像模板改为 20 个亲和动物 emoji 图标 + 背景色选择，并保留昵称首字模式。
- 2026-06-12 v0.10 头像模板补调：20 个头像 emoji 全部改为更适合做头像的亲和动物，包含狗、猫、猪、兔等高辨识度选项。
- 2026-06-12 v0.11 左侧导航图标修正：参考用户给定样式只重画聊天/通讯录图标本体，导航栏仍保持既有 56px 茶青侧栏。
- 2026-06-12 v0.12 讨论组发起流程修正：建群改为两步弹窗，第一步联系人快速搜索与多选，第二步设置组名、二次确认管理密码与密码提示。
- 2026-06-12 v0.13 设置页结构修正：设置页顺序按常见桌面软件重排为账号资料、通用外观、消息通知、聊天与文件、网络连接、快捷键、安全与高级、关于；右侧统一为页头说明、分组面板和带说明的设置行。
- 2026-06-12 v0.14 群聊媒体交互：群聊图片/文件/文件夹入口在有在线群成员时启用；发送端以一条消息汇总多成员传输状态，收端在群会话内展示图片或文件卡片。
- 2026-06-12 v0.15 群聊图片阈值交互：群聊图片超过 10MB 时按普通文件卡片展示，接收者手动点击后才开始传输；单聊图片仍沿用 20MB 免确认。
- 2026-06-12 v0.16 表情面板关闭规则修正：移除鼠标移出自动关闭，改为点击面板外部关闭，避免选择表情包时误关。
- 2026-06-12 v0.17 表情面板尺寸修正：内容区固定高度，切换到表情包页签或空表情包状态时不再突然缩小。
- 2026-06-12 v0.18 单聊头部副标题试验：昵称下方增加灰色 IP 副标题，在线/离线状态保持原位。
- 2026-06-12 v0.19 群聊头像修正：群聊中对方消息左侧显示发送者圆形头像，私聊消息仍不显示气泡旁头像。
- 2026-06-12 v0.20 添加表情反馈修正：聊天图片/表情添加到表情包后，反馈从气泡下方小字改为消息侧边垂直居中的状态胶囊，对方消息在右侧，自己消息在左侧。
- 2026-06-12 v0.21 会话内历史搜索：群聊/私聊输入区工具条最右侧新增历史搜索按钮，弹出小浮层支持关键词、图片、文件、日期范围筛选，结果点击跳转并高亮。
- 2026-06-12 v0.22 会话内历史搜索 UI 精修：搜索面板改为设置页尺度的居中弹窗，左侧筛选、右侧结果；图片命中直接展示缩略图，缩略图不可用时才显示占位。
- 2026-06-12 v0.23 会话内历史搜索默认结果与日期组件：搜索面板打开后默认显示最近记录；日期筛选改为连续点击日期的日历范围选择，不再使用两个原生日期输入框。
- 2026-06-12 v0.24 私聊头部资料弹窗：单聊昵称区域可点击打开对方资料框，并在框内直接修改本地备注。
- 2026-06-12 v0.25 输入框 hint 颜色修正：新增 `--text-placeholder`，所有输入框占位提示统一比辅助文字更轻。
- 2026-06-12 v0.26 品牌 logo 三件套：确定“茶杯气泡”统一标识，补菜单栏单色模板、彩色小 logo 与大图标规范。
- 2026-06-12 v0.27 联系人资料页重设计：通讯录单击展示完整资料页，双击联系人直接进入单聊。
- 2026-06-12 v0.28 左侧主导航图标尺寸微调：聊天/通讯录图标放大到 25px，侧栏宽度、按钮热区与设置入口保持不变。
- 2026-06-12 v0.29 托盘 / 菜单栏未读提示：macOS 显示菜单栏数字与 Dock 角标；Windows 任务栏 overlay 数字并闪烁托盘图标；Linux 以系统角标尝试 + 托盘闪烁兜底。
- 2026-06-12 v0.30 菜单栏 logo 尺寸微调：单色菜单栏 / 托盘标识缩至 32px 画布约 82% 内容区，彩色小 logo 与大图标保持原比例。
- 2026-06-12 v0.31 Win7 / UOS emoji 兼容：头像模板改为本地 SVG 动物图标；emoji 面板与消息正文对内置 emoji 子集使用本地 SVG 渲染，避免缺字方框。
- 2026-06-12 v0.32 头像美术资源修正：头像模板与内置 emoji 兼容显示改用 Twemoji 本地 SVG 子集，设置 About 页补 Twemoji CC-BY 4.0 署名。
- 2026-06-12 v0.33 输入框 emoji 兼容补齐：聊天输入框编辑态增加 Twemoji 本地 SVG 镜像渲染层，补齐 Win7 / UOS 输入时仍显示方框的问题。
- 2026-06-12 v0.34 输入框镜像光标对齐修正：镜像层 emoji 占位宽度改为按输入框实际字体逐字符测量（隐藏 DOM 探针，与 textarea 同一排版引擎；canvas measureText 对 emoji 与 DOM 排版不一致，不可用），与 textarea 内真实字符逐一对齐，消除表情越多光标偏移越大的问题。
- 2026-06-12 v0.35 沉浸式无标题栏（决议 #49）：主窗与设置窗去系统标题栏，顶部 32px 隐形拖拽带；macOS 红绿灯内嵌，Windows / Linux 右上角自绘窗口控制按钮；§2/§8 同步规格。
- 2026-06-12 v0.36 设置页头像编辑器重设计（决议 #50）：大预览 + 「图标头像 / 昵称首字」分段切换 + 图标网格与背景色板分节，选中/悬停反馈明确；§8 同步规格。
- 2026-06-12 v0.37 沉浸式跨平台修正（决议 #51/#52）：mac 主窗红绿灯移至列表栏顶部留白区（x=68，不跨导航栏分界线）；Linux 拖拽带改 JS 实现（CSS 拖拽区在 Linux 吞点击导致 UOS 双击联系人/发起讨论组失效），用户可感知行为不变。
- 2026-06-12 v0.38 emoji 三处尺寸统一（§9 新增 token 行）：表情面板格子 22px 字号缩至 16px、输入框镜像图标改固定 1.3em（此前 Win7/UOS 随窄字符槽缩小），与消息正文 1.3em 同一视觉尺度；同轮删除聊天头部 no-drag 残留，修复 Win7 聊天区顶部 32px 无法拖动窗口。
- 2026-06-12 v0.39 第三十二轮（决议 #56–#59）：输入框 emoji 等宽空白字形字体（textarea 内 emoji 恒占 1.3em，对齐/重叠矛盾终结）；快捷键设置改录制式 + 注册失败红字提示 + 恢复默认；托盘图标 Win/Linux 彩色填充版（§9 新增 token 行）；Win/Linux 自绘关闭按钮改走 win:close IPC，修复点关闭未进托盘直接退出。
- 2026-06-13 v0.40 决议 #61：全局自绘滚动条（10px 槽 + 内缩细 thumb，深浅分色，根治 Win7 overlay 滚动条抽搐）；输入框 textarea 与镜像层加顶部 5px 留白，修 Win7 首行字形/hint 遮挡。
- 2026-06-13 v0.41 决议 #63：撤回窗口 2→5 分钟；右键"撤回"项实时倒计时 mm:ss，超时变灰为"撤回（超时）"。
- 2026-06-13 v0.42 决议 #64：品牌 logo / 应用图标重设计为饱满扁平茶杯气泡（§1、§10#8 同步）；三件套 SVG + PantryBrandLogo 同造型，托盘沿用决议 #58 彩色版。
- 2026-06-13 v0.43 决议 #67：群信息面板改覆盖右侧整列（不挤压消息）+ 成员紧凑头像（§5 同步）。
- 2026-06-13 v0.44 决议 #68：导航栏 56→68px 容纳 mac 红绿灯，红绿灯回左上角 (14,18)，废弃 #51 的列表栏顶部方案（§2 同步）。
- 2026-06-13 v0.45 决议 #70：导航栏底色由茶青深色改中性浅灰（`--rail-bg`，微信式，图标默认深灰 / 选中茶青），让 mac 红绿灯落在浅色上自然；茶青主色保留在选中态/气泡/按钮（§2 同步）。
- 2026-06-13 v0.46 决议 #71：会话列表选中态改实心茶青底 + 白字（微信式，§3 同步），hover 淡灰区分，列表观感更精致。
- 2026-06-13 v0.47 决议 #74：会话内历史搜索结果体验优化（§6.1 同步）——摘要走 Twemoji 彩色渲染（修 Win7/UOS emoji 不显示/黑白）、文本命中去类型标题与图标且摘要升为主体、图片命中单击放大·双击定位、修 Win7 结果末行被裁、修「回到最新」在最新位置误显。
- 2026-06-13 v0.48 决议 #75：文件消息卡片优化（§5 同步）——群聊文件发送进度改「已接收 / 总数」x/x + hover 已接收名单；文件卡按扩展名显示彩色类型图标（FileTypeIcon），彩色徽标为单色系统图标基线的内容标识例外。
- 2026-06-13 v0.49 决议 #76：复制的文件可直接 Ctrl+V 粘贴到输入框发送（§5 输入区同步），单图按图片发、其余按文件发，与拖拽一致。
- 2026-06-13 v0.50 决议 #77：应用图标背板由浅青 `#EBF4EF` 改纯白 `#FFFFFF`（§1 同步），更通用清爽；茶杯造型与配色不变，重生成全套图标。
- 2026-06-14 v0.51 决议 #81/#82：会话列表 + 私聊顶部补在线/离线状态点（§3/§6 同步）；群成员在线展示由「整头像离线灰显」改为「绿/灰状态点」（§5 同步，与私聊统一，头像始终彩色）；主窗右侧空态去版本号、改随机名人名言；私聊顶部 IP 副标题加宽避免截断。
- 2026-06-15 v0.52 决议 #87/#88：消息流系统提示新增群名变更格式；私聊顶部副标题取消省略号截断，IP 必须完整显示。
- 2026-06-15 v0.53 决议 #89/#90：文件卡片状态文字右置为「左信息·右状态」徽标（§5 同步），传输中进度并入大小行；设置·关于页信息分层，默认只露版本/许可/源码链接/安心说明、开发者信息收进可折叠「更多」（§8 同步），新增 info/external 自绘图标。
- 2026-06-15 v0.54 决议 #91：关于页整合为单卡片视觉重设计（§8 同步）——居中品牌标识 + shield 信任徽条、版本/许可/源码键值行（等宽版本号 + 茶青真链接）、灰底开发者信息折叠托盘（淡入 + 尊重 prefers-reduced-motion）；shield 图标并入 PantryIcon。taste-skill 适配本项目原生 CSS / 茶青 / 明暗双主题。
- 2026-06-15 v0.57 决议 #97：图片查看窗口底部半透明菜单新增 OCR 识别/复制全部；小图自动识别，大图手动识别；识别命中框叠在图片本身上，支持拖选后复制，不做“大爆炸”分词面板。
- 2026-06-15 v0.60 决议 #98：OCR 可选区域光标改为系统文本选择光标，识别 token 从文字块细化到 symbol/字符粒度，拖选体验更接近真实文本。
- 2026-06-15 v0.61 决议 #99：图片查看器双击放大以鼠标位置为锚点；OCR 结果按图片缓存并优先恢复，缩放/再次点击识别不重复跑 OCR。
- 2026-06-15 v0.62 决议 #100：图片平面改显式中心定位，修双击放大仍像从左上角展开；OCR 层只让文字 token/复制按钮接收 pointer 事件，无文字区域可继续拖动图片。
- 2026-06-15 v0.63 决议 #101：OCR 文字拖选取消矩形框选层，改为起止字符范围高亮，未选中文字不显示淡框/描边。
- 2026-06-15 v0.64 决议 #102：OCR 结果缓存上移到主进程会话级内存，图片重新打开后直接恢复文字选择层，不重复识别。
- 2026-06-15 v0.65 决议 #103：OCR 拖选改为光标边界半开区间选择，按字符中心线判断前/后边界，减少起点前一个字符被误选。
- 2026-06-15 v0.66 决议 #104：品牌 logo 系统按参考图刷新为线性茶杯气泡 + 双叶 + 三点消息；空状态、关于页、打包图标、菜单栏/托盘和未读闪烁帧统一同一标识轮廓。
- 2026-06-15 v0.67 决议 #105：取消图片上的 OCR 覆盖拖选层，识别完成后用右侧文本结果窗展示全部文字；用户在 textarea 内原生选择复制，图片拖拽/缩放/旋转不再被 OCR 交互影响。
- 2026-06-15 v0.68 决议 #106：品牌 logo 杯身从偏方的圆角杯订正为参考图式下缘半圆茶杯气泡；三件套 SVG、`PantryBrandLogo`、托盘/未读图标和生成位图同步。
- 2026-06-15 v0.69 决议 #107：品牌系统改以用户提供 SVG 套件为源；`PantryBrandLogo`、打包图标、托盘和未读闪烁帧都从对应 SVG 渲染，不再手写杯身路径。
- 2026-06-15 v0.70 决议 #88 后续修正：私聊头部名字/IP 区域由固定窄宽度改为随内容区响应式拉宽，副标题不再设置 ellipsis / overflow 截断，保证「在线 · IP」完整单行显示。
- 2026-06-15 v0.71 私聊头部 hover 修正：名字/IP 区域保留响应式拉宽，但鼠标悬停和资料弹窗激活时不再整块铺灰，只用标题文字茶青反馈，避免宽区域产生大块灰底。
- 2026-06-15 v0.72 决议 #108：UOS / Linux 系统通知正文使用 `[表情]` 等文本占位避免 emoji 方框，Linux / Windows 通知显式传入真实应用图标。
- 2026-06-15 v0.73 决议 #109：单聊输入工具栏新增“窗口震动”按钮，群聊不显示；震动成功/限流失败只在工具栏旁短提示，收端窗口短抖但不向消息流插入系统行。
- 2026-06-15 v0.74 决议 #110：震动成功与收端收到都改为消息区系统提示行；收端收到震动后唤起主窗并自动切到发起人的单聊，避免停留在错误页面。
- 2026-06-15 v0.75 决议 #111：消息区滚动位置按会话恢复；首次打开、后台直达和主窗隐藏后重新点开会话默认到最新，新增消息不再无条件打断历史阅读。
- 2026-06-15 v0.76 决议 #112/#113：免打扰会话未读数量改灰色角标，免打扰屏蔽入站震动置前/震动动作；Windows 收到非免打扰震动时短暂置前，避免主窗被其他窗口遮挡时不弹到最上方。群元数据新增创建者 nodeId，配合主进程修复虚拟机/多网卡下群名同步被创建 IP 校验误拒。
- 2026-06-16 v0.77 决议 #114：网络连接页已保存扫描网段追加来源标签（本机 / 来自某同事）；移除同步网段后本机记录忽略，防止低频自动分享再次把同一 CIDR 加回。
- 2026-06-16 v0.78 决议 #115：主窗口左侧导航栏设置按钮上方新增刷新全局用户按钮；点击后扫描所有已保存网段并在按钮下方显示细进度条，运行中禁用按钮，完成后进度条短暂停留再收起。
- 2026-06-16 v0.79 决议 #116：左侧导航按钮统一为延迟淡入中文 tooltip；自己头像悬停显示个人信息卡，移出收起。交互遵循现有微信式克制产品语言，不引入夸张动效或新视觉体系。
- 2026-06-16 v0.80 决议 #117：自己头像信息卡改为仅鼠标悬停触发，移除焦点态误弹；卡片改为头像标题区 + 分组资料区，减少小字堆叠，提升可读性。
- 2026-06-16 v0.81 决议 #118：左侧导航 tooltip 取消菱形箭头和位移动画，改为单体圆角标签 + 短透明度显隐，避免浮层显示/消失时形状分离。
- 2026-06-16 v0.82 决议 #119：左侧导航 tooltip 改为 pointermove 后计时触发，移除 CSS hover/focus 直接显示，修复应用打开后“聊天”说明误弹。
- 2026-06-16 v0.83 决议 #122：细节 UI 精修轮（taste 原则，限现有设计语言与 token）——首启向导接入真实品牌 logo + `茶话间 Teahouse` 中英文字标，并修正第三步"文件传输即将到来"过期文案；私聊头部资料浮层在线状态由内嵌「● 在线」文本字形改为对称状态点 + 文案（补齐决议 #81 在浮层的遗漏）；设置页会话标签 / 传输记录分隔符由 ` - ` 统一为 ` · `；全局搜索结果分类标题去掉贴标签下划线、改用组间分隔线 + 间距节奏；表情包空态文案两行化对齐会话空态。纯渲染层精修，茶青主色与明暗主题不变。
- 2026-06-16 v0.84 决议 #123：设置窗弹出位置由屏幕中心改为主窗几何中心——按主窗 `getBounds()` 计算居中坐标并跟随主窗所在显示器，结果夹取工作区防越界，主窗不可用时回退默认屏幕居中。只影响 `settings-window.ts` 创建定位。
- 2026-06-16 v0.85 决议 #124：英文品牌名 Pantry → Teahouse（中文「茶话间」不变）。关于页 / 首启向导中英文字标、主窗口标题改 `…-🍵Teahouse`；构建产物 productName / 安装名 / 产物名同步 Teahouse。内部代码标识（`window.pantry`、协议、组件名、字体、包名、appId、仓库 URL）一律保持不变，userData 仍按「茶话间」派生、聊天数据零迁移。
- 2026-06-16 v0.86 决议 #125：「移除会话」改为删除聊天内容（§3 同步）——右键先弹二次确认，删除后底部居中浮条「已删除…」+「撤回」10 秒倒计时，超时才落库删除（消息 + 全文索引 + 会话条目）；撤回窗口期间不落库。早期"只移出列表、不删历史"行为废弃。
- 2026-06-16 v0.87 决议 #126：会话列表置顶项整条加淡灰底色 `--bg-pinned`（§3/§9 同步），与普通会话区分；新增明暗双主题 token，hover（`--line`）与选中（茶青实底）层叠在其后定义、仍优先覆盖。
- 2026-06-16 v0.88 决议 #127：① 左侧列表搜索栏由约 70px 改为与聊天头部 `.head` 等高 84px + 底部分隔线，两栏顶栏连成一条（§5 同步）；② 聊天输入框高度改为可拖拽调节——输入区顶部加 `ns-resize` 手柄，上下拖改 `.input-shell` 高度（48–320px），localStorage 记忆（§5 同步）。纯渲染层 + 现有 token。
- 2026-06-16 v0.89 决议 #128：修复会话列表右键菜单弹出后单击聊天栏不关闭的 bug——菜单打开期间挂 document `click`/`contextmenu` 与 window `blur` 全局关闭监听（菜单本体 `@click.stop` 不受影响），关闭即移除。
- 2026-06-16 v0.90 决议 #129：文件消息卡片三栏（类型图标 / 文件名·大小 / 右侧操作·状态）改为共享同一垂直中心线（`.card` 加 `align-items: center`），修左右高低错位；图标盒与 36px 字形等宽。
- 2026-06-15 v0.55 决议 #92：关于页品牌标识改用真实 app 图标（PantryBrandLogo icon 变体，替换「茶」字占位，§8 同步）；保存类按钮（资料/端口/快捷键/文件位置）成功后就地「✓ 保存成功！」+ 确认弹动 + 对勾弹入、3 秒恢复（次要弱底按钮成功提升为实心茶青白字；尊重 prefers-reduced-motion）。
- 2026-06-15 v0.56 决议 #93：大图查看器升级为常见桌面看图体验（§5 同步）——顶部浮动工具条、缩放比例、缩小/放大/适应窗口/原始大小/旋转/另存为/关闭，支持滚轮缩放、拖拽平移、双击切换、快捷键和加载失败态；沿用茶青单主色、8px 圆角、明暗主题与 reduced-motion。
- 2026-06-15 v0.57 决议 #94：图片查看器由主窗口全屏覆盖层改为独立普通窗口（§5 同步），避免遮挡聊天与历史搜索；保留 #93 看图工具，并取消空白处单击关闭的遮罩习惯。
- 2026-06-15 v0.58 决议 #95：图片窗口标题栏显示文件名；操作菜单移到底部半透明覆盖条并移除内部关闭按钮；初始显示按屏幕工作区 70% 阈值决定原始尺寸或等比缩放。
- 2026-06-15 v0.59 决议 #96：图片查看器大图显示改为显式宽高缩放，`transform` 只保留平移/旋转；图片窗口最小内容尺寸降到系统允许最低，避免超宽/超高图片被最小尺寸撑出多余空白。
- 2026-06-17 v0.60 决议 #134：消息区"回到最新"由仅历史页出现的胶囊按钮改为右下角悬浮圆按钮（§表 14 同步）——搜索 / 跳转到历史页、或在最新页向上滚动距底超约两屏时出现，回到近底自动隐藏；白底圆形 + 茶青向下箭头 + 柔和阴影，淡入上移动效复用既有缓动 `cubic-bezier(0.16,1,0.3,1)`，`prefers-reduced-motion` 降级，相对消息区右下定位不受输入框拖拽高度（决议 #127）影响。
- 2026-06-17 v0.61 决议 #135：输入框粘贴顺序修正——文件管理器复制的真实文件仍优先发送；剪贴板带 `text/plain` 时交给 textarea 原生粘贴，避免富文本 emoji 的图片副本被当截图；无文本图片剪贴板继续按截图发送。"回到最新"按钮 hover 保持白底不透明，只增强茶青描边和阴影。
- 2026-06-17 v0.62 决议 #136：聊天图片 / 表情包消息右键菜单新增「复制」，写入系统图片剪贴板；之后在输入框 Ctrl+V 沿用图片粘贴发送链路。GIF 表情复制为当前帧 PNG，完整动图发送继续用「转发」或表情面板。
- 2026-06-17 v0.63 决议 #137：聊天图片 / 表情包消息「复制」改为主进程 Electron 原生剪贴板写入并读回校验，修复 Chromium `ClipboardItem` 写入后仍无法粘贴回输入框的问题。
- 2026-06-17 v0.64 决议 #138：主进程 `before-input-event` 捕获 Command+V，渲染层确认输入框聚焦后，在无文本、无浏览器图片项时从主进程读取 Electron 原生图片剪贴板作为兜底，修复表情复制后粘贴不触发发送的问题。
- 2026-06-17 v0.65 决议 #139：新增 PK 分歧解决交互设计。输入工具条在表情后增加拳击手套 / 拳头相碰语义的 `PK` 入口，小浮层并排选择「猜拳」「骰子」；消息区新增 PK 气泡，本地动画约 1.5s 后定格到载荷结果，并在他人的 PK 旁显示「我也来 / 掷一下」按钮。动画尊重 `prefers-reduced-motion`，历史消息直接显示最终结果；通知、预览、搜索不透结果；PK 可撤回但不可复制 / 转发；不使用真实 GIF 或远程素材。
- 2026-06-17 v0.66 决议 #140：PK UI 高可用打磨。入口图标改为更简洁的线性拳头相碰；玩法浮层使用同套线性「猜拳 / 骰子」图标，补齐 hover / active / focus / disabled；PK 气泡收紧宽度、层级和参与按钮，动画只使用 transform / opacity，`prefers-reduced-motion` 直接显示结果。
- 2026-06-18 v0.67 决议 #141：PK 三件套系统性视觉重设计。气泡「统一开奖窗」骨架让骰面点阵与猜拳 Twemoji 手势住进同一展示窗（同尺寸 / 圆角 / 底色 / 内描边），三层收紧为「玩法标签 → 开奖窗 → 弱化结果」；参与按钮融入气泡底部一体行；揭晓定格加开奖窗 scale 回弹 + 茶青高亮一闪，仍只 transform / opacity、尊重 `prefers-reduced-motion`；工具条入口改线性拳碰图标 + 清晰 `PK` 文字并排；玩法浮层与开奖窗呼应。
- 2026-06-18 v0.68 决议 #142：PK 开奖 3D 化。骰子→CSS 3D 立方体（六面真实点数，rAF 翻滚 + 弹性落定结果面），猜拳→CSS 3D 翻牌（双面 Twemoji 手势绕 Y 翻转，停结果正面）；`pk-rps` 图标改「张开剪刀」。仍只 transform / opacity，历史与 reduced-motion 直接定位。
- 2026-06-18 v0.69 决议 #143：PK 入口图标改「拳击手套对碰」（圆润拳头 + 拇指 + 腕带），去掉并排 `PK` 文字，按钮回纯图标形态（30×28、18px），tooltip 仍「PK」。不改浮层与气泡。
- 2026-06-18 v0.70 决议 #144：PK 入口图标改斜体 `PK` 字标（`skewX(-10)` 右倾 + 加粗描边）取代拳套；修复玩法浮层被聊天面板左缘裁切——由居中弹出改为左对齐按钮向右展开。纯渲染层，不改气泡。
- 2026-06-18 v0.71 决议 #145：PK 气泡重设计为横向卡瘦身——「开奖窗左 + 标签&结果两行 + 参与按钮胶囊右」，开奖窗 62→42px、骰子立方体 40→28px、猜拳翻牌同步缩小（3D 动画逻辑不变），高度 ~170px → ~60px。纯渲染层，不改协议 / 随机 / 投递。
- 2026-06-18 v0.72 决议 #146：猜拳揭晓动画中结果文字改为固定「出拳中…」（与骰子「投掷中…」对齐），不随翻牌轮播跳动，定格后显示「出了石头 / 剪刀 / 布」。
- 2026-06-18 v0.73 决议 #147：PK 卡片改固定宽度（他人 200px / 自己 132px），信息列 flex:1 撑满、结果左对齐留白，消除不同结果与动画/定格态的宽度跳变。
- 2026-06-18 v0.74 决议 #148：修复 PK 猜拳「石头」手势图不显示（剪刀 / 布正常）——根因本地 twemoji 缺 `270a.svg`（✊），`rpsSrc` 取空致翻牌卡片不渲染；改石头映射 ✊ → 👊（`1f44a`，已内置同套画风），复用既有资源、零新增。纯渲染层，不改协议 / 随机 / 投递。版本 0.22.8 → 0.22.9（bug 修复，按决议 #73 patch +1）。
- 2026-06-19 v0.75 决议 #149：「窗口震动」工具条图标由「显示器 + 底座 + 放射线」（易读成屏幕发光）重绘为「带标题栏窗口 + 左右振动弧」，消除发光感、语义更准。纯渲染层，仅改 `PantryIcon.vue` 的 `nudge` path。版本 0.22.9 → 0.22.10（UI 微调，按决议 #73 patch +1）。
- 2026-06-19 v0.76 决议 #150：设置页整体重设计——信息架构 8→7 组（端口归网络、发送键归聊天、删「安全与高级」杂物组、「通用外观」→「通用」、「消息通知」→「通知」只留提醒）；保存策略统一为「偏好即时生效、输入类留保存」；主题/发送键改分段控件；删 4 个置灰占位；视觉收敛到设置页专属标尺（间距台阶 / 圆角 Shape Lock / 字号 4 级）。§8 设置分组表同步更新。版本 0.22.10 → 0.23.0（功能更新，按决议 #73 minor +1、patch 归 0）。
- 2026-06-19 v0.77 决议 #151：设置全面实时保存——文本输入（资料 / 端口 / 快捷键）失焦即存、头像 / 目录选定即存、偏好即时生效；删全部保存按钮；值未变跳过、校验不过复原并提示；保存后底部居中「设置已保存」胶囊 toast 淡入 + 上移、约 1.8s 后淡出。§8 保存策略段同步更新。版本 0.23.0 → 0.24.0（功能更新，按决议 #73 minor +1、patch 归 0）。
- 2026-06-19 v0.78 决议 #152：修复「设置已保存」toast 显示错乱（#151 的 `.toast` 样式编辑中途丢失、裸 div 在 flex 里乱放）——补回并 taste 重写：居中于右侧内容区（calc 补偿侧栏）、底部上浮、深色胶囊 + currentColor 对勾 + 茶青柔影、淡入上移 / 淡出下移、尊重 reduced-motion。版本 0.24.0 → 0.24.1（bug 修复，按决议 #73 patch +1）。
- 2026-06-20 v0.79 决议 #155：关于页 / 主界面空状态 / 首启向导三处品牌 logo 由 icon 变体（白卡圆角底板 + 阴影）改为 color 变体纯茶杯图形（`pantry-mark.svg`）——白卡在纯白 `#ffffff` 背景上只剩阴影方块、在深色 `#171a18` 背景上又是突兀白块，去掉底板后纯茶青图形直接融入明暗两种背景。仅改三处 `PantryBrandLogo` 的 `variant`、尺寸不变，SVG 套件与 icon/mono variant API 保留。§8 关于 / 向导段同步。版本 0.24.4 → 0.24.5（UI 微调，按决议 #73 patch +1）。
- 2026-06-20 v0.80 决议 #156：品牌 mark logo 描边由 331 段折线（trace 产物，放大 / 软件渲染下棱角呈锯齿）平滑为约 338 段贝塞尔曲线（闭合向心 Catmull-Rom 拟合，8 子路径），关于页 / 空态 / 向导的茶杯 logo 边缘顺滑、消除锯齿；造型不变、贴合原轮廓不过冲（标准 CR 会过冲吹胖扭曲，故用向心参数化）。仅 `pantry-mark.svg`（应用内 color 变体）。版本 0.24.5 → 0.24.6（资源微调，按决议 #73 patch +1）。
- 2026-06-20 v0.81 决议 #157：mark logo 重描去抖（接 #156）——向心平滑仍复现描摹抖动，改 Douglas-Peucker 简化抓主干（约 280→76 点）+ 向心曲线 + 三点改正圆 `<circle>`，保留原造型、线条顺滑、点正圆。已知瑕疵：杯把根/气泡尾/叶根部件交接处略生硬（原设计分件描摹的结构，干净后凸显，待设计精修），用户知情接受先落地。版本 0.24.6 → 0.24.7。
- 2026-06-26 v0.82 决议 #166：新增 §7.6 局域网自更新交互——主界面发现同平台 + 更高版本 + 可作源的在线节点时，导航 / 列表栏顶部出「内网有新版」淡茶青徽标（红点、不弹窗打断、无源不显示）；点开更新弹窗显示新版本号 / 来源同事 / 包大小 / 当前版本 + 「立即更新 / 以后再说」；确认后后台拉包（复用传输进度）→ 校验通过提示「即将重启更新」→ 自动安装重启（Win 静默 / Linux 弹一次授权）。遵循茶青单主色 / 8px 圆角 / 明暗主题 / reduced-motion。本期 Windows 安装版 + Linux deb，mac 暂缓。本轮第一步只落地「内网有新版」提示入口，拉包弹窗与安装为后续步骤。
- 2026-06-26 v0.83 决议 #167：设置页关于组（§8）新增「检测内网更新」按钮——键值区（版本 / 许可 / 源码）下方，点击主动查同平台更高版本的在线源，结果就地显示「内网有新版 vX（来自 某同事）」或「当前已是内网最新版本」，与主界面被动提示（决议 #166 / §7.6）互补。版本 0.27.0 → 0.27.1（微调，按决议 #73 patch +1）。
- 2026-06-27 v0.84 决议 #168：设置页关于组「检测内网更新」改为紧凑状态行，补 idle / checking / found / none / error 五态、内网范围说明、失败提示和带图标按钮；纯渲染层 taste 微调，复用既有 `update:check`，不新增拉包安装入口。版本 0.27.2 → 0.27.3（UI 微调，按决议 #73 patch +1）。
- 2026-06-27 v0.85 决议 #170：主界面更新弹层与设置-关于检测区在发现新版后接入「同步更新」请求：点击后仅发起内网索包并显示 requesting / requested / 暂不可用文案，不展示普通传输卡片、不承诺安装完成；进度呈现、包校验通过后的安装重启与失败重试闭环仍为后续。
- 2026-06-27 v0.98 决议 #171：设置-关于检测更新区按方案 A 键值行化，并入版本 / 许可 / 源码同组 `about-row`；移除独立状态卡与左侧图标块，右侧用短状态 + 小说明 + 紧凑动作按钮承载检测和索包入口，found 状态按钮为茶青弱底「同步更新」。版本 0.27.5 → 0.27.6（UI 微调，按决议 #73 patch +1）。
- 2026-06-28 v0.99 决议 #172：「内网更新」相关界面去掉默认可见的解释性说明；设置-关于行与主窗口新版弹层都新增圆形 `?` 帮助图标，hover / focus 显示自绘 tooltip 承载已发现同事范围、不访问外网、索包不静默安装等机制说明。正文只保留版本来源、错误重试等必要反馈。版本 0.27.6 → 0.27.7（UI 微调，按决议 #73 patch +1）。
- 2026-06-28 v1.00 决议 #173：修复设置-关于问号 tooltip 被卡片 / 窗口底部裁切；tooltip 改为向上浮出并提高层级，主窗口弹层保持同一上浮策略。版本 0.27.7 → 0.27.8（UI bug 修复，按决议 #73 patch +1）。
- 2026-06-28 v1.02 决议 #174：私聊文件「直接发送」交互实现。文件 / 文件夹按钮和拖拽入口保持普通发送；发送方普通私聊文件卡片出现后，若对方在线且支持 `fd1`，卡片右侧显示「直接发送」按钮。接收方自动接收后保存到 `文件保存位置/发送人名字/`，文件卡完成态显示「已保存到 发送人 文件夹」。群聊不允许直接发送；设置页「聊天与文件」新增默认开启的「允许同事直接发送文件」总开关。
- 2026-06-28 v1.03 决议 #176：用 taste skill 收紧文件卡片：等待状态入 meta 行、直接发送与取消同一行、取消改图标按钮、直接接收完成态只显示「已保存本地」。纯渲染层 UI / 文案微调，版本 0.28.1 → 0.28.2。
- 2026-06-28 v1.04 决议 #177：文件卡片等待状态改为文件名同行固定状态片，避免 `等待接收` 被截断；发送方完成态统一为「发送成功」。纯渲染层 UI / 文案微调，版本 0.28.2 → 0.28.3。
- 2026-06-28 v1.05 决议 #178：接收态文件卡右侧操作由纵向三按钮改为一行动作组，`另存为` 与 `拒绝接收` 改图标按钮，卡片高度回到紧凑状态。版本 0.28.3 → 0.28.4。
- 2026-06-28 v1.06 决议 #179：默认接收目录统一为联系人子目录。文件卡点击「接收」与直接发送自动接收都保存到 `文件保存位置/联系人名称/`；文件夹图标「另存为」继续保存到用户选择目录，不额外套子目录。版本 0.28.4 → 0.28.5。
- 2026-06-28 v1.07 决议 #180：修复第三方截图工具粘贴重复发送。浏览器 paste 事件处理过文件 / 文本 / 图片后取消主进程原生图片兜底；兜底只在 paste 没有可用图片项时延迟执行。版本 0.28.5 → 0.28.6。
- 2026-06-30 v1.08 决议 #188：图片 / 文件撤回交互实现。图片右键菜单新增「撤回（mm:ss）」，5 分钟内可撤回并隐藏气泡；文件卡右键菜单新增「撤回」，仅未完成接收的文件可撤回，已接收完成文件不支持撤回，群文件需全部收件人都未完成接收才可撤回。版本 0.29.6 → 0.30.0。
- 2026-06-30 v1.09 决议 #189：发送图片 / 截图后保持聊天视口贴底；内置截图框选结束后主窗口恢复到前台并短暂置顶再释放，避免被系统截图窗口或前一应用压在后面。版本 0.30.0 → 0.30.1。
- 2026-07-02 v1.10 决议 #190：表格粘贴图片消息 UI 实现。输入框识别 Excel / WPS / LibreOffice / 网页表格后默认发送图片；带表格文字元数据的图片气泡上沿新增小型分段滑块「图片 / 文字」，默认图片视图，文字视图展示原始 TSV 且可选择复制；若文字视图被截断，提示「文字视图已截断，图片完整」。滑块不遮挡图片、不新增工具栏感，150ms 轻动效并尊重 reduced-motion。版本 0.30.1 → 0.31.0。
- 2026-07-02 v1.11 决议 #191：图片查看器小图窗口最小尺寸修复。独立看图窗口与初始内容区预留最小空间，小图仍按原始尺寸居中展示，底部工具条不再因窗口过窄过矮而拥挤。版本 0.31.0 → 0.31.1。
- 2026-07-02 v1.12 决议 #192：会话打开默认定位最新消息修复。会话列表、联系人、通知、托盘、震动等入口打开会话时默认重载最新页并滚到底部；历史搜索跳转仍定位目标消息，阅读历史时收到新消息不强制抢滚。版本 0.31.1 → 0.31.2。
- 2026-07-03 v1.13 决议 #193：群成员面板的密码组管理改为内嵌密码输入框，提示文案保留在面板内；改名、添加成员、移出成员共用该输入，缺密码或密码错误时在面板内显示轻量错误，不再使用原生 prompt / alert。版本 0.31.2 → 0.31.3。
- 2026-07-08 v1.14 决议 #194：内网通兼容模式 UI 设计立项。设置页网络组新增兼容开关、兼容端口、独立 IP 段、手动兼容节点与状态行；通讯录新增「内网通兼容」分组；兼容会话仅保留普通文本收发、ACK 状态、失败重发和历史搜索，隐藏茶话间主协议专属能力。详见 [nwt-compat-design.md](nwt-compat-design.md)。
- 2026-07-08 v1.15 决议 #195：补充内网通兼容 UI 标志与能力降级。联系人名称和会话头部显示「内网通」徽标，头部副标题增加「功能受限」；输入区统一由 `ConversationCapabilities` 门控，隐藏 PK、震动、截图、图片、文件、文件夹、直接发送、媒体撤回等入口。IPMSG 文件 / 剪贴板图片 offer 仅作为实验“兼容文件”卡片设计，完成 TCP 拉取闭环前不进入默认工具栏。
- 2026-07-09 v1.16 决议 #197：主界面「刷新全局用户」增加居中二次确认弹窗——每次可点均确认；正文为网段列表摘要；确认钮为茶青「开始扫描」；遮罩 / 取消 / Esc 关闭不扫。§2 同步；实现于 `App.vue`。
- 2026-07-09 v1.17 决议 #197 文案/视觉收紧：去掉长说明、来源后缀与底部提示；仅「标题 + 将探测 N 个网段 + CIDR（最多 4 条）」；卡片收窄到约 300px。
- 2026-07-09 v1.18 决议 #197 补警示视觉：弹窗顶部居中暖琥珀圆标 + `warning` 三角叹号图标（`PantryIcon` 新增），传达「留意此操作」而不用危险红、不堆说明文案。
- 2026-07-09 v1.19 决议 #202：修正图片查看器 OCR 当前引擎表述：现用内置 PaddleOCR PP-OCRv6 tiny + onnxruntime-web 本地 wasm，识别结果仍走主进程会话级内存缓存与文本结果窗；历史决议中的旧 OCR 方案保留为演进记录。
- 2026-07-10 v1.20 决议 #208：截图标注工具条改为基于真实尺寸的视口内定位，优先选区下方、空间不足翻到上方，并处理右缘与极小视口夹紧；截图窗口统一拦截导航。版本 0.32.24 → **0.32.25**。
- 2026-07-10 v1.21 决议 #211：文件卡接收态补两处——排队时进度行显示「排队等待发送方」；接收方取消后（对端支持时）显示「重新下载」按钮走断点续传，「继续 / 重新下载」都只在本会话传输上下文仍可恢复时展示。版本 0.33.1 → **0.33.2**。
- 2026-07-13 v1.22 决议 #212：左上角个人信息卡重设计。移除端口展示，新增本机 IPv4；头像和昵称作为主层级，本机 IP 使用淡茶青重点区，组织与设备改为简洁键值行，节点 ID 收进弱化页脚，取消卡内多层小卡片。沿用现有主题 token、8px 圆角与克制悬停动效。版本 0.33.3 → **0.34.0**。
- 2026-07-13 v1.23 决议 #214：Linux 系统托盘未读帧改为高对比红色注意图标，与常规茶青图标每 800ms 交替；未读清零立即恢复常规图标。版本 0.34.0 → **0.34.1**。
- 2026-07-13 v1.24 决议 #215：Naive UI 以混合模式进入 renderer。首批只迁移设置窗口的标准输入、选择、开关、单选和常规按钮，视觉通过主题覆盖继续服从茶青 token、明暗主题及既有圆角/字号标尺；核心聊天、品牌图标、窗口系统和媒体交互保持自绘。版本 0.34.1 → **0.35.0**。
- 2026-07-13 v1.25 决议 #216：参考 Apple Design 完成主窗口与设置窗口的整体视觉升级设计。导航、列表、聊天头部和输入区引入分级材质与即时按压反馈；会话选中态改为弱茶青材质 + 左侧强调线；气泡、设置面板和浮层统一扩大圆角标尺。Naive UI 第二批进入主窗口动态入口，承载搜索与主要按钮。版本 0.35.0 → **0.36.0**。
- 2026-07-13 v1.26 决议 #217：修复主窗口 Provider 高度回归。Naive UI Provider 改用 `abstract` 模式，避免额外布局节点打断 `.shell` 的 100% 高度链；窗口验收新增默认尺寸、最小尺寸、最大化 / 还原、空态与会话态。版本 0.36.0 → **0.36.1**。
- 2026-07-13 v1.27 决议 #218：设置保存 toast 改为按 640×480 设置窗口整体水平居中；截图动态入口通过 preload 回放初始化截图，加载间隙使用黑色底，真实 Electron 验收覆盖桌面可见、框选、工具条与 Esc 退出。版本 0.36.1 → **0.36.2**。
- 2026-07-13 v1.28 决议 #219：磨砂收敛到浮层——主窗口导航 / 列表、聊天头部 / 输入区、设置侧栏 / 分组面板移除对纯色背景无效的 `backdrop-filter`，半透明底、内描边与阴影层级保持不变；浮层磨砂保留，`prefers-reduced-transparency` 降级留作 Chrome 118+ 前向增强。版本 0.36.2 → **0.36.3**。
- 2026-07-13 v1.29 决议 #220：§7.3 补未读判定——窗口可见且聚焦才即时置读当前会话新消息；最小化 / 托盘 / 失焦期间保留未读驱动托盘闪烁与角标，重新聚焦自动补置读。修复 UOS 20 / Win7 离窗收消息托盘不闪。版本 0.36.3 → **0.36.4**。
- 2026-07-13 v1.30 决议 #221：Windows 截图蒙层改为与系统工作区严格对齐，桌面源按实际像素裁掉任务栏；框选渲染改为单桌面图 + 四块纯色遮罩并按动画帧合并鼠标移动，位图首帧就绪后才显示窗口，消除 Win7 双层任务栏、画面变形与软渲染卡顿。版本 0.36.4 → **0.36.5**。
- 2026-07-13 v1.31 决议 #222：Windows / Linux 打开设置时，主窗口增加静态暗色 scrim 并阻断交互；设置窗补 1px 高对比内描边，系统阴影弱或缺失时仍能识别边界。macOS 保持原生阴影，不增加遮罩。版本 0.36.5 → **0.36.6**。
- 2026-07-13 v1.32 决议 #223：账号资料、主题和发送键统一为同一双段滑块；主题使用本地太阳 / 月亮线性图标，滑块选中块做 160ms transform 位移并兼容键盘焦点与减少动态效果。版本 0.36.6 → **0.36.7**。
- 2026-07-13 v1.33 决议 #224：发送键滑块使用平台自适应键帽图标；macOS 显示 Command + 回车，Windows / Linux 显示 Control + 回车，完整语义保留在无障碍名称中。版本 0.36.7 → **0.36.8**。
- 2026-07-14 v1.34 决议 #225：设置「关于」页许可值更新为 `GPL-3.0-only`，与根许可证、包元数据和 README 保持一致。版本 0.36.8 → **0.37.0**。
- 2026-07-14 v1.35 决议 #226：应用图标、应用内品牌展示和 Windows / Linux 彩色托盘切换为生图方案 A 的同源位图体系；macOS 菜单栏保留单色模板 SVG。版本 0.37.0 → **0.38.0**。
- 2026-07-14 v1.36 决议 #227：私聊头部资料入口移除整块 hover 背景和按下缩放，只保留昵称茶青高亮；点击热区、资料弹窗与键盘焦点轮廓保持。版本 0.38.0 → **0.38.1**。
- 2026-07-14 v1.37 决议 #228：文字消息气泡去除顶部高光，文件卡与文字气泡统一为四角 14px；Word / Excel / PPT 等文件类型图标改用 ImageGen 生成并透明归一化的本地 PNG atlas。版本 0.38.1 → **0.39.0**。
- 2026-07-14 v1.38 决议 #230：转发目标弹窗通过 Teleport 提升到 renderer 全局顶层，完整遮罩主窗口并补焦点、Esc 与减少动态效果兼容，避免消息列表覆盖。版本 0.39.1 → **0.39.2**。
- 2026-07-14 v1.39 决议 #231：Win7 / Linux 软渲染画像关闭浮层磨砂并收紧阴影；图片查看器停用小图自动 OCR、保留手动识别；OCR 图源 I/O 改为异步。版本 0.39.2 → **0.39.3**。
- 2026-07-14 v1.40 决议 #232：主窗口保留 Naive UI，并登记四个标准表单 / 操作组件的后续复用候选；聊天图片 / 表情消息、记录搜索缩略图与表情包网格启用惰性加载和异步解码。版本 0.39.3 → **0.39.4**。
- 2026-07-14 v1.41 决议 #233：`GroupCreator` 与 `ProfileCard` 的普通输入框、主次操作统一复用主窗口 Naive UI 主题；成员勾选、已选成员标签和专用布局保持轻量。版本 0.39.4 → **0.39.5**。
- 2026-07-14 v1.42 决议 #234：内联图片增加真实格式、8192px 单边和 3200 万总像素校验；聊天气泡与记录搜索切换为近视口生成、128MB LRU 管理的 320px WebP 缩略图，独立看图继续读取原图。版本 0.39.5 → **0.39.6**。
- 2026-07-14 v1.43 决议 #235：输入区发送图片按钮改用专用图片多选对话框，只展示六种支持格式；普通文件与文件夹继续走各自入口。版本 0.39.6 → **0.39.7**。
- 2026-07-14 v1.44 决议 #236：发送方主动取消文件传输后，文件卡与消息行使用灰色“发送取消”状态；接收方取消仍显示“已取消”，迟到失败不再把主动取消翻成“传输失败”。版本 0.39.7 → **0.39.8**。
- 2026-07-14 v1.45 决议 #237：发起讨论组的联系人结果压缩为 34px 单行布局，组织路径移到姓名右侧并在剩余空间内省略，离线标记固定在行尾。版本 0.39.8 → **0.39.9**。
- 2026-07-14 v1.46 决议 #238：设置侧栏移除重复的头像 / 昵称摘要；个人信息卡出现等待缩短到 120ms，并以透明桥接区连接头像与可交互卡片，离开整体悬停范围后收起。版本 0.39.9 → **0.39.10**。
- 2026-07-14 v1.47 决议 #239：设置侧栏 7 个分组选项增加统一 18px 自绘线性图标，采用身份卡、滑杆、铃铛、聊天文件、局域网节点、键盘与信息圆标语义，颜色随行状态变化。版本 0.39.10 → **0.39.11**。
- 2026-07-14 v1.48 决议 #240：网络页 UDP / TCP 端口默认锁定；点击后先显示配置不一致可能中断发现、消息和文件传输的确认框，取消保持原值，红色“确认修改”只解锁当前输入框。版本 0.39.11 → **0.39.12**。
- 2026-07-14 v1.49 决议 #241：群成员面板新增群主/管理员徽标与群主盾牌任免入口；添加成员向所有成员开放，管理员只可移出普通成员；旧版本兼容提示不阻断操作，角色和成员变化统一写系统提示。版本 0.39.12 → **0.40.0**。
- 2026-07-14 v1.50 决议 #242：群成员面板的添加入口改为 Teleport 全局模态弹窗，复用建群联系人搜索、多选、组织信息、离线状态和已选标签；隐藏已有成员，按群剩余名额一次批量邀请。版本 0.40.0 → **0.41.0**。
- 2026-07-14 v1.51 决议 #243：设置增加图片头像模式，用户与群共用 Teleport 圆形裁剪弹窗，支持拖动、滑杆/滚轮缩放、重置、错误保留和焦点恢复；自定义群头像覆盖会话列表、群聊头部和群成员面板。版本 0.41.0 → **0.42.0**。
- 2026-07-14 v1.52 决议 #244：设置图片头像入口去重；未设置自定义头像时点击“图片头像”直接选图，已有图片后才显示“更换图片 / 恢复默认”。版本 0.42.0 → **0.42.1**。
- 2026-07-14 v1.53 决议 #245：头像模式统一命名为“动物表情 / 昵称首字 / 自定义头像”；动物模式展示动物与颜色，首字模式只展示颜色，自定义模式只展示上传按钮。版本 0.42.1 → **0.43.0**。
- 2026-07-14 v1.54 决议 #246：头像裁剪拖动改为窗口级指针跟踪并完整处理捕获丢失，确保放大后仍可移动；本人、联系人和群头像统一使用可被 Chromium 正常加载的受管头像 URL。版本 0.43.0 → **0.43.1**。
- 2026-07-15 v1.55 决议 #247：头像裁剪改用窗口级鼠标拖动，覆盖鼠标与触控板；应用时从原始字节创建位图并拒绝全透明结果，失败保留裁剪状态。版本 0.43.1 → **0.43.2**。
- 2026-07-15 v1.56 决议 #250：全局联系人搜索移除隐藏主机名匹配；联系人结果补圆形头像、自定义头像、离线灰显和备注优先名称，副行继续显示组织路径或 IP。版本 0.44.0 → **0.44.1**。
- 2026-07-15 v1.57 决议 #251：消息右键菜单撤回项改为等高单行布局；“撤回”固定在左，倒计时或不可用原因以小号状态放右，数字使用等宽排版，最后 10 秒才增强危险色。版本 0.44.1 → **0.44.2**。
- 2026-07-15 v1.58 决议 #252：通讯录联系人行以行尾绿 / 灰圆点表达在线状态，移除昵称后的“· 离线”重复文字，离线灰显与在线优先排序保持。版本 0.44.2 → **0.44.3**。
- 2026-07-16 v1.59 决议 #259：Win7 聊天框临时切换到无自定义字体、无 emoji 镜像、无透明覆盖和无 composition 自定义处理的基础 textarea，保留粘贴、发送与高度调节，用于搜狗候选窗定位 A/B。版本 **0.44.9-beta.1 → 0.44.9-beta.2**。
- 2026-07-16 v1.60 决议 #260：Win7 基础 textarea 已验证搜狗定位正常；输入区移除诊断内框并恢复一体化材质，彩色 emoji 改由不参与真实输入布局的本地 Twemoji 展示覆盖层绘制。版本 **0.44.9-beta.2 → 0.44.9-beta.3**。
- 2026-07-16 v1.61 决议 #261：Win7 安全 textarea 与展示层恢复 `PantryEmojiBlank` 的 `1.3em` 等宽表情槽，使输入表情与消息正文同尺寸，连续表情的图形与原生 caret 保持逐槽对齐。版本 **0.44.9-beta.3 → 0.44.9-beta.4**。
- 2026-07-16 v1.62 决议 #262：撤销会触发 Win7 搜狗候选窗失位的真实 textarea 空白字体；Win7 输入改用系统字体 contenteditable，表情以 `1.3em` 原子 Twemoji 节点参与原生光标布局。版本 **0.44.9-beta.4 → 0.44.9-beta.5**。
- 2026-07-21 v1.63 决议 #263：私聊/群聊普通文件增加统一 24 小时领取期限；到期后发送方显示「发送已到期」，接收方显示「文件已过期」，关闭接收、另存、继续和重新下载入口，群发卡保留已接收计数。版本 **0.44.9-beta.5 → 0.45.0**。
- 2026-07-21 v1.64 决议 #268：聊天输入框统一 composition 状态保护；候选确认 Enter 交给原生输入法，候选完成后的下一次 Enter 才执行发送键设置，普通 textarea 与 Win7 contenteditable 行为一致。版本 **0.45.4 → 0.45.5**。
- 2026-07-22 v1.65 决议 #269：建群提交改为可恢复状态，IPC 失败后恢复按钮并显示行内错误；提交期间锁定返回、取消和遮罩关闭。遮罩仅响应从自身开始并结束的点击，输入框拖选移出不再关闭弹窗。版本 **0.45.5 → 0.45.6**。
- 2026-07-25 v1.66 决议 #270：表格粘贴不再自动发送。输入区新增「检测到表格 · 发送为图片 · 忽略」轻提示条，粘贴只插入原文，直接回车即发纯文字；草稿超上限时改文案并保持常驻，改动草稿 / 切换会话即收起。新增 `table` 线性图标。版本 **0.45.6 → 0.46.0**。
- 2026-07-25 v1.67 决议 #271–#277（共享文件柜，**只落档、代码零改动**）：私聊头部 `⋯` 左侧新增「文件柜」图标按钮，离线 / 未开共享 / 未对我开放 / 版本不支持时灰显并悬停说明原因；新增 320px 右侧覆盖式文件柜面板（头部 + 面包屑 + 分页列表 + 权限徽标底栏），下载按钮随勾选出现、上传按钮语义与当前层级无关并常驻落点说明，支持拖入上传，进度就地显示不进聊天流。§7 新增 7.8 三条流程（开自己的柜 / 去别人的柜 / 往别人柜里放）。设置页「聊天与文件」新增「我的文件柜」一组：共享目录选择（拦截主目录根 / 系统盘根 / 数据目录）、默认权限三段滑块（默认不共享）、按联系人例外紧凑表格。沿用既有 token、8px 圆角、茶青单主色与 `prefers-reduced-motion`，不引入新组件库或图标库。
- 2026-07-25 v1.68 决议 #271/#272/#276 第 ① 步实现：设置页「聊天与文件」新增「我的文件柜」面板——共享目录行（未设置时空态提示，已设置时显示路径并提供打开 / 更改 / 停止共享，被拒时就地红字说明原因）、默认权限三段滑块（复用头像模式的等分底座，未设目录时禁用，滑块下方随档位切换说明文案）、按联系人例外（可搜索下拉 + 添加，新例外默认「只读」；紧凑三列表格含头像、离线灰显、就地改档与 ✕ 移除）。全部即时保存并弹既有「设置已保存」胶囊。私聊头部按钮与文件柜面板属第 ② 步，尚未实现。版本 **0.46.0 → 0.47.0**。
- 2026-07-25 v1.69 决议 #273 第 ② 步实现：`FileCabinetPanel.vue` 落地——320px 右侧覆盖面板，含标题栏、面包屑 + 返回上级 + 刷新、全选与逐项勾选的分页列表（40px 行高、类型图标、大小与修改时间、滚动到底自动加载下一页、截断提示与空态）、进度行（进度条 / 取消 / 打开位置）与底栏（权限徽标 + 另存为 + 下载（N））。私聊头部 `⋯` 左侧加「文件柜」图标按钮，不可用时灰显并以原生悬停提示说明原因，打开时自动收起群信息面板。上传入口属第 ③ 步，本版底栏只有下载。版本 **0.47.0 → 0.48.0**。
- 2026-07-25 v1.70 决议 #272/#274 第 ③ 步实现：文件柜面板底栏在有写权限且未勾选条目时显示「上传文件夹」「上传到 TA 的文件柜」，底部常驻一行 `--text-3` 小字说明落点（「文件会放进 TA 文件柜的『你的名字』文件夹里」），面板整体接受拖入上传并在拖拽悬停时显示茶青虚线描边；上传进度复用同一条进度行，完成后文案区分「上传完成 / 下载完成」。私聊里的文件柜上传系统提示为可点击行（hover 变茶青加下划线），点击打开本机落盘目录。版本 **0.48.0 → 0.49.0**。
- 2026-07-26 v1.71 决议 #278：文件柜面板失败态分层。首屏（进目录 / 刷新 / 面包屑）失败仍整片替换为居中错误态；滚动翻页失败改为只在列表末尾追加一行横排小字 + 「重试」，保留已加载条目，并暂停自动续拉直到用户点重试——对方列目录限流为 10 秒 5 次，连滚几页撞上「操作太频繁」原本会把整屏条目清空。切换会话时文件柜面板与群信息面板一起收起。版本 **0.49.0 → 0.49.1**。
- 2026-07-26 v1.72 决议 #281：全局字节显示统一为「字节不带小数、其余单位 ≥10 取整、<10 保留一位」（文件卡、传输记录、设置页、文件柜面板同源于 `utils/format.ts`），此前文件柜显示「12 MB」而设置页显示「12.3 MB」。文件柜面板：截断提示改用 `SHARE_DIR_MAX_ENTRIES` 插值、还有下一页时勾选标签改称「选择已加载」（全部取回后才是「全选」）、列表行文件名补原生 tooltip、拖拽高亮改判 `relatedTarget` 不再抖动。截图窗标注描边、箭头、文字与主按钮统一引用 `--primary`，按下态改 `filter: brightness(0.9)`，工具选中态保留 85% 主色字面量（tokens 无等价档位）。版本 **0.49.3 → 0.49.4**。
- 2026-07-26 v1.73 决议 #283：文件柜从设置里搬出来，成为一等入口。§2 导航栏底部工具组最上一格新增「文件柜」按钮（`cabinet` 图标 21px、悬停气泡、不加角标）；§8 拆为 8.1 设置页与 **8.2 文件柜窗口**——1000×680 可缩放非模态独立窗，左栏 280px（我的文件柜摘要卡 + 声明 `shr1` 的同事列表，在线在前离线灰显、带搜索），右栏为浏览器（头部权限徽标 / 列表·网格切换 / 刷新、面包屑、表头全选、40px 详情行、进度行、两行底栏含落点说明）或「我的文件柜」页（共享目录 / 默认权限 / 按人例外 / **最近有人放进来**）。浏览手感对齐系统文件管理器：单击选中、**双击进目录**、Ctrl/Shift 多选、Ctrl+A、空格、Backspace、F5、右键下载/另存为/复制名称，没有删除改名；排序固定不可切、不做缩略图。设置页「聊天与文件」的我的文件柜整组移走，只留一行指路 + 「打开文件柜」。§5 私聊内 320px 面板同步重画（头部加权限徽标与「在文件柜窗口打开」、行高收到 36px、单击选中双击进目录），§7.8 三条流程随之更新。版本 **0.49.5 → 0.50.0**。
- 2026-07-27 v1.74 决议 #284：文件柜形态由独立窗口改为**主窗第三个页签**。§2 导航栏文件柜按钮改为切页签并带选中态；§8 更名为「设置页与文件柜页」，8.2 重写为「文件柜页（主窗第三个页签）」——列表栏（272px，顶部让出 36px 拖拽带、不显示全局搜索框）放我的柜子摘要卡与同事列表，内容区（头部 52px + 32px 拖拽带，与聊天头部等高 84px）放文件浏览器或我的柜子管理页；§5 私聊面板的按钮改称「在文件柜里打开」，§7.8 三条流程与设置页指路行同步。视图、交互、空态与失败态一条没减，只是不再弹窗。版本 **0.50.0 → 0.51.0**。
- 2026-08-10 v1.75 决议 #285：新增英文当前 UI / 交互规范与中英双向入口。应用界面语言、布局、视觉 token 与交互行为保持，仓库版本 **0.51.0 → 0.51.1**。
- 2026-08-26 v1.76 决议 #286：截图失败不再静默。主窗可见时在顶部居中显示 7 秒可关闭、支持辅助技术播报的提示条；主窗原本隐藏时优先用系统通知，通知不可用才显示主窗。Linux 截图前等待窗口隐藏与合成器退场，避免主窗口残影进入画面。版本 **0.51.1 → 0.51.2**。
- 2026-08-27 v1.77 决议 #287：表情包页头新增可访问的「导入」按钮并反馈导入中、成功、部分失败或失败；四列表情行按正方形实际高度排列，固定 200px 内容区溢出滚动；群聊只要至少一位其他成员在线即可发送收藏表情。版本 **0.51.2 → 0.52.0**。
- 2026-08-29 v1.78 决议 #289：ARM64 Wayland 点击截图时不再进入 Electron 22 的原生屏幕枚举，而是立即显示既有系统截图 + `Ctrl+V` 退路；主窗不闪退、不先隐藏。其他平台交互保持，版本 **0.53.0 → 0.53.1**。
- 2026-08-31 v1.79 决议 #290：群成员面板新增群简介与群公告的查看、设置和清空入口；两者复用一套 Teleport 文本弹窗与既有管理密码路径，保存成功后立即刷新群视图，失败保留内容并显示行内提示。弹窗支持遮罩、取消、`Esc`、初始聚焦、焦点恢复与提交忙态。版本 **0.53.1 → 0.54.0**。
- 2026-09-05 v1.80 决议 #291：PR #39 复审修复统一使用一个 `GroupTextDialog`；普通成员保存简介 / 公告时复用群面板已有管理密码，成功刷新群视图，失败保留输入并清理跨弹窗遗留提示；字号、圆角、遮罩均使用现有 token。版本 **0.54.0 → 0.54.1**。
- 2026-09-05 v1.81 决议 #293：主窗口新增 Esc 隐藏，输入法、弹窗、菜单和文件柜选择优先处理，保留现有全局快捷键及关闭偏好；版本 **0.54.2 → 0.54.3**。
- 2026-09-05 v1.82 决议 #294：会话菜单根据实际尺寸避让视口，兼容窗口调整与既有页面缩放；版本 **0.54.3 → 0.54.4**。
- 2026-09-05 v1.83 决议 #295：补齐全局搜索等待和失败反馈，隔离过期请求结果；版本 **0.54.4 → 0.54.5**。
- 2026-09-05 v1.84 决议 #301：三类列表的时间、摘要、计数、IP、组织路径和状态提示局部使用 `--text-2`，保留灰显语义与全局视觉 token；版本 **0.54.10 → 0.54.11**。
- 2026-09-05 v1.85 决议 #302：三类列表行支持原生 Tab/Enter/Space 与茶青内焦点轮廓，分组展开、当前会话、联系人在线状态补可访问语义；版本 **0.54.11 → 0.54.12**。

- 2026-09-06 v1.86 决议 #303：图片画布两侧增加前后切换箭头与边界、等待、失败反馈，切图保持窗口并复位图片状态；版本 **0.54.12 → 0.55.0**。

- 2026-09-06 v1.87 决议 #304：改图片内透明行文字层与原生选区、底部选区复制；全平台手动触发、缓存恢复、识别可取消，移除独立结果框；版本 **0.55.0 → 0.56.0**。

- 2026-09-06 v1.88 决议 #305：修复文字选区过高与跨行拖选光标跳变；版本 **0.56.0 → 0.56.1**。

- 2026-09-07 v1.89 决议 #306：修复 emoji 选区复制与 Win7 编辑器剪切，Linux 开启 NumLock 时恢复小键盘数字输入；版本 **0.56.1 → 0.56.2**。

## 多语言（决议 #307）

设置「通用」增加「语言 / Language」选择，选项始终显示「简体中文」与「English」，切换后即时保存且不重建窗口、不清空输入。首次向导也提供同一选择。所有窗口、托盘、应用自有提示及 Naive UI 文案同步，HTML lang 同步为 zh-CN / en。英文检查设置最小尺寸、长按钮、工具提示、空状态和日期/数量表达；布局继续使用既有 token。用户文本、名字、路径与文件名不参与翻译。系统操作生成的新提示根据结构化元数据重新显示，历史纯文本提示保留原文。系统原生对话框内部按钮与安装器语言由操作系统 / 安装器管理，应用传入的标题与筛选名称随当前语言显示。

- 2026-09-09 v1.90 决议 #307：新增中英文界面与全窗口即时语言切换，版本 **0.56.2 → 0.57.0**。

- 2026-09-16 v1.91 决议 #308（仅文档）：新增 §7.9 的查看请求、逐次选屏同意、共享持续提示、画面状态与结束流程；独立查看窗口为待确认建议，应用 **v0.57.0** 尚未实现此入口。

- 2026-09-16 方案补充（待确认）：加入自动/节省资源/标准/流畅四档选择的控件建议；用户尚未确认默认模式与记忆策略，未实现。

- 2026-09-16 v1.93 决议 #310：独立查看/共享确认窗口及私聊入口落地，默认自动与三个手动档、适应/100%、慢帧提示、持续置顶停止入口和私聊终态反馈均同步中英文。应用 **0.58.0**。

- 2026-09-17 v1.94 决议 #311：修复选屏确认区溢出，重排紧凑查看工具栏、状态反馈和置顶共享小窗；沿用视觉 token 与原生控件，降低软渲染开销。应用 **0.58.1**。

- 2026-09-17 v1.95 决议 #312：320×56 贴边共享条，发起说明与确认，以及含起止时间/时长的持久聊天卡片；应用 **0.59.0**。

- #312 实现细节：发送确认复用 Chrome 108 原生 dialog 的焦点约束；全局 Esc 浮层判断识别 `dialog[open]`，确认层显式居中。原生 backdrop 在该基线不继承根变量，因此在 token 源同时定义其遮罩变量，保持明暗主题一致。

- 2026-09-17 v1.96 决议 #313：协助卡片复用普通消息左右布局，以持久化发起方决定方向；应用 **0.59.1**。

- 2026-09-17 v1.97 决议 #314：一次协助一张两行摘要卡片，宽度上限 260px，使用左右气泡色；移除重复状态条和时间表格，完整详情保留在悬停与无障碍说明。应用 **0.59.2**。

### 诊断与反馈（决议 #315）

- 沿用七组设置导航，在“关于”品牌/版本面板之后放独立紧凑分组。标题“诊断与反馈”，说明“遇到问题时，导出诊断包并附在问题反馈中。”，无需切换到技术详情。
- 主操作“导出诊断包”，次操作“复制环境信息”；显示“仅保存到本机，不会自动上传。包含环境信息与近期运行记录，不含聊天和文件内容。”。
- 原生复选框“附带真实网络地址”默认关闭，仅本次窗口有效。提示地址会随附件公开，按排障需要勾选；默认日志中的匿名地址不还原，上次运行的地址可能不在映射中。
- 系统保存对话框选择 ZIP 路径；生成中禁用重复导出并显示“正在整理诊断包…”。关闭/取消保存对话框不报错。成功显示文件名与“打开所在文件夹”；失败显示简短可操作提示，可重试，聊天不被阻塞。
- 复制仅脱敏环境摘要，不附带真实 IP；操作结果使用行内 aria-live 提示，失败不复用过期成功文案。提醒反馈写明发生时间/操作步骤；传输故障建议双方导出。
- 复用现有字号、茶青按钮及明暗 token，不加动效/模糊/实时仪表盘；640×480 与放大字体允许自然换行，设置主区滚动。

- 2026-09-17 v1.98 决议 #315：关于页诊断导出、复制、隐私选项及成功/失败状态设计；应用 **0.60.0**。
